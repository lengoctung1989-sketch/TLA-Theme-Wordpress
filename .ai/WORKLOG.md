# WORKLOG — bàn giao công việc giữa các model

**Repo:** `tungleads-theme-cp` (child theme caophat.vn) · nhánh `main` · **không có remote** (Tùng chốt 2026-09-14: dự án không dùng remote — đừng đề xuất push)
**Parent:** `tungleads-theme` (pin version bằng git tag, xem dòng `Base:` trong `README.md`)

> Chỉ dùng **2 model: `claude` (Claude Code) và `deepseek` (DeepSeek)** — **chạy tuần tự, không đồng thời**.
> **Không cần script, không cần cài gì** — chỉ đọc và sửa markdown.
> Luật chi tiết + bảng số CP ở **`CLAUDE.md`** (nguồn duy nhất); `AGENTS.md` chỉ là bản tóm tắt bắt buộc (cấu trúc Tùng chốt 2026-09-15).
> P-index của repo này dùng tiền tố **`CP`** (`CP1`–`CP5`) — bảng nghĩa ở `CLAUDE.md`.
>
> 1. **Đầu phiên:** đọc `§1 ĐANG LÀM` + 10 dòng cuối của `§2`.
> 2. **Bắt đầu việc:** cập nhật `§1` (model, việc, file sẽ chạm, trạng thái).
> 3. **Hết việc/phiên:** cập nhật lại `§1` (xong / dang dở + việc tiếp theo) rồi ghi 1 dòng vào `§2`.

> 4. `§1` **ghi đè** (chỉ giữ khối mới nhất) · `§2` **chỉ ghi thêm**, không sửa dòng cũ.

---

## §1 ĐANG LÀM (bàn giao — ghi đè mỗi phiên, chỉ giữ 1 khối)

- **Model:** phiên này chạy **Cline** (VS Code) — **nhãn commit do Tùng chốt: `[deepseek]`**.
- **Việc ĐÃ XONG phiên 2026-09-18:** (1) `.cp-spec__ic` 40→30px; (2) `.cp-contact-btn` thêm `padding: 10px`;
  (3) plugin: bỏ emoji khỏi 2 chuỗi Settings (ảnh vỡ Twemoji); (4) **theme cha** fix P2.3: gỡ twemoji cả trong wp-admin;
  (5) **tối ưu tài liệu**: `CLAUDE.md` thành **chỉ mục CP** (131,6 KB → **6,9 KB**, -95%), chi tiết dồn về `.ai/FEATURE_MAP.md`,
  thêm **hạn mức chống phình** + luật “một sự thật một nhà” vào `AGENTS.md`. Chi tiết từng việc: §2 các dòng `08:06` · `10:48` · `10:58` · `11:56` · `12:20`.
- **Đang dở:** (không) — cả 5 repo đã commit SẠCH.
- **Chờ TÙNG (việc duy nhất còn lại):** chạy `./deploy-caophat.sh --go` để đẩy production — checklist 16 bước nằm ngay trong
  file đó (nhóm A/B/C/D). Khi deploy: plugin đã **v1.2.0** + **theme cha có fix P2.3** ⇒ phải ship **cả parent**.
  Muốn dùng 2FA / đổi đường dẫn admin thì **bật SAU** khi deploy xong.
- **Còn tồn trên production (phần Tùng):** regenerate thumbnail (96 ảnh lỗi) · ảnh `.webp` do LiteSpeed sinh · sửa dữ liệu host.

---
## §2 NHẬT KÝ (chỉ ghi thêm, mới nhất ở dưới)

| Thời gian | Model | Việc đã làm | File chính | Trạng thái |
|---|---|---|---|---|

> 📦 **Rotate 2026-09-18:** 144 dòng cũ (trước 2026-09-13 19:45) đã chuyển **nguyên văn** sang `.ai/WORKLOG-archive-2026-Q3.md`. §2 chỉ giữ **≤20 dòng gần nhất** (luật ở `AGENTS.md`). Tra việc cũ: `grep -n "<khoá>" .ai/WORKLOG-archive-2026-Q3.md`

| 2026-09-17 12:20 (giờ thật) | cline | **CP3.2 — HỘP GIÁ Ở TRANG CHI TIẾT SP TÁCH 2 HÀNG** (yêu cầu Tùng ngay sau CP3.9: *“số đánh giá của khách hàng nằm trên 1 dòng riêng, phía dưới là phần giá”* — kèm link `/san-pham/cua-nhua-han-quoc-kos-2/`). **Nguyên nhân (đo TRƯỚC):** `.cp-price-box` là `flex row` ⇒ `.woocommerce-product-rating` (**159×54**) nằm NGANG hàng `.price`, và chữ “(N đánh giá của khách hàng)” bị xuống 2 hàng vì chỉ được 159px. **Sửa:** đổi sang **GRID 2 cột** (`minmax(0,1fr) auto`) — hàng 1 = rating `grid-column: 1 / -1` + `display:flex` (sao & chữ cùng hàng), hàng 2 = giá (cột 1) + tag `.cp-price-off` `justify-self:end` (cột 2); bỏ `margin-top: .5em` (**6.75px**) của `.star-rating`; ≤768px 1 cột (rating / giá / tag, tag canh trái). **Đo SAU (5 khổ):** hàng 1 = rating **435×27** (1440) · **355×27** (1280) · **403×27** (1024) · **664×27** (768) · **286×35** (390) — 1 hàng, trải hết bề ngang; hàng 2 = giá 300×32 + tag 123×26 (cùng tâm y **351.6**); hộp giá **102 → 99px**; **lệch tâm sao↔chữ 3px → 0.0px** (DOM; pixel 317 vs 314 → 313 vs 314); link “(N đánh giá của khách hàng)” `elementFromPoint` trả về chính `<a>` ở **cả 5 khổ**, bấm → tab **“Đánh giá (6)”** ✓; SP chưa có đánh giá → hàng 1 tự bỏ, hộp 475×64; **tràn ngang 0**; smoke **24/24**. **Dữ liệu:** SP 4816 nay có **6 đánh giá / avg 4.50** — 5 bài seed của phiên + **1 bài TÙNG tự gửi lúc 04:36** (`Ngọc Diễn`, caophatdoor@gmail.com, user 3, tự duyệt vì là admin) ⇒ GIỮ NGUYÊN, không xoá. | `assets/caophat.css`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`; gốc: `docs/measure/{price-row.mjs,star-pixel.mjs}` | xong |
| 2026-09-17 13:10 (giờ thật) | cline | **`gap: 0` cho hàng số đánh giá + MÀU SAO DÙNG CHUNG cho mọi thẻ card** (2 việc Tùng giao tiếp trong cùng khung chat). **(1) `gap: 8px → 0`** ở `.woocommerce div.product .summary .cp-price-box .woocommerce-product-rating` ⇒ sao + chữ sát nhau: chữ dịch trái **8px** (x 668.9 → **660.9**), hàng ở **390px 286×35 → 286×27** (chữ vừa 1 dòng), hàng 1440 vẫn 435×27, **lệch tâm sao↔chữ vẫn 0.0px** (DOM; pixel −1px), link “(N đánh giá của khách hàng)” vẫn bấm được → mở tab “Đánh giá (6)” ✓. **(2) Màu sao đồng bộ:** thêm token **`--cp-star: #e2a53a`** vào `:root` + rule nền **`body.cp .star-rating { color: var(--cp-star); }`** (ngay sau khối token, trước mọi rule khác; độ ưu tiên (0,2,1) thắng màu KẾ THỪA từ card) và đổi **6 chỗ hard-code `#e2a53a`** sang `var(--cp-star)` (CP3.2 summary · `.cp-rv__score` · thẻ đánh giá trong tab · `.cp-rv__chip i` · `.cp-rv__row-star i` · `p.stars a::before` của form) ⇒ còn **1 nguồn màu duy nhất**. **Đo TRƯỚC → SAU:** card của SP 4816 ở `/danh-muc/cua-nhua-abs-han-quoc/` (`star-rating` nằm trong `.cp-card-body`) **`rgb(38,34,30)`** (màu chữ — không rule nào đặt màu) → **`rgb(226,165,58)`** ✓; trang chi tiết **8/8 sao vẫn `rgb(226,165,58)`** ✓; **sao RỖNG giữ `rgb(207,200,216)`** (`#cfc8d8` — WC khai trực tiếp trong `.star-rating::before`, cố ý để sao đầy nổi). ⚠️ **Bẫy khi đo:** trang chủ/cửa hàng/danh mục/tìm kiếm hiện có **0 `.star-rating`** — WooCommerce CHỈ in sao khi SP có ≥1 đánh giá (chỉ SP 4816 có) ⇒ phải mở đúng trang danh mục của SP đã có đánh giá mới thấy sao trên card. **Hồi quy:** smoke test **24/24** · tràn ngang 0 ở 1440/1280/1024/768/390. | `assets/caophat.css`, `CLAUDE.md` (CP3.2 + CP4.1), `.ai/FEATURE_MAP.md`; gốc: `docs/measure/star-audit.mjs` | xong |
| 2026-09-17 14:10 (giờ thật) | cline | **CP8 — MỤC LỤC NỘI DUNG (plugin `tl-site-caophat` v0.4.0)** (Tùng: *“xây dựng tiếp tính năng mục lục thêm vào plugin cao phát để quản lý”*, demo `tungleads.com/khoa-hoc/…`). **Đo demo trước khi làm:** trang demo có `.toc-toggle` (`fixed; right:0; top:500px; translateY(-50%)`, 41×163, chữ `vertical-rl`) mở `.toc-panel` (`fixed; z-index:60; 340px; #fff`), mục `li.toc-level-2/3` có `<span class="toc-item-number">`, mục đang xem tô nền `rgb(234,241,255)`. **Làm:** PLUGIN thuần (`includes/toc.php` mới + `assets/toc.css`/`toc.js` mới + `require_once` + version 0.4.0) — **theme không đổi file nào**; 11 tuỳ chọn ở **Settings → Cao Phát** (mặc định **TẮT**). Filter `the_content` prio 12 quét `<h2-4>` → **thêm `id`** (slug bỏ dấu) + đánh số `1 · 2 · 2.1`; markup in ở `wp_footer` **prio 5**; JS: mở/đóng (`inert`, `aria-expanded`, `body.tlcp-toc-open` ẩn widget liên hệ), cuộn **có bù header sticky**, scroll-spy, ✕/`Esc`/bấm ngoài đóng. **Số đo:** nút 35×160 nền `rgb(200,71,31)`; drawer 340×900 z-index 90; 14 mục (bài 87KB) / 5 mục (trang chính sách); bấm mục → heading dừng **14px dưới header** ở cả 1440 (91−77) và 390 (79−65); spy `is-active` + `aria-current` (nền `color(srgb .974 .913 .895)` = 12% `--cp-accent`); SP không bật ⇒ **không nạp CSS/JS**; `min`/`exclude`/`mobile=off` đúng; **tràn ngang 0**; E2E Settings đổi mép/màu/tắt số → front-end ăn theo; `php -l` × 2 + `node --check` sạch. **2 BẪY:** ① `wp_footer` prio **5** (không 20 — core in `<script>` ở prio 20 ⇒ markup phải ra trước, không thì bấm nút không mở) · ② **ảnh lazy làm trang cao thêm 2541px** (43469 → 46010) ⇒ cuộn 1 lần hụt **1565px** ⇒ phải “dò lại bám đích” (~3s, tự huỷ khi người dùng tự cuộn). | `plugins/tl-site-caophat/includes/toc.php` (**mới**), `assets/toc.css` + `assets/toc.js` (**mới**), `tl-site-caophat.php` (hằng số + `require_once` + v0.4.0), `README.md` plugin; docs: `CLAUDE.md` (CP8), `.ai/FEATURE_MAP.md` (mục CP8), `.ai/WORKLOG.md`; script đo `docs/measure/toc-{probe3,e2e,check,scrolldbg,admin}.mjs` (repo gốc) | ✅ |
| 2026-09-17 14:35 (giờ thật) | cline | **Đo lại mục lục theo CẤU HÌNH TÙNG TỰ CHỈNH** (Tùng vào Settings → Cao Phát đổi: nhãn **“Mục lục”**, mép **TRÁI**, màu **`#ffa305`**): xác nhận từng tuỳ chọn ăn ra front-end — nút dính mép trái `0..35` ở 1440 (`0..31` ở 390), nền `rgb(255,163,5)`, **drawer mở từ TRÁI** `0..340` (390 → `0..320`), `fixed z90`, shadow đổ sang phải (`18px 0 48px`), nhãn chữ dọc vẫn vừa (100px); **tràn ngang 0** ở cả 2 khổ; nút ở dải dọc 400..500 nên **không đè logo** (logo `20..270` phía header). Script: `docs/measure/toc-left.mjs`. | `docs/measure/toc-left.mjs`, `docs/measure/README.md` | ✅ |
| 2026-09-17 14:12 (giờ thật) | cline | **CP8 v0.4.1 — THÊM KHỐI MỤC LỤC TRONG NỘI DUNG BÀI VIẾT** (Tùng: *“thêm tuỳ chọn mục lục hiển thị trong phần nội dung bài viết”*, ngay sau khi tự tay chỉnh Settings): 4 khoá option mới — `float` (nút dọc + drawer) · `inline` (khối trong bài) · `inline_pos` (`top` đầu bài / `p1` sau đoạn mở đầu) · `inline_open` (mở sẵn) ⇒ **2 cách hiển thị bật/tắt ĐỘC LẬP**. Khối = `<details class="tlcp-toc-inline">` + `<summary>` (nhãn) + `<ol>` số, chèn vào `$content` **sau khi quét heading** (`tlcp_toc_inline_insert()`) ⇒ dùng `<details>` HTML gốc nên **thu/mở chạy được cả khi tắt JS**; JS tách 2 phần độc lập (khối + drawer) dùng chung `offsetTop()`/`goTo()`/`bindAnchors()`; tắt cả 2 cách ⇒ **không nạp CSS/JS**. **Đo:** khối **990×226 @y320** (`top`, trên heading đầu, nằm trong `.cp-article__content`) / **990×616 @y507** (`p1`, dưới đoạn mở đầu) / thu gọn **60px** (bấm tiêu đề: `open false → true`); viền trái 4px + số `rgb(255,163,5)` khớp màu Settings; bấm mục trong khối → heading dừng **14px dưới header** (91−77) như drawer; **7 biến thể đúng** (`float_off` · `inline_off` · **`both_off` ⇒ 0 request CSS/JS** · `collapse` · `p1` · `top` · `depth3`); SP (Tùng bật `product`) → khối nằm trong tab mô tả WC **986×432 / 11 mục**; **tràn ngang 0**; smoke **24/24**; `php -l` ×2 + `node --check` sạch. **3 BẪY:** ① in markup `wp_footer` phải **prio 5** (core in `<script>` ở prio 20 ⇒ markup prio 20 ra sau thẻ script, bấm nút không mở) · ② **cuộn quãng DÀI phải NHẢY THẲNG** — ảnh lazy nạp dần khi cuộn mượt đẩy đích **36.567px → 78.599px**, 12 lần dò/3s vẫn hụt ⇒ `delta > 1,5 × màn hình` thì `behavior:'auto'` (quãng ngắn vẫn mượt) · ③ **màu Settings phải truyền `--tlcp-toc-color` vào khối inline** (khối không nằm trong wrapper `.tlcp-toc`). Thêm `html { scroll-padding-top: var(--tlcp-scroll-pad) }` để deep link `#mục` dừng dưới header. **Cách đo không đụng cấu hình Tùng:** mu-plugin tạm đọc `?toc_test=<case>` (đã xoá sau khi đo); cấu hình đã trả về đúng giá trị Tùng đang dùng (types `post`+`product` · depth 2 · mép phải · nhãn “Mục lục” · màu `#ffa305`). | plugin: `includes/toc.php`, `assets/toc.css`, `assets/toc.js`, `tl-site-caophat.php` (**v0.4.1**), `README.md`; docs: `CLAUDE.md` (CP8), `.ai/FEATURE_MAP.md` (mục CP8), `.ai/WORKLOG.md`; script đo: `docs/measure/toc-{inline,vars,inline-dbg}.mjs` | ✅ |
| 2026-09-17 14:38 (giờ thật) | cline | **CP1.9 — “CHI NHÁNH & HOTLINE CAO PHÁT” CHUYỂN TỪ PLUGIN VỀ THEME** (Tùng hỏi *“chuyển phần (Hotline chi nhánh) trong plugin ra ngoài phần cài đặt … trong theme sẽ hợp lý hơn → bạn thấy thế nào?”*; tôi kết luận **hợp lý** vì đây là NỘI DUNG hiển thị chứ không phải business logic, mà hotline CHÍNH `cp_hotline_tel` đã ở Customizer ⇒ gộp 1 chỗ; Tùng chốt **dùng repeater có sẵn của theme**, không làm trang admin riêng). **Theme:** `inc/customizer.php` thêm control `CP_Branches_Repeater_Control` (type `cp_branches_repeater`, mỗi dòng tên+số, nút × xoá, tay kéo ⋮⋮) + section `cp_branches` (priority 26) + setting JSON `cp_branches` + `cp_sanitize_branches()`; asset mới `assets/customizer-branches.js` (clone/xoá/sortable/`sync()` → `wp.customize().set()`) + `.css`; `functions.php` thêm `cp_support_branches_default()` / `cp_support_branches_default_json()` / `cp_support_branches()` = **theme_mod → CẦU NỐI đọc thẳng option cũ `tlcp_support_branches` → mặc định** (filter `cp_support_branches` giữ nguyên); `inc/woocommerce.php` bỏ đoạn đọc plugin. **Plugin `tl-site-caophat` v0.5.0 gỡ hẳn** option + textarea + `tlcp_support_branches_default()/()/_text()` + `register_setting` (thay bằng khối ghi chú CP1.9); trang Settings còn 3 mục + dòng link “Cần sửa SỐ HOTLINE? → Giao diện → Tuỳ biến”. **Chuyển dữ liệu:** `docs/prod-migrate-branches.php` (DRY-RUN mặc định; `go` để ghi; `force` để ghi đè) + 2 dòng mới trong `deploy-caophat.sh` bước 5b. **Đo (Playwright + user admin tạm):** Customizer đúng section, 4 dòng mặc định, `ui-sortable` init, JS/CSS nạp; “+ Thêm chi nhánh” → 5 dòng + input ẩn & `wp.customize('cp_branches').get()` cập nhật; **kéo chuột thật** dòng cuối lên đầu → DOM + JSON đổi thứ tự → Lưu → `wp theme mod get cp_branches` khớp; front-end box “Hỗ trợ trực tuyến” 5 dòng (link `tel:0834484484` đúng) → **xoá theme_mod ⇒ về 4 dòng mặc định**, box **320×352**, tràn ngang **0**; **cầu nối** (option cũ `CN Cầu Nối \| 0900.999.888`) ⇒ front-end hiện đúng 1 dòng; script chuyển: dry-run in đúng dòng, `go` ⇒ theme_mod ghi + option cũ **bị xoá**; **smoke 24/24**; `php -l` ×4 + `node --check` sạch. **3 bẫy:** ① `wp eval-file … -- --go` bị WP-CLI chặn (`unknown --go parameter`) ⇒ dùng tham số **vị trí** `go`/`force`; ② cầu nối phải đọc **thẳng option** (plugin đã gỡ hàm nên `function_exists()` = false ⇒ mất số cũ); ③ `$args` của `eval-file` chỉ chứa tham số vị trí. | theme: `inc/customizer.php`, `functions.php`, `inc/woocommerce.php`, `assets/customizer-branches.js` (**mới**), `assets/customizer-branches.css` (**mới**), `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` · plugin: `tl-site-caophat.php` (**v0.5.0**, gỡ mục Hotline), `README.md` · gốc repo: `docs/prod-migrate-branches.php` (**mới**), `deploy-caophat.sh` · script đo `docs/measure/branches-*.mjs` | ✅ |
| 2026-09-17 14:57 (giờ thật) | cline | **1) TRẢ LỜI CÂU HỎI “mang plugin sang WordPress khác có chạy không?” — ĐÃ ĐO THẬT:** dựng **một WordPress MỚI** trong Docker (WP 7.1 + theme mặc định `twentytwentyfive` + **KHÔNG có WooCommerce**, cổng 8889, đã xoá sạch sau khi đo), copy nguyên plugin v0.5.0 vào → **kích hoạt OK, front-end + trang Settings 0 warning/fatal** (log container sạch); **Mục lục chạy đủ** trên theme lạ (nút 35×157, khối 4 mục, bấm mở drawer, không tràn ngang, màu dùng fallback vì theme không có `--cp-*`); **bảo trì ⇒ khách HTTP 503**; **AJAX đặt hàng nhanh thiếu WC ⇒ 403 + JSON, KHÔNG fatal** (guard `function_exists('wc_get_product')` + `instanceof WC_Product`); “Thông số kỹ thuật” cần WC (thiếu thì im lặng); **NHƯNG 4 ID tracking của caophat.vn vẫn bắn nguyên** (`GTM-KCVHR8P` ×2 · `G-L37N4Q06LP` ×2 · `AW-10871632223` ×1 · Pixel ×2). **2) Tùng hỏi “sao chưa thấy trường nhập 4 ID?” ⇒ LÀM v0.6.0 — ĐƯA 4 ID LÊN SETTINGS** (trước đây là hằng số trong file): thêm mục **“ID tracking”** (mục ĐẦU trang Settings → Cao Phát) gồm **công tắc “In 4 khối tracking ở trên”** + **4 ô** (GTM `GTM-…` · GA4 `G-…` · Google Ads `AW-…` · Meta Pixel số) prefill sẵn 4 ID hiện tại; option `tlcp_tracking_ids` + `tlcp_tracking_ids()` (chưa lưu ⇒ dùng 4 hằng số mặc định = **hành vi y hệt bản cũ**, đã lưu ⇒ option là chuẩn, **ô trống = không in khối đó**, bỏ tick = không in khối nào) + `tlcp_sanitize_tracking_ids()` (chỉ giữ `A–Z 0–9 - _`, viết hoa) + filter `tlcp_tracking_ids`; 2 hook `wp_head`/`wp_body_open` in theo ID đang nhập (mỗi khối có guard riêng). **ĐO E2E (Playwright, user admin tạm đã xoá):** front-end **GỐC** gtm 3 · ga4 2 · gads 1 · pixel 2 · `dataLayer` 5 → **đổi GTM ⇒ `GTM-TEST999` ×3, ID cũ 0** → **để trống GA4+Pixel ⇒ 0/0, gads vẫn 1** → **bỏ tick công tắc ⇒ cả 4 = 0, `dataLayer` 0** → **trả lại 4 ID thật + bật ⇒ y hệt gốc** (đã xoá option để site về đúng trạng thái trước phiên). **Ghi chú deploy:** 4 khối này sao y Flatsome Global HTML ⇒ khi deploy phải GỠ 4 khối khỏi Flatsome cùng lúc (đếm đôi). | plugin: `tl-site-caophat.php` (**v0.6.0**), `README.md` (mục “ID tracking” + mục mới “Mang sang website WordPress khác”); docs: `.ai/WORKLOG.md`; script đo `docs/measure/tracking-ids-e2e.mjs` | ✅ |
| 2026-09-17 15:10 (giờ thật) | cline | **ĐỔI TÊN + GHI CHÚ PLUGIN THEO HƯỚNG DÙNG CHUNG (v0.7.0)** (Tùng: *“tận dụng Plugin này cho các dự án website WordPress khác → các thông tin từ tên plugin đến ghi chú nên sửa lại theo hướng dùng chung”*): header plugin **`Plugin Name: PL Tiện Ích - TungLeAds`** (trước: “TL Site — Cao Phát”), Description viết lại = liệt kê 6 tiện ích (4 khối tracking · chèn mã 3 vị trí · bảo trì · mục lục · thông số SP · đặt hàng nhanh) + “Cấu hình ở Settings → PL Tiện Ích”; Author/Author URI = **Tùng Lê Ads** / tungleads.com; `@package TL\Utilities`. Menu admin: Settings → **“PL Tiện Ích”**, H1 trang = **“PL Tiện Ích - TungLeAds”**; **dòng `description` đầu trang đổi đúng nguyên văn Tùng yêu cầu: “Tiện ích Plugin đa chức năng được phát triển bởi Tùng Lê Ads”** (bỏ hẳn câu “Cần sửa SỐ HOTLINE… Giao diện → Tuỳ biến”). **Xoá toàn bộ text “Cao Phát”/“caophat.vn” khỏi plugin** (22 chỗ: tiêu đề, ghi chú khối tracking, comment CP8/CP1.9, `assets/toc.js`/`toc.css`, dòng ghi công, thông báo đặt hàng nhanh `Đã tạo đơn #%s. Tùng Lê Ads sẽ gọi xác nhận…`, nhắc “skin Cao Phát” → “skin của theme”, “theme Cao Phát” → “theme đang dùng”, “Chi nhánh & Hotline Cao Phát” → “Customizer của theme đang dùng”); README plugin viết lại hướng dùng chung (tiêu đề mới, intro, 9 chỗ “Settings → Cao Phát” → “PL Tiện Ích”, mục Deploy bỏ tên script của dự án). **GIỮ NGUYÊN các định danh nội bộ** (`tlcp_*` option/hàm, text-domain `tl-site-caophat`, tên thư mục `tl-site-caophat`) để KHÔNG phá dữ liệu đã lưu + không phải sửa 15 file tham chiếu (deploy script, theme, docs) — đã ghi rõ trong README. **Đo (Playwright, user admin tạm đã xoá):** `/wp-admin/plugins.php` hiện **“PL Tiện Ích - TungLeAds”** + mô tả mới; menu Settings = “PL Tiện Ích”; H1 = tên đầy đủ; dòng dưới H1 đúng nguyên văn; 4 mục (ID tracking · Chèn mã tracking · Chế độ bảo trì · Mục lục nội dung); dòng ghi công **“Phiên bản 0.7.0 \| Bởi Tùng Lê Ads”**; **`/Cao Phát\|caophat/i` trên trang = false**; smoke test **24/24**; `php -l` ×2 + `node --check` sạch. | plugin: `tl-site-caophat.php` (**v0.7.0**), `includes/toc.php`, `assets/toc.js`, `assets/toc.css`, `README.md`; docs: `CLAUDE.md` (dòng mô tả plugin), `.ai/WORKLOG.md`; gốc repo: `deploy-caophat.sh` (bước 2), `docs/CHUYEN-SITE.md` (bảng bước 4); script đo `docs/measure/rebrand-check.mjs` | ✅ |
| 2026-09-17 15:21 (giờ thật) | cline | **XOÁ ẢNH EMOJI LỖI TRONG PLUGIN** (Tùng: *“Xoá link ảnh lỗi trong plugin `https://cdn.jsdelivr.net/gh/jdecked/twemoji@17.0.1/assets/26a0.svg`”*): **truy nguồn** — trong plugin/theme **không hề có link twemoji/jsdelivr** nào; link đó do **LÕI WORDPRESS** sinh ra: script emoji của core đổi **ký tự emoji** trong HTML hiển thị thành `<img src="https://cdn.jsdelivr.net/gh/jdecked/twemoji@…/26a0.svg">`. Ký tự gây lỗi = **⚠️ (U+26A0 U+FE0F)** trong **1 chuỗi hiển thị duy nhất** ở `tl-site-caophat.php` (dòng cảnh báo đỏ dưới mục “ID tracking”). **ĐO TRƯỚC:** trang Settings có **1 `<img>` CDN** đúng URL Tùng dán (`img.emoji` = 1); front-end **0**. **SỬA:** đổi `'⚠️ Mang plugin sang website KHÁC…'` → `'Lưu ý: mang plugin sang…'` + **đổi 7 ⚠️ trong comment** (5 ở `tl-site-caophat.php`, 2 ở `assets/toc.js`) thành “Lưu ý:” ⇒ plugin **không còn ký tự emoji nào** trong code (README `.md` vẫn giữ ⚠️/✅ vì markdown KHÔNG bao giờ được WP render ⇒ không sinh `<img>`). **ĐO SAU:** trang Settings **`soImgCDN: 0`, `img.emoji: 0`**; quét cây text trong khối plugin (`trongKhoiPlugin`) = **[]** — ký tự emoji còn lại trên trang chỉ nằm trong `<script>` của lõi WP (dữ liệu emoji-loader, bình thường); front-end vẫn **0**; smoke test **24/24**; `php -l` ×2 + `node --check` sạch. | plugin: `tl-site-caophat.php`, `assets/toc.js`; script đo: `docs/measure/emoji-check.mjs` + `emoji-where.mjs` (mới) | ✅ |
| 2026-09-17 15:38 (giờ thật) | cline | **PLUGIN v1.0.0 — ĐỔI TÊN “SẠCH HẲN” ĐỂ DÙNG CHUNG** (Tùng: *“Muốn đổi luôn cho ‘sạch’ hẳn không? -> đổi luôn cho mình -> test lại kỹ đảm bảo không lỗi khi vận hành”*). **Đổi 498 chỗ trong plugin:** thư mục + file chính **`tl-site-caophat` → `pl-tien-ich-tungleads`** (git mv, giữ lịch sử), text-domain → `pl-tien-ich-tungleads`, tiền tố **`tlcp_`→`tlpi_`** (173) · **`TL_CP_`→`TLPI_`** (69) · **`tlcp-`→`tlpi-`** (152, class/id CSS-JS + slug Settings `tlpi-support`) · **`tl-site-caophat`→`pl-tien-ich-tungleads`** (95) · **`cp_quick_order`→`tlpi_quick_order`** (action AJAX + hàm nonce) · **`cp_hp`→`tlpi_hp`** (honeypot) · **`cp-quick-order`→`tlpi-quick-order`** (`created_via` đơn hàng) · meta `_tlcp_spec_*`→`_tlpi_spec_*`; version **1.0.0**. **CẦU NỐI TƯƠNG THÍCH NGƯỢC (để site đang chạy KHÔNG mất gì):** hàm mới `tlpi_option()` (đọc option mới → tự lùi tên cũ `tlcp_*`) cho cả 4 option (tracking_ids · tracking_code · maintenance · toc); AJAX vẫn đăng ký action cũ `cp_quick_order`; honeypot nhận cả `cp_hp`; nonce nhận cả nonce tạo theo tên cũ; theme đọc meta mới **có lùi về `_tlcp_spec_*`**. **Theme (4 điểm nối):** `tlpi_quick_order_nonce()` (3 chỗ) · input `action=tlpi_quick_order` + `name=tlpi_hp` trong popup · meta `_tlpi_spec_` (+ fallback) · comment. **Script chuyển dữ liệu** `docs/prod-migrate-plugin-prefix.php` (dry-run mặc định; tham số VỊ TRÍ `go`; **bỏ qua giá trị RỖNG** để không tắt tracking; chuyển cả meta `_tlcp_spec_*`). **TEST KỸ:** ① local — admin `?page=tlpi-support` 4 mục, **0 lỗi PHP**, **0 ảnh emoji**, form đọc đúng cấu hình Tùng đã lưu từ option CŨ (bridge chạy) · front-end bài viết: tracking in đủ (GTM 3 · Pixel 2), khối mục lục 990×226 vàng `rgb(255,163,5)`, **bấm mục dừng 14px dưới header** (chế độ nút dọc) / 46px (chế độ khối — do ảnh lazy, có từ trước) · **E2E đặt hàng nhanh qua action MỚI ⇒ tạo đơn thật #11754 `created_via=tlpi-quick-order`** (đã xoá đơn test) · trang SP đọc meta mới (16 dòng thông số) · bảo trì ⇒ khách **503** → tắt ⇒ 200 · smoke **24/24**. ② **WordPress MỚI** (WP 7.1 + twentytwentyfive + KHÔNG WooCommerce): activate OK, bài viết 200 với `.tlpi-toc__toggle` + khối inline + id heading, tracking in, bảo trì 503, trang Settings `pl-tien-ich-tungleads/pl-tien-ich-tungleads.php` **0 lỗi PHP/JS**. **3 BẪY mới ghi lại:** ① **option đã lưu nhưng RỖNG ⇒ TẮT tracking** (đã xử lý: xoá option rỗng + script bỏ qua giá trị rỗng + ghi cảnh báo vào README) · ② đổi tên thư mục ⇒ **`active_plugins` còn đường dẫn cũ** phải xoá entry (đã xử lý) · ③ `wp eval-file … -- --go` bị WP-CLI chặn (dùng tham số vị trí `go`). | plugin: **đổi tên thư mục + `pl-tien-ich-tungleads.php`**, `includes/toc.php`, `assets/toc.js|toc.css`, `README.md` (mục “Chuyển từ bản cũ” + 3 bẫy) · theme: `inc/woocommerce.php`, `assets/quick-order.js`, `functions.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.github/instructions/…`, `.ai/WORKLOG.md` · gốc repo: `deploy-caophat.sh`, `docs/CHUYEN-SITE.md`, `docs/prod-migrate-plugin-prefix.php` (**mới**), `PRODUCT.md`, `docs/measure/*` (selector mới), `.ai/WORKLOG.md` · widget plugin (1 comment) | ✅ |
| 2026-09-17 15:58 (giờ thật) | cline | **CP3.9 — SỬA “CÓ SP HIỆN, CÓ SP KHÔNG HIỆN TAB ĐÁNH GIÁ”** (Tùng: *“kiểm tra phần đánh giá sản phẩm làm sao để hiện đánh giá lên các sản phẩm → hiện tại có sản phẩm hiện, có sản phẩm không hiện tab đánh giá”*). **Truy nguyên nhân:** WooCommerce **chỉ thêm tab** khi `comments_open()` của SP (`wc-template-functions.php`: `if ( comments_open() ) { $tabs['reviews'] = … }`) ⇒ phụ thuộc **`comment_status` của TỪNG sản phẩm**, KHÔNG chỉ option toàn cục; theme không hề ẩn tab (2 filter `woocommerce_product_tabs` chỉ đổi `callback` — CP3.8/CP3.9). **Đo:** `woocommerce_enable_reviews=yes` · `comment_registration=0` · `default_comment_status=open` ✓ nhưng **46/363 SP `closed`** (5949 · 5937 · 5933 · 5907 · 5900 · 5731 …) ⇒ đúng 46 SP đó thiếu tab. **Đã sửa:** mở khoá cả 46 (`wp_update_post comment_status=open`) ⇒ **363/363 `open`**, `comments_open()` TRUE toàn bộ; **kiểm front-end:** SP 5949 & 5937 nay có tab **“Đánh giá (0)”** (hộp tổng kết CP3.9 986×187 @1440 → 314×364 @390 · 5 thanh · trạng thái rỗng “Chưa có đánh giá nào.” · form đủ · **tràn ngang 0**), SP 4816 **“Đánh giá (6)”** + 12 chip + 6 thẻ. **Script production mới:** `docs/prod-open-product-reviews.php` (dry-run mặc định; `go` để chạy; chạy lại vô hại; đặt luôn 4 option) — **đã test cả 2 chế độ** bằng cách khoá lại 3 SP rồi chạy: dry-run báo “3 còn KHOÁ”, `go` báo “đã mở khoá 3 SP”, kiểm lại **SQL: 363/363 `open`**; đã thêm vào `deploy-caophat.sh` bước 5b. **Cách kiểm 1 SP:** Sản phẩm → “Dữ liệu sản phẩm” → **“Nâng cao” → “Bật đánh giá”**. | `CLAUDE.md` (CP3.9), `.ai/FEATURE_MAP.md` (CP3.9), `.ai/WORKLOG.md` (§1 mục ① nay ✅), gốc repo: `docs/prod-open-product-reviews.php` (**mới**), `deploy-caophat.sh`, `docs/measure/reviews-tab-check.mjs` + `reviews-cp39-check.mjs` (mới) | ✅ |
| 2026-09-17 16:13 (giờ thật) | cline | **BÀN GIAO DEPLOY CHO PHIÊN SAU (Claude) + 2 bug bắt được khi test** (Tùng: *“Claude hiện không biết các thay đổi mới nhất của bạn để push lên production — bạn cần note lại”*). **Đã note:** ① **khối bàn giao ở §1** (ngay trên) tóm tắt 5 thay đổi mới + cách deploy + dữ liệu chỉ có ở local + 2 bẫy; ② **viết lại checklist deploy trong `deploy-caophat.sh`** thành **16 bước chia 4 nhóm A/B/C/D** (backup DB → bật bảo trì → PHP 8.2 → activate plugin MỚI + **xoá thư mục plugin cũ** → **gỡ 4 khối tracking khỏi Flatsome** → **5 script DB đúng thứ tự** → đặt tay logo/menu/khối → tắt bảo trì → **LiteSpeed Purge All** → test + rollback); ③ **script mới `docs/prod-import-plugin-options.php`** (nhập cấu hình plugin cho production: option `tlpi_toc` + IN RA 4 ID tracking để đối chiếu; dry-run mặc định, `go` để ghi). **2 BUG bắt được khi test (đã sửa):** ① script nhập option ghi **CHUỖI JSON** trong khi plugin đọc bằng `get_option()` mong nhận **MẢNG** ⇒ option mục lục **về mặc định** (mất cấu hình Tùng) — đã sửa sang ghi mảng + test `go` lại ⇒ DB là mảng 360 ký tự ✓ cấu hình Tùng khôi phục ✓; ② phát hiện option `tlpi_tracking_ids` đang ở trạng thái `on:""` ⇒ **front-end KHÔNG in khối tracking nào** ⇒ đã xoá option (plugin dùng lại 4 ID mặc định) ⇒ tracking trở lại (GTM 2 · GA4 2 · Ads 1 · Pixel 2) ✓. **Kiểm chứng cuối:** mục lục inline hiện đúng (nút dọc tắt theo cấu hình Tùng) · tracking đủ 4 khối · **smoke 24/24** · `php -l`/`bash -n` sạch. 2 bẫy này đã ghi thêm vào README plugin — mục “Chuyển từ bản cũ” nay là **5 điều đã dính**. | `deploy-caophat.sh`, `docs/prod-import-plugin-options.php` (**mới**), plugin `README.md`, `.ai/WORKLOG.md` (§1 + dòng này) | ✅ |
| 2026-09-17 21:59 (giờ thật) | cline | **PLUGIN v1.1.0 — TÍNH NĂNG 1/2: ĐỔI ĐƯỜNG DẪN ĐĂNG NHẬP (ẩn wp-admin)** (Tùng: *“Tiếp tục thêm cho tôi chức năng thay đổi đường dẫn mặc định của admin + xác minh 2FA trong plugin PL Tiện ích”* — mới làm phần **đường dẫn admin**, phần 2FA đang chờ Tùng chốt phương thức). **Code:** `includes/login-path.php` (**mới**, 400 dòng) — option `tlpi_login` (`on` · `slug` · `old_path` = `404`/`home`); hook `init` prio 1 (sớm hơn `auth_redirect()`), vào ĐÚNG slug ⇒ `require_once ABSPATH.'wp-login.php'` + `exit` (nên **mọi action chạy y như core**: login · logout · lostpassword · resetpass · postpass · register), vào đường dẫn CŨ (`wp-login.php`, `wp-admin/…`) với khách ⇒ **404 tự chứa** (`X-Robots-Tag: noindex`, không nạp theme) hoặc 302 về trang chủ; filter `site_url`/`network_site_url`/`wp_redirect` đổi link WP sinh ra; slug bị chặn nếu trùng đường dẫn hệ thống (admin/login/wp-login…); **tự TẮT nếu permalink đang để “Mặc định”** (URL đẹp cần rewrite) + Settings hiện cảnh báo đỏ. **KHÔNG chặn** (whitelist): admin-ajax.php · admin-post.php · load-styles/scripts.php · `/wp-admin/(css\|js\|images\|fonts)/…` · REST · cron · XML-RPC · wp-cli. **Kèm:** tracking tự bỏ qua trang đăng nhập (không tính vào GA/Pixel). **TEST KỸ (đã bật thật rồi tắt):** URL mới **200** + form + `action` trỏ URL mới + **0 request lỗi** (whitelist đủ, 6 file CSS/JS core tải được) · `wp-login.php` & `wp-admin/` & trang Settings plugin (khách) = **404** · admin-ajax **400** (không bị chặn) · `/wp-json/` **200** · trang chủ + trang SP **200** · **đăng nhập qua URL mới bằng trình duyệt ⇒ vào `/wp-admin/` OK** + admin bar + trang Settings 0 lỗi PHP · link **Đăng xuất** ở admin bar = `…/dang-nhap-quan-tri/?action=logout&_wpnonce=…` ⇒ bấm thoát OK (`?loggedout=true`) · link “Quên mật khẩu” = URL mới · **trang bảo trì (request thật)** in link đăng nhập = URL mới ✓ · tracking ở trang đăng nhập = **0** ✓ · **smoke 24/24** cả khi BẬT lẫn khi TẮT. **Trạng thái local cuối:** tính năng **TẮT nhưng điền sẵn slug `dang-nhap-quan-tri`** (tick 1 ô là bật) — cố ý để TẮT cho các script đo tự động (chúng đăng nhập ở `/wp-login.php`) và tránh Tùng bị 404 khi vào wp-admin. | plugin: `includes/login-path.php` (**mới**), `pl-tien-ich-tungleads.php` (require + register option + UI + bỏ tracking ở trang login + **v1.1.0**), `README.md`; gốc repo: `docs/measure/login-path-e2e.mjs` + `login-assets-check.mjs` (**mới**) + README | ✅ |
| 2026-09-17 23:31 (giờ thật) | cline | **PLUGIN v1.2.0 — TÍNH NĂNG 2/2: XÁC MINH 2 LỚP (2FA) BẰNG APP XÁC THỰC (TOTP)** (Tùng chốt phương án **B — app xác thực**, không dùng email). **Code:** `includes/two-factor.php` (**mới**, 842 dòng) — option `tlpi_2fa` (`on` · `roles` mặc định `administrator` · `remember` = 30 ngày); **TOTP chuẩn RFC 6238** tự viết (Base32 + HMAC-SHA1 + dynamic truncation, cửa sổ ±30s); **luồng 2 bước**: `authenticate` prio 99 chặn sau khi mật khẩu đúng → tạo “phiên chờ” (transient 10 phút, token ngẫu nhiên) → `wp_login_failed` chuyển sang màn `wp-login.php?action=tlpi_totp&token=…` (hook `login_form_tlpi_totp`, dùng `login_header()`/`login_footer()` của core) → nhập mã 6 số **hoặc mã dự phòng** → `wp_set_auth_cookie()` + `do_action('wp_login')` ⇒ **mật khẩu đúng nhưng chưa xác minh thì KHÔNG có phiên**. **Ghi danh tự động** khi tài khoản bắt buộc chưa có mã bí mật (hiện mã 32 ký tự Base32 + link `otpauth://`, rồi in **10 mã dự phòng 1 lần**, lưu hash buộc với user, mỗi mã dùng 1 lần); **cookie “nhớ thiết bị”** HMAC buộc với user + mã bí mật (đặt lại 2FA ⇒ mọi thiết bị cũ hết hiệu lực); **tối đa 5 lần nhập sai** ⇒ huỷ phiên; mã bí mật lưu **mã hoá sodium secretbox** (khoá từ `AUTH_KEY`+`SECURE_AUTH_SALT`); trang **Hồ sơ** có trạng thái + “Tạo lại 10 mã dự phòng” + “Đặt lại 2FA” + ô tự nguyện bật (meta `_tlpi_totp_want`). **TEST KỸ:** • **TOTP đối chiếu RFC 6238** ở 2 vector (`287082`, `081804`) — plugin PHP **khớp** với bản python viết độc lập • **curl**: mật khẩu ⇒ **302 → action=tlpi_totp**; màn nhập mã **không lộ lại mã bí mật**; mã sai ⇒ “Mã không đúng. Còn 4 lần thử.”; mã dự phòng ⇒ **302 → wp-admin 200** và **dùng lại thì bị huỷ**; **POST `tlpi_done` khi chưa xác minh ⇒ KHÔNG vào được** (0 lần chuyển wp-admin — lỗ hổng bỏ qua đã phát hiện & vá bằng cờ `codes_ready`) • **trình duyệt (Playwright)**: mật khẩu ⇒ màn nhập mã (ô 270px, CSS core hiện) ⇒ mã TOTP ⇒ **/wp-admin/ + admin bar**; cookie `tlpi_2fa_dev` ⇒ lần sau **bỏ qua 2FA** (302 thẳng wp-admin 200); Hồ sơ “Đã bật” + 2 nút; **bấm “Đặt lại 2FA” ⇒ “Chưa bật”** và đăng nhập lại **ra màn ghi danh với mã bí mật MỚI** • trang Settings + Hồ sơ **0 lỗi PHP** (đã sửa 1 fatal `Call to undefined function tlpi_2fa_settings_ui()` do khối Settings bị chèn **vào giữa closure** `personal_options_update` — tokenizer phát hiện hàm bị lồng ở độ sâu 1) • **smoke 24/24**. **Trạng thái local cuối:** 2FA **TẮT** với `roles=[administrator]`, `remember=30` (tick 1 ô là bật — cố ý để TẮT vì các script đo có đăng nhập sẽ không chạy được khi 2FA bật); tài khoản thử `cpverify` đã xoá. | plugin: `includes/two-factor.php` (**mới**), `pl-tien-ich-tungleads.php` (require + register option + UI + **v1.2.0**), `README.md`; gốc repo: `docs/measure/2fa-e2e.mjs` + `2fa-api-check.sh` (**mới**) + README | ✅ |
| 2026-09-18 07:59 (giờ thật) | cline | **CHILD THEME CP3.12 — Ô “MIÊU TẢ” CỦA DANH MỤC SẢN PHẨM THÀNH TRÌNH SOẠN THẢO ĐẦY ĐỦ (như trang thêm bài viết)** (Tùng yêu cầu). **Code:** `inc/admin-editor.php` (**mới**, 176 dòng) — `cp_term_editor_taxonomies()` (mặc định `product_cat`, mở rộng qua filter) · `cp_term_editor_screen()` (`''|'add'|'edit'` theo `$pagenow` + `taxonomy`) · `cp_term_editor_help()` · `cp_term_editor_field()` (in `wp_editor()` **sau** field core, giữ `name="description"`; ẩn field core bằng `<style>.term-description-wrap{display:none!important}</style>`; markup theo màn: `<div class="form-field cp-term-editor-wrap">` cho THÊM / `<tr>` cho SỬA) · hook `product_cat_add_form_fields` + `product_cat_edit_form_fields`; `assets/admin-term-editor.js` (**mới**) — `disableCoreField()` (chặn ô core gửi dữ liệu) + `tinymce.triggerSave()` ở **pha capture trên `document`** cho cả `click` và `submit`; `functions.php` (require); `assets/caophat.css` (khối CP3.12 cuối file: mở lại `list-style` + cỡ tiêu đề trong `.cp-shop-desc__body`). **BA CÁI BẪY đã dính & đã xử lý** (đo trước khi code + bằng script): (1) **`wp-admin/js/tags.js` ở màn THÊM không submit form** — nó bắt **click `#submit`** rồi `$('#addtag').serialize()`; bắt `submit` là không kịp ⇒ nội dung editor **không được gửi** (lần đầu test: DB chỉ có text thô, không thẻ); (2) **core gắn `wp_filter_kses` vào `pre_term_description`** (`default-filters.php:49`) = chỉ cho thẻ INLINE ⇒ **bóc sạch `<p>/<ul>/<h3>`** khi lưu (round-trip wp-cli: `<p>Doan 1</p><ul><li>Muc A</li></ul><strong>Dam</strong><script>alert(1)</script>` ⇒ `Doan 1Muc A<strong>Dam</strong>alert(1)`) ⇒ đổi sang `wp_kses_post` (đúng bộ thẻ bài viết, vẫn chặn `<script>`, người sửa mô tả danh mục cũng là người viết được bài ⇒ cùng mức tin cậy); (3) **màn SỬA core cũng đặt `id="description"`** cho textarea của nó ⇒ `wp_editor(..., 'description')` bị **trùng id**, TinyMCE init nhầm vào ô core (đang ẩn + `disabled`) ⇒ editor rỗng, **không gõ được**; đổi id editor thành **`cp-description`** (dữ liệu gửi đi do `name` quyết định ⇒ vẫn `name="description"`) + label `for="cp-description"`. Field core: ẩn CSS + `disabled` bằng JS ⇒ kiểm bằng DOM: `soFieldGuiDuoc = 1` (chỉ 1 ô `description` được gửi). **TEST (Playwright + wp-cli + curl, tài khoản admin tạm `cpverify` đã xoá sau khi xong):** • **cấu hình editor giống hệt trang bài viết**: `toolbar1`/`toolbar2`/`plugins`/`block_formats` khớp — khác DUY NHẤT nút `dfw` (chế độ viết tập trung) vì WP chỉ thêm nút đó khi `$set['_content_editor_dfw']` (dành cho editor `content` của bài viết, không phải setting của `wp_editor()`) • màn THÊM: payload AJAX `action=add-tag` chứa `description=<p>Cửa phòng ngủ </p><p>Cách âm tốt</p><p>Dễ vệ sinh</p>` ✓ + term mới lưu đúng HTML ✓ (test lại sau khi vá kses: `<p> <ul><li> <h3> <strong> <a>` giữ nguyên, `<script>` bị bóc) • màn SỬA: editor nạp đúng HTML cũ ✓, bấm **“Cập nhật”** ⇒ DB có `…<ul><li>Y mot</li><li>Y hai</li></ul>` ✓ • **cơ chế đồng bộ**: đặt nội dung rồi phát sự kiện `submit` ⇒ textarea nhận `<p>Dong 1 <strong>dam</strong></p>\n<ul>\n\t<li>Muc A</li>\n</ul>` (trước khi submit là rỗng) ✓ • **front-end** `/danh-muc/cp-test-editor-3-12/`: `.cp-shop-desc__body > .term-description` in ra `<p> <strong> <h3> <ul> <li> <a>` (không escape) ✓; đo computed @1440 & @390: `list-style: disc`, `padding-left: 22px`, `h3 18px`, `p margin-bottom 12px`, **tràn ngang = 0** ✓ (trước khi thêm CSS: `list-style: none`, `padding-left: 0` do reset `.cp ul` dòng 49) • **smoke 24/24** • `php -l` + `node --check` sạch • **đã xoá** term test + user tạm (số term `product_cat` về đúng như trước). | child: `inc/admin-editor.php` (**mới**), `assets/admin-term-editor.js` (**mới**), `functions.php`, `assets/caophat.css`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`; gốc repo: `docs/measure/term-editor-check.mjs` (**mới**) + README | ✅ |
| 2026-09-18 08:06 (giờ thật) | cline | **CSS (làn A) — icon khối “Thông số kỹ thuật” `.cp-spec__ic` 40×40 → 30×30** (Tùng yêu cầu, chỉ đổi kích thước). `assets/caophat.css` dòng 1651. **Đo (Playwright, trang `/san-pham/bang-gia-cua-nhua-pvc-cao-cap/`):** icon **30×30** @1440 **và** @390 (trước 40×40); hàng `.cp-spec__row` **72 → 62px**; glyph SVG giữ 20×20 (khai báo trong `inc/woocommerce.php`, padding 5px mỗi bên); `border-radius 10px` · `padding 16px 18px` · `gap 14px` **không đổi**; **tràn ngang = 0** cả 2 khổ; **smoke 24/24**. Không đụng PHP/JS (`php -l`/`node --check` không áp dụng). | child: `assets/caophat.css` | ✅ |
| 2026-09-18 10:48 (giờ thật) | cline | **PLUGIN — XOÁ EMOJI `⚠️` KHỎI 2 CHUỖI SETTINGS (ảnh vỡ Twemoji jsdelivr)** (Tùng: *“Xoá đường dẫn icon https://cdn.jsdelivr.net/gh/jdecked/twemoji@17.0.1/assets/26a0.svg -> trong plugin. Lần sau đừng thêm vào vì link bị hỏng”*). **Sửa:** `includes/login-path.php` — bỏ `⚠️ ` ở đầu **2 câu** trong mục Settings “Đường dẫn đăng nhập”: dòng 322 (`'Tính năng này CẦN “Permalink đẹp”…'`) và dòng 356 (`'LƯU LẠI đường dẫn này (bookmark)…'`) — câu chữ + màu `#b32d2e`/`font-weight:600` giữ nguyên nên vẫn nổi bật. `README.md` bẫy #5 viết lại thành cơ chế đầy đủ + cách kiểm. **Cơ chế ĐO ĐƯỢC (đây là gốc rễ, KHÔNG phải plugin):** core nạp twemoji ở admin với `_wpemojiSettings.svgUrl = false` (theme CHA `tungleads-theme/src/Features/Performance.php` đặt `emoji_svg_url` = `__return_false`) ⇒ twemoji rơi về base mặc định của thư viện = `cdn.jsdelivr.net/gh/jdecked/twemoji@17.0.1/assets/` và build `<base><code>.svg` ⇒ **404** (curl xác nhận: `assets/26a0.svg` = **404**, `assets/72x72/26a0.png` = 200, `s.w.org/.../svg/26a0.svg` = 200); core mặc định THẬT là `s.w.org/images/core/emoji/17.0.2/svg/` (`formatting.php:6050`). Theme cha gỡ emoji ở `wp_head` + `admin_print_scripts` khi `init`, **nhưng `wp-admin/includes/admin-filters.php:59` gắn lại sau `init`** ⇒ **front-end sạch, admin vẫn chạy twemoji** (đo: `coScriptEmoji: true` ở Settings/form bài viết/list SP; `false` ở trang chủ + trang SP). **TEST:** `docs/measure/emoji-check.mjs` ⇒ Settings (ADMIN): `soImgCDN 0`, `soImgEmoji 0`; front-end: `0/0`; `docs/measure/emoji-where.mjs` ⇒ `trongKhoiPlugin: []` (ký tự duy nhất còn lại là U+1FAC8 trong NỘI DUNG `<script>` của core — không render). Quét lại toàn plugin: **0 emoji trong code** (chỉ còn trong `README.md` = tài liệu, không render). Lưu ý cho phiên sau: **khi sửa chuỗi hiển thị thì chạy lại 2 script emoji** — lần này lọt vì thêm `⚠️` SAU khi script đã chạy. | plugin: `includes/login-path.php`, `README.md`; child: `.ai/WORKLOG.md` | ✅ |
| 2026-09-18 10:58 (giờ thật) | cline | **THEME CHA `tungleads-theme` — FIX GỐC RỄ ẢNH VỠ EMOJI TWEMOJI (P2.3)** (Tùng chốt phương án **A: sửa ở theme cha**). `src/Features/Performance.php`: (1) `disableEmoji()` nay chạy ở **cả `init` LẪN `admin_init`** — vì hook `print_emoji_detection_script` của admin nằm ở `wp-admin/includes/admin-filters.php:59`, file này nạp **SAU** `init` nên **gắn lại** hook vừa gỡ (đó là lý do front-end sạch mà admin vẫn nạp twemoji); (2) gỡ thêm `embed_head` + `admin_enqueue_scripts` → `wp_enqueue_emoji_styles`; (3) **bỏ `add_filter('emoji_svg_url','__return_false')`** — URL rỗng làm thư viện twemoji rơi về base mặc định `cdn.jsdelivr.net/gh/jdecked/twemoji@17.0.1/assets/` rồi build `<base><code>.svg` ⇒ **404** ⇒ ảnh vỡ. **ĐO (Playwright, tài khoản admin tạm đã xoá):** 4 màn admin (Settings plugin · soạn bài viết · danh sách SP · người dùng) + front-end ⇒ `_wpemojiSettings` **không còn**, `window.twemoji` = `undefined`, `soImgCDN` = 0; **tiêm `⚠️ ✅ 🔴 ★` vào DOM + chờ 1,5s ⇒ 0 `<img>`** (vẫn là ký tự) — trước fix chính chỗ này sinh ảnh vỡ. `php -l` + `vendor/bin/phpcs` sạch; **smoke 24/24**. Tài liệu: `CHANGELOG.md` mục “Sửa lỗi — gỡ emoji/twemoji triệt để (P2.3)” + `.ai/FEATURE_MAP.md` (dòng P2.3) + `.ai/WORKLOG.md` §1/§2 của **cả parent lẫn child**. ⚠️ Lưu ý deploy: fix này áp cho **MỌI child theme** ⇒ phải ship parent. | parent: `src/Features/Performance.php`, `CHANGELOG.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md`; child: `.ai/WORKLOG.md` | ✅ |
| 2026-09-18 11:56 (giờ thật) | cline | **CSS (làn A) — `.cp-contact-btn` thêm `padding: 10px`** (Tùng yêu cầu; rule này TRƯỚC ĐÓ không có padding, dùng `height: 48px` + flex center). `assets/caophat.css:1506` + chú thích 3 dòng ghi rõ “hiện không đổi hình, giữ để sẵn khi bỏ height”. **Đo (Playwright, trang `/san-pham/bang-gia-cua-nhua-pvc-cao-cap/`):** computed `padding` = **10px** ✓ nhưng kích thước nút **Y HỆT** (1440: 232×48 · 1280: 192×48 (nhãn “Gọi ngay 0834.021.021” 2 dòng) · 1024: 216×48 · 768: 342×48 · 390: 153×48); số dòng chữ không đổi; **tràn ngang 0** ở mọi khổ; **smoke 24/24**. Làn A: không đụng `CLAUDE.md`/`FEATURE_MAP`. | child: `assets/caophat.css` | ✅ |
































| 2026-09-18 12:20 (giờ thật) | cline | **Tối ưu tài liệu: `CLAUDE.md` → CHỈ MỤC CP** (113,9 KB → 6,9 KB, -94%); chi tiết dồn về FEATURE_MAP; thêm hạn mức chống phình ở AGENTS.md. Chi tiết: FEATURE_MAP `### Tối ưu tài liệu 2026-09-18`. | `CLAUDE.md`, `AGENTS.md`, FEATURE_MAP, WORKLOG | ✅ |
| 2026-09-18 12:50 (giờ thật) | cline | **Rotate §2** (144 dòng cũ → `.ai/WORKLOG-archive-2026-Q3.md`, nguyên văn; §2 còn 20 dòng) + **tách 4 hàng CP > 3 KB** trong FEATURE_MAP xuống mục `### CPx.y`. | WORKLOG · archive · FEATURE_MAP · AGENTS | ✅ |
