# WORKLOG — bàn giao công việc giữa các model

**Repo:** `tungleads-theme-cp` (child theme caophat.vn) · nhánh `main` · không có remote
**Parent:** `tungleads-theme` (pin version bằng git tag, xem dòng `Base:` trong `README.md`)

> Chỉ dùng **2 model: `claude` (Claude Code) và `deepseek` (DeepSeek)** — **chạy tuần tự, không đồng thời**.
> **Không cần script, không cần cài gì** — chỉ đọc và sửa markdown.
> Luật chi tiết ở `CLAUDE.md` (Claude Code) và `AGENTS.md` (DeepSeek), mục "Phối hợp giữa các model".
> P-index của repo này dùng tiền tố **`CP`** (`CP1`–`CP4`) — bảng nghĩa ở `CLAUDE.md`.
>
> 1. **Đầu phiên:** đọc `§1 ĐANG LÀM` + 10 dòng cuối của `§2`.
> 2. **Bắt đầu việc:** cập nhật `§1` (model, việc, file sẽ chạm, trạng thái).
> 3. **Hết việc/phiên:** cập nhật lại `§1` (xong / dang dở + việc tiếp theo) rồi ghi 1 dòng vào `§2`.
> 4. `§1` **ghi đè** (chỉ giữ khối mới nhất) · `§2` **chỉ ghi thêm**, không sửa dòng cũ.

---

## §1 ĐANG LÀM (bàn giao — ghi đè mỗi phiên, chỉ giữ 1 khối)

- **Model:** deepseek
- **Việc:** **CP1.5 — nút mở menu con trên mobile: 30×30, đúng tâm hàng, vùng bấm 44px** (yêu cầu Tùng 23:45 → xử lý 2 đề xuất ở 23:50) + 2 quyết định Tùng chốt 23:55 (giữ 50/50 nhãn 2 dòng · giữ icon 35px) + hover `.cp-btn-accent` (Tùng giao quyết → GIỮ `#333333`) + plugin `tl-site-caophat` đã tạo git repo riêng (phương án A)
- **File đang chạm:** `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` + repo ngoài: plugin `tl-site-caophat` (repo mới), `.ai/WORKLOG.md` + `.gitignore` của repo gốc
- **Trạng thái:** XONG — **working tree SẠCH cả 2 repo** (child theme + repo gốc). Các commit trong phiên (đều LOCAL, KHÔNG remote): `c60a6e5`, `cc1820d`, `4d35874`, `5665e17`, `33ed5e9`, `81a7eae`, `f96a497`, `5236985` + commit cuối phiên. Plugin `tl-site-caophat` giờ có repo riêng (baseline `1da7fd6`). Chưa deploy production (`deploy-caophat.sh --go` chờ Tùng yêu cầu). Việc cần Tùng duyệt: xem mục \"Việc tiếp theo (còn lại)\" ở cuối §1.
- **Nguyên nhân GỐC (khác hẳn lần trước):** panel mega khi ĐANG ĐÓNG vẫn bắt chuột → `li:hover = true` → panel tự mở. Thủ phạm: khối reset cấp 3 `.cp-nav .sub-menu--mega > li > .sub-menu { position: static; visibility: visible; ... }` — cấp 3 cũng mang class `.sub-menu` nên `visibility: visible` ở đây ĐÈ rule gốc `visibility: hidden` của panel, mà `opacity: 0` KHÔNG chặn hit-test trong Chromium → toàn bộ danh sách cấp 3 vẫn nhận chuột. Panel mega là dải ngang full chiều rộng × cao ~422–620px → chuột lướt vào bất kỳ đâu trong vùng đó là menu mở.
- **Bằng chứng đo được:** đường di chuột thật 34 bước từ giữa trang lên chữ → panel mở ở bước 8 tại (747,493), cách chữ "Sản Phẩm" (887,111) **95px sang trái / 400px xuống dưới** (`trong_chu: false`).
- **Đã sửa:** (1) bỏ `visibility: visible` khỏi khối reset luôn-áp của cấp 3 → chuyển sang rule CÓ ĐIỀU KIỆN `li:hover`/`li:has(:focus-visible)`; (2) `pointer-events: none` cho `.cp-nav .sub-menu` (panel đóng) + `auto` ở rule reveal; (3) thêm "cầu" hover `li.cp-mega:hover > .sub-menu::before { top:-28px; height:28px }` lấp khoảng hở ~20px giữa link và panel (panel neo vào header); (4) **bật lại `pointer-events: auto` cho panel mobile** (mobile mở bằng `.is-open`, không qua `:hover`) — thiếu dòng này link menu con trên mobile KHÔNG bấm được.
- **Kiểm chứng sau khi sửa (1440×900):** teleport vào giữa hộp panel (713,365) và góc trái (80,546) khi panel đóng → phần tử dưới chuột là nội dung trang (`div.cp-container`, `p`), panel vẫn `hidden`; lệch 10px quanh chữ (trái/phải/trên/dưới) → không mở; hover đúng chữ → 80ms còn hidden, 480ms visible (đúng trễ 120ms); **rê CHẬM 4px/bước xuống panel → panel vẫn mở** (cầu hover chạy); rời ra ngoài → hidden; click link cấp 3 trong panel → điều hướng `/danh-muc/cua-go-phong-ngu/`; mobile 390×844: burger → toggle mở menu con (`pointer-events: auto`) → click link con điều hướng OK. CSS cân bằng ngoặc 722/722.
- **BỔ SUNG (18:20) — CP1.4b: cắt đuôi "…" cho tên cấp 3 quá dài (Tùng báo):** link cấp 3 có `white-space: nowrap`, tên dài hơn bề ngang cột (cột ~195px) tràn `overflow: visible` sang cột bên → thêm `overflow: hidden; text-overflow: ellipsis`. Đo trước/sau: **4/35 link bị cắt** (dài nhất "Cửa gỗ công nghiệp MDF Melamine" 242px) → **0 link tràn khỏi cột**; ảnh chụp panel xác nhận hiện "…" và lưới cột thẳng hàng. Mobile 390px: 44 link con đều nằm trong panel (`nav.scrollWidth` = 390) → không cần sửa.
- **BỔ SUNG (18:40) — CP1.4b: bỏ vạch dọc ở cột đầu HÀNG MỚI (Tùng báo):** khi panel xuống dòng, cột đầu của hàng 2+ vẫn dính `border-left` + thụt lề 22px (việc trước chỉ bỏ cho `:first-child`). Grid `auto-fit` không cho CSS biết cột nào xuống hàng → `nav-menu.js` thêm `markRowStarts()` đo `offsetTop` từng cột, gắn `.cp-row-start`; CSS bỏ `padding-left` + `border-left` cho class này (rule đặt sau `:first-child` cùng độ ưu tiên). Gọi lại khi `resize` + sau `document.fonts.ready`. Đo ở 1440: 2 hàng × 5 cột, mỗi hàng đúng 1 cột `border=0/pad=0` tại `x=20`, 4 cột còn lại `border=1`; resize 1200 → 4 cột/hàng, đánh dấu lại đúng; `node --check` OK, CSS 723/723.
- **BỔ SUNG (19:05) — CP3.1/CP3.2: màu giá trong card (Tùng báo):** giá trong card danh mục + card liên quan đang là **olive `rgb(149,142,9)`, 13.71px, gạch chân** thay vì cam accent như `.cp-price` trang chủ. Nguyên nhân: rule của WooCommerce `.woocommerce:where(body:not(.woocommerce-uses-block-theme)) ul.products li.product .price` (**0,4,2**) thắng rule child 3-class (0,3,2) → ép màu olive + `font-size:.857em; font-weight:400`; `ins` còn bị gạch chân (mặc định trình duyệt, `.cp-price` đã tắt). Sửa: nâng selector lên 5 class -> NGHIỆM THU: giá hiện tại `rgb(200,71,31)` 17px/700 không gạch chân, giá gốc muted 13px + gạch ngang (card danh mục + card liên quan + hộp giá chi tiết SP giữ nguyên accent 20px).
- **BỔ SUNG (19:40) — 5 chỉnh CSS + bỏ nhãn danh mục khỏi card (yêu cầu Tùng):** (1) `.cp-single-side` sticky `top: 120px → 100px`; (2) `.cp-topbar .cp-container` `min-height: 40px → 30px` (đo thực tế topbar cao 30px); (3) `.cp-btn-accent` nền `--cp-accent` → **`#000000`** + hover `--cp-accent-dark` → `#333333` (nếu giữ hover cam thì đen → cam lệch tông; Tùng chưa chốt, sửa 1 dòng nếu muốn kiểu khác); (4) + (5) thêm `.woocommerce-message { border-top-color: #fbaf02 }` + `.woocommerce-message::before { color: #fbaf02 }` — đo bằng notice thật (thêm SP #5731 vào giỏ → mở /cart/): viền trên `rgb(251,175,2)` 3px + icon `rgb(251,175,2)` ✅ (lưu ý: `content` computed rỗng nên icon không hiển thị, parent theme đã bỏ icon font). **Bỏ nhãn danh mục trong card:** xoá `.cp-card-cat` khỏi `cp_product_card()` (card trang chủ) + `cp_loop_body_open()` (loop shop/related); GIỮ ở trang chi tiết SP (`cp_single_cat_label`) vì không phải card. Đo: trang chủ 0 / danh mục 0 / trang chi tiết SP 1 nhãn ✅; `php -l` 2 file sạch, CSS 727/727.
- **BỔ SUNG (19:55) — khôi phục nhãn nút mua (yêu cầu Tùng):** bỏ hẳn filter `woocommerce_product_single_add_to_cart_text` trong `inc/woocommerce.php` (filter này trước đây rút ngắn nhãn thành "Thêm giỏ hàng") → nút giờ lấy mặc định WooCommerce "Thêm vào giỏ hàng", CSS `text-transform: uppercase` hiển thị "THÊM VÀO GIỎ HÀNG". Đo: `innerText` = "THÊM VÀO GIỎ HÀNG", `php -l` sạch, không còn chuỗi "Thêm giỏ hàng" trong theme. **Tác dụng phụ cần chốt:** nút vàng cần ~195px (đo bằng `white-space: nowrap`) nhưng cột grid chỉ 152px (`1fr 2fr` trong `form.cart`) → nhãn xuống 2 dòng; nút MUA HÀNG 304px vẫn 1 dòng. Chờ Tùng chọn: giữ 2 dòng / đổi tỉ lệ cột (1.5fr 2fr → 195px) / cho 2 nút bằng nhau (1fr 1fr → 222px).
- **Việc tiếp theo:** commit (working tree gộp ~25 nhóm), đơn test #11511–#11513 + #11515 + #11517, chữ `.cp-cta` trắng trên nền vàng, FAB che số tiền trang hoàn tất đơn, vị trí menu `footer` TRỐNG, header mobile 3 hàng ở ≤480px, notice "đã thêm vào giỏ hàng" hiện 2 LẦN trên trang giỏ, **nút "THÊM VÀO GIỎ HÀNG" đang xuống 2 dòng — chờ Tùng chốt cách cân lại cột**, hover `.cp-btn-accent` nền đen.
- **BỔ SUNG (20:05) — xoá nút "Xem chi tiết" trên card (yêu cầu Tùng):** bỏ 2 chỗ in nút — `cp_product_card()` (`functions.php`, card trang chủ) và filter `woocommerce_loop_add_to_cart_link` → `cp_loop_detail_link()` (`inc/woocommerce.php`, card shop/liên quan). ⚠️ Lưu ý đã xử lý: bỏ filter KHÔNG đủ, nếu chỉ bỏ filter thì nút "Thêm vào giỏ hàng" mặc định của WooCommerce sẽ hiện ra → thêm `remove_action('woocommerce_after_shop_loop_item','woocommerce_template_loop_add_to_cart',10)`. Xoá luôn CSS chết `.cp-card-btn` (3 chỗ). Đo: trang chủ 8 card / 0 nút, danh mục 15 card / 0 nút, card liên quan 0 nút; mỗi card còn 2 link (ảnh + tiêu đề); giá vẫn canh đáy đều 26px; 8 card trang chủ cùng cao 521px; `php -l` sạch, CSS 723/723.
- **BỔ SUNG (20:20) — CP1.6: chuyển ô search vào trong `.cp-header-cta` (yêu cầu Tùng):** `header.php` — form `.cp-search` thành CON ĐẦU TIÊN của `.cp-header-cta` (giữ nguyên vị trí bên trái hotline). ⚠️ Tài liệu cũ ghi rõ "KHÔNG nhét vào cta" vì sẽ không wrap xuống hàng riêng được → đã xử lý: `.cp-header-cta` ở ≤1024px đổi `margin-left: auto` → `flex: 1 1 100%; flex-wrap: wrap; justify-content: flex-end` (cta chiếm hết hàng rồi tự wrap), `.cp-search` bỏ `margin-left: 20px` (dùng `gap: 14px` của cta). Đo 8 mức bề rộng (1440→390): desktop 1 hàng (search 230px, header 77px), ≤1024 search xuống hàng riêng full chiều ngang rộng bằng cta (969/845px, header 155px), ≤768 3 hàng (header 163px), KHÔNG tràn ngang ở mọi mức. Test chức năng: gõ "cửa gỗ" + Enter → `/?s=cửa+gỗ&post_type=product`, 20 kết quả, ô search vẫn trong cta và giữ giá trị đã gõ.
- **BỔ SUNG (20:30) — `.cp-nav { padding-right: 20px }` (yêu cầu Tùng):** thêm vào rule base `.cp-nav` → tạo 20px giữa mục menu cuối và khối `.cp-header-cta`. Đo: mục "Liên hệ" phải 918 → search trái 938 = **20px**; header vẫn 77px, không tràn ngang; **panel mega vẫn full chiều rộng** (x=0, 1425px — không bị đẩy 20px vì panel neo vào `.cp-header`); panel mobile KHÔNG đổi (đo 390px: `padding = 6px 0px 10px 0px` — shorthand của khối `≤768px` ghi đè).
- **BỔ SUNG (20:45) — nền autofill của Chrome (yêu cầu Tùng "xoá background-color trong `input:-internal-autofill-selected`"):** ⚠️ Kiểm chứng trước khi làm — rule đó **KHÔNG có trong file nào của mình**: grep child theme + parent theme + plugin = 0, `wp_get_custom_css()` cả 2 theme = rỗng, quét DB (`wp_options LIKE %autofill%`) = 0. Đây là rule **UA stylesheet của Chrome** (DevTools ghi "user agent stylesheet") → không xoá được; author CSS cũng không hợp lệ với selector `:-internal-*`. Thử ép trạng thái bằng CDP `CSS.forcePseudoState` (`-webkit-autofill` và `-internal-autofill-selected`) → computed background KHÔNG đổi → xác nhận Chrome vẽ nền này ở tầng nội bộ, không qua cascade. **Đã làm:** thêm khối đè `input:-webkit-autofill` (+ `:hover/:focus/:active`) dùng `box-shadow: 0 0 0 1000px #fff inset !important` + `-webkit-text-fill-color: var(--cp-text) !important` (đặt trước mục "WordPress adaptations"). Đã kiểm: rule parse OK, có mặt trong `document.styleSheets` (`caophat.css`) đúng selector + khai báo, CSS 725/725. **CHƯA kiểm được bằng máy** vì trình duyệt test không tạo được autofill thật → cần Tùng test tay (xem mục cần test).
- **BỔ SUNG (21:00) — nền xanh khi gõ trong ô search (Tùng gửi ảnh):** màu `#e8f0fe` = đúng màu Chrome phủ khi **autofill / gợi ý giá trị đã gõ** (cùng gốc với rule `input:-internal-autofill-selected` ở task 20:45 — UA stylesheet, không xoá được). Xử lý 2 lớp: (1) `header.php`: ô search thêm `autocomplete="off" autocapitalize="off" spellcheck="false"` → chặn GỐC (Chrome không gợi ý/điền lịch sử vào ô này); (2) CSS: rule đè riêng cho ô search với màu pill `--cp-surface` (không dùng `#fff` như rule chung) — đúng vì ô search trong suốt nằm trên pill kem, nếu phủ `#fff` sẽ thấy 1 ô trắng trong pill. Đã kiểm: `el.autocomplete === 'off'` ✅, rule parse OK trong `caophat.css` ✅, trạng thái thường không đổi (input trong suốt, pill `rgb(246,244,241)`) ✅, CSS 729/729. **Chưa verify được trạng thái autofill thật** (CDP không ép được `-webkit-autofill`) → cần Tùng hard-reload + test tay.
- **BỔ SUNG (21:15) — ẩn nút × trong ô search (yêu cầu Tùng):** nút × là `::-webkit-search-cancel-button` của Chrome/Safari. ⚠️ Từ Chrome 83, pseudo này chỉ style/ẩn được khi input có `appearance: none` → đã thêm `-webkit-appearance: none; appearance: none` vào rule `.cp-search input[type="search"]` + 4 pseudo `::-webkit-search-cancel-button/-decoration/-results-button/-results-decoration { -webkit-appearance: none; appearance: none; display: none }`. Kiểm: `getComputedStyle(input).appearance = none`, ảnh chụp khi ô có chữ "cửa gỗ công nghiệp" → **không còn ×**, kích thước không đổi (input 186×38 / pill 230×40, padding `0 4px 0 14px`), CSS 730/730.
- **BỔ SUNG (22:00) — loạt fix theo review của Tùng (đã duyệt):** (1) **a11y màu**: dải CTA đổi nền `--cp-primary*` → `--cp-accent-dark`→`--cp-accent` để chữ trắng đạt AA (4.8–6.6:1; nền vàng cũ chỉ 1.9–3.0:1) — giữ chữ trắng theo lựa chọn của Tùng; thêm focus ring 2px vàng cho `a`/`button:focus-visible`; thêm `@media (prefers-reduced-motion: reduce)`. (2) **tap target ≥44px**: `.cp-hotline` `min-height:44px`, `.cp-cart-link` 40→44px, `.cp-burger` 44px (≤768px), ô search 38→44px (≤1024px). (3) **hiệu năng ảnh**: `hero.png` **986KB → hero.webp 37KB** + `hero-652.webp` 18KB (có `srcset`/`sizes`/`fetchpriority=high`); `door-hdf.png` **656KB → door-hdf.webp 24KB**. (4) **`alt` ảnh**: filter `wp_get_attachment_image_attributes` — kiểm tra DB cho thấy thư viện media của SP mẫu có `_wp_attachment_image_alt` **RỖNG** nên filter chính là thứ cấp alt = tên SP (15/15 ảnh card); thêm filter `the_content` dùng `WP_HTML_Tag_Processor` gán `alt=""` cho 28 ảnh trong mô tả danh mục → **46/46 ảnh có alt** (trước: 29 thiếu). (5) **CP3.2**: `form.cart` `2fr 1fr` → `minmax(0,2fr) minmax(200px,1fr)` → nút giỏ **200×71, 1 dòng** (trước 152px/2 dòng). (6) **CP3.6**: `.cp-thankyou-section` chừa đáy 120px → FAB cách nội dung 9px, không che "Tổng".
- **ĐÍNH CHÍNH:** "thông báo thêm-giỏ hiện 2 lần ở trang giỏ" **KHÔNG phải lỗi site** — do lúc test tôi thêm SP vào giỏ 2 lần nên WooCommerce xếp 2 notice; test lại sạch chỉ còn **1** notice. Bỏ khỏi danh sách lỗi (tôi đã báo sai).
- **BỔ SUNG (23:20) — CP1.2b: header mobile 1 hàng (yêu cầu Tùng):** ≤768px từ **3 hàng/163px → 1 hàng/65px**: `[burger trái][logo giữa][icon search][icon giỏ]`, ẩn hotline. `header.php`: burger thành CON ĐẦU TIÊN của `.cp-container` (desktop vẫn `display: none`); thêm nút `.cp-search-toggle` (icon kính lúp, `aria-expanded`/`aria-controls="cp-search"`) ngay trước icon giỏ; `id="cp-search"` cho form. `nav-menu.js` bước 4: bấm nút → gắn `.cp-header.is-search-open` + focus input; đóng bằng Esc (trả focus về nút) / click ngoài / bấm lại nút. CSS: khi mở, ô search trượt xuống **dải riêng full chiều ngang** (`position: absolute; top: 100%`) — KHÔNG dùng `flex-wrap` (đo được: cta bị đẩy xuống hàng mới → header 111px, logo lệch). **3 bẫy đã đo & sửa:** (1) căn giữa logo bằng `position: absolute; left: 50%` → logo 188px ĐÈ icon search (vùng 257..282 ở 390px) → icon KHÔNG bấm được (`elementFromPoint` = img, Playwright click timeout) → chuyển sang căn giữa theo KHOẢNG TRỐNG (`.cp-logo { flex: 1 1 auto; justify-content: center }`); (2) rule `.cp-header .custom-logo { width: 250px }` nằm CUỐI file cùng specificity (0,2,0) ĐÈ rule mobile → `max-width: 118px` chưa bao giờ chạy (đo 188×32) → đổi selector thành `.cp-header .cp-logo .custom-logo` (0,3,0); (3) clamp cả `max-width: 100%` + `max-height` → box 169×26 MÉO tỉ lệ (6.5 vs 5.44 gốc) → chỉ dùng `max-width: min(160px, 100%)` + `object-fit: contain`. **Đo sau khi sửa:** 320/360/390/768 → logo 99×18 / 139×26 / 160×29 / 160×29 (đúng tỉ lệ 5.44), khe hở 12px mỗi bên, header 65px 1 hàng, 0 lỗi JS; bấm icon search → focus `cp-search-input`, dải search rộng 100% chiều ngang; gõ "cửa gỗ" + Enter → `/?s=cửa+gỗ&post_type=product`, 20 kết quả; 769/1024/1440 KHÔNG đổi (161/161/77px, hotline hiện, burger ẩn); burger vẫn mở panel nav (3 mục). ⚠️ Logo căn giữa theo khoảng trống nên lệch tâm container **−27px** (muốn đúng tâm thì logo phải ≤110px ở 390px — vì bên phải 2 icon 98px còn bên trái burger 44px).
- **BỔ SUNG (23:35) — icon 35px + logo vào giữa (yêu cầu Tùng):** (1) `.cp-cart-link` + `.cp-search-toggle` `44×44 → **35×35px**` (đổi ở rule base → áp cả desktop; đo lại 1440: header vẫn 77px, hotline 46px, search 230px, giỏ 35×35). ⚠️ 35px < mức tap target 44px khuyến nghị (tôi đã nâng lên 44 ở bản review 22:00) — muốn quay lại 44 thì sửa 2 rule đó. (2) **Logo vào ĐÚNG TÂM:** cách cũ `justify-content: center` KHÔNG đủ — `a.custom-logo-link` bị KÉO GIÃN hết khung (đo `a` 361px / ảnh 160px) nên ảnh nằm ở ĐẦU khung, lệch **−157px** ở 768px (đo được); cộng thêm khối phải (2 icon 80px) ≠ burger (44px). Sửa 2 lớp: `.cp-logo { padding-left: 36px }` (= 80 − 44, bù phần lệch) + `margin: 0 auto` cho ảnh → đo 320/360/390/414/600/768: **lệch tâm = 0** hết; logo 81×15 / 121×22 / 151×28 / 160×29 (đúng tỉ lệ 5.44). Burger cách lề trái 20px, giỏ cách lề phải 20px (= padding container); `elementFromPoint` xác nhận 2 icon vẫn bấm được, 0 lỗi JS.
- **BỔ SUNG (23:45) — CP1.5: nút mở menu con trên mobile 38 → 30px (yêu cầu Tùng):** `.cp-nav__toggle` trong `@media (max-width: 768px)`: `top: 7px → 6px`, `width/height: 38px → 30px` (5 thuộc tính còn lại — `display: block`, `position: absolute`, `right: 8px`, `z-index: 2` — đã đúng sẵn). ⚠️ Kiểm tra bắt buộc: absolute chỉ neo đúng nếu `<li>` là `position: relative` — đã có ở rule base `.cp-nav li` (dòng 159); riêng `.cp-nav li.cp-mega { position: static }` nằm trong `@media (min-width: 769px)` → không ảnh hưởng mobile. **Đo ở 390px:** nút 30×30 tại `y = 6..36` trong `<li>` cao 52px (đóng) → hơi cao so với tâm hàng 5px (muốn đúng tâm thì `top: 11px`, chờ Tùng chốt); cách lề phải 8px; cách cuối chữ link 255px (không đè chữ); chevron 9px lệch tâm nút −3px (bản 38px lệch ~7px → thu nhỏ nút làm chevron cân hơn); bấm nút → `li.is-open` + submenu `display: block` (10 link) + chevron xoay đúng; 0 lỗi JS. ⚠️ 30px < 44px tap target → hàng cao 52px nên bấm trệch sẽ mở LINK CHA (điều hướng) thay vì mở menu con.
- **BỔ SUNG (23:50) — xử lý 2 đề xuất của nút toggle (Tùng duyệt):** (1) **Căn đúng tâm hàng:** `top: 6px → **11px**` → nút `y = 11..41` trong `<li>` cao 52px → lệch tâm **0** (trước lệch 5px). (2) **Vùng bấm 44px mà vẫn hiển thị 30px:** thêm lớp phủ trong suốt `.cp-nav__toggle::before { content:''; position:absolute; top/left:50%; width/height:44px; transform: translate(-50%,-50%) }` (nới 7px mỗi bên) — quét `elementFromPoint` thấy vùng bấm phủ **−6..+38px** quanh nút, đều 4 phía; **bấm 5px NGOÀI nút ở cả 4 phía đều MỞ MENU CON và KHÔNG điều hướng** (url vẫn `/`) → hết rủi ro bấm trệch bị mở link cha. ⚠️ Ghi chú kỹ thuật: `top` PHẢI là số CỐ ĐỊNH — không dùng `top: 50%` vì khi mở menu con `<li>` cao thêm cả submenu nên nút sẽ bị đẩy xuống giữa submenu. Vẫn giữ: cách lề phải 8px, cách cuối chữ link 255px, chevron lệch tâm −3px, mở menu con 10 link + bấm link con điều hướng OK, 0 lỗi JS.
- **Việc tiếp theo (còn lại):** (a) 54 request 404 khi tải trang — chủ yếu là bản `.webp` do **LiteSpeed sinh trên production** (local không có) + vài bản `-768x768` → phải đối chiếu trên production; (b) ảnh SP đang crop **vuông 400×400** nhưng card hiển thị 243×304 (dọc) → đổi size ảnh trong WooCommerce + regenerate thumbnails (thao tác trên production); (c) 4 ảnh PNG không dùng (~1.4MB) + bản gốc `hero.png`/`door-hdf.png` còn giữ → xoá cần Tùng đồng ý; (d) ~~header mobile 3 hàng 163px~~ **XONG 23:20 (CP1.2b)** · ~~logo lệch tâm container 27px~~ **XONG 23:35 → lệch tâm = 0**; (e) đơn test #11511–#11517 xoá cần Tùng đồng ý; (f) vị trí menu `footer` trống; (g) nút giỏ cao 71px (trước 56px) — muốn thấp lại thì giảm padding; **(h) 2 nút mua 50/50 xuống 2 dòng ở 769–1280px** (nặng nhất 1100px: mỗi nút 143px, nút đặt nhanh 3 dòng/79px) → **TÙNG CHỐT 2026-09-14: GIỮ NGUYÊN (phương án c)**, không sửa gì thêm; **(i) `.cp-cart-link` + `.cp-search-toggle` 35px < 44px tap target** → **TÙNG CHỐT 2026-09-14: GIỮ 35px**, không thêm lớp phủ bấm 44px (chấp nhận dưới chuẩn tap target); **(j) file logo bị cắt mất chữ "C"** (473×87) → nên gửi file logo mới; **(k) plugin `tl-site-caophat` KHÔNG được git quản lý** (repo gốc ignore cả `/wordpress/`) và child theme **không có remote** → chưa có backup ngoài máy; **(l) test tay** (máy không tự kiểm được): nền xanh autofill Chrome trong ô search sau khi hard-reload, và logo/header mobile trên máy thật.
- **BỔ SUNG (22:30) — nút đặt nhanh ở trang chi tiết SP (yêu cầu Tùng):** `.cp-buynow` đổi nhãn "Mua hàng" → **"Đặt hàng nhanh"** (CSS `text-transform: uppercase` → "ĐẶT HÀNG NHANH") và **bỏ dòng phụ** "Gọi điện xác nhận và giao hàng tận nơi" — xoá `<span>` trong markup + xoá CSS chết `.cp-buynow__txt span`. Kiểm: nhãn **1 dòng** (rect 148×19), nút **256×56** (trước 256×71 — cao 56 trở lại vì bớt 1 dòng), nút thêm-giỏ 200×56 cùng hàng; vẫn giữ `href="tel:0834021021"` (fallback JS tắt) + `data-cp-quick-order`; **bấm nút → popup "Đặt hàng nhanh" mở OK**; tên control đọc bởi screen reader gọn hơn (trước: "Mua hàng Gọi điện xác nhận…").
- **BỔ SUNG (22:45) — đồng bộ chiều cao 2 nút ở mobile (Tùng báo):** mobile đo được nút "THÊM VÀO GIỎ HÀNG" **56px** còn "ĐẶT HÀNG NHANH" **46px** → nguyên nhân: ở desktop 2 nút cùng hàng nên `form.cart { align-items: stretch }` tự cân bằng, nhưng ở mobile mỗi nút 1 hàng nên `stretch` vô tác dụng, mà chỉ nút giỏ có `min-height: 56px`. Sửa: thêm `min-height: 56px` cho `.cp-buynow`. Đo lại: mobile 390 → **303×56 = 303×56**; tablet 768 → **681×56 = 681×56**; desktop 1440 → giỏ 200×56 / đặt nhanh 256×56 cùng `y=736` (không đổi).
- **BỔ SUNG (23:00) — chia 50/50 chiều rộng 2 nút (yêu cầu Tùng):** `form.cart` → `grid-template-columns: repeat(2, minmax(0, 1fr))`. Đo 8 mức bề rộng (1440/1280/1100/1024/900/769/768/390): **2 nút luôn bằng nhau (chênh 0px)**. ⚠️ **Phát sinh cần Tùng chốt:** ở **1440px** mỗi nút 228px → nhãn 1 dòng ✅; ở **769–1280px** cột form chỉ ~296–437px → mỗi nút còn **143–212px**, nhỏ hơn mức nhãn "THÊM VÀO GIỎ HÀNG" cần (~195px) nên nhãn **xuống 2 dòng**, nút cao **59–79px** (nặng nhất ở 1100px: 143px, nút đặt nhanh 3 dòng, cao 79px). ≤768px xếp 1 cột full chiều ngang nên không bị. 3 cách xử lý: (a) xếp dọc khi < 1400px, (b) nới cột sidebar (tỉ lệ 9/3 → 8/4 hoặc 7/5) ở 1025–1439px, (c) chấp nhận nhãn 2 dòng.
- **Đã xong:** Đặt số CP3.5 trước khi code; refactor helper `cp_woo_page_wrap()` dùng chung cho CP3.4+CP3.5 (1 filter `the_content`); `cp_is_checkout_page()` loại trừ trang "đơn đã nhận"; lưới `#customer_details` (793px) | `#order_review` (400px); field grid 2 cột; select2 style; card đơn hàng; nút "Đặt hàng" accent 54px (thắng nền tím mặc định bằng selector 2 ID)
- **Kiểm chứng:** desktop — billing trái (x=45) / shipping phải (x=431) sau khi gán cột tường minh, input 48px bo 10px, nút 54px `rgb(200,71,31)`, `#payment` nền trong suốt, select2 cao 44px, không tràn ngang; mobile 390px — xếp dọc, input 48px, nút 54px, không tràn ngang; **hồi quy trang giỏ đạt** (hero + lưới 833/360 + thumb 72 + nhãn "Vận chuyển")
- **Bẫy mới (ghi FEATURE_MAP):** `.col-1`/`.col-2` bị ĐẢO CỘT dù DOM đúng thứ tự, `order: 0`, không float, `direction: ltr` → phải gán `grid-column` tường minh + reset `grid-column: 1` ở mobile.
- **ĐÍNH CHÍNH:** ghi chú cũ của tôi nói "font icon WooCommerce không được nạp" là **SAI** — đã kiểm tra: `WooCommerce.woff2/.woff/.ttf` **có** trong plugin; ô vuông là do glyph lỗi trên môi trường này. Đã sửa lại comment CSS cho đúng.
- **CẦN TÙNG XEM:** (1) breadcrumb 2 trang vẫn hiện slug tiếng Anh "Cart"/"Checkout" vì tiêu đề trang trong WP đang là tiếng Anh — muốn tôi đổi tiêu đề trang thành "Giỏ hàng"/"Thanh toán" (ảnh hưởng cả `<title>` SEO) thì nói; (2) logo header render đúng kích thước gốc 500×107 → header cao 108px (giống mọi trang khác, không phải lỗi mới)
- **BỔ SUNG (13:45):** `.col2-set` chuyển về **1 cột** — "Giao hàng đến một địa chỉ khác?" + "Ghi chú đơn hàng" xuống DƯỚI "Thông tin thanh toán" (yêu cầu Tùng); ô ghi chú cho chiếm hết chiều ngang (trước đó chỉ nửa card vì field của WooCommerce là `form-row notes`, không phải `form-row-wide`). Đo lại: `.col-2` nằm dưới `.col-1` (top 1319 > bottom 1295), cùng mép trái x=45; ghi chú 737/745px ở desktop, full width ở mobile
- **BỔ SUNG (13:55):** CP3.5 — chuyển tiêu đề "Đơn hàng của bạn" vào TRONG thẻ `#order_review` (WC 11 in heading RA NGOÀI thẻ, cách thẻ ~250px): CSS ẩn `#order_review_heading` gốc + PHP `cp_order_review_title()` in bản thật qua hook `woocommerce_checkout_order_review` prio 5. Kèm chỉnh `#order_review` sang `grid-row: 1 / span 2` để 2 card ngang mép trên. Đã kiểm chứng sống sót qua AJAX
- **BỔ SUNG (14:35):** CP3.5 — **rút gọn form "Thông tin thanh toán"** theo yêu cầu Tùng: gộp Họ+Tên thành "Họ và tên", bỏ Tên công ty / Quốc gia / Căn hộ-dãy phòng / Mã bưu điện, email không bắt buộc, ô ghi chú đồng bộ popup CP3.3 (nhãn "Ghi chú (không bắt buộc)" 13px + textarea padding 11px 13px cao 75px bo 10px). Còn 5 field + ghi chú.
- **ĐÃ DÍNH 2 BẪY + ĐÃ SỬA:** (1) bỏ `billing_country` làm `shipping_country` rỗng → WooCommerce chặn đơn ở `validate_checkout()` với lỗi "Xin hãy nhập một địa chỉ để tiếp tục." (bản copy billing→shipping trong `get_posted_data()` chạy TRƯỚC filter của ta) → phải tự điền `shipping_country` khi trống; (2) `.form-row-first/-last` bị WC đè → ô nửa hàng co còn 170px, và textarea bị đè bo góc 4px → phải dùng selector 3 class. Chi tiết ở `.ai/FEATURE_MAP.md` (mục CP3.5 — chi tiết).
- **Kiểm chứng:** desktop 5 hàng (Họ và tên 743 | SĐT 362 + Email 362 | Địa chỉ 743 | Thành phố 743 | Ghi chú 743); mobile 390px 1 cột 301px, không tràn ngang; **đặt hàng end-to-end bằng KHÁCH VÃNG LAI, email để trống → tạo được đơn thật #11515** (status on-hold, `bill_country=VN`, `ship_country=VN`, `bill_email` rỗng, tên/SĐT/địa chỉ/thành phố đúng) → xác nhận "email không bắt buộc" chạy đúng ở tầng server. Log tạm để chẩn đoán đã GỠ SẠCH (grep `CP-DEBUG` = 0).
- **BỔ SUNG (14:50): CP3.6 — Trang hoàn tất đơn hàng** (`/checkout/order-received/`): trước đây trang này không có container/hero (chữ sát mép, `article` rộng 1265px từ x=0). Đã bọc bằng `cp_woo_page_wrap( 'Đặt hàng thành công', 'cp-thankyou', ... )` với guard mới `cp_is_order_received_page()` (đặt TRƯỚC `cp_is_checkout_page()`), skin khối cảm ơn + 4 ô tóm tắt + card chi tiết + nút "Về trang chủ" (`woocommerce_thankyou` prio 30).
- **Chuyển trang sau khi đặt nhanh:** plugin `tl-site-caophat` trả thêm `redirect` = `$order->get_checkout_order_received_url()`; `assets/quick-order.js` hiện thông báo ~0,8s rồi `window.location.assign()` và **giữ nút khoá** (`redirecting`) để không tạo đơn trùng.
- **2 bẫy CSS mới của CP3.6 (đã sửa):** (1) clearfix `.woocommerce ul.order_details::before` (0,2,1) mạnh hơn rule của ta (0,2,0) → 2 pseudo-element thành grid item, đẩy 4 ô tóm tắt thành 3+1 (đo được tops `[134.99 ×3, 220.45]`) → phải dùng selector 3 class `.cp-thankyou .woocommerce-order-overview.order_details::before`; (2) dòng hướng dẫn COD "Trả tiền mặt khi giao hàng" là `<p>` KHÔNG class (hook `woocommerce_thankyou_cod`) → dùng selector cấu trúc `.cp-thankyou .woocommerce-order > p:not([class])`.
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~12 nhóm), đơn test #11511–#11513 + #11515 (checkout) + #11517 (popup, của tôi) + #11516 (Tùng tự test), bo góc popup 10px→5px, nhãn "Thị trấn / Thành phố", mục Giao hàng khi tick "địa chỉ khác" vẫn đủ trường, bảng chi tiết trên mobile 390px bị wrap nhãn nhiều dòng, **nút hotline nổi (FAB) che số tiền trên trang hoàn tất đơn**
- **BỔ SUNG (15:00):** Tùng báo chữ trong ô tóm tắt quá sát lề trái + chữ "TỔNG" phải sang phải → tìm ra bẫy độ ưu tiên `.woocommerce ul.order_details li` (0,2,2) đè rule của ta (0,2,1) → đổi selector sang `ul.woocommerce-order-overview.order_details li` (0,3,1) + reset tường minh (float/margin/padding/border/font-size) + sửa cả selector trong media query mobile; thêm `thead th:last-child { text-align: right }`. Đo lại: chữ cách mép ô 17px (trước 1px), 4 ô đều 296px, "TỔNG" thẳng cột với số tiền.
- **BỔ SUNG (15:35):** 3 thay đổi màu theo yêu cầu Tùng — (1) `.cp-cta` chữ → `#fff` (⚠️ nền còn vàng nên tương phản chỉ ~1.9–3.0:1, dưới WCAG AA 4.5:1; nếu đổi nền sang `--cp-accent` #c8471f thì đạt 4.79:1); (2) `.cp-topbar` nền `--cp-primary-dark` → `--cp-primary` + sửa hover `#fff` → `--cp-accent` (trắng trên vàng chỉ ~1.75:1); (3) `.cp-pagehero` padding `20px 0` + tông sáng (gradient trắng→kem).
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~14 nhóm), đơn test #11511–#11513 + #11515 + #11517, bo góc popup 10px→5px, nhãn "Thị trấn / Thành phố", mục Giao hàng khi tick "địa chỉ khác" vẫn đủ trường, bảng chi tiết trên mobile bị wrap nhãn, FAB che số tiền ở trang hoàn tất đơn, **file ảnh logo bị cắt mất chữ "C"** (lỗi ở file, không phải CSS), **menu chính `.cp-nav` đang rỗng** (chưa gán menu vào vị trí `primary`)
- **BỔ SUNG (16:05) — CP1.4 + CP1.5 (menu header):** nguyên nhân menu không hiện **không phải code** mà là **dữ liệu**: theme đăng ký 2 vị trí (`primary`, `footer`) nhưng **cả 2 đều trống**. Đã gán menu **"Menu chinh" (id 66)** vào `primary` → 6 mục cấp 1 + 5 dropdown. Code mới: dropdown 2 cấp (`:hover` + `:focus-within`, mũi tên ▾, 2 mục cuối canh phải chống tràn), panel mobile burger (`assets/nav-menu.js`: `aria-expanded`/`aria-controls`, accordion nút riêng, Esc trả focus, khoá cuộn body), bỏ lồng `<a>` quanh logo (HTML sai → `.cp-logo` rỗng 0×0). Đã đo: 5 dropdown hiện `visible/opacity 1` không tràn khung; mobile panel top 110 rộng 390, accordion mở 12 con, Esc đóng OK, không lỗi JS.
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~15 nhóm), đơn test #11511–#11513 + #11515 + #11517, **vị trí menu `footer` vẫn TRỐNG** (footer không có menu), chữ `.cp-cta` trắng trên nền vàng (chờ quyết đổi nền), file ảnh logo bị cắt mất chữ "C", 1 ảnh sản phẩm 404 (`cua-go-cong-nghiep-2-canh.webp`), FAB che số tiền ở trang hoàn tất đơn
- **BỔ SUNG (16:25) — CP1.6 + CP1.7 (search + giỏ hàng trên header):** thanh search sản phẩm (GET + `post_type=product`) nằm ngay TRÁI khối hotline, icon giỏ hàng có badge nằm ngay PHẢI khối hotline. Search là con trực tiếp của `.cp-container` (không nhét vào `.cp-header-cta`) để còn wrap xuống hàng riêng được ở ≤1024px. Giỏ hàng cập nhật AJAX bằng fragment `a.cp-cart-link`. Đã đo: 1280px 1 hàng (logo·nav·search[792→1022]·cta[1022→1245]), **không chồng lấn ở mọi bề rộng** (1280/1100/1024/900/768/600/390), badge đúng số lượng (test lúc giỏ có 2 món → badge "2", `aria-label="Giỏ hàng, 2 sản phẩm"`), 0 lỗi JS. Mobile 390px: 3 hàng (logo / hotline+giỏ+burger / search full) cao 161px — do logo 250px + cụm hotline 213px không đủ chỗ 350px; muốn gọn 2 hàng thì cho logo co lại trên mobile.
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~16 nhóm), đơn test #11511–#11513 + #11515 + #11517, vị trí menu `footer` vẫn TRỐNG, chữ `.cp-cta` trắng trên nền vàng (chờ quyết đổi nền), 1 ảnh sản phẩm 404 (`cua-go-cong-nghiep-2-canh.webp`), FAB che số tiền ở trang hoàn tất đơn, header mobile 3 hàng ở ≤480px
- **Ghi chú:** Tùng đã tự đổi menu chính sang "Main menu" và đổi logo mới (chữ "C" đã đủ) — phần menu/logo trong ghi chú cũ đã không còn đúng.
- **BỔ SUNG (16:45) — CP1.4b (mega menu 3 cấp "Các Loại Cửa"):** `header.php` đổi `depth` 2→3; `nav-menu.js` gắn `.cp-mega` (mục cấp 1 có cháu) + `.sub-menu--mega`; CSS panel thành dải ngang full chiều rộng, cấp 2 = tiêu đề CỘT (chữ hoa + gạch chân vàng), cấp 3 = danh sách phẳng trong cột. **Dữ liệu:** danh mục sản phẩm của site đang PHẲNG (chỉ "Phụ kiện cửa" và "Cửa chống cháy" có con) nên 3 cột phải gom nhóm thủ công → đã tạo mục "Các Loại Cửa" (id 11527) ở vị trí 2 trong menu #87 "Main menu": Cửa gỗ (12 mục) / Cửa nhựa (9) / Cửa chống cháy (4). **2 lỗi tự gây + đã sửa:** (1) `grid-auto-flow: column` làm panel rộng 2486px tràn màn hình khi menu có 12 nhánh → đổi sang `repeat(auto-fit, minmax(200px,240px))` tự xuống hàng; (2) rule chống tràn `:nth-last-child(-n+2) > .sub-menu { left:auto; right:0 }` áp nhầm vào mục mega → panel co còn 280px 1 cột → thêm `:not(.cp-mega)`. Đo lại: panel full 1265px, 3 cột cùng 1 hàng, không tràn; mobile accordion 3 cấp OK.
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~17 nhóm), đơn test #11511–#11513 + #11515 + #11517, chữ `.cp-cta` trắng trên nền vàng, FAB che số tiền ở trang hoàn tất đơn, vị trí menu `footer` TRỐNG, header mobile 3 hàng ở ≤480px. **Cần Tùng chốt:** nhóm danh mục trong 3 cột mega (tôi gom theo tên, sửa được trong admin), có dựng thêm mục nav như demo (KHÓA ĐIỆN TỬ, VIDEO, CÔNG TRÌNH, KHUYẾN MÃI...) không, và menu #88 "Danh mục sản phẩm" chỉ có 1 mục cấp 1 nên nếu muốn dùng làm menu header thì phải tách thành nhiều mục cấp 1.
- **BỔ SUNG (17:05) — CP1.4b cột gom:** các mục cấp 2 KHÔNG có danh mục con giờ được gom chung vào 1 cột `.cp-mega__misc` ở cuối (trước đây mỗi mục chiếm 1 cột với danh sách rỗng). Đo trên menu #88 (12 nhánh cấp 2, 6 nhánh rỗng): **12 cột → 10 cột, không còn cột rỗng**, cột gom chứa 3 mục. Tiêu đề mọi cột được cố định chiều cao 2 dòng để danh sách thẳng hàng. Lưu ý khi đo: tab front-end ở nền làm trình duyệt tạm dừng transition nên panel báo `hidden` — không phải lỗi CSS (selector khớp + rule có `visibility: visible`); đã ép hiện tạm để chụp ảnh.
- **Việc tiếp theo:** việc còn treo — commit (working tree gộp ~17 nhóm), đơn test #11511–#11513 + #11515 + #11517, chữ `.cp-cta` trắng trên nền vàng, FAB che số tiền trang hoàn tất đơn, vị trí menu `footer` TRỐNG, header mobile 3 hàng ở ≤480px. **Vị trí `primary` hiện = #88 "Danh mục sản phẩm"** (Tùng đang dựng menu này — root đổi tên thành "Sản Phẩm", href "#"); #87 "Main menu" (chứa "Các Loại Cửa" 3 cột) vẫn còn nguyên.
- **BỔ SUNG (17:25) — sửa lỗi panel mega "dính mở" (Tùng báo):** tìm ra 2 nguyên nhân. (1) **Lỗi thật:** `:focus-within` — click chuột vào mục menu làm link nhận focus nên panel giữ mở mãi dù chuột đi xa (`li:focus-within=true` + panel visible khi chuột ở đáy trang). Sửa thành `:has(:focus-visible)` → click chuột không còn giữ mở, Tab bàn phím vẫn mở được (đã test Tab 12 lần tới mục menu → panel visible + `hasFV=true`). (2) Panel mở **ngay lập tức** khi chuột lướt qua → thêm hover intent `transition-delay: .12s` (chỉ hướng mở bằng chuột; đóng vẫn tức thì). Đã đo lại trong tab active: chưa tương tác → hidden; lướt nhanh 60ms qua mục → **hidden** (không mở); hover 500ms → visible; rời chuột ra ngoài panel → hidden; **click rồi rời ra ngoài → hidden**. - **⚠️ ĐÍNH CHÍNH (18:00):** câu "Vùng hover chính xác: cách trái 30px / trên-dưới 20px đều không mở" ở trên là **kết luận SAI** — lần đó đo bằng cách teleport chuột (không đi xuyên qua vùng panel) nên không phát hiện lỗi hit-test; thực tế panel vẫn tự mở khi chuột lướt vào vùng panel. Nguyên nhân GỐC + cách sửa: xem khối đầu §1.
- **Cập nhật lúc:** 2026-09-14 18:00

---

## §2 NHẬT KÝ (chỉ ghi thêm, mới nhất ở dưới)

| Thời gian | Model | Việc đã làm | File chính | Trạng thái |
|---|---|---|---|---|
| 2026-09-13 19:45 | deepseek | Tạo worklog bàn giao + luật phối hợp model | `.ai/WORKLOG.md`, `CLAUDE.md`, `AGENTS.md` | xong |
| 2026-09-13 19:50 | deepseek | Chốt nhãn model chỉ còn `claude`/`deepseek` (bỏ codex/copilot) | như trên + 3 file parent | xong |
| 2026-09-13 20:05 | deepseek | CP3.2: chiều cao thu gọn mô tả ngắn `7.5em` → `15em` + đồng bộ 3 lớp tài liệu | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:15 | deepseek | CP3.2: nhãn nút mua → "THÊM GIỎ HÀNG"; ẩn số hotline trên mobile (chỉ còn "Gọi ngay") | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:25 | deepseek | CP3.2: thông số kỹ thuật — nhãn + giá trị cùng 1 dòng ("Kích thước: 1m x 3m") | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:30 | deepseek | CP3.2: box "Hỗ trợ trực tuyến" thêm 4 hotline chi nhánh (filter `cp_support_branches`) | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:40 | deepseek | CP3.2: box "Hỗ trợ trực tuyến" — icon đầu mỗi dòng + canh trái toàn bộ | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:45 | deepseek | CP3.2: hotline chi nhánh sửa được trong admin (plugin thêm Settings → Cao Phát, option `tlcp_support_branches`) | `plugins/tl-site-caophat/tl-site-caophat.php` (+README), `themes/tungleads-theme-cp/inc/woocommerce.php` | xong |
| 2026-09-14 09:10 | deepseek | CP3.2 mobile: padding thẻ SP + ô tabs 28px → 10px; sửa rule chết `.cp-single-layout > div.product` | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:25 | deepseek | CP3.2 mobile: padding thẻ SP + ô tabs 10px → 15px | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:35 | deepseek | CP3.2: `.cp-side-branches` canh đều trái/phải, bỏ icon đầu dòng + bỏ dấu ":" | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:50 | deepseek | CP3.2 mobile: đưa 2 box "Hỗ trợ trực tuyến" + "Cam kết Cao Phát" lên trước khối "Thông số kỹ thuật" (display:contents + order) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 11:35 | deepseek | CP3.3 Đặt hàng nhanh: popup "Mua hàng" → AJAX tạo đơn WooCommerce thật (nonce + honeypot + rate-limit), UI ở theme · handler ở plugin | `inc/woocommerce.php`, `assets/quick-order.js`, `assets/caophat.css`, `plugins/tl-site-caophat/tl-site-caophat.php` | xong (chưa commit) |
| 2026-09-14 11:50 | deepseek | CP3.3: đưa ô "Số lượng" vào khối sản phẩm trong popup (khối sản phẩm chuyển vào trong form để FormData gửi được qty) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 12:00 | deepseek | CP3.3: ô nhập trong popup bo góc `--cp-radius-sm` (10px) → 5px | `assets/caophat.css` | xong |
| 2026-09-14 12:10 | deepseek | CP3.3: ô `input[type="text"]` trong popup padding 11px 13px → 5px 5px (chỉ ô Họ tên, các ô khác chưa đổi) | `assets/caophat.css` | xong |
| 2026-09-14 12:20 | deepseek | CP3.3: padding 5px cho cả 3 loại ô 1 dòng (text/tel/number, gồm ô SL); textarea giữ 11px 13px | `assets/caophat.css` | xong |
| 2026-09-14 12:35 | deepseek | CP3.3: ô "Địa chỉ nhận hàng" textarea → input[type=text] (giống ô Họ tên); plugin đổi sanitize_textarea_field → sanitize_text_field | `inc/woocommerce.php`, `plugins/tl-site-caophat/tl-site-caophat.php` | xong |
| 2026-09-14 12:45 | deepseek | CP3.3: thêm icon mũi tên giấy cho nút "Gửi đơn hàng" (nút chuyển inline-flex + gap 8px) | `inc/woocommerce.php`, `assets/caophat.css` | xong |
| 2026-09-14 12:55 | deepseek | CP3.3: padding ô nhập 1 dòng 5px 5px → 5px 10px (gồm ô Số lượng, textarea giữ nguyên) | `assets/caophat.css` | xong |
| 2026-09-14 13:05 | deepseek | CP3.4 Trang giỏ hàng: hero + container qua filter the_content, lưới 2 cột, skin bảng giỏ/tổng tiền, nhãn Shipment → Vận chuyển (gettext_with_context), mobile thẻ dọc | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 13:35 | deepseek | CP3.5 Trang thanh toán: hero+container qua helper chung cp_woo_page_wrap(), lưới 2 cột, skin form/khối đơn hàng/nút Đặt hàng; fix bẫy đảo cột col-1/col-2; đính chính ghi chú font icon WC | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 13:45 | deepseek | CP3.5: đưa "Giao hàng đến một địa chỉ khác?" + "Ghi chú đơn hàng" xuống dưới "Thông tin thanh toán" (.col2-set về 1 cột); ô ghi chú full chiều ngang | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 14:35 | deepseek | CP3.5: rút gọn form "Thông tin thanh toán" (gộp họ+tên, bỏ 4 trường, email không bắt buộc, ô ghi chú đồng bộ popup CP3.3) + sửa 2 bẫy (shipping_country rỗng, độ ưu tiên CSS) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit); đơn test #11515 |
| 2026-09-14 14:50 | deepseek | CP3.6: thêm trang hoàn tất đơn (hero + container + skin + nút về trang chủ) và chuyển trang sau khi đặt nhanh (plugin trả redirect + JS location.assign) | `inc/woocommerce.php`, `assets/caophat.css`, `assets/quick-order.js`, plugin `tl-site-caophat`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit); đơn test #11517 |
| 2026-09-14 15:10 | deepseek | .cp-pagehero: padding 20px 0 (trước 54px) + đổi sang tông sáng (gradient trắng→kem, viền dưới 1px, chữ --cp-text, breadcrumb 13.5px --cp-muted) — áp cho cả 4 trang dùng class này | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:20 | deepseek | CP1.2: sửa selector logo thành `.cp-header .custom-logo` (selector cũ `.cp-logo .custom-logo` là selector CHẾT vì `the_custom_logo()` lồng `<a>` trong `<a>`) → 250px, bỏ padding; bỏ `gap` của `.cp-header .cp-container` (mobile vẫn 12px) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:26 | deepseek | CP1.2: topbar nền `--cp-primary-dark` → `--cp-primary`; kèm sửa hover `.cp-topbar a:hover` `#fff` → `--cp-accent` + gạch chân (trắng trên vàng sáng ~1.75:1) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:35 | deepseek | `.cp-cta`: chữ `--cp-on-primary` → `#fff` (theo yêu cầu). ⚠️ Nền còn vàng nên tương phản tụt từ 8.94:1 xuống ~1.9–3.0:1 (dưới WCAG AA) | `assets/caophat.css` | xong (chưa commit) |
| 2026-09-14 16:05 | deepseek | CP1.4 + CP1.5: xây menu header — dropdown 2 cấp (hover + bàn phím), panel mobile burger + accordion (`assets/nav-menu.js` mới), sửa header: bỏ lồng `<a>` quanh logo, thêm aria cho burger/panel; **gán menu "Menu chinh" (id 66) vào vị trí `primary`** (trước đó cả 2 vị trí đều trống nên menu không hiện) | `assets/caophat.css`, `assets/nav-menu.js`, `header.php`, `functions.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 16:25 | deepseek | CP1.6 + CP1.7: thanh search sản phẩm (GET, `post_type=product`, pill bo tròn, icon kính lúp là nút submit) nằm ngay TRÁI khối hotline; icon giỏ hàng + badge số lượng nằm ngay PHẢI khối hotline, badge tự cập nhật sau khi thêm giỏ bằng AJAX qua `woocommerce_add_to_cart_fragments` (fragment `a.cp-cart-link`) | `header.php`, `functions.php`, `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 16:45 | deepseek | CP1.4b: mega menu 3 cấp — `depth=3`, JS gắn `.cp-mega`/`.sub-menu--mega`, CSS panel full chiều rộng + cột tự xuống hàng; tạo mục **"Các Loại Cửa"** (3 cột: Cửa gỗ 12 / Cửa nhựa 9 / Cửa chống cháy 4 mục) trong menu #87; gán lại `primary` = "Main menu" (#87) để xem thiết kế (trước đó Tùng đang để #88 "Danh mục sản phẩm") | `header.php`, `assets/nav-menu.js`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 17:05 | deepseek | CP1.4b: gom các mục cấp 2 KHÔNG có danh mục con vào 1 cột `.cp-mega__misc` (JS di chuyển + CSS chèn spacer cao bằng tiêu đề cột) + cố định `min-height` tiêu đề cột để danh sách thẳng hàng | `assets/nav-menu.js`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 17:25 | deepseek | CP1.4: sửa lỗi panel mega "dính mở" — thay `:focus-within` bằng `:has(:focus-visible)` (click chuột không còn giữ panel mở) + thêm hover intent trễ 120ms khi mở bằng chuột | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `CLAUDE.md`, `AGENTS.md` | xong (chưa commit) |
| 2026-09-14 18:00 | deepseek | CP1.4/CP1.4b: sửa GỐC lỗi menu tự mở khi chưa rê tới chữ — panel ĐÓNG vẫn bắt chuột vì cấp 3 có `visibility: visible` đè rule ẩn của panel (`opacity: 0` không chặn hit-test) → chuyển `visibility` vào rule có điều kiện hover/focus + `pointer-events: none/auto` + "cầu" hover 28px lấp khoảng hở link↔panel + bật lại `pointer-events: auto` cho panel mobile | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 18:20 | deepseek | CP1.4b: cắt đuôi "…" cho tên danh mục cấp 3 quá dài (`overflow: hidden` + `text-overflow: ellipsis`) — 4/35 link tràn khỏi cột (dài nhất 242px / cột 195px) → 0; mobile 390px không cần sửa | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 18:40 | deepseek | CP1.4b: bỏ vạch dọc + thụt lề ở cột ĐẦU của hàng mới khi panel xuống dòng — thêm `markRowStarts()` (đo `offsetTop` → gắn `.cp-row-start`) + chạy lại khi resize/font load | `assets/nav-menu.js`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:05 | deepseek | CP3.1/CP3.2: giá trong card danh mục + card liên quan bị rule WooCommerce (0,4,2) đè thành olive 13.71px + gạch chân → nâng selector lên 5 class + `ins{text-decoration:none}`; giá giờ accent `rgb(200,71,31)` 17px/700 như `.cp-price` trang chủ | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:40 | deepseek | CP4.1 (skin) + CP1.2 + CP3.2: `.cp-single-side` sticky top 100px; topbar `min-height` 30px; `.cp-btn-accent` nền `#000000` (hover `#333333`); thêm `.woocommerce-message` viền trên + icon `#fbaf02` (đo notice thật: `rgb(251,175,2)`); **bỏ nhãn danh mục khỏi card** (home + loop shop/related), giữ ở trang chi tiết SP | `assets/caophat.css`, `functions.php`, `inc/woocommerce.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:55 | deepseek | CP3.2: khôi phục nhãn nút mua — bỏ filter `woocommerce_product_single_add_to_cart_text`, nút về "THÊM VÀO GIỎ HÀNG"; phát sinh: nhãn dài hơn 43px nên xuống 2 dòng (cần 195px, cột 152px) | `inc/woocommerce.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:05 | deepseek | CP3.1: xoá nút trên card — bỏ link "Xem chi tiết" (2 chỗ in) + `remove_action` nút thêm-giỏ mặc định của WooCommerce trong loop + xoá CSS chết `.cp-card-btn`; card chỉ còn ảnh/tiêu đề/giá | `functions.php`, `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:20 | deepseek | CP1.6: chuyển ô search vào TRONG `.cp-header-cta` (con đầu tiên, vẫn trái hotline); xử lý lại wrap ≤1024px (cta `flex: 1 1 100%` + `flex-wrap` + `justify-content: flex-end`) để search vẫn xuống hàng riêng; bỏ `margin-left: 20px` | `header.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:30 | deepseek | CP4.1: `.cp-nav { padding-right: 20px }` — khoảng 20px giữa menu và khối cta; panel mega vẫn full chiều rộng, panel mobile không đổi | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:45 | deepseek | CP4.1: chặn nền autofill của Chrome bằng `input:-webkit-autofill { box-shadow: 0 0 0 1000px #fff inset !important }` — xác minh rule `input:-internal-autofill-selected` là của UA stylesheet (không có trong repo/Customizer/DB) nên không xoá được, chỉ đè được | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 21:00 | deepseek | CP1.6: nền xanh khi gõ trong ô search — thêm `autocomplete="off" autocapitalize="off" spellcheck="false"` cho input (`header.php`) + rule đè riêng `.cp-search input:-webkit-autofill { box-shadow: 0 0 0 1000px var(--cp-surface) inset !important }` | `header.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 21:15 | deepseek | CP1.6: ẩn nút xoá nhanh (×) của Chrome/Safari trong ô search — `appearance: none` cho input + `::-webkit-search-cancel-button/-decoration/-results-*{display:none}` | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 22:00 | deepseek | Loạt fix theo review (Tùng duyệt): a11y (CTA đạt AA, focus ring, reduced-motion, tap target ≥44px); hiệu năng ảnh (hero 986KB→37KB, door-hdf 656KB→24KB, srcset+fetchpriority); `alt` ảnh (2 filter → 46/46 ảnh có alt); nút giỏ 1 dòng (`minmax` cột `form.cart`); chừa đáy cho FAB ở trang hoàn tất; ĐÍNH CHÍNH lỗi notice trùng là artifact khi test | `assets/caophat.css`, `front-page.php`, `functions.php`, `inc/woocommerce.php`, `template-parts/home-categories.php`, `assets/images/hero*.webp`, `assets/images/door-hdf.webp`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (đã commit) |
| 2026-09-14 22:30 | deepseek | CP3.3: nút `.cp-buynow` đổi nhãn "Mua hàng" → "Đặt hàng nhanh" + bỏ dòng phụ "Gọi điện xác nhận và giao hàng tận nơi" (xoá markup + CSS chết `.cp-buynow__txt span`) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `CLAUDE.md`, `AGENTS.md` | xong (đã commit) |
| 2026-09-14 22:45 | deepseek | CP3.2: đồng bộ chiều cao 2 nút mua ở mobile — thêm `min-height: 56px` cho `.cp-buynow` (mobile mỗi nút 1 hàng nên `align-items: stretch` không còn tác dụng) → 303×56 = 303×56 | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit) |
| 2026-09-14 23:00 | deepseek | CP3.2: chia 50/50 chiều rộng 2 nút (`repeat(2, minmax(0,1fr))`) — 8 mức bề rộng đều chênh 0px; phát sinh ở 769–1280px cột hẹp nên nhãn xuống 2 dòng (nút 59–79px), đã ghi 3 phương án xử lý | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (commit cùng 23:35) |
| 2026-09-14 23:20 | deepseek | CP1.2b: header mobile **1 hàng** (burger trái · logo giữa · icon search + giỏ phải · ẩn hotline) 163px → **65px**; nút `.cp-search-toggle` mở ô search thành dải riêng dưới header (Esc/click ngoài/bấm lại để đóng); sửa 3 bẫy: logo đè icon search, specificity rule logo cuối file, clamp 2 chiều làm méo tỉ lệ ảnh | `header.php`, `assets/nav-menu.js`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (commit cùng 23:35) |
| 2026-09-14 23:35 | deepseek | CP1.2b: icon search + giỏ hàng **35×35px** (trước 44px); logo vào **ĐÚNG TÂM** — phát hiện `a.custom-logo-link` bị kéo giãn hết khung nên ảnh nằm ở đầu khung (lệch −157px ở 768px) → sửa bằng `padding-left: 36px` cho `.cp-logo` + `margin: 0 auto` cho ảnh; đo lại lệch tâm = 0 tại 320/360/390/414/600/768 | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 5665e17) |
| 2026-09-14 23:45 | deepseek | CP1.5: nút mở menu con trên mobile `38×38 → **30×30px**`, `top: 7 → 6px` (5 thuộc tính còn lại đã đúng sẵn) — đo 390px: nút y=6..36 trong li cao 52px, cách lề phải 8px, không đè chữ link, chevron lệch tâm −3px, bấm mở submenu OK | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 33ed5e9) |
| 2026-09-14 23:50 | deepseek | CP1.5: xử lý 2 đề xuất — `top: 6 → 11px` (nút đúng tâm hàng, lệch tâm 0) + thêm `.cp-nav__toggle::before` 44×44 làm **vùng bấm 44px** (nút vẫn hiển thị 30px); bấm 5px ngoài nút ở cả 4 phía vẫn mở menu con, không điều hướng | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 81a7eae) |
| 2026-09-14 23:55 | deepseek | 3 việc Tùng chốt: (1) giữ 50/50 — chấp nhận nhãn nút xuống 2 dòng ở 769–1280px; (2) giữ icon giỏ/search mobile **35px**, không thêm lớp phủ 44px; (3) hover `.cp-btn-accent` **GIỮ `#333333`** (đo: hover `rgb(51,51,51)` + nhấc 1px, chữ trắng giữ, tương phản 21:1→12.6:1; không đổi sang cam vì nút luôn nằm trên dải CTA nền cam). Kèm: plugin `tl-site-caophat` đã tạo **git repo riêng** (phương án A, baseline `1da7fd6`) | `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit) |
| 2026-09-14 23:55 | deepseek | 3 việc Tùng chốt: (1) giữ 50/50 — chấp nhận nhãn nút xuống 2 dòng ở 769–1280px; (2) giữ icon giỏ/search mobile **35px**, không thêm lớp phủ 44px; (3) hover `.cp-btn-accent` **GIỮ `#333333`** (đo: hover `rgb(51,51,51)` + nhấc 1px, chữ trắng giữ, tương phản 21:1→12.6:1; không đổi sang cam vì nút luôn nằm trên dải CTA nền cam). Kèm: plugin `tl-site-caophat` đã tạo **git repo riêng** (phương án A, baseline `1da7fd6`) | `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit) |