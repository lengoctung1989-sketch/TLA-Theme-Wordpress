# WORKLOG — bàn giao công việc giữa các model

**Repo:** `tungleads-theme-cp` (child theme caophat.vn) · nhánh `main` · **không có remote** (Tùng chốt 2026-09-14: dự án không dùng remote — đừng đề xuất push)
**Parent:** `tungleads-theme` (pin version bằng git tag, xem dòng `Base:` trong `README.md`)

> Chỉ dùng **2 model: `claude` (Claude Code) và `deepseek` (DeepSeek)** — **chạy tuần tự, không đồng thời**.
> **Không cần script, không cần cài gì** — chỉ đọc và sửa markdown.
> Luật chi tiết + bảng số CP ở **`CLAUDE.md`** (nguồn duy nhất); `AGENTS.md` chỉ là bản tóm tắt bắt buộc (cấu trúc Tùng chốt 2026-09-15).
> P-index của repo này dùng tiền tố **`CP`** (`CP1`–`CP5`) — bảng nghĩa ở `CLAUDE.md`.
>
> 1. **Đầu phiên:** đọc `§1 ĐANG LÀM` + 10 dòng cuối của `§2`.
> 2. **Bắt đầu việc:** cập nhật `§1` (model, việc, file sẽ chạm, trạng thái).
> 3. **Hết việc/phiên:** cập nhật lại `§1` (xong / dang dở + việc tiếp theo) rồi ghi 1 dòng vào `§2`.
> 4. `§1` **ghi đè** (chỉ giữ khối mới nhất) · `§2` **chỉ ghi thêm**, không sửa dòng cũ.

---

## §1 ĐANG LÀM (bàn giao — ghi đè mỗi phiên, chỉ giữ 1 khối)

- **Model:** `cline` (Cline / VS Code) — **nhãn commit do Tùng chốt: `[deepseek]`** (theo 5 commit gần nhất của repo).
- **Trạng thái phiên:** ✅ **XONG + ĐÃ COMMIT** (1 commit gộp 32 file: 14 sửa + 2 xoá + 16 mới), `git status` SẠCH. **Không** deploy/push (dự án không có remote; deploy chỉ chạy khi Tùng yêu cầu).
- **Việc đã xong trong phiên (nhiều CP):** CP2.4/2.5 (khối trang chủ theo danh mục — repeater Customizer, 25 control) · CP2.6 (khối “Danh mục nổi bật”: tick + kéo thả + bố cục) · CP2.7 (chọn **sản phẩm ưu tiên** trong từng khối — AJAX + kéo thả) · CP2.8 (nút cuộn cho khối dạng cuộn) · CP2.9 (khối tabs sản phẩm) · CP2.2 (đầu mục mobile + nút dạng viên) · CP5.1/5.2 (tin tức: trang danh mục + chi tiết, ẩn bình luận) · CP6.1/6.2 (trang nội dung tĩnh — `page.php`) · CP4.1 (padding `.cp-section` 64→40px, gap lưới SP mobile) · **CP1.3 — footer** (mục Customizer **20 control**: nền màu/ảnh/opacity, 3–4 cột, mỗi cột tiêu đề + ô soạn thảo TinyMCE; **chữ tự đổi theo nền sáng/tối**; ô soạn thảo **WYSIWYG**; nút **Mã HTML** + **Cỡ chữ** + **Màu chữ**; cột để trống = cột TRỐNG; **iframe bản đồ/video** qua KSES mở rộng host tin cậy; dòng ghi công cố định có link; line **7px vàng** ở top; **mobile 1 cột**) · đính chính + phục hồi rule `.cp-cart-section`/`.cp-checkout-section` (bị xoá nhầm khi audit CSS mồ côi) · **dọn 51 rule CSS mồ côi + biến chết `--cp-surface-2`** · xoá 2 template-part cũ.
- **Tái cấu trúc tài liệu (Tùng chốt):** `AGENTS.md` **66,6KB → 6,6KB** (chỉ checklist + làn A/B/C + luật bất biến); **chi tiết + bảng CP ở `CLAUDE.md`** (nguồn duy nhất), bản đồ file `.ai/FEATURE_MAP.md`, nhật ký `.ai/WORKLOG.md`.
- **Việc tiếp theo (bàn giao cho Tùng):** (1) chạy `wp media regenerate` **trên PRODUCTION** cho thumbnail `.webp`/`.jpg` hỏng metadata (local thiếu 234 file gốc) — ảnh hưởng cả khối tin trang chủ; (2) `/my-account/` **chưa có skin** (hero/container) — nói là làm; (3) (tuỳ chọn) `home.php` cho trang “Bài viết”, lọc tin theo chuyên mục, skin trang tag; (4) muốn hết hẳn việc **nút gọi nổi phủ lên link ghi công** khi cuộn tới đáy: nâng `.cp-fab` hoặc chừa `padding-bottom` cho `.cp-footer` (đã ghi chú trong CSS, đang chờ Tùng quyết).
- **BẪY đã gặp trong phiên (đã ghi 3 lớp — đọc trước khi sửa):** `wp.editor.getEditor()` **không tồn tại** (dùng `tinymce.get`) · WP **ẩn HÀNG 2 toolbar** (nút tuỳ biến phải nằm ở `toolbar1`) · **class ghép động** trong PHP (`cp_woo_page_wrap()` in `'cp-section ' . $class . '-section'`) ⇒ audit “CSS mồ côi” phải kiểm **DOM THẬT** chứ không chỉ grep file · `wp_kses_post` **xoá `<iframe>`** (đã mở rộng KSES theo host tin cậy) · reset `.cp ul { list-style: none }` phá **dấu danh sách** trong ô soạn thảo · nút gọi nổi (FAB) phủ lên link khi cuộn tới đáy · **URL thật của giỏ/thanh toán là `/cart/` + `/checkout/`** (KHÔNG phải `/gio-hang/`, `/thanh-toan/`).

---

## §2 NHẬT KÝ (chỉ ghi thêm, mới nhất ở dưới)

| Thời gian | Model | Việc đã làm | File chính | Trạng thái |
|---|---|---|---|---|
| 2026-09-13 19:45 | deepseek | Tạo worklog bàn giao + luật phối hợp model | `.ai/WORKLOG.md`, `CLAUDE.md`, `AGENTS.md` | xong |
| 2026-09-13 19:50 | deepseek | Chốt nhãn model chỉ còn `claude`/`deepseek` (bỏ codex/copilot) | như trên + 3 file parent | xong |
| 2026-09-13 20:05 | deepseek | CP3.2: chiều cao thu gọn mô tả ngắn `7.5em` → `15em` + đồng bộ 3 lớp tài liệu | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:15 | deepseek | CP3.2: nhãn nút mua → "THÊM GIỎ HÀNG"; ẩn số hotline trên mobile (chỉ còn "Gọi ngay") | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:25 | deepseek | CP3.2: thông số kỹ thuật — nhãn + giá trị cùng 1 dòng ("Kích thước: 1m x 3m") | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:30 | deepseek | CP3.2: box "Hỗ trợ trực tuyến" thêm 4 hotline chi nhánh (filter `cp_support_branches`) | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:40 | deepseek | CP3.2: box "Hỗ trợ trực tuyến" — icon đầu mỗi dòng + canh trái toàn bộ | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-13 20:45 | deepseek | CP3.2: hotline chi nhánh sửa được trong admin (plugin thêm Settings → Cao Phát, option `tlcp_support_branches`) | `plugins/tl-site-caophat/tl-site-caophat.php` (+README), `themes/tungleads-theme-cp/inc/woocommerce.php` | xong |
| 2026-09-14 09:10 | deepseek | CP3.2 mobile: padding thẻ SP + ô tabs 28px → 10px; sửa rule chết `.cp-single-layout > div.product` | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:25 | deepseek | CP3.2 mobile: padding thẻ SP + ô tabs 10px → 15px | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:35 | deepseek | CP3.2: `.cp-side-branches` canh đều trái/phải, bỏ icon đầu dòng + bỏ dấu ":" | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 09:50 | deepseek | CP3.2 mobile: đưa 2 box "Hỗ trợ trực tuyến" + "Cam kết Cao Phát" lên trước khối "Thông số kỹ thuật" (display:contents + order) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 11:35 | deepseek | CP3.3 Đặt hàng nhanh: popup "Mua hàng" → AJAX tạo đơn WooCommerce thật (nonce + honeypot + rate-limit), UI ở theme · handler ở plugin | `inc/woocommerce.php`, `assets/quick-order.js`, `assets/caophat.css`, `plugins/tl-site-caophat/tl-site-caophat.php` | xong (chưa commit) |
| 2026-09-14 11:50 | deepseek | CP3.3: đưa ô "Số lượng" vào khối sản phẩm trong popup (khối sản phẩm chuyển vào trong form để FormData gửi được qty) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong |
| 2026-09-14 12:00 | deepseek | CP3.3: ô nhập trong popup bo góc `--cp-radius-sm` (10px) → 5px | `assets/caophat.css` | xong |
| 2026-09-14 12:10 | deepseek | CP3.3: ô `input[type="text"]` trong popup padding 11px 13px → 5px 5px (chỉ ô Họ tên, các ô khác chưa đổi) | `assets/caophat.css` | xong |
| 2026-09-14 12:20 | deepseek | CP3.3: padding 5px cho cả 3 loại ô 1 dòng (text/tel/number, gồm ô SL); textarea giữ 11px 13px | `assets/caophat.css` | xong |
| 2026-09-14 12:35 | deepseek | CP3.3: ô "Địa chỉ nhận hàng" textarea → input[type=text] (giống ô Họ tên); plugin đổi sanitize_textarea_field → sanitize_text_field | `inc/woocommerce.php`, `plugins/tl-site-caophat/tl-site-caophat.php` | xong |
| 2026-09-14 12:45 | deepseek | CP3.3: thêm icon mũi tên giấy cho nút "Gửi đơn hàng" (nút chuyển inline-flex + gap 8px) | `inc/woocommerce.php`, `assets/caophat.css` | xong |
| 2026-09-14 12:55 | deepseek | CP3.3: padding ô nhập 1 dòng 5px 5px → 5px 10px (gồm ô Số lượng, textarea giữ nguyên) | `assets/caophat.css` | xong |
| 2026-09-14 13:05 | deepseek | CP3.4 Trang giỏ hàng: hero + container qua filter the_content, lưới 2 cột, skin bảng giỏ/tổng tiền, nhãn Shipment → Vận chuyển (gettext_with_context), mobile thẻ dọc | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 13:35 | deepseek | CP3.5 Trang thanh toán: hero+container qua helper chung cp_woo_page_wrap(), lưới 2 cột, skin form/khối đơn hàng/nút Đặt hàng; fix bẫy đảo cột col-1/col-2; đính chính ghi chú font icon WC | `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 13:45 | deepseek | CP3.5: đưa "Giao hàng đến một địa chỉ khác?" + "Ghi chú đơn hàng" xuống dưới "Thông tin thanh toán" (.col2-set về 1 cột); ô ghi chú full chiều ngang | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 14:35 | deepseek | CP3.5: rút gọn form "Thông tin thanh toán" (gộp họ+tên, bỏ 4 trường, email không bắt buộc, ô ghi chú đồng bộ popup CP3.3) + sửa 2 bẫy (shipping_country rỗng, độ ưu tiên CSS) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit); đơn test #11515 |
| 2026-09-14 14:50 | deepseek | CP3.6: thêm trang hoàn tất đơn (hero + container + skin + nút về trang chủ) và chuyển trang sau khi đặt nhanh (plugin trả redirect + JS location.assign) | `inc/woocommerce.php`, `assets/caophat.css`, `assets/quick-order.js`, plugin `tl-site-caophat`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit); đơn test #11517 |
| 2026-09-14 15:10 | deepseek | .cp-pagehero: padding 20px 0 (trước 54px) + đổi sang tông sáng (gradient trắng→kem, viền dưới 1px, chữ --cp-text, breadcrumb 13.5px --cp-muted) — áp cho cả 4 trang dùng class này | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:20 | deepseek | CP1.2: sửa selector logo thành `.cp-header .custom-logo` (selector cũ `.cp-logo .custom-logo` là selector CHẾT vì `the_custom_logo()` lồng `<a>` trong `<a>`) → 250px, bỏ padding; bỏ `gap` của `.cp-header .cp-container` (mobile vẫn 12px) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:26 | deepseek | CP1.2: topbar nền `--cp-primary-dark` → `--cp-primary`; kèm sửa hover `.cp-topbar a:hover` `#fff` → `--cp-accent` + gạch chân (trắng trên vàng sáng ~1.75:1) | `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 15:35 | deepseek | `.cp-cta`: chữ `--cp-on-primary` → `#fff` (theo yêu cầu). ⚠️ Nền còn vàng nên tương phản tụt từ 8.94:1 xuống ~1.9–3.0:1 (dưới WCAG AA) | `assets/caophat.css` | xong (chưa commit) |
| 2026-09-14 16:05 | deepseek | CP1.4 + CP1.5: xây menu header — dropdown 2 cấp (hover + bàn phím), panel mobile burger + accordion (`assets/nav-menu.js` mới), sửa header: bỏ lồng `<a>` quanh logo, thêm aria cho burger/panel; **gán menu "Menu chinh" (id 66) vào vị trí `primary`** (trước đó cả 2 vị trí đều trống nên menu không hiện) | `assets/caophat.css`, `assets/nav-menu.js`, `header.php`, `functions.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 16:25 | deepseek | CP1.6 + CP1.7: thanh search sản phẩm (GET, `post_type=product`, pill bo tròn, icon kính lúp là nút submit) nằm ngay TRÁI khối hotline; icon giỏ hàng + badge số lượng nằm ngay PHẢI khối hotline, badge tự cập nhật sau khi thêm giỏ bằng AJAX qua `woocommerce_add_to_cart_fragments` (fragment `a.cp-cart-link`) | `header.php`, `functions.php`, `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 16:45 | deepseek | CP1.4b: mega menu 3 cấp — `depth=3`, JS gắn `.cp-mega`/`.sub-menu--mega`, CSS panel full chiều rộng + cột tự xuống hàng; tạo mục **"Các Loại Cửa"** (3 cột: Cửa gỗ 12 / Cửa nhựa 9 / Cửa chống cháy 4 mục) trong menu #87; gán lại `primary` = "Main menu" (#87) để xem thiết kế (trước đó Tùng đang để #88 "Danh mục sản phẩm") | `header.php`, `assets/nav-menu.js`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 17:05 | deepseek | CP1.4b: gom các mục cấp 2 KHÔNG có danh mục con vào 1 cột `.cp-mega__misc` (JS di chuyển + CSS chèn spacer cao bằng tiêu đề cột) + cố định `min-height` tiêu đề cột để danh sách thẳng hàng | `assets/nav-menu.js`, `assets/caophat.css`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 17:25 | deepseek | CP1.4: sửa lỗi panel mega "dính mở" — thay `:focus-within` bằng `:has(:focus-visible)` (click chuột không còn giữ panel mở) + thêm hover intent trễ 120ms khi mở bằng chuột | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `CLAUDE.md`, `AGENTS.md` | xong (chưa commit) |
| 2026-09-14 18:00 | deepseek | CP1.4/CP1.4b: sửa GỐC lỗi menu tự mở khi chưa rê tới chữ — panel ĐÓNG vẫn bắt chuột vì cấp 3 có `visibility: visible` đè rule ẩn của panel (`opacity: 0` không chặn hit-test) → chuyển `visibility` vào rule có điều kiện hover/focus + `pointer-events: none/auto` + "cầu" hover 28px lấp khoảng hở link↔panel + bật lại `pointer-events: auto` cho panel mobile | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 18:20 | deepseek | CP1.4b: cắt đuôi "…" cho tên danh mục cấp 3 quá dài (`overflow: hidden` + `text-overflow: ellipsis`) — 4/35 link tràn khỏi cột (dài nhất 242px / cột 195px) → 0; mobile 390px không cần sửa | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 18:40 | deepseek | CP1.4b: bỏ vạch dọc + thụt lề ở cột ĐẦU của hàng mới khi panel xuống dòng — thêm `markRowStarts()` (đo `offsetTop` → gắn `.cp-row-start`) + chạy lại khi resize/font load | `assets/nav-menu.js`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:05 | deepseek | CP3.1/CP3.2: giá trong card danh mục + card liên quan bị rule WooCommerce (0,4,2) đè thành olive 13.71px + gạch chân → nâng selector lên 5 class + `ins{text-decoration:none}`; giá giờ accent `rgb(200,71,31)` 17px/700 như `.cp-price` trang chủ | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:40 | deepseek | CP4.1 (skin) + CP1.2 + CP3.2: `.cp-single-side` sticky top 100px; topbar `min-height` 30px; `.cp-btn-accent` nền `#000000` (hover `#333333`); thêm `.woocommerce-message` viền trên + icon `#fbaf02` (đo notice thật: `rgb(251,175,2)`); **bỏ nhãn danh mục khỏi card** (home + loop shop/related), giữ ở trang chi tiết SP | `assets/caophat.css`, `functions.php`, `inc/woocommerce.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 19:55 | deepseek | CP3.2: khôi phục nhãn nút mua — bỏ filter `woocommerce_product_single_add_to_cart_text`, nút về "THÊM VÀO GIỎ HÀNG"; phát sinh: nhãn dài hơn 43px nên xuống 2 dòng (cần 195px, cột 152px) | `inc/woocommerce.php`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:05 | deepseek | CP3.1: xoá nút trên card — bỏ link "Xem chi tiết" (2 chỗ in) + `remove_action` nút thêm-giỏ mặc định của WooCommerce trong loop + xoá CSS chết `.cp-card-btn`; card chỉ còn ảnh/tiêu đề/giá | `functions.php`, `inc/woocommerce.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:20 | deepseek | CP1.6: chuyển ô search vào TRONG `.cp-header-cta` (con đầu tiên, vẫn trái hotline); xử lý lại wrap ≤1024px (cta `flex: 1 1 100%` + `flex-wrap` + `justify-content: flex-end`) để search vẫn xuống hàng riêng; bỏ `margin-left: 20px` | `header.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:30 | deepseek | CP4.1: `.cp-nav { padding-right: 20px }` — khoảng 20px giữa menu và khối cta; panel mega vẫn full chiều rộng, panel mobile không đổi | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 20:45 | deepseek | CP4.1: chặn nền autofill của Chrome bằng `input:-webkit-autofill { box-shadow: 0 0 0 1000px #fff inset !important }` — xác minh rule `input:-internal-autofill-selected` là của UA stylesheet (không có trong repo/Customizer/DB) nên không xoá được, chỉ đè được | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 21:00 | deepseek | CP1.6: nền xanh khi gõ trong ô search — thêm `autocomplete="off" autocapitalize="off" spellcheck="false"` cho input (`header.php`) + rule đè riêng `.cp-search input:-webkit-autofill { box-shadow: 0 0 0 1000px var(--cp-surface) inset !important }` | `header.php`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 21:15 | deepseek | CP1.6: ẩn nút xoá nhanh (×) của Chrome/Safari trong ô search — `appearance: none` cho input + `::-webkit-search-cancel-button/-decoration/-results-*{display:none}` | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (chưa commit) |
| 2026-09-14 22:00 | deepseek | Loạt fix theo review (Tùng duyệt): a11y (CTA đạt AA, focus ring, reduced-motion, tap target ≥44px); hiệu năng ảnh (hero 986KB→37KB, door-hdf 656KB→24KB, srcset+fetchpriority); `alt` ảnh (2 filter → 46/46 ảnh có alt); nút giỏ 1 dòng (`minmax` cột `form.cart`); chừa đáy cho FAB ở trang hoàn tất; ĐÍNH CHÍNH lỗi notice trùng là artifact khi test | `assets/caophat.css`, `front-page.php`, `functions.php`, `inc/woocommerce.php`, `template-parts/home-categories.php`, `assets/images/hero*.webp`, `assets/images/door-hdf.webp`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (đã commit) |
| 2026-09-14 22:30 | deepseek | CP3.3: nút `.cp-buynow` đổi nhãn "Mua hàng" → "Đặt hàng nhanh" + bỏ dòng phụ "Gọi điện xác nhận và giao hàng tận nơi" (xoá markup + CSS chết `.cp-buynow__txt span`) | `inc/woocommerce.php`, `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `CLAUDE.md`, `AGENTS.md` | xong (đã commit) |
| 2026-09-14 22:45 | deepseek | CP3.2: đồng bộ chiều cao 2 nút mua ở mobile — thêm `min-height: 56px` cho `.cp-buynow` (mobile mỗi nút 1 hàng nên `align-items: stretch` không còn tác dụng) → 303×56 = 303×56 | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit) |
| 2026-09-14 23:00 | deepseek | CP3.2: chia 50/50 chiều rộng 2 nút (`repeat(2, minmax(0,1fr))`) — 8 mức bề rộng đều chênh 0px; phát sinh ở 769–1280px cột hẹp nên nhãn xuống 2 dòng (nút 59–79px), đã ghi 3 phương án xử lý | `assets/caophat.css`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (commit cùng 23:35) |
| 2026-09-14 23:20 | deepseek | CP1.2b: header mobile **1 hàng** (burger trái · logo giữa · icon search + giỏ phải · ẩn hotline) 163px → **65px**; nút `.cp-search-toggle` mở ô search thành dải riêng dưới header (Esc/click ngoài/bấm lại để đóng); sửa 3 bẫy: logo đè icon search, specificity rule logo cuối file, clamp 2 chiều làm méo tỉ lệ ảnh | `header.php`, `assets/nav-menu.js`, `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md` | xong (commit cùng 23:35) |
| 2026-09-14 23:35 | deepseek | CP1.2b: icon search + giỏ hàng **35×35px** (trước 44px); logo vào **ĐÚNG TÂM** — phát hiện `a.custom-logo-link` bị kéo giãn hết khung nên ảnh nằm ở đầu khung (lệch −157px ở 768px) → sửa bằng `padding-left: 36px` cho `.cp-logo` + `margin: 0 auto` cho ảnh; đo lại lệch tâm = 0 tại 320/360/390/414/600/768 | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 5665e17) |
| 2026-09-14 23:45 | deepseek | CP1.5: nút mở menu con trên mobile `38×38 → **30×30px**`, `top: 7 → 6px` (5 thuộc tính còn lại đã đúng sẵn) — đo 390px: nút y=6..36 trong li cao 52px, cách lề phải 8px, không đè chữ link, chevron lệch tâm −3px, bấm mở submenu OK | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 33ed5e9) |
| 2026-09-14 23:50 | deepseek | CP1.5: xử lý 2 đề xuất — `top: 6 → 11px` (nút đúng tâm hàng, lệch tâm 0) + thêm `.cp-nav__toggle::before` 44×44 làm **vùng bấm 44px** (nút vẫn hiển thị 30px); bấm 5px ngoài nút ở cả 4 phía vẫn mở menu con, không điều hướng | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit 81a7eae) |
| 2026-09-14 23:55 | deepseek | 3 việc Tùng chốt: (1) giữ 50/50 — chấp nhận nhãn nút xuống 2 dòng ở 769–1280px; (2) giữ icon giỏ/search mobile **35px**, không thêm lớp phủ 44px; (3) hover `.cp-btn-accent` **GIỮ `#333333`** (đo: hover `rgb(51,51,51)` + nhấc 1px, chữ trắng giữ, tương phản 21:1→12.6:1; không đổi sang cam vì nút luôn nằm trên dải CTA nền cam). Kèm: plugin `tl-site-caophat` đã tạo **git repo riêng** (phương án A, baseline `1da7fd6`) | `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit ced9c74) |
| 2026-09-14 20:47 **(giờ thật — dãy giờ các dòng trên lệch)** | deepseek | Phát hiện & sửa **file WORKLOG bị hỏng do 2 nguồn cùng ghi** (tool sửa file + buffer VS Code đang mở → VS Code hiện hộp thoại compare/overwrite): repo gốc mất 1 dòng §2 (khôi phục **nguyên văn 100%** từ git), child theme bị **nhân đôi** 1 dòng §2 (do tôi thử lại 1 edit đã thành công — bài học: tool báo nhiều lỗi thì phải grep xác định lỗi nào TRƯỚC khi thử lại). Kiểm tra lại: 4 repo sạch · 0 dòng trùng · mọi mốc §2 xuất hiện đúng 1 lần | `.ai/WORKLOG.md` (child) + `.ai/WORKLOG.md` (repo gốc) | xong (đã commit `2a2e595` child · `6f4e608` root) |
| 2026-09-14 20:52 (giờ thật) | deepseek | Kiểm tra "còn gì chưa commit": **4 repo đều sạch, 0 file bị ignore**, 9/9 ảnh `assets/images/` đã được theo dõi → không có gì cần commit. Sửa §1: bỏ con số đếm commit cứng (tự mâu thuẫn sau mỗi lần commit tài liệu) → ghi "bắt đầu từ hash, xem `git log`" | `.ai/WORKLOG.md` (child) | xong (đã commit) |
| 2026-09-14 21:05 (giờ thật) | deepseek | CP3.2: nút zoom gallery WooCommerce `z-index: 99 → 9` để menu cấp 2-3 luôn nằm trên (99 > header 50 và panel 60); selector (0,4,2); đo `elementFromPoint`: menu mở → panel che nút zoom ✅, menu đóng → nút zoom trên cùng + vẫn bấm được ✅ | `assets/caophat.css`, `CLAUDE.md`, `AGENTS.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (đã commit) |
| 2026-09-14 21:30 (giờ thật) | deepseek | Dọn file thừa: xoá **6 PNG gốc** không dùng (2.8MB → 84KB) + thư mục rỗng `patterns/` — đã kiểm tra 0 tham chiếu (code child/parent/plugin + DB) trước khi xoá; thêm `.env` vào `.gitignore` cả 3 repo; ghi phân vai DeepSeek (chỉ code + commit local, production do Tùng + Claude Code) | `assets/images/*`, `.ai/FEATURE_MAP.md`, `CLAUDE.md`, `AGENTS.md`, `.ai/WORKLOG.md` | xong (đã commit) |
| 2026-09-15 12:05 (giờ thật) | deepseek | Audit + **GIỮ** kết quả lệnh `/init` do cửa sổ khác chạy (Tùng không cố ý): tạo `.github/` 5 file (2 instructions, skill `cp-ui-verify` + references, prompt `/cp-wrapup`) + sửa `AGENTS.md` (+40/−1) và `CLAUDE.md` (+4/−1, kèm fix tên plugin `tl-site-plugin` → `tl-site-caophat`). Đã kiểm tra: không đụng code/plugin/DB, 3 repo khác sạch, không có commit nào; nội dung đúng luật đã chốt; `.github/` bị deploy loại trừ | `.github/**`, `AGENTS.md`, `CLAUDE.md`, `.ai/WORKLOG.md` | xong (đã commit) |
| 2026-09-15 13:27 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP5.1 — lập nhóm P-index mới `CP5` + làm TRANG DANH MỤC TIN TỨC** (yêu cầu Tùng): thêm `category.php` override (chỉ category) + `inc/news.php` (breadcrumb/thẻ bài viết/sidebar 3 hộp/phân trang/mô tả thu gọn/`cp_news_archive_url()`), khối CSS `CP5.1` (lưới `1048/320`, thẻ 300px, thẻ đầu featured 21/9, không sticky sidebar), sửa link "Xem tất cả →" khối tin trang chủ (trước trỏ về trang chủ vì `page_for_posts` = 0). **2 bẫy đo được:** `:not()` nâng specificity đè rule 1-cột ≤768px → cột chữ 38px + tràn ngang 457>390 ở 390px; `max-height`+`aspect-ratio` co ảnh featured còn 817.8px trong thẻ 1046px. Kiểm chứng: 6 mức bề rộng không tràn ngang, `elementFromPoint` 6 điểm OK, phân trang/click link chạy, 0 PHP notice; **3/12 ảnh thumb hỏng ở LOCAL do metadata trỏ .webp không có file — thuộc phần bàn giao production** | `category.php` (mới), `inc/news.php` (mới), `functions.php`, `assets/caophat.css`, `front-page.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 13:41 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP5.2 — trang chi tiết tin tức** (Tùng giao nối tiếp): thêm `single.php` override (chỉ `post`) + hàm CP5.2 trong `inc/news.php` (breadcrumb 3 cấp · meta chuyên mục/ngày/tác giả/phút đọc · chip tag · điều hướng bài trước–sau · dải "Bài viết liên quan" 4 thẻ mini) + typography `.cp-article__content`; **tái dùng** `cp_news_sidebar()` + lưới 2 cột của CP5.1 nên không phải viết lại breakpoint. **3 bẫy đo được:** (1) tràn ngang do dữ liệu — `<span>` 73 ký tự "=" rộng 731.8px → `scrollWidth` 769 ở 390px → `overflow-wrap: break-word`; (2) ảnh đầu bài DỌC 992×1388 cao 1388px → chốt `max-height: 620px` + `width: auto` + `margin-inline: auto` (443×620, ratioBox 0.714 = natural 0.715, không méo/crop); (3) `auto-fit` cho mục lẻ làm "Bài trước" giãn 1048px → `repeat(2, minmax(0,1fr))` + `:only-child`. Kiểm chứng: 6 mức bề rộng không tràn ngang, 5 luồng click đúng đích, `ul` trả lại `disc`, 0 PHP notice | `single.php` (mới), `inc/news.php`, `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 13:46 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP5.2 — ẩn mục "Để lại một bình luận"** khỏi trang chi tiết tin tức (yêu cầu Tùng): bỏ `comments_template()` trong `single.php` (bỏ cả truy vấn, không ẩn bằng CSS). Kiểm DB trước khi bỏ: 7/7 comment toàn site là ghi chú đơn hàng gắn `shop_order` đã xoá, **không bài tin nào có bình luận thật** → không giấu nội dung. Kiểm chứng: `grep -c 'id="comments"|comment-respond|Để lại một bình luận'` = **0**; `php -l` sạch; template khác không đổi | `single.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 13:56 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP6.1 + CP6.2 — trang nội dung tĩnh (Admin → Trang)** (Tùng giao): thêm `page.php` override + `inc/page.php` (breadcrumb phân cấp · guard `cp_is_woo_endpoint_page()` để cart/checkout/order-received/my-account không bị bọc hero 2 lần · `cp_page_sidebar()` = hộp sub-nav "Trong mục này" + "Hỗ trợ trực tuyến" + "Cam kết Cao Phát") + khối CSS `.cp-page*`; **tái dùng** lưới `.cp-news-layout`/`.cp-news-aside` (CP5.1) và typography `.cp-article__content` (CP5.2) → không nhân đôi CSS. TRƯỚC: trang dùng `page.php` theme cha → không `.cp-container`, **không H1**, nội dung bó 720px. **Bẫy đo được:** caption WordPress cũ `<figure class="wp-caption" style="width:1000px">` → `scrollWidth` **1037** ở 1024/768/390, phải `!important` mới đè được inline style. Kiểm chứng: 4/4 trang × 6 mức bề rộng không tràn ngang; sub-nav 10 mục + `current_page_item` accent; hồi quy `/cart/` `/checkout/` mỗi trang đúng 1 hero + 0 `.cp-container` lồng; `/` vẫn `front-page.php`; 0 PHP notice; đã xoá session WooCommerce test, không tạo đơn rác | `page.php` (mới), `inc/page.php` (mới), `functions.php`, `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 14:01 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CSS theo yêu cầu Tùng:** `.cp-nav__toggle` trong `@media (max-width: 768px)` — `top: 11px` → **`top: 5px`** (hiểu là nút mở menu con trên mobile; lệnh Tùng gửi không kèm selector). Đo ở 390px: `<li>` cao 51.8px, nút 30×30 cách mép hàng 5px → tâm nút 20px (trước 26px = tâm hàng) ⇒ chevron cao hơn tâm hàng **5.9px**, vùng bấm 44px phủ `-2 .. 42px` **nhô 2px** lên mép hàng trên. Kiểm chứng: `elementFromPoint` tâm nút / 1px trên / 5px trên / 5px dưới đều trả về nút; bấm 2 lần mở rồi đóng submenu, không điều hướng; `php`/CSS ngoặc vẫn cân | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 14:09 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **Điều tra sự cố dữ liệu DB** (Tùng báo Warning ở Rank Math wizard): tìm ra **9 option + 348 attachment metadata** serialize HỎNG ĐỘ DÀI do migrate domain không hiểu serialize (`s:23:"http://localhost:8888/"` trong khi chuỗi thật 22 ký tự — URL cũ `https://www.caophat.vn/` = 23) → `maybe_unserialize()` = false → `get_option()` = false → Rank Math truy cập `$all_services['sites']` (class-search-console.php:78) phát warning; 2 warning `headers already sent` là hệ quả của `WP_DEBUG_DISPLAY=true` ở docker-compose.yml. **Tái hiện** bằng user admin tạm + Playwright (3 URL wizard đều warning) → **sửa 9/9 option** (giữ nguyên dữ liệu, backup `/tmp/cp-serialize-backup-20260915-070806.json`) → **warning = 0**; xoá user test. **Còn lại:** 348/2627 attachment metadata hỏng cùng kiểu (234 thiếu file gốc ở local) → sửa/`wp media regenerate` ở production | DB local (`wp_options` ×9) + `/tmp/fix-serialize.php`, `/tmp/audit-serialize.php`, `/tmp/audit2.php` (script tạm, không thuộc repo theme), `.ai/WORKLOG.md` | xong phần option; phần attachment chờ Tùng quyết |
| 2026-09-15 14:18 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CSS theo yêu cầu Tùng (trang danh mục sản phẩm, mobile):** lưới sản phẩm `gap: 22px → 10px` (`@media (max-width: 900px)` — cùng breakpoint lúc lưới về 2 cột) + `@media (max-width: 1024px)`: `.cp-shop-aside { order: 2 }` / `.cp-shop-main { order: 1 }` đưa khối "Danh mục sản phẩm" xuống DƯỚI lưới sản phẩm (dùng `order`, không đổi DOM). Đo 390px `/danh-muc/phu-kien-cua/`: trước main y=1870 (hộp danh mục cao 1603.8px ở trên) → sau main y=234.6 / aside y=4126.3; gap 22→10px, card 141→147px; 7/7 mức bề rộng không tràn ngang; desktop không đổi | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 14:24 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CSS theo yêu cầu Tùng:** `.cp-section { padding: 64px 0 → 40px 0 }` — đo phạm vi: đổi ở rule gốc nên 5 khối trang chủ + `.cp-section` shop/danh mục SP + `.cp-news-section` + dải CTA trang chi tiết SP = **64/64 → 40/40**; các khối padding riêng không đổi (single 28/0, cart/checkout 40/64, thankyou 40/120, related-band 40/44, article 26/44, page 28/44); 8 URL ở 390px không tràn ngang. **Sửa kèm lỗi tràn ngang phát hiện khi đo:** `/san-pham/` 390px `scrollWidth` 501 do phân trang 19 trang (dãy 458px) → thêm `flex-wrap: wrap` + `justify-content: center` cho `ul.page-numbers` trong `.cp-shop-main` → 390 (hết tràn) | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 14:26 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CSS theo yêu cầu Tùng (mobile):** `.cp-shop-main { padding: 22px → 10px }` (đặt cùng block `@media (max-width: 900px)` với `gap: 10px`). Đo: card sản phẩm 390px **147 → 159px**, 768px **336 → 348px**, 900px **402 → 414px**; desktop ≥1024px giữ 22px; 7/7 mức bề rộng không tràn ngang; CSS ngoặc cân | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 14:47 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.4 + CP2.5 — khối trang chủ theo danh mục (yêu cầu Tùng):** Customizer 2 section mới (3 slot sản phẩm theo `product_cat`, 2 slot tin theo `category`) — mỗi slot chọn **danh mục · tiêu đề · sắp xếp (mới/cũ nhất) · số lượng · bố cục (4/3/2 cột · cuộn ngang)**, để trống danh mục = ẩn khối; nút "Xem thêm" trỏ về trang danh mục. Code: `functions.php` (helper + 25 control), `template-parts/home-cat-products.php` + `home-cat-news.php` (mới), `front-page.php`, CSS `.cp-cat-block`. Kiểm chứng: UI Customize liệt kê đúng danh mục từng loại; 4 khối render đúng số lượng/bố cục; sắp xếp cũ nhất → bài 584; slot trống → ẩn; click "Xem thêm" → đúng trang danh mục; 6/6 mức bề rộng không tràn ngang. **Bẫy:** bố cục "cuộn ngang" làm document tràn 390px (scrollWidth 557) → chỉ `contain: paint` sửa được (A/B đã loại 5 cách khác). Theme mod demo đã set; user admin test đã xoá | `functions.php`, `front-page.php`, `template-parts/home-cat-products.php` (mới), `template-parts/home-cat-news.php` (mới), `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:01 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.6 — khối "Danh mục nổi bật" (yêu cầu Tùng):** Admin chọn danh mục hiển thị + **kéo thả sắp thứ tự** + chọn bố cục (Lưới 6/4/3 cột · Hiển thị ngang). Thêm `inc/customizer.php` (control tuỳ biến `CP_Term_Order_Control` = checkbox + jQuery UI sortable, setting `cp_cats_selected` = CSV term_id theo thứ tự), `assets/customizer-cats.js` + `.css` (chỉ nạp trong Customize), cập nhật `template-parts/home-categories.php` (rỗng → fallback 6 danh mục nhiều SP nhất) + CSS `.cp-cats--cols-*` / `--horizontal`. Kiểm chứng: tick 3 danh mục → `89,69,83`; kéo thả → `69,83,89`; **Publish** → front-end hiện đúng thứ tự đó + href đúng; `horizontal` = 1 hàng cuộn (contain: paint); 4/4 mức bề rộng không tràn document. User admin test đã xoá, demo chốt `cols-3` + 6 danh mục | `inc/customizer.php` (mới), `assets/customizer-cats.js` (mới), `assets/customizer-cats.css` (mới), `template-parts/home-categories.php`, `functions.php`, `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:12 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **Bỏ dòng đếm "N sản phẩm" dưới tiêu đề thẻ danh mục** (yêu cầu Tùng) — `template-parts/home-categories.php`: bỏ nhánh `else` in `_n( '%d sản phẩm' )`, giữ mô tả 8 từ khi danh mục có mô tả. Kiểm: `grep -c '&gt;[0-9]+ sản phẩm&lt;'` trên trang chủ = **0**; thẻ không có mô tả chỉ còn tiêu đề; `php -l` sạch. Ghi nhận thêm: theme mod lúc kiểm là `cp_cats_selected=[74,68,75,71,72,73,77,78,76,70,995,994]` + `cols-6` (**Tùng đang cấu hình song song** → 12 thẻ là đúng) và **4/4 mức bề rộng không tràn** (6→3→2→1 cột) | `template-parts/home-categories.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:14 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **Bỏ nhãn `.cp-eyebrow` "Danh mục sản phẩm" phía trên tên danh mục** (yêu cầu Tùng) — `template-parts/home-cat-products.php`: bỏ `<span class="cp-eyebrow">`, section head còn `<h2>` + nút "Xem thêm". Kiểm: `grep -c '>Danh mục sản phẩm<'` trên trang chủ = **0**; còn đúng 5 eyebrow khác (Bán chạy nhất · Danh mục nổi bật · Khuyến mãi · Kiến thức & báo giá · Tin tức); `php -l` sạch | `template-parts/home-cat-products.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:21 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.4 + CP2.5 — chuyển 3 khối cố định sang REPEATER** (yêu cầu Tùng: nút "Thêm danh mục" tự hiện trường nội dung, không giới hạn số khối): thêm `CP_Blocks_Repeater_Control` (`inc/customizer.php`) — mỗi hàng = 1 khối có handle kéo thả + nút xoá × + 5 trường (danh mục · tiêu đề · sắp xếp · số lượng · bố cục) + hàng mẫu ẩn để clone; setting `cp_pblocks`/`cp_nblocks` lưu **JSON** (sanitize `cp_blocks_sanitize()`); JS `assets/customizer-blocks.js` + `.css` (chỉ nạp trong Customize). Template part `home-cat-products.php` / `home-cat-news.php` đọc JSON, gọi 1 lần (bỏ `index`). Xoá code 3-slot cũ khỏi `functions.php` (187 dòng) + dọn 25 theme mod cũ, migrate cấu hình demo sang JSON. Kiểm chứng (wp-admin thật): 3 hàng số đúng 1-2-3 + nút "+ Thêm danh mục sản phẩm" + sortable; bấm thêm → 4 hàng; chọn danh mục 632 + bố cục 2 cột → JSON đổi; kéo khối mới lên đầu → JSON đổi thứ tự; **Publish** → front-end 4 khối đúng thứ tự (cols-2/4/3/scroll); xoá 1 khối → Publish → còn 3; section tin cũng repeater (1 hàng + nút "+ Thêm chuyên mục tin tức"). 6/6 mức bề rộng không tràn ngang, 0 PHP notice, user test đã xoá | `inc/customizer.php`, `assets/customizer-blocks.js` (mới), `assets/customizer-blocks.css` (mới), `template-parts/home-cat-products.php`, `template-parts/home-cat-news.php`, `front-page.php`, `functions.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:33 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.7 — chọn sản phẩm ưu tiên trong từng khối SP (yêu cầu Tùng):** mỗi khối trong repeater có mục "Sản phẩm ưu tiên" — mở panel → AJAX (`wp_ajax_cp_block_products`, nonce + `edit_theme_options`) trả sản phẩm CỦA DANH MỤC của khối → tick từng SP + **kéo thả** thứ tự → lưu CSV vào khoá `products` của JSON `cp_pblocks`. Render: `wc_get_product()` theo đúng thứ tự CSV **đứng trước**, phần còn thiếu của `count` mới lấp từ danh mục (`exclude` = ID đã chọn). Kiểm chứng: 12 SP tải qua AJAX; tick 929+919 → `929,919`; kéo 919 lên trước → `919,929`; Publish → front-end 6 SP với **919, 929 đứng đầu** rồi 4150/1712/1708/915; 6/6 mức bề rộng không tràn, 0 PHP notice; user test đã xoá. Seed 1 khối demo (2 SP ưu tiên) để Tùng xem | `inc/customizer.php`, `assets/customizer-blocks.js`, `assets/customizer-blocks.css`, `template-parts/home-cat-products.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:46 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.8 — bố cục "Cuộn ngang": ẩn thanh trượt + nút ‹ › hai đầu** (yêu cầu Tùng): thêm `cp_scroller_open()/cp_scroller_close()` (`functions.php`) in `.cp-scroller` + 2 nút CHỈ khi layout `scroll`, `assets/scroller.js` (vanilla) cuộn 1 thẻ/lần + tự ẩn nút ở đầu/cuối (`.is-start`/`.is-end`, `.cp-scroller--static`), CSS ẩn scrollbar (`scrollbar-width: none` + `::-webkit-scrollbar`) + nút tròn 40px (34px ≤768px). Kiểm chứng: scrollbar chiếm **0px** + computed `none`; `elementFromPoint` tâm nút = BUTTON; bấm › → scrollLeft 0 → **270** (đúng 1 thẻ 250+20), nút ‹ hiện; bấm ‹ → về 0, nút ‹ ẩn; 6/6 mức bề rộng không tràn ngang, 0 PHP notice. Ghi nhận: Tùng đang chỉnh song song (`cat=78, count=20, layout=scroll`) | `functions.php`, `assets/scroller.js` (mới), `assets/caophat.css`, `template-parts/home-cat-products.php`, `template-parts/home-cat-news.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 15:51 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.8 — chỉnh vị trí nút cuộn (yêu cầu Tùng):** desktop `.cp-scroller__btn--prev { left: -18px }` (trước -6px) → nút phải đổi tương ứng `right: -18px`; mobile ≤768px về **-15px** (tỷ lệ nút 34/40 × 18 ≈ 15) để 2 nút vẫn đối xứng. Đo lại: 1440/1024 → lệch 2 đầu `-18/-18` (đối xứng), 768/390 → `-15/-15`, nút trái vẫn trong viewport, `scrollWidth == innerWidth` ở cả 4 mức | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:01 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.4/2.5 — mobile: link "Xem thêm →" của khối danh mục xuống DƯỚI list + canh giữa** (yêu cầu Tùng): CSS only — `.cp-cat-block .cp-container` → `flex column`, `.cp-section-head { display: contents }`, `order` (title 1 · list/scroller 2 · link 3 + `align-self: center` + `margin-top: 20px`), KHÔNG đổi DOM/JS. Đo: 390px → link `top 7970 ≥ list bottom 7950` và **lệch tâm 0**; 768px → lệch tâm 0; 1440px KHÔNG đổi (link vẫn trên-phải, `head display: flex`, container `block`); không tràn ngang ở cả 3 mức | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:09 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.2 — mobile trang chủ: tiêu đề khối nổi bật hơn + "Xem thêm →" thành BUTTON** (yêu cầu Tùng): CSS thuần `@media (max-width:768px)` — `.cp-section-head h2` **24px → 20px** + gạch nhấn vàng 46×3px (`h2::after`), head xếp **dọc** (`column`/gap 14px/`margin-bottom: 20px`), eyebrow 11.5px, `.cp-section-head p` 14px; `.cp-section-head .cp-link-more` → pill `inline-flex` **44px**, nền `--cp-primary` + chữ `--cp-on-primary` (8.9:1). Đo 390px: h2 20/27px, bar 46×3px `rgb(251,175,2)`, nút 136×44 / 129×44 (khối danh mục), `elementFromPoint` tâm **và** sát đáy nút trên cả 7 nút → `A.cp-link-more`, click → đúng URL (vd `/danh-muc/cua-nhua-composite/`); 360/390/768/1024/1440 không tràn ngang; **desktop 1440 không đổi** (h2 34px, head `row`, link 25.6px trong suốt). Ghi nhận: desktop `.cp-link-more` vẫn là chữ thường màu `inherit` (do `.cp a` (0,1,1) đè rule 1 class) — chưa đụng tới, Tùng muốn đổi thì báo | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:15 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.2 — DESKTOP: tiêu đề khối nổi bật/hiện đại hơn** (yêu cầu Tùng): sửa rule GỐC `.cp-section-head` (không phải media query) — eyebrow thành **chip** (`padding 5px 13px`, `radius 999px`, nền `rgba(251,175,2,.16)`, chữ `--cp-accent-dark` vì `--cp-accent` chỉ 4.32:1 < AA → `#a63817` cho **5.91:1**/nền trắng và **5.46:1**/nền kem), h2 `clamp(24px,3vw,34px)` → **`clamp(26px,2.7vw,38px)`** + `line-height 1.2` + `letter-spacing -.6px`, **gạch nhấn vàng 56×4px** (`h2::after`), head `gap 28px` + `padding-bottom 22px` + **kẻ mảnh `1px var(--cp-line)`** + `margin-bottom 34px`; block mobile thêm reset `padding-bottom: 0; border-bottom: 0`. Đo: 1440 h2 **38px/45.6** (trước 34px), 1280 → 34.56 · 1100 → 29.7 · 1024 → 27.65 · 769 → 26; 768/390 vẫn bản mobile (h2 20px, bar 46×3, nút 136×44); 7/7 link `elementFromPoint` = `A.cp-link-more` + click → `/san-pham/`; 7 mức bề rộng không tràn ngang; `/san-pham/` và `/chuyen-muc/tin-tuc/` có **0** `.cp-section-head` (thay đổi chỉ nằm ở trang chủ); 0 PHP notice | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:24 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.6 — khối "Danh mục nổi bật": thêm 2 trường nhập liệu trong admin + bỏ mô tả phụ và link "Xem tất cả →"** (yêu cầu Tùng): setting **`cp_cats_label`** ("Nhãn nhỏ trên tiêu đề") + **`cp_cats_title`** ("Tiêu đề khối") trong section Customizer `cp_home_cats` (`inc/customizer.php`, `sanitize_text_field`, transport refresh); `front-page.php` đọc `get_theme_mod(...) ?: __(...)` (rỗng → mặc định "Danh mục nổi bật" / "Khám phá theo dòng sản phẩm"); xoá `<p>Đầy đủ các dòng cửa…</p>` + `<a class="cp-link-more">Xem tất cả →</a>` khỏi head của khối. E2E (Playwright + admin tạm, đã dọn sạch sau test): Customizer section có **4 control** (2 text mới + `cp_cats_selected` + `cp_cats_layout`), nhập chuỗi thử + bấm Publish → `wp theme mod list` ghi đúng giá trị → trang chủ hiển thị đúng chuỗi mới; sau khi xoá mod thử → tự về mặc định. Đo: head **188 → 127px** (1440) · **183 → 73px** (390), `soP = 0`, `soLinkMore` của khối = 0 (toàn trang còn 6), 390/768/1440 không tràn ngang, 0 PHP notice | `inc/customizer.php`, `front-page.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:27 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP4.1 — mobile ≤768px: `.cp-btn { padding: 10px 14px }`** (yêu cầu Tùng; trước `12px 22px`): thêm 1 media query ngay dưới nhóm `.cp-btn` (rule gốc không đổi → desktop giữ nguyên). Đo 390/768px: `padding 10px 14px`, cao **50 → 46px** (còn ≥ 44px tap target), ngang −16px mỗi nút (`Xem sản phẩm` 155→139 · `Nhận báo giá 24/7` 187→171 · `Gọi ngay` 219→203); 1024/1440 vẫn `12px 22px` 50px; không tràn ngang cả 4 mức; CSS ngoặc 930/930 | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:31 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.6 + CP2.8 — "Danh mục nổi bật" trên mobile = 1 HÀNG 3 CỘT + nút cuộn ‹ ›** (yêu cầu Tùng): CSS ≤768px đổi mọi `cp_cats_layout` sang `display: flex; gap: 10px; overflow-x: auto; scroll-snap-type: x mandatory; contain: paint` + ẩn thanh trượt, thẻ `flex: 0 0 calc((100% - 20px)/3)` (bỏ lưới 2 cột & 1 cột ở ≤520); `template-parts/home-categories.php` bọc `cp_scroller_open()/cp_scroller_close()`; `assets/scroller.js` thêm `.cp-cats` vào `LIST_SEL`; chữ trên thẻ gọn (padding 10px, `b` 13px, mô tả clamp 2 dòng). Đo: 390px → thẻ **110px, đúng 3 thẻ/hàng** (360 → 100 · 520 → 153 · 768 → 236; scrollW 1430 > 350), thanh trượt **0px**; nút › `elementFromPoint` = **BUTTON**, bấm › → `scrollLeft 0 → 120` (1 thẻ) + nút ‹ hiện, bấm ‹ → về 0, cuối dải `.is-end` + nút › ẩn; **desktop 1024/1440 không đổi** (`grid`, gap 22, `.cp-scroller--static` ẩn cả 2 nút); không tràn ngang ở 6 mức bề rộng; 0 PHP notice; `php -l` + `node --check` sạch; CSS ngoặc 934/934 | `assets/caophat.css`, `assets/scroller.js`, `template-parts/home-categories.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:50 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.9 (MỚI) — khối "tab sản phẩm" trên trang chủ, 4 tab CỐ ĐỊNH** (Tùng chốt qua lựa chọn: Mới · Bán chạy · Hot · Khuyến mãi, mỗi tab cấu hình tiêu đề + số lượng + bố cục): `functions.php` thêm `cp_product_tabs()` / `cp_tab_layouts()` / `cp_tab_products()` (new = date DESC · best = `total_sales` · hot = cờ "Nổi bật" WC · sale = `wc_get_product_ids_on_sale()`; filter `cp_tab_products`) + enqueue `assets/tabs.js`; `inc/customizer.php` thêm section **`cp_home_tabs`** (16 control: show/title/count/layout × 4 tab); `template-parts/home-tabs.php` (MỚI) render tab pill + panel tái dùng `.cp-cat-block--<layout>` + `cp_scroller_open()/close()`; `assets/tabs.js` (MỚI) đổi tab + a11y (tablist/tab/tabpanel, ← → Home End) + bắn `resize` cho scroller; `front-page.php` gọi template part sau khối "Danh mục nổi bật"; CSS block CP2.9 cuối `caophat.css`. **BẪY đã dính + đã sửa:** tên `.cp-tabs` / `.cp-tab-panel` bị hệ tab CŨ (dead CSS dòng ~747: `.cp-tab-panel { display: none }` + `.cp-tabs { margin-top: 56px }`) đè → panel cao **0px**; đổi sang `.cp-tablist` / `.cp-tablist__btn` / `.cp-tabpane`. Đo: 1440 → 4 tab + lưới 4 cột (card 334px) · 1024 → 3 cột · 768/390 → 2 cột (tab xuống 2 hàng ở 390); `elementFromPoint` tâm + sát đáy 4 tab = BUTTON (active 8.9:1); tab 3 + ← → aria/hidden đúng; tab 2 layout `scroll` → bấm › `scrollLeft 0 → 270`; E2E admin tạm: Customizer 16 control → nhập tiêu đề + bỏ tick tab hot → Publish → trang chủ 3 tab đúng nhãn (đã xoá mod thử + user tạm); 0 PHP notice; `php -l`/`node --check` sạch; CSS ngoặc 948/948 | `functions.php`, `inc/customizer.php`, `template-parts/home-tabs.php`, `assets/tabs.js`, `assets/caophat.css`, `front-page.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:54 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.9 — mobile: thanh tab = 1 DÒNG + trượt ngang** (yêu cầu Tùng): trong `@media (max-width:768px)` của block CP2.9 đổi `.cp-tablist` sang `flex-wrap: nowrap; overflow-x: auto; scroll-snap-type: x proximity; contain: paint; padding: 4px 2px` + ẩn thanh trượt (`scrollbar-width: none` + `::-webkit-scrollbar`), nút `.cp-tablist__btn { flex: 0 0 auto }` (không bị co chữ). Đo: 390px → thanh **48px / 1 hàng** (trước 2 hàng), nút 125·162·123·176, `scrollW 613 > clientW 350` → trượt được, thanh trượt **0px**; trượt hết → `scrollLeft 263`, tab "Sản phẩm khuyến mãi" hiện đủ + `elementFromPoint` = BUTTON → bấm được (panel 4 hiện, `hidden` đúng); 360/520 trượt được, **768 vừa khít**; `≥769px` giữ `flex-wrap: wrap` (desktop không đổi); không tràn ngang ở 360/390/520/768/769/1024/1440; CSS ngoặc 949/949; 0 PHP notice | `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:56 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.2 — xoá 2 khối "Bán chạy nhất" + "Khuyến mãi" trên trang chủ** (yêu cầu Tùng: 2 mục này không dùng nữa, đã có trong khối tab CP2.9): bỏ 2 `<section class="cp-section alt">` trong `front-page.php` + **`git rm template-parts/home-products.php`** (chỉ 2 chỗ đó dùng); cập nhật `README.md` + FEATURE_MAP (dòng ghi rõ ĐÃ XOÁ, khôi phục bằng `git checkout HEAD -- …`). Kiểm: trang chủ còn **9 khối** (hero · features · Danh mục · **tab CP2.9** · 2 khối danh mục · CTA · khối tin · Blog), `.cp-section-head` **5**, `.cp-link-more` **4**, `.cp-products` **6**, tab 4 + đúng 1 panel hiện; 0 dấu vết "Sản phẩm được ưa chuộng"/"Sản phẩm đang giảm giá"; không tràn ngang ở 390/768/1440; 0 PHP notice (chỉ 1 dòng khớp grep là do chính comment giải thích trong `front-page.php`); `php -l` sạch | `front-page.php`, `template-parts/home-products.php` (xoá), `README.md`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 16:59 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.9 — thêm icon trước mỗi nhãn tab** (yêu cầu Tùng): `cp_tab_icon( $source )` (`functions.php`) trả SVG inline 24×24 `stroke: currentColor` — new = 2 tia sáng · best = biểu đồ đi lên · hot = ngọn lửa · sale = thẻ giảm giá; `template-parts/home-tabs.php` in trong `.cp-tablist__icon` (`aria-hidden`, TRƯỚC chữ); CSS `.cp-tablist__btn { gap: 7px }` + `.cp-tablist__icon svg { 16px }`, mobile 14px/gap 6px/padding 13px. Đo: 1440 → nút **163·203·160·218** (trước 140·180·137·195), cao vẫn **42px**; 390 → icon 14px, nút 40px, dải **vẫn trượt được** (768 vừa khít); nút active nền `rgb(251,175,2)` + chữ/icon `rgb(36,29,5)` = **8.9:1**, `iconKeThuaMauChu = true`; bấm ĐÚNG tâm icon (`elementFromPoint = path`) → đổi tab (panel `cp-tab-panel-hot` hiện); `ArrowRight` → tab kế (`sale`); không tràn ngang 390/768/1440; `php -l` sạch; CSS ngoặc 952/952; 0 PHP notice | `functions.php`, `template-parts/home-tabs.php`, `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 17:04 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.2 — xoá khối "BLOG" ("Kiến thức & báo giá" / "Bài viết mới nhất") trên trang chủ** (yêu cầu Tùng): bỏ `<section class="cp-section">` cuối `front-page.php` + **`git rm template-parts/home-blog.php`** (chỉ khối này dùng); cập nhật `README.md` (thay bằng 2 dòng template-part repeater) + FEATURE_MAP (dòng ĐÃ XOÁ + khôi phục bằng `git checkout HEAD -- …`). Kiểm: trang chủ còn **8 khối** (hero · features · Danh mục · tab CP2.9 · 2 khối danh mục · CTA · khối tin), `.cp-section-head` **4**, `.cp-link-more` **3**, `.cp-posts` **0**, tab 4; `textContent` KHÔNG còn "Bài viết mới nhất"/"Kiến thức & báo giá" (1 dòng khớp grep HTML là comment giải thích); không tràn ngang ở 390/768/1440; 0 PHP notice; `php -l` sạch | `front-page.php`, `template-parts/home-blog.php` (xoá), `README.md`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 17:08 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.3 — thêm 6 trường nhập vào mục admin "Trang chủ Cao Phát"** (yêu cầu Tùng): `cp_hero_btn1` (Xem sản phẩm) · `cp_hero_btn2` (Nhận báo giá 24/7) · `cp_hero_stat1_value` (10+ năm) · `cp_hero_stat1_label` (Kinh nghiệm thi công) · `cp_hero_stat2_value` (5.000+) · `cp_hero_stat2_label` (Công trình hoàn thiện) — thêm vào `$fields` của section `cp_home` (`functions.php`), `sanitize_text_field` + transport `refresh`; `front-page.php` đọc `get_theme_mod(...) ?: __(...)` và `esc_html` (2 nút hero + 2 ô `.cp-hero-float`). E2E (admin tạm, đã dọn sạch): mục "Trang chủ Cao Phát" có **12 control** (6 cũ + 6 mới) đúng nhãn; nhập thử nút 1 = "XEM SẢN PHẨM THỬ" + số liệu 2 = "9.999+" → Publish → hero hiển thị đúng chuỗi mới, các ô không nhập vẫn giữ mặc định; sau khi xoá mod thử → về mặc định (`10+ năm`, `Xem sản phẩm`); 0 PHP notice; `php -l` sạch | `functions.php`, `front-page.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 17:12 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.3 — thêm 2 ô nhập LINK cho 2 nút hero** (yêu cầu Tùng): setting `cp_hero_btn1_url` ("trống = trang cửa hàng") + `cp_hero_btn2_url` ("trống = gọi hotline"), control type `url`; `functions.php` thêm nhánh sanitize **`esc_url_raw`** cho type `url` trong loop `$fields` (giữ `tel:`, `/lien-he/`, query string — `sanitize_text_field` không đảm bảo); `front-page.php` `$cp_hero_btn1_url = get_theme_mod(...) ?: $cp_shop` và `$cp_hero_btn2_url = ?: 'tel:' . $cp_tel`, echo `esc_url`. E2E (admin tạm, đã dọn sạch): section "Trang chủ Cao Phát" có **14 control** (12 cũ + 2 link), 2 control là `input[type=url]` rỗng mặc định; nhập `/lien-he/` + `tel:0901234567` → Publish → hero `href` = `/lien-he/` và `tel:0901234567`; sau khi xoá mod thử → `http://localhost:8888/san-pham/` + `tel:0834021021`; `php -l` sạch; 0 PHP notice | `functions.php`, `front-page.php`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 17:29 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.5 — thêm bố cục "Nổi bật: 1 bài trái + 5 bài phải"** (yêu cầu Tùng; Tùng chọn PA "thêm bố cục mới, GIỮ 3 bố cục cũ rồi set khối demo sang bố cục này"): option `featured` trong select "Bố cục" của khối tin (`inc/customizer.php`; "Số lượng" = TỔNG số bài: bài 1 → thẻ lớn cột trái, `count − 1` → danh sách cột phải; ghi chú vào mô tả section) + markup mới `.cp-news-feature` > `__main` (ảnh 16:9 + nhãn chuyên mục + `h3` + ngày `d/m/Y` + trích đoạn 28 từ) & `__list` > `.cp-news-row` (thumb 108px 4:3 + `h3` + ngày, gạch phân cách) ở `template-parts/home-cat-news.php` + CSS mới ở `assets/caophat.css` (lưới `1.35fr/1fr` gap 24; ≤1024 1 cột + ảnh 21:9; ≤768 thumb 88px). **BẪY đã dính + đã sửa:** bài KHÔNG có ảnh đại diện → `.cp-news-row__body` là con duy nhất nên rơi vào cột `108px` (đo 1440: body **108** thay vì 547.5, tiêu đề cao 65px) → thêm `cp-news-row--noimg` + rule **`.cp-news-row.cp-news-row--noimg`** (2 class = 0,2,0, nếu 1 class thì rule `.cp-news-row` trong `@media ≤768` ở cuối file đè lại). **Đo Playwright:** 1440 lưới `790.45/585.55` gap 24, thẻ trái 790.5×608.5 (ảnh 16:9 = 1.778), list 585.5×525.8, 5 dòng thumb 108×81 (4:3); 1280 `698.5/517.5` · 1100 `595.1/440.9` · 1024 1 cột `984` + ảnh 21:9 · 768 thumb 88×66 · 390 `350`; **6/6 mức bề rộng không tràn ngang**; `elementFromPoint` tại đầu dòng tiêu đề + link "Xem thêm →" đều trả `<a>`; click thẻ trái → bài, dòng 3 → bài, nhãn chuyên mục + "Xem thêm" → `/chuyen-muc/tin-tuc/`; mobile 390 "Xem thêm →" nằm DƯỚI khối (gap 20px) và bấm được; 0 PHP notice. Demo `cp_nblocks` đặt `layout=featured, count=6` (1 + 5). Ghi nhận **2/5 thumb trong khối vỡ là do metadata ảnh local** (`2299-533x400.webp` / `2277-533x400.webp` không có trong `uploads/`), không do CSS/PHP | `inc/customizer.php`, `template-parts/home-cat-news.php`, `assets/caophat.css`, `AGENTS.md`, `CLAUDE.md`, `.ai/FEATURE_MAP.md`, `.ai/WORKLOG.md` | xong (chưa commit — chờ Tùng chốt nhãn model) |
| 2026-09-15 18:05 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.5 — canh để cột trái `.cp-news-feature__main` LUÔN cao BẰNG cột phải `.cp-news-feature__list`** (yêu cầu Tùng, tiếp việc bố cục `featured`): `.cp-news-feature` đổi `align-items: start → stretch`; cột trái `.cp-news-feature__main .cp-news-feature__media { flex: 1 1 auto; min-height: 0; max-height: 520px }` + `.cp-news-feature__main .cp-news-feature__body { flex: 1 1 auto }`; cột phải `.cp-news-feature__list { justify-content: space-between }` (3 rule ở `assets/caophat.css` ~2490–2535, có ghi chú tại chỗ). **Đo 13 mức bề rộng:** `lech = mainH − listH = **0**` ở MỌI mức side-by-side — 1440 `608.5/608.5` · 1366 `584.6/584.6` · 1280/1220/1150/1100/1060/1025 `559/559`; trước đổi: 1440 lệch **−82.8** (`listVsMainBottom −82.8 → 0`), 1100 lệch **+34.5**. Dòng cột phải giữ nguyên **110px** → phần chênh thành khoảng giữa các dòng (1440: `12.4px × 4`). Ảnh cột trái crop nhẹ (do phải giãn cho bằng): `1.778 @1440/1366` (không đổi — vốn đã cao hơn) · `1.773 @1280` · `1.726 @1220` · `1.666 @1150` · `1.621 @1100` · `1.584 @1060` · `1.551 @1025`. **Trần `max-height: 520px` thêm sau khi đo ca xấu:** đặt `count=12` (11 dòng) mà không có trần thì ảnh bị kéo thành **ảnh DỌC tỉ lệ 0.75**; có trần → ảnh dừng **520px**, thẻ vẫn cao **bằng** cột phải (`1185.8/1185.8`), phần chênh dồn xuống phần chữ. ≤1024 xếp 1 cột **KHÔNG đổi** (ảnh 21:9 = **2.334**; mỗi khối là 1 hàng lưới riêng nên `stretch` vô hiệu). **13/13 mức `scrollWidth == innerWidth`**, `elementFromPoint` + click vẫn đúng (`cp-link-more`, `A`), dòng cuối cột phải vẫn bỏ gạch (`border-bottom: 0`), 0 lỗi JS, 0 PHP notice. Khối demo giữ `layout=featured` + `count=6`. Ảnh đối chiếu: `/tmp/cp-pw/shot-eq-1440.png` · `-1100.png` · `-390.png`. |
| 2026-09-15 18:08 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP2.3 — sửa lỗi chữ DÍNH nhau ở 2 ô số liệu hero** (yêu cầu Tùng: "10+ nămKinh nghiệm thi công" / "5.000+Công trình hoàn thiện"): nguyên nhân là **CSS**, không phải PHP — `.cp-hero-float b` (20px, `--cp-primary`) và `small` (12.5px, `--cp-muted`) đều là **inline**, markup `front-page.php` không có khoảng trắng giữa `</b><small>` (kiểm `curl | grep -c 'năm</b><small'` = 1) → ở bề rộng đủ chỗ (1440/768) 2 chữ nằm CÙNG 1 dòng dính liền, ở 1024/390 thì `small` tự xuống dòng tuỳ tiện (đo: `small` cao 43px = 2 dòng ở 1024). Sửa 1 rule: `.cp-hero-float > div { display: flex; flex-direction: column; line-height: 1.25 }` (`assets/caophat.css` ~416 + ghi chú tại chỗ). **Đo sau khi sửa — giống hệt nhau ở 1440/1024/768/390:** mỗi ô là cột dọc, `b` **126.4×25** tại `top 595` · `small` **126.4×15.6** tại `top 620` → khe hở dọc **0** (số liệu trên, nhãn dưới), 2 ô thẳng hàng nhau; khối float **462.5×56 → 303.7×64.6** (hẹp hơn 159px, cao hơn 8.6px); 4/4 mức `scrollWidth == innerWidth`; 0 PHP notice. Ảnh đối chiếu: `/tmp/cp-pw/shot-hero-stats-1440.png` · `-390.png`. |
| 2026-09-15 18:35 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP1.3 — mục Customizer “Footer Cao Phát”** (yêu cầu Tùng): section `cp_footer` (priority **40**) trong `inc/customizer.php` với **16 control** — 14 setting `cp_footer_*` (`about` textarea · `title_products`/`title_support`/`title_contact` · 3 cặp `linkN_label` + `linkN_url` · `address` · `email` · `copyright` · `credit`; type `url` → `esc_url_raw`) + 2 setting **hotline toàn site** `cp_hotline_display`/`cp_hotline_tel`. Thêm **`cp_footer_defaults()` / `cp_footer_get()` / `cp_footer_text()`** vào `functions.php` = 1 nguồn sự thật cho default (setting default = đúng chuỗi đang chạy, ô trống → `footer.php` dùng lại mặc định; `{year}`/`{site}` thay lúc in). `cp_hotline_display()`/`cp_hotline_tel()` nay đọc theme_mod rồi mới qua filter (filter cũ vẫn thắng; `tel:` bỏ dấu `.`). `footer.php`: bỏ mọi chuỗi cứng, 3 liên kết “Hỗ trợ” từ `href="#"` → trang chính sách thật (3 URL đều **200**). **E2E (admin tạm + Playwright, đã xoá user):** section có 16 control, default đúng chuỗi đang chạy; sửa 6 ô → Publish → front-end đổi đúng (`0909.000.111` hiện **5 chỗ** + **6 link** `tel:0909000111` → đổi hotline là đổi toàn site; `© {year} {site} — TEST` → `© 2026 Cao Phát Door — TEST`); **xoá ô về mặc định đã đo thật**: theme mod lưu `""` → front-end hiện lại mặc định ✓; đã xoá 6 mod thử → về nguyên trạng, `grep TEST` = 0. **Đo footer 6 mức bề rộng:** 1440 → 349.2px · 1280 ×349.2 · 1100 ×373.9 · 1024/768 ×520.8 · 390 → 809.1; **6/6 mức `scrollWidth == innerWidth`**; 4 link `elementFromPoint` = `A` (phải đo bằng `getClientRects()[0]` — `<a>` inline wrap 2 dòng ở 390 làm tâm union-rect rơi vào khe, trả về `LI`); click “Chính sách vận chuyển” → đúng trang; 0 lỗi JS, 0 PHP notice. Ảnh: `/tmp/cp-pw/shot-footer-final-1440.png` · `-390.png` (+ bản `-test-*` lúc đang test). |
| 2026-09-15 19:20 (giờ thật) | cline (chưa có nhãn trong quy ước claude/deepseek — **chờ Tùng chốt**) | **CP1.3 — ĐỔI MÔ HÌNH FOOTER theo yêu cầu Tùng** (nền + số cột + mỗi cột = tiêu đề + ô soạn thảo): Customizer section `cp_footer` nay **21 control** — `cp_footer_bg_color` (`WP_Customize_Color_Control`) · `cp_footer_bg_image` · `cp_footer_bg_opacity` (range 0–100) · `cp_footer_cols` (3\|4) · `cp_footer_logo` · 4 × (`cp_footer_colN_title` + `_source` + `_content`) · `cp_footer_copyright`/`_credit` · 2 hotline. Thêm control mới **`CP_Text_Editor_Control`** (textarea + `id`) và **`assets/customizer-footer.js`** nâng nó thành **TinyMCE** bằng `wp.editor.initialize` (nạp `wp_enqueue_editor()`; init khi section được mở vì init lúc còn ẩn → iframe rộng 0px). `footer.php` render vòng lặp `cp_footer_columns()` (logo ở cột 1 nếu bật → `<h4>` tiêu đề → `.cp-col__body` `wp_kses_post` hoặc `wp_menu("footer")` khi `_source = menu`). CSS: `.cp-footer` `background-color` + `background-image` (cover) và **`.cp-footer::before` = LỚP MÀU đè lên ảnh** (`opacity: var(--cp-footer-bg-op)`, container `z-index: 1`) → 100% chỉ thấy màu, 0% chỉ thấy ảnh; `.cp-footer-grid--cols-3` = `repeat(3, minmax(0,1fr))`; typography `.cp-col__body` + icon tel/mailto bằng CSS `mask` (không phải nhúng SVG vào ô soạn thảo). **Đo thật:** 3 cột → lưới `442.66×3` @1440 + chỉ 3 cột render; ảnh nền + opacity 30 → `background-image: url(…)` + `::before` opacity **0.3**; mặc định 4 cột ở 6 mức bề rộng → **318.7 / 318.7 / 343.4 / 487.3 / 487.3 / 656.9px**, **6/6 mức `scrollWidth == innerWidth`**, 8 link `elementFromPoint` = `A`, click đúng trang, 0 lỗi JS, 0 PHP notice. **4 BẪY đã dính + đã sửa:** (1) `wp.editor.getEditor()` **không tồn tại** → dùng `tinymce.get(id)`; (2) textarea của control **thiếu `id`** → TinyMCE không init (probe thấy `ids: [""…]`); (3) JS sync cả khi `init`/`blur` → mở section + Publish là ghi theme mod cho CẢ 4 cột (giá trị = default nhưng “đóng băng” default) → chỉ sync khi nội dung khác bản gốc (`change keyup undo redo` + so `$ta.val()`); đo lại: mở section **không setting nào dirty** ✓; (4) email dài trong cột 157px ở 390px (`caophatdoor@gmail.com` **183px**) → tràn **396 > 390** → thêm `overflow-wrap: break-word`. **E2E (admin tạm + Playwright, đã xoá user + changeset rỗng):** 21 control đúng danh sách; **4 editor TinyMCE** (toolbar ✓, iframe **275×200**), gõ trong editor → setting đổi theo, Publish → front-end đổi đúng (`--cp-footer-bg:#c8471f`, `cp-footer-grid--cols-3`, tiêu đề/nội dung cột 1, cột 2 `menu→editor`); mod thử đã dọn sạch (0 `cp_footer_*`). Lưu ý default: **mọi cột = ô soạn thảo** (vị trí menu `footer` chưa gán menu nào ở local). Ảnh: `/tmp/cp-pw/shot-footer-final2-1440.png` · `-390.png` · `-bg3-1440.png` (test ảnh nền + 3 cột). |
| 2026-09-15 19:45 (giờ thật) | cline | **Luật mới “Làn thay đổi A/B/C”** (Tùng chốt sau khi tôi đo lại chi phí: 1 thuộc tính CSS tốn ~6,3KB tài liệu + ~20 tool call) — thêm cuối mục “Quy trình / DoD” của `AGENTS.md` + `CLAUDE.md` (block y hệt, copy bằng script); bỏ luật cũ “việc nhỏ vẫn phải đồng bộ CPx.y + FEATURE_MAP”: làn A nay chỉ ghi 1 dòng §2. |
| 2026-09-15 20:20 (giờ thật) | cline | **Tái cấu trúc tài liệu theo template gọn của Tùng:** `AGENTS.md` 66.587 B → **6.593 B (−90%, tiết kiệm ~20k token mỗi tin nhắn)** — chỉ còn checklist trước/sau · làn A/B/C · quy trình 1 khung chát + 2 lệnh chốt/tạm dừng · luật bất biến · môi trường. Bảng CP chi tiết (56.260 B) + 3 mục Môi trường/Kiểm chứng/File huấn luyện **chuyển sang `CLAUDE.md`** (63.244 → 66.701 B) = nguồn chi tiết duy nhất. Sửa 9 chỗ trỏ “bảng CP ở AGENTS.md” → `CLAUDE.md` (.github/instructions×2, cp-wrapup, skill cp-ui-verify, WORKLOG). Kiểm: 30/30 số CP giữ nguyên, 0 tham chiếu sai, đã bù lại quyết định “PHẠM VI CỦA DEEPSEEK” (suýt mất) vào CLAUDE.md. |
| 2026-09-15 20:45 (giờ thật) | cline | **Dọn code thừa (Tùng yêu cầu):** xoá **51 rule CSS mồ côi** thuộc 20 class (`.cp-attrs` `.cp-cart-section` `.cp-checkout-section` `.cp-field` `.cp-foot-contact` `.cp-form*` `.cp-gallery*` `.cp-map` `.cp-meta-list` `.cp-post(s)` `.cp-qty` `.cp-rating` `.cp-save` `.cp-stars` `.cp-summary` `.cp-tab-nav`) — bằng chứng mồ côi: 0 nơi sinh ra ở PHP/JS + theme cha + plugin + 5 trang render thật; thêm **1 biến chết `--cp-surface-2`**: `assets/caophat.css` 160.917 → **156.044 B (−4,9KB)**, rule trình duyệt nạp 1590 → **1544**, braces 957/957. **Chứng minh không đổi giao diện:** chụp computed style (width/padding/color/font/grid/display + rect) của 6 trang (/, /san-pham/, SP thật, /gio-hang/, /thanh-toan/, /lien-he/) TRƯỚC và SAU → **giống hệt**. Sửa 1 comment lỗi thời (nhắc `.cp-cart-section`/`.cp-checkout-section`). Rà thêm: **0/77 hàm PHP thừa · 8/8 JS được enqueue · 4/4 template-part được gọi · 5 "selector trùng" đều là cộng thêm thuộc tính theo breakpoint (không thừa) · tên file đã xoá còn trong tài liệu đều là dòng ghi chú lịch sử có đánh dấu ĐÃ XOÁ**. |
| 2026-09-15 21:10 (giờ thật) | cline | **ĐÍNH CHÍNH dòng 20:45 + sửa lỗi xoá nhầm:** (1) URL trong dòng 20:45 SAI — `/gio-hang/` và `/thanh-toan/` là **404**; URL thật là **`/cart/`** + **`/checkout/`** (page ID 9/10) nên 2 trang giỏ/thanh toán CHƯA hề được đo. (2) `.cp-cart-section` + `.cp-checkout-section` **KHÔNG mồ côi** — PHP ghép động tên class trong `cp_woo_page_wrap()` (`'cp-section ' . $class . '-section'`, `$class = 'cp-cart'`/`'cp-checkout'`) nên grep chuỗi trong PHP không thấy. **Đã phục hồi nguyên văn** (khớp byte với HEAD) 2 rule + 2 banner CP3.4/CP3.5 bị xoá kèm + thêm ghi chú ⚠️ trong CSS & CLAUDE.md; đo lại **`/cart/`: 40/64 (1440) · 28/44 (768 + 390)** và **`/checkout/`: 40/64 · 28/48**, overflow 0. **18 class còn lại kiểm lại trên 17 loại URL thật** (/, shop, SP, cart, checkout, my-account + edit-account/edit-address, tin-tuc, tìm kiếm, danh mục, trang chính sách, liên hệ, 404) **+ toàn bộ plugin** (không chỉ `tl-site-caophat`) → vẫn mồ côi thật ⇒ phần xoá còn lại ĐÚNG. **Bài học:** đối chiếu DOM THẬT của mọi loại trang; grep file chỉ là bổ trợ (class có thể ghép động). |
| 2026-09-15 21:15 (giờ thật) | cline | **CP3.4/CP3.5 — CSS theo yêu cầu Tùng (làn A):** thêm `.woocommerce-error, .woocommerce-info, .woocommerce-message { margin: 1em 0 2em; }` vào nhóm "Thông báo WooCommerce" (cạnh rule viền `#fbaf02`). Đo THẬT `/cart/` (notice thêm-hàng-thành-công): margin **16/0/32/0** ở **1440 · 768 · 390** (fs 16px ⇒ đúng `1em 0 2em`), viền trên vẫn `rgb(251,175,2)`, `scrollWidth-innerWidth = 0`. Phần tử giả `.woocommerce-error`/`.woocommerce-info` = `13.5/0/27` khi fs 13.5px ⇒ rule áp cho **cả 3 class**, không cần `!important` (CSSOM: `woocommerce.css` trước `caophat.css`, cùng độ ưu tiên ⇒ theme thắng). `/checkout/` không sinh notice nào (info/message/error = 0) nên rule `.cp-checkout …` cũ vẫn nguyên. `assets/caophat.css` 156.887 B · braces 971/971. |
| 2026-09-15 21:40 (giờ thật) | cline | **CP1.3 — ô soạn thảo footer = WYSIWYG (yêu cầu Tùng: "định dạng chữ trong ô Nội dung phần footer phải hiện y như đã nhập"):** đo ra gốc rễ — nền footer Tùng đặt là **SÁNG** (`cp_footer_bg_color #d6d6d6` + ảnh `model-1phong.png` opacity 95 → nền thực tế **rgb(214,214,214)**) và nội dung nhập là chữ **`#000000`**, nhưng CSS theme ép `.cp-col__body strong, b { color: #fff }` ⇒ chữ đậm **trắng trên nền sáng = mất chữ** (contrast 1.18). **Đã xoá** rule đó (yêu cầu Tùng), bỏ ép màu/cỡ `h5` (giữ margin), hover link đổi `color:#fff` → `opacity:.78`. **E2E đạt:** mod test `cp_footer_col2_content` = `<strong style="color:#b8860b;font-size:19px">` + `<span style="color:#0a4d8c;font-size:11px">` → front-end hiện **đúng y** (rgb(184,134,11)/19px/700 · rgb(10,77,140)/11px) → đã xoá mod test; chữ đậm mặc định nay **đen rgb(0,0,0) contrast 17.78** (trước là trắng 1.18). Đo **1440 · 1024 · 768 · 390**: `scrollWidth-innerWidth = 0`; css 157.432 B · braces 961/961; 0 PHP notice. **Còn tồn (đã ghi CLAUDE.md CP1.3 + §1):** `h4` tiêu đề cột #fff **1.18** · `p` #9a8f80 **1.44** · link cột 2–3 (nội dung mặc định `functions.php`) #cabfb2 **1.11** — màu mặc định của theme trên nền sáng, chờ Tùng quyết (hướng: tự đổi theo nền). |
| 2026-09-15 22:05 (giờ thật) | cline | **CP1.3 — bỏ dữ liệu mặc định của cột + chữ theme tự đổi theo nền (yêu cầu Tùng):** (1) `cp_footer_columns_defaults()` nay `title=''` + `content=''` (giữ `source='editor'`) ⇒ **ô để trống = cột TRỐNG**, không còn tự hiện chữ mẫu “Sản phẩm / Hỗ trợ / Liên hệ”; đo: cột trống = **0 B**, không có `.cp-col__body`, `footer.php` không in gì. (2) Đo ra chữ DO THEME quyết định (tiêu đề cột, dòng bản quyền, `p`) là màu nền-tối trên nền SÁNG Tùng mới đổi (`h4` `#fff` contrast **1.18** = vô hình) → thêm `cp_footer_bg_is_light()` + `cp_footer_style()` in `--cp-footer-title/fg/muted/line` khi nền sáng; CSS dùng `var(…, <màu cũ>)` cho `.cp-footer`/`h4`/`a:hover`/`.cp-logo-text b|span`/`p`/`.cp-footer-bottom` ⇒ **nền tối không đổi gì** (đã đo ngược: nền `#211d19` → không in var, `h4` `#fff` ✓). Kết quả nền trắng: chữ nền `rgb(43,39,35)` **5.12** · `h4` **8.65** · bản quyền `rgb(87,80,74)`; **1440 · 1024 · 768 · 390** overflow **0**; `php -l` OK · css braces OK. ⚠️ **ĐÍNH CHÍNH TRUNG THỰC:** trong lúc Tùng đang sửa Customizer song song, script đo của tôi đã **ghi đè `cp_footer_bg_color`** (`#ffffff` → `#d6d6d6` giá trị cũ) — **đã set lại `#ffffff`** đúng giá trị lúc đó; các mod khác (`bg_image=''`, `bg_opacity=100`, `cols=4`, tiêu đề/nội dung cột 3–4) là của Tùng, tôi không sửa. Từ giờ hết ghi mod khi Tùng đang làm việc. |
| 2026-09-15 22:20 (giờ thật) | cline | **CP1.3 (làn A — yêu cầu Tùng):** thêm **line ngang 7px màu vàng chủ đạo** sát mép trên `.cp-footer` = `border-top: 7px solid var(--cp-primary, #fbaf02)` (KHÔNG dùng `::after` vì `.cp-footer::before` đã là LỚP MÀU đè ảnh nền; border nằm ngoài padding-box nên lớp đó không phủ lên line). Kiểm bằng **đọc điểm ảnh screenshot**: đúng **7 dòng `y=20..26` = rgb(251,175,2)** suốt ngang (mẫu x=40/400/720/1100/1400), trên là rgb(246,244,241), dưới là nền footer #ffffff; css 158.256 B · braces 961/961; **1440 · 768 · 390 `overflow = 0`**. |
| 2026-09-15 22:40 (giờ thật) | cline | **CP1.3 — danh sách `ul`/`li` trong ô soạn thảo footer (Tùng báo “dùng ul/li mà ra trang không thấy”):** chẩn đoán — `<ul>`/`<li>` **CÓ** tới DOM (6 `<ul>`, 20 `<li>` trong footer), nhưng CSS ép `list-style: none; padding: 0` ở 2 lớp (reset chung `.cp ul` dòng 48 + `.cp-footer .cp-col__body ul, ol` dòng 683 trước đây) ⇒ đo được **text lệch so với `<ul>` = 0px**, không dấu không thụt. Sửa: giữ ĐÚNG định dạng như trong ô soạn thảo — `ul { list-style: disc }` · `ol { decimal }` · lồng `ul ul = circle` / `ol ol = lower-alpha` + `padding-left: 20px` (cột nguồn “Menu” không đi qua `.cp-col__body` nên vẫn không dấu, giữ thiết kế cũ). **Kiểm chứng:** 6/6 danh sách nay `list-style=disc` · `padding-left=20px` · chữ lệch **20px**; **điểm ảnh** (tự giải mã PNG) thấy cụm dấu chấm `#####` ở page x 23–27, dòng 11–15 — bên trái chữ ✓; **1440 · 1024 · 768 · 390 `overflow = 0`** (footer cao 813 / 886 / 979 / 1638px). Ảnh: `/tmp/cp-pw/ul-bullet.png`. |
| 2026-09-15 23:05 (giờ thật) | cline | **CP1.3 — thêm nút “Mã HTML” (icon `<>`) vào ô soạn thảo footer (yêu cầu Tùng: “chọn thêm icon mã code và html”):** WP **7.1 không kèm plugin `code`** của TinyMCE (bản bundle chỉ có charmap/colorpicker/…/wpview) và Customizer chỉ có 1 TinyMCE nên trước đây không có chỗ xem/sửa mã HTML. Đã tự đăng ký plugin TinyMCE 4 (`tinymce.PluginManager.add('cpcode')` — `assets/customizer-footer.js`) thêm nút `icon: 'code'` (skin `lightgray` có sẵn `.mce-i-code`) mở **hộp thoại “Mã HTML”** (textarea monospace 620×380, `getContent({source_view:true})` → `setContent()` + `undoManager.add()` + `fire('change')` để `sync()` đẩy vào setting). Nối vào config: `plugins: '…,cpcode'` + `toolbar1: <toolbar cũ>,cpcode`. **E2E (admin tạm, KHÔNG Publish, đã xoá user):** 4/4 editor có nút (`.mce-i-code` ×4) → bấm mở dialog title “Mã HTML”, textarea nạp đúng HTML hiện tại; sửa + bấm **“Đồng ý”** → dialog đóng, nội dung editor VÀ `wp.customize('cp_footer_col1_content')` đều có dấu test (`dirty=true`) — **không bấm Publish nên DB không ghi gì** (kiểm: `theme mod get` = `0` dấu test). Lỗi mạng duy nhất là vấn đề có sẵn ở local (Vite theme cha `127.0.0.1:5173`, thumbnail 404, doubleclick) — **0 lỗi JS từ code mới**. `node --check` JS OK. |
| 2026-09-15 23:45 (giờ thật) | cline | **CP1.3 — Cột 4 không hiện nội dung + thêm nút cỡ chữ/màu chữ:** (1) **Chẩn đoán Cột 4:** `cp_footer_col4_content` trong DB chỉ còn **`<p></p>`** — do `wp_kses_post` **không cho `iframe`** (đo: `IN: <iframe src="…maps…"></iframe><p>VT</p>` → `OUT: <p>VT</p>`), và `iframe` bị xoá ở **cả 2 lớp**: `sanitize_callback` lúc lưu + `wp_kses_post` lúc in ở `footer.php`. (2) **Sửa:** thêm `cp_footer_kses_content()` + `cp_footer_iframe_host_ok()` (`functions.php`) — `wp_kses` + cho phép ĐÚNG thẻ `iframe` (11 thuộc tính nhúng; không thêm `script`/`style`/`embed`) rồi kiểm `src` https + host tin cậy (google/youtube/youtu.be/openstreetmap) — áp ở **cả 2 lớp** (customizer sanitize + footer.php in ra). Đo: maps/youtube GIỮ ✓ · host lạ + `http://` BỎ ✓ · `<script>` xoá ✓ · front-end in ra `<iframe src="https://www.google.com/maps/embed?pb=…">` ✓ · host lạ trên front-end **0** ✓. (3) **Thêm CSS chống tràn iframe:** iframe `width="600"` cứng trong cột 157–346px → `overflow` **257/234/423** (1440/768/390); sau `.cp-col__body iframe { max-width:100% … }` → **0** và iframe = đúng bề rộng cột (323/346/157). (4) **2 nút mới hàng 2 toolbar:** `fontsizeselect` + `forecolor` (+ plugin `textcolor`, cỡ px 12→32) — E2E: 4/4 editor 2 hàng toolbar, gõ ở Cột 4 → **editor + setting đồng bộ** (`dirty=true`), dialog “Mã HTML” vẫn chạy, **0 lỗi JS**; user tạm đã xoá. (5) **Đã trả `cp_footer_col4_content` về đúng `<p></p>`** như trước khi test (khớp snapshot `/tmp/mods4.json`) — không bấm Publish nên dữ liệu của Tùng không đổi. Lint: `php -l` ×3 + `node --check` OK. |
| 2026-09-15 00:20 (giờ thật) | cline | **ĐÍNH CHÍNH + sửa: 2 nút cỡ chữ/màu chữ bị ẨN (Tùng báo “vẫn chưa thấy”)** — đặt ở `toolbar2` là sai vì **WP ẩn hàng toolbar thứ 2** (chỉ hiện khi bấm “Toolbar Toggle”): nút có trong DOM nhưng `rect = 0×0`, `offsetParent = null`; lần trước tôi chỉ đếm DOM nên tưởng đạt. **Đã chuyển `fontsizeselect,forecolor` sang `toolbar1`** (+ `toolbar2: ''`, TinyMCE tự xuống dòng ở sidebar hẹp). **Đo lại:** 4/4 editor hiện **Cỡ chữ 121×26px** + **Màu chữ 47×26px** (`offsetParent=true`), toolbar 275×71px, không tràn ngang; chạy thật: chọn `20px` → `<span style=\"font-size: 20px\">` ✓; Màu chữ = split-button (icon = áp màu hiện tại, mũi tên `.mce-open` = **bảng 40 màu** → `<span style=\"color: #000000\">`) ✓, `aria-label` “Màu chữ văn bản”. **End-to-end:** Tùng đã Publish **Google Maps thật** vào Cột 4 (`<iframe src=\"https://www.google.com/maps/embed?pb=!1m14…\">` trong DB) và front-end render đúng, co theo cột (323/346/157px), **overflow 0** ở 1440/768/390 ⇒ fix KSES chạy đúng cả luồng lưu → in. User tạm đã xoá; chỉ 2 khoá `cp_footer_col1_content` + `cp_footer_col4_content` khác snapshot = **do chính Tùng sửa**, không phải do phiên này. |
| 2026-09-16 00:50 (giờ thật) | cline | **CP1.3 — dòng “Thiết kế bởi tungleads.com” cố định trong code + gắn link, bỏ khỏi admin (yêu cầu Tùng):** (1) **Bỏ khỏi Customizer:** xoá setting + control `cp_footer_credit` (`inc/customizer.php`) và key `credit` khỏi `cp_footer_defaults()` (`functions.php`) ⇒ section `cp_footer` còn **20 setting** (đo bằng E2E: `wp.customize.section('cp_footer').controls()` = 20, không còn key chứa `credit`; chỉ còn ô “Dòng cuối — bản quyền”). (2) **In cứng ở `footer.php`:** `<span class="cp-footer-credit">Thiết kế bởi <a href="https://tungleads.com/" target="_blank" rel="noopener">tungleads.com</a></span>` (`printf( esc_html__( 'Thiết kế bởi %s' ), … )`; **không** `nofollow` vì là site của chính Tùng). (3) **CSS:** `.cp-footer-bottom .cp-footer-credit a { color: inherit; text-decoration: underline; text-underline-offset: 2px }` + hover `--cp-footer-title` (vì `.cp a` chung đã bỏ gạch chân). **Đo front-end:** có đúng `href=https://tungleads.com/ target=_blank rel=noopener`, màu `rgb(87,80,74)`, gạch chân `underline`, rect **95×16**, dòng cuối cao 62/93/113px ở 1440/768/390, **không tràn ngang**. (4) **Sửa lỗi phát hiện khi hit-test:** nút gọi nổi `.cp-fab` (fixed, 54px, cách phải 18px) **ĐÈ LÊN link ghi công** khi cuộn tới đáy — đo 1440/768 giao nhau **832px²**, click vào giữa link lại trúng `A.call` (chỉ mép trái bấm được); thêm `@media (min-width: 561px) { .cp-footer-bottom { padding-right: 84px } }` → giao **0px²**, hit-test giữa link = `[A, SPAN.cp-footer-credit]` ✓ (390px vốn không giao nhau). `php -l` ×3 OK · css 160.737 B · braces 970/970 · 0 PHP notice · user admin tạm đã xoá · không Publish. |
| 2026-09-16 01:15 (giờ thật) | cline | **CP1.3 — chữ ghi công: thêm dấu hai chấm + SÁT LỀ PHẢI (yêu cầu Tùng):** `footer.php` đổi `'Thiết kế bởi %s'` → `'Thiết kế bởi: %s'` (link `https://tungleads.com/` giữ nguyên). CSS: **bỏ** `@media (min-width:561px){ .cp-footer-bottom { padding-right: 84px } }` + thêm `.cp-footer-bottom .cp-footer-credit { margin-left: auto }` (để ≤560px xuống hàng vẫn sát phải). **Đo 1440/768/390:** `link.right == bar.right` = **1420 / 748 / 370** (cách mép viewport 20px = padding của `.cp-container`), chữ = “Thiết kế bởi: tungleads.com”, `href=https://tungleads.com/`, không tràn ngang. ⚠️ **Đánh đổi đã đo + đã ghi chú trong CSS:** khi cuộn tới đáy, `.cp-fab` đè phần phải của link ở **cả 3 mức** (giao **832px²**, click giữa link trúng `A.call`) — Tùng chọn sát phải nên giữ; muốn hết thì nâng `.cp-fab` hoặc chừa padding-bottom cho `.cp-footer` (chờ Tùng quyết). `php -l footer.php` OK · css 161.041 B · braces 969/969 · 0 PHP notice. |
| 2026-09-16 01:40 (giờ thật) | cline | **CP1.3 — footer về 1 CỘT trên mobile (yêu cầu Tùng):** nguyên nhân — `@media (max-width:1024px)` chỉ đổi `.cp-footer-grid` sang `1fr 1fr` (2 cột) và **không có rule nào cho mobile** ⇒ đo TRƯỚC ở 390px: 4 cột xếp **2 hàng**, mỗi cột chỉ **157px**. **Sửa:** thêm vào `@media (max-width:768px)` (block CP1.5, nằm SAU nên thắng theo thứ tự nguồn) `.cp-footer-grid, .cp-footer-grid--cols-3 { grid-template-columns: 1fr; }`. **Đo SAU** (8 mức): 1440 → 4 cột `[431,269,269,323]` · 1100 → 4 cột · 1024/900 → 2 cột (không đổi) · **768 → 1 cột 728px** · 560 → 520px · 480 → 440px · **390 → 350px** (4 hàng), `overflow = 0` ở mọi mức; footer cao 743/837/1195/1289/**1845/1899/2013/2106px**. Chọn breakpoint **768** = breakpoint “mobile” của theme (header mobile + menu burger) — muốn chỉ ≤480 mới 1 cột thì đổi 1 số. Ảnh: `/tmp/cp-pw/footer-390-1col.png`. |
| 2026-09-16 02:00 (giờ thật) | cline | **CHỐT PHIÊN — ĐÃ COMMIT:** nhãn model Tùng chốt = **`[deepseek]`**; **1 commit gộp 32 file** (14 sửa + 2 xoá + 16 mới) gồm CP2.2/2.4/2.5/2.6/2.7/2.8/2.9 · CP3.4/3.5 (đính chính + phục hồi 2 rule bị xoá nhầm) · CP4.1 · CP5.1/5.2 · CP6.1/6.2 · **CP1.3 footer** (20 control, WYSIWYG, nút Mã HTML/cỡ chữ/màu chữ, iframe bản đồ qua KSES, ghi công cố định có link, line 7px vàng, mobile 1 cột) · tái cấu trúc tài liệu (AGENTS.md 66,6KB → 6,6KB) · dọn 51 rule CSS mồ côi. Trước khi commit: `php -l` (15 file) + `node --check` (8 file JS) xanh, 3 lớp đồng bộ (CLAUDE.md ↔ tag CP trong code ↔ FEATURE_MAP), `git status` SẠCH sau commit. |
