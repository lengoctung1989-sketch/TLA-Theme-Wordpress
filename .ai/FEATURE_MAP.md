# FEATURE_MAP — tungleads-theme-cp

Nghĩa từng số ở `CLAUDE.md`. Bảng này liệt kê file + chi tiết kỹ thuật.
Tiền tố `CP` để tách khỏi P-index của theme cha.

| CP-index | Chức năng / nhiệm vụ | File | Chi tiết kỹ thuật |
| :--- | :--- | :--- | :--- |
| **CP1.1** bootstrap | Nạp font + CSS skin sau bundle theme cha, khai báo custom-logo, gắn body class `cp`, cung cấp helper dùng chung. | `functions.php` | `wp_enqueue_scripts` prio 20: `cp-fonts` (Be Vietnam Pro) + `cp-style` (`assets/caophat.css`). `after_setup_theme`: `add_theme_support('custom-logo')` + **`title-tag`** (2026-09-16 — cha TẮT `title-tag` khi có plugin SEO active ⇒ site **mất hẳn thẻ `<title>`** vì Rank Math chỉ lọc tiêu đề; child khai lại). `body_class` += `cp`. `cp_hotline_display()`/`cp_hotline_tel()` (filterable), `cp_product_card(WC_Product)` |
| **CP1.2** header | Topbar (3 ưu điểm + hotline) + header sticky (logo, menu chính, khối hotline). | `header.php` | `the_custom_logo()` bọc trong `<div.cp-logo>` (KHÔNG bọc thêm `<a>` — lồng thẻ `<a>` là HTML không hợp lệ, đã sửa) · fallback `<a.cp-logo>` + logo chữ. `wp_nav_menu('primary', items_wrap ul)` — menu thật xem CP1.4/CP1.5. `<button.cp-burger aria-expanded aria-controls="cp-nav">` — JS ở **CP1.5**. Không mở `<main>` (template tự mở) |
| **CP1.2** logo (CSS) | `.cp-header .custom-logo { width: 250px; height: auto; max-width: 100%; display: block }` — **KHÔNG dùng `.cp-logo .custom-logo`: đó là selector CHẾT.** `the_custom_logo()` in ra `<a class="custom-logo-link">` LỒNG trong `<a class="cp-logo">`; HTML lồng `<a>` không hợp lệ nên trình duyệt tự đóng thẻ `<a>` ngoài → `.cp-logo` thành thẻ **rỗng 0×0**, logo nằm trực tiếp trong `.cp-container`. Hệ quả: rule `width: 200px` cũ chưa bao giờ chạy (logo hiện đúng cỡ gốc 473×87 → header cao 108px); đổi sang selector sống thì logo 250×46px, header còn **77px** (mobile: logo co theo `max-width: 100%`, còn ~152px, header 65px). Đã bỏ `gap` của `.cp-header .cp-container` (desktop) — **mobile vẫn còn `gap: 12px`** trong `@media (max-width: 768px)`. Tồn tại: file logo hiện tại (`uploads/2026/09/cropped-cropped-logo-cao-phat-fna.png`) **bị cắt mất chữ “C”** — lỗi ở ảnh, không phải CSS. **Topbar (CP1.2):** nền đổi `--cp-primary-dark` → **`--cp-primary`** (#fbaf02) theo yêu cầu; chữ `--cp-on-primary` (#241d05) đọc tốt; kèm sửa hover của `.cp-topbar a:hover` từ `#fff` (trắng trên vàng chỉ ~1.75:1, gần như mất chữ) → `--cp-accent` + gạch chân. (19:40 ngày 2026-09-14) `min-height` của `.cp-topbar .cp-container` giảm **40px → 30px** |
| **CP1.2b** header mobile 1 hàng | ≤768px: **1 HÀNG** `[burger][logo][icon search][icon giỏ]`, ẩn hotline (trước 3 hàng/163px → 65px); nút icon search mở ô search thành dải riêng dưới header. | `header.php`, `assets/nav-menu.js` (bước 4, tag `// CP1.2b`), `assets/caophat.css` (rule base `.cp-search-toggle` ~dòng 295 + khối CP1.2b trong `@media (max-width: 768px)`) | DOM: burger = **con ĐẦU TIÊN** của `.cp-container` (`display: none` ở desktop, `order: -1` ở mobile) → `.cp-logo` → `.cp-nav` → `.cp-header-cta` (`order: 1`) chứa [form search · hotline · nút toggle · icon giỏ]. **Bẫy 1 — đè icon:** căn giữa logo bằng `position: absolute; left: 50%` → logo 188px tràn qua icon search (đo vùng chồng 257..282 ở 390px) → `elementFromPoint` trả về `img` và Playwright `click` timeout 10s (icon KHÔNG bấm được) → sửa thành căn giữa theo khoảng trống: `.cp-logo { flex: 1 1 auto; min-width: 0; justify-content: center; position: static }`. **Bẫy 2 — specificity:** rule base `.cp-header .custom-logo { width: 250px; height: auto; max-width: 100% }` nằm CUỐI file vẫn (0,2,0) → rule mobile cùng specificity bị đè, `max-width: 118px` chưa bao giờ chạy (đo logo 188×32) → selector mobile phải là `.cp-header .cp-logo .custom-logo` (0,3,0). **Bẫy 3 — méo tỉ lệ:** clamp cả `max-width: 100%` + `max-height` → box 169×26 (tỉ lệ 6.5 ≠ 5.44 gốc) → chỉ dùng `max-width: min(160px, 100%)` (+ `object-fit: contain` phòng hờ). **Bẫy 4 — wrap:** `.cp-header.is-search-open .cp-container { flex-wrap: wrap }` làm `.cp-header-cta` xuống hàng mới (header 111px, logo tụt y=85) → thay bằng dải absolute `top: 100%; left: 0; right: 0; height: 48px; z-index: 5`. JS `setSearchOpen()`: `aria-expanded` + `.is-search-open` + focus input; đóng bằng Esc (trả focus về nút) / click ngoài / bấm lại nút. Đo sau khi sửa (320/360/390/768): logo 99×18 / 139×26 / 160×29 / 160×29 — **đúng tỉ lệ 5.44**, khe hở 12px mỗi bên, header 65px, 0 lỗi JS, 769/1024/1440 không đổi (161/161/77px). ⚠️ Logo lệch tâm container **−27px** vì căn giữa theo khoảng trống (trái burger 44px ≠ phải 2 icon 98px); muốn đúng tâm phải hạ logo ≤110px ở 390px và ≤80px ở 320px |
| **CP1.2b** cập nhật 23:35 | Icon search + giỏ hàng **35×35px** (trước 44px); logo căn ĐÚNG TÂM. → **TÙNG CHỐT 2026-09-14: GIỮ 35px**, không thêm lớp phủ bấm 44px (chấp nhận dưới chuẩn tap target). | `assets/caophat.css` (`.cp-cart-link`, `.cp-search-toggle` base; khối CP1.2b mobile) | **Vì sao `justify-content: center` không đủ:** `.cp-logo` là flex item nhưng `a.custom-logo-link` bên trong bị **kéo giãn hết khung** (đo: `a` = 361px trong khi ảnh 160px) → ảnh `display: block` nằm ở ĐẦU khung → logo lệch tới **−157px** ở 768px (đo được; trước đó tưởng là lệch do khối phải rộng hơn burger). Sửa 2 lớp: `.cp-logo { padding-left: 36px }` (36 = khối phải 80px − burger 44px, bù chênh lệch 2 bên) + `margin: 0 auto` cho ảnh → lệch tâm = **0** tại 320/360/390/414/600/768; logo 81×15 / 121×22 / 151×28 / 160×29. Burger cách lề trái **20px**, giỏ cách lề phải **20px** (đúng padding container); icon 35px: `elementFromPoint` xác nhận cả 2 icon vẫn bấm được; desktop 1440 không đổi (header 77px, hotline+search hiện) |
| **CP1.3** footer | Footer **3/4 cột** (mỗi cột = tiêu đề + ô soạn thảo TinyMCE) · nền tuỳ biến (màu + ảnh + opacity lớp màu) · bản quyền/ghi công (2 ô hotline toàn site ĐÃ CHUYỂN sang section `cp_home` “Trang chủ Cao Phát” ngày 2026-09-16 — xem CP2.3). | `footer.php`, `functions.php`, `inc/customizer.php`, `assets/customizer-footer.js`, `assets/caophat.css` | **2026-09-15 (yêu cầu Tùng) — Customizer “Footer Cao Phát”** (section `cp_footer`, priority 40, **21 control**): `cp_footer_bg_color` + `cp_footer_bg_image` + `cp_footer_bg_opacity` (range 0–100) · `cp_footer_cols` (3\|4) · `cp_footer_logo` · `cp_footer_colN_title/_source/_content` (N = 1–4; `_content` dùng `CP_Text_Editor_Control` → TinyMCE qua `wp.editor.initialize` trong `assets/customizer-footer.js`, sanitize `wp_kses_post`) · `cp_footer_copyright` / `cp_footer_credit` · `cp_hotline_display` / `cp_hotline_tel`. Default = `cp_footer_defaults()` + `cp_footer_columns_defaults()`; đọc bằng `cp_footer_get()` / `cp_footer_flag()` / `cp_footer_col_get()` / `cp_footer_columns()`; token `{hotline}` `{hotline_tel}` `{email}` `{year}` `{site}` → `cp_footer_tokens()`. Nền in ra qua `cp_footer_style()` (`wp_head`, id `cp-footer-css`): `.cp-footer` có `background-color` + `background-image`, **`.cp-footer::before` = lớp màu đè lên ảnh** (`opacity: var(--cp-footer-bg-op)`) + container `z-index: 1`. `footer.php` render vòng lặp `cp_footer_columns()`: cột 1 có logo (nếu bật), rồi `<h4>` tiêu đề (trống = không in), rồi `.cp-col__body` (`wp_kses_post`) hoặc `wp_nav_menu('footer')` khi `_source = menu`. Bẫy đã gặp: `wp.editor.getEditor()` không tồn tại → dùng `tinymce.get()`; textarea phải có `id`; chỉ sync khi nội dung khác gốc (tránh ghi mod cho cả 4 cột khi mở section + Publish); `.cp-col__body { overflow-wrap: break-word }` chống tràn ngang do email dài. **2026-09-16 (Tùng yêu cầu) — XOÁ nút hotline nổi (FAB):** bỏ markup `<div class="cp-fab">` trong `footer.php` + toàn bộ `.cp-fab*` và `@keyframes cp-pulse` trong `caophat.css` + 2 biến chết `$cp_tel`/`$cp_disp`; chức năng gọi/Zalo chuyển sang plugin riêng `button-call-zalo-tungleads`. Đo lại: **0 phần tử `.cp-fab`, 0 rule CSS**; bấm giữa link ghi công ở đáy trang trúng `A` (link ghi công) thay vì `A.call` — hết lỗi phủ 832px². |
| **CP1.4** menu desktop | Dropdown 2 cấp cho menu vị trí `primary`: mục có menu con hiện mũi tên ▾, submenu nền trắng bo góc nổi trên header. | `assets/caophat.css` (khối CP1.4) | Mở bằng `:hover` (có độ trễ 120ms — "hover intent", lướt ngang qua không mở) HOẶC `:has(:focus-visible)` khi Tab. **KHÔNG dùng `:focus-within`**: click chuột cũng làm link nhận focus → panel dính MỞ MÃI dù chuột đã đi xa (lỗi Tùng báo 17:20, đã kiểm chứng: `li:focus-within=true` + panel visible khi chuột ở đáy trang). Submenu ẩn bằng `visibility+opacity` (giữ được transition), `min-width:250px`, `z-index:60`, `top:100%`; `left:0` nhưng **2 mục cuối đổi `right:0`** để không tràn khung; mũi tên vẽ bằng `::before` (vì `::after` đã là gạch chân cấp 1) và bị reset `content:none` trong submenu; WP vẫn gắn class `menu-item-has-children` cho mục còn con ở cấp sâu hơn nên phải reset như trên. `depth => 3` ở `header.php` → menu in đủ 3 cấp (cấp 3 chỉ dùng cho mục mega, xem CP1.4b). **Bẫy hit-test (sửa 18:00 ngày 2026-09-14):** panel ĐÓNG phải có `pointer-events: none` (rule reveal bật lại `auto`) — `opacity: 0` KHÔNG chặn chuột trong Chromium, nên chỉ cần MỘT phần tử con lỡ đặt `visibility: visible` là cả hộp panel bắt hover `li:hover` → menu tự mở dù chuột chưa tới chữ (đã xảy ra với cấp 3 của mega, xem CP1.4b). **Lưu ý mobile:** panel mobile mở bằng class `.is-open` (không qua `:hover`) nên khối `@media (max-width: 768px)` PHẢI có `pointer-events: auto` |
| **CP1.4b** mega menu 3 cấp | Mục cấp 1 có menu **3 cấp** (VD "Các Loại Cửa") → menu cấp 2 xếp thành **các CỘT**: cấp 2 = tiêu đề cột (chữ hoa + gạch chân vàng), cấp 3 = danh sách phẳng trong cột; panel là **dải ngang full chiều rộng** dưới header. | `header.php` (`depth => 3`), `assets/nav-menu.js` (gắn class), `assets/caophat.css` (khối CP1.4b, `@media (min-width: 769px)`) | JS gắn `.cp-mega` cho mục cấp 1 có cháu + `.sub-menu--mega` cho menu cấp 2 (chỉ mục CẤP 1 — cấp sâu hơn vẫn là dropdown/accordion bình thường). CSS: `li.cp-mega { position: static }` để panel neo vào `.cp-header` (sticky) → `left:0; right:0` thành dải ngang; `li.cp-mega > a { position: relative }` giữ mũi tên ▾. Grid `repeat(auto-fit, minmax(200px, 240px))` — 3 cột nằm 1 hàng, menu nhiều cột thì tự xuống hàng (nếu dùng `grid-auto-flow: column` thì 12 cột sẽ nở ngang, tràn ra ngoài màn hình). **Bẫy:** rule chống tràn `.cp-nav > ul > li:nth-last-child(-n+2) > .sub-menu { left:auto; right:0 }` phải thêm `:not(.cp-mega)`, nếu không mục mega bị co lại còn 280px (1 cột). **Bẫy hit-test (sửa 18:00 ngày 2026-09-14):** `visibility: visible` của danh sách cấp 3 KHÔNG được nằm trong khối reset luôn-áp — cấp 3 cũng mang class `.sub-menu` nên nó đè `visibility: hidden` của panel; khi panel ĐÓNG, danh sách cấp 3 vẫn nhận chuột, mà panel là dải ngang full width × cao ~420–620px → `li:hover` bật true và menu tự mở dù chuột cách chữ 95px ngang / 400px dọc (đo được: mở tại (747,493) khi chữ ở (887,111)). Cách sửa: đặt `visibility` vào rule có điều kiện `li:hover` / `li:has(:focus-visible)`. Kèm "cầu" hover `li.cp-mega:hover > .sub-menu::before { top:-28px; height:28px }` lấp khoảng hở ~20px giữa link và mép trên panel (panel neo vào header) để rê chuột từ chữ xuống panel không mất hover. **Cắt tên dài (18:20 ngày 2026-09-14):** link cấp 3 có `white-space: nowrap` nên tên dài hơn bề ngang cột (cột ~195px, "Cửa gỗ công nghiệp MDF Melamine" ≈ 242px) tràn sang cột bên cạnh → thêm `overflow: hidden; text-overflow: ellipsis` cho `.cp-nav .sub-menu--mega .sub-menu a` (4/35 link bị cắt, còn 0 link tràn cột). Mobile KHÔNG cần: 44 link con ở 390px đều nằm trong panel. **Cột đầu hàng mới (18:40 ngày 2026-09-14):** `.sub-menu--mega > li` có `border-left` làm vạch dọc giữa các cột, nhưng khi panel xuống dòng thì cột đầu của hàng 2+ cũng dính vạch + thụt lề (trước đây chỉ `:first-child` được bỏ) → `nav-menu.js` thêm `markRowStarts()` đo `offsetTop` của từng cột để gắn `.cp-row-start`, CSS bỏ `padding-left` + `border-left` cho class đó; gọi lại khi `resize` và sau `document.fonts.ready` (số cột mỗi hàng phụ thuộc bề rộng + font). Đo ở 1440: 2 hàng × 5 cột, mỗi hàng đúng 1 cột `border=0/pad=0` tại `x=20` (thẳng hàng cột đầu hàng 1), 4 cột còn lại `border=1`; resize 1200 → tự còn 4 cột/hàng và đánh dấu lại đúng |
| **CP1.4b** mục cấp 2 = 1 CỘT | **2026-09-16 (yêu cầu Tùng) — BỎ cơ chế “cột gom” `.cp-mega__misc`:** mục cấp 2 KHÔNG có danh mục con giờ là **1 CỘT riêng** như các mục có danh mục con (tiêu đề in hoa 13px + gạch vàng 2px, cột chỉ có tiêu đề). **Lý do (Tùng phát hiện qua ảnh demo):** chúng bị JS gom vào `li.cp-mega__misc` bọc trong `ul.sub-menu` ⇒ mang style **CẤP 3** (14px · chữ thường · w500 · không gạch) ⇒ nhìn như mục CON của cột cuối — trong khi DB xác nhận chúng là danh mục **CẤP 2** (VD “Cửa gỗ công nghiệp”, “Cửa Thép Vân Gỗ”, “Sản Phẩm Khuyến Mãi”; kiểm bằng `wp_get_nav_menu_items` + đếm `menu_item_parent`). | `assets/nav-menu.js` (XOÁ đoạn gom cột ~dòng 41–55), `assets/caophat.css` (XOÁ `.cp-mega__misc::before` + rule mobile `.cp-mega__misc > .sub-menu`) | **Đo TRƯỚC → SAU (Playwright):** 1440px → `<li>` cấp 2 **10 → 12**, panel **422 → 618px** (5 cột/hàng × 3 hàng), 3 mục đó đổi từ style cấp 3 → **13px IN HOA w800 + gạch 2px** và nằm **CÙNG HÀNG** với “Cửa chống cháy / Sản phẩm Bán Chạy Nhất” (cùng `top = 332px`) ✓; 1024px → panel **774 → 693px** (3 cột/hàng); 769px → **975 → 1054px** (2 cột/hàng). Hover mở panel ✓ · 3 mục **bấm được** (`elementFromPoint` OK) + href đúng (`/danh-muc/cua-go-cong-nghiep/`…) ✓ · **0 lỗi JS** ✓ · **mobile 390px cũng hết lồng cấp** (cấp `<ul>` = 2 — trước đây các mục này nằm sâu thêm 1 cấp trong accordion). ⚠️ **Hạn chế CÒN LẠI (có TRƯỚC, không do sửa này):** ở 769–1024px panel có thể CAO HƠN khung nhìn (769px: đáy 1269px > viewport 900) ⇒ phần dưới không tới được; hết thì cần `max-height: calc(100vh - 120px)` + `overflow-y: auto` (kèm xử lý “cầu hover” 28px vì `overflow` sẽ cắt nó) — chờ Tùng quyết. | ✅ |
| **CP1.4b** panel-fit | **Panel mega không cao quá khung nhìn** (2026-09-16): 769px panel **1054px** > viewport 900 ⇒ cột dưới không tới được; 1024px cũng chỉ vừa khít (đáy 888). | `assets/nav-menu.js` (`setMegaMax()`), `assets/caophat.css` | JS đặt biến `--cp-mega-max` = viewport − đáy header − 10px (header sticky cao thay đổi theo khổ: **77 / 161 / 186px**; tính lại khi `resize` — không thể làm bằng CSS thuần) ⇒ CSS `max-height: var(--cp-mega-max, calc(100vh - 120px))` + `overflow-y: auto` + `overscroll-behavior: contain`. **Đo TRƯỚC → SAU:** panel 618 → **648** (1440) · 1054 → **693** (1024) · 1054 → **674** (769); đáy **724 / 882 / 889** ≤ 900 ✓; 769px **cuộn 380px**, cột cuối “Phụ kiện cửa” bấm được ✓. **CẦU HOVER ĐỔI CƠ CHẾ:** pseudo cũ nằm TRONG panel nên bị `overflow` CẮT ⇒ nay dùng `margin-top: calc(-1 * var(--cp-bridge))` + `padding: calc(var(--cp-bridge) + 18px) 20px 18px` + `background-clip: content-box` ⇒ dải trong suốt vẫn thuộc panel (con của `<li>`) nên giữ được `li:hover`; `--cp-bridge` do JS đo (tạm đặt 0 rồi đo mép panel để tránh vòng lặp co dần). **Chỉ bật cầu khi khoảng hở ≤ 32px:** 1440 = 30px ⇒ rê chuột từ chữ xuống panel vẫn mở ✓; **1024/769 hở 126/115px ⇒ đặt 0** vì dải đó sẽ phủ `.cp-search` / `.cp-hotline` / `.cp-cart-link` (đo: cả 3 **BỊ CHẶN BẤM** khi panel mở) — đánh đổi: hover qua khoảng hở ở ≤1024 không giữ panel (hành vi CÓ TRƯỚC, không phải do sửa này). **Header elements vẫn bấm được khi panel mở ở cả 3 khổ** ✓ · 0 lỗi JS · `node --check` sạch. | ✅ |
| **CP1.4b** nền trắng phủ hết panel | **2026-09-17 (yêu cầu Tùng: “background màu trắng không phủ hết tất cả các ô menu — toàn bộ ô menu dịch lên trên quá cao”, kèm yêu cầu “tham khảo cách hiển thị trước khi thay đổi phiên gần nhất”) — BỎ cơ chế kéo panel lên của 2026-09-16.** | `assets/caophat.css` (khối CP1.4b: bỏ `margin-top: -var(--cp-bridge)` + `padding: calc(var(--cp-bridge) + 18px) 20px 18px` + `background-clip: content-box` ⇒ `padding: 18px 20px`; thêm `.cp-nav li.cp-mega:hover::before` làm cầu hover MỚI), `assets/nav-menu.js` (`setMegaMax()` đo thẳng `header.bottom − link.bottom + 2`) | **Nguyên nhân (đo TRƯỚC):** hộp panel bị kéo cao hơn thân panel 30px ⇒ (1) mép trên hộp nằm TRONG header — 1440: `panel.top` **76** vs đáy header **107** = **−31px**, vạch `border-top` 1px cắt ngang header; (2) `background-clip: content-box` ⇒ nền trắng chỉ phủ content box, hở **48px** trên · **20px** hai bên · **18px** dưới — pixel 1440 mép trái `rgb(249,247,245)` · mép phải `rgb(242,240,238)` · dải trên `rgb(246,244,241)` = nền trang, không phải `#fff`. **Sau:** `panel.top − header.bottom = −1` ở **1440/1280/1024/769** (panel liền mạch header) và **4 điểm đo (mép trái · mép phải · dải trên · dải dưới) đều `rgb(255,255,255)`**; nội dung vẫn bắt đầu `header.bottom + 18px` (125/125/209/234) ⇒ KHÔNG đổi vị trí chữ; panel 1440 **648 → 618px** (hộp không còn cộng 30px cầu, nội dung y hệt: cột cuối đáy 705 cả 2 lần). **Cầu hover mới:** pseudo của `<li>` (không bị `overflow-y: auto` cắt như pseudo cũ trong panel) — đo 1440/1280: chuột ở dải cầu hover khi CHƯA hover chữ = `hidden` (4 vị trí thử) · hover chữ rồi đi xuống dải = `visible` · rời chuột = `hidden` ⇒ rê từ chữ xuống cấp 3 **GIỮ ĐƯỢC panel** ✓, không có vùng “ma” tự mở. **Hồi quy:** tràn 0 (`scrollWidth = innerWidth`) ở 1920/1600/1440/1280/1180/1024 · chồng lấn cột 0 · `.cp-row-start` đúng ở mọi khổ + sau resize · `.cp-search`/`.cp-hotline`/`.cp-cart-link`/`.cp-logo` **bấm được khi panel mở** ở 1440/1024/769 · smoke test **24/24** (12 trang × 1440/390) 0 lỗi PHP · `node --check nav-menu.js` sạch. | ✅ |
| **CP1.5** menu mobile | Nút burger mở/đóng panel dọc `.cp-nav.is-open`; mục có menu con được gắn nút mở riêng, xếp dạng accordion. | `assets/nav-menu.js` (mới) + CSS trong `@media (max-width: 768px)`, nạp ở `functions.php` | JS tạo `<button.cp-nav__toggle>` là **anh em của `<a>`** (không lồng button trong a). `aria-expanded` + `aria-controls="cp-nav"`; đóng bằng Esc (trả focus về burger) / click ngoài / resize lên desktop; `body.cp-nav-open` khoá cuộn; nút toggle chỉ hiện ≤768px; burger đổi thành dấu X qua `[aria-expanded="true"]`. **Cập nhật 23:50:** `.cp-nav__toggle` **38×38 → 30×30px**, `top: 7px → 11px → 5px` (`5px` theo yêu cầu Tùng 2026-09-15; đo ở 390px: hàng cao 51.8px, nút cách mép hàng 5px → tâm nút **20px** (≠ tâm hàng 26px) nên chevron cao hơn tâm hàng **5.9px** và vùng bấm 44px phủ `-2 .. 42px`, **nhô 2px** lên mép hàng trên; muốn về đúng tâm hàng → `top: 11px`), giữ `right: 8px`/`z-index: 2`/`position: absolute`. (số đo CŨ khi `top: 11px` — phiên 23:50: nút `y = 11..41` trong `<li>` cao **52px** → lệch tâm 0); cách lề phải 8px; còn **255px** tới cuối chữ link (link cha đã có `padding-right: 68px`) → không đè chữ; chevron 9px lệch tâm nút **−3px** (bản 38px cũ lệch ~7px → thu nhỏ nút làm chevron cân hơn). **Vùng bấm 44px (chuẩn a11y) trong khi vẫn hiển thị 30px:** thêm lớp phủ trong suốt `.cp-nav__toggle::before { content:''; position: absolute; top/left: 50%; width/height: 44px; transform: translate(-50%,-50%) }` — quét `elementFromPoint` cho thấy vùng bấm phủ **−6..+38px** quanh nút, đều cả 4 phía; bấm **5px NGOÀI** nút ở cả 4 phía đều MỞ MENU CON và KHÔNG điều hướng (url vẫn `/`); bấm nút → `li.is-open` + submenu `display: block` (10 link con) + chevron xoay `matrix(-0.707,-0.707,0.707,-0.707)`; bấm link con → `/danh-muc/cua-phong-ngu/` OK; 0 lỗi JS. ⚠️ `top` PHẢI là số CỐ ĐỊNH, KHÔNG dùng `top: 50%` — khi mở menu con `<li>` cao thêm cả submenu nên `50%` sẽ đẩy nút xuống giữa submenu |
| **CP1.6** ô tìm kiếm | Form tìm sản phẩm trên header (`role="search"`, GET về trang chủ, `s` + `post_type=product`). | `header.php` + `assets/caophat.css` (khối CP1.6) | Nằm **TRONG `.cp-header-cta`** (con ĐẦU TIÊN của khối, `header.php` — 20:20 ngày 2026-09-14 chuyển vào theo yêu cầu Tùng) → desktop vẫn ở ngay trái hotline. ĐÃ BỎ `margin-left: 20px` của `.cp-search` (khoảng cách nay do `gap: 14px` của khối cta). ≤1024px: `.cp-header .cp-container { flex-wrap: wrap }` + `.cp-header-cta { order: 1; flex: 1 1 100%; flex-wrap: wrap; justify-content: flex-end }` + `.cp-search { order: 2; flex: 1 1 100%; height: 38px }` → cta chiếm hết hàng rồi tự wrap: hàng 1 hotline/giỏ/burger canh phải, hàng 2 search full chiều ngang. ⚠️ Đây là lý do trước đây search phải là con trực tiếp của `.cp-container`: nhét vào cta mà cta KHÔNG tự wrap + không full hàng thì search không xuống hàng riêng được. Đo lại ở 1440/1280/1100/1024/900/768/600/390: header 77px (desktop, 1 hàng) / 155px (≤1024, search hàng riêng) / 163px (≤768), không tràn ngang. **Nền xanh khi gõ (21:00 ngày 2026-09-14):** ảnh Tùng chụp cho thấy nền `#e8f0fe` (đúng màu Chrome phủ khi autofill/gợi ý giá trị đã gõ) trong ô search. Xử lý 2 lớp: (1) input thêm `autocomplete="off" autocapitalize="off" spellcheck="false"` (`header.php`) — chặn GỐC, Chrome không gợi ý/điền lịch sử vào ô này nữa; (2) rule đè `.cp-search input[type="search"]:-webkit-autofill` (+ `:hover/:focus/:active`) `{ box-shadow: 0 0 0 1000px var(--cp-surface) inset !important }` — phủ ĐÚNG màu pill `rgb(246,244,241)` chứ không `#fff` (ô search trong suốt nằm trên pill kem, phủ `#fff` sẽ thấy 1 ô trắng trong pill); selector (0,3,1) đặc thù hơn rule chung (0,1,1) nên thắng dù khai báo trước. ⚠️ KHÔNG kiểm chứng được bằng máy: CDP `CSS.forcePseudoState` chỉ hỗ trợ pseudo chuẩn (ép `-webkit-autofill` → computed box-shadow vẫn `none`), Chrome vẽ nền autofill ở tầng nội bộ → phải test tay. **Ẩn nút xoá nhanh × (21:15 ngày 2026-09-14):** `input[type=search]` của Chrome/Safari có nút × (pseudo `::-webkit-search-cancel-button`). Từ Chrome 83 chỉ ẩn được nếu input có `appearance: none` → thêm `-webkit-appearance: none; appearance: none` vào rule `.cp-search input[type="search"]`, kèm 4 pseudo `::-webkit-search-cancel-button/-decoration/-results-button/-results-decoration { -webkit-appearance: none; appearance: none; display: none }`. Đã chụp ảnh kiểm chứng khi ô có chữ: không còn ×; kích thước input/pill không đổi (input 186×38 trong pill 230×40, padding `0 4px 0 14px`) |
| **CP1.7** icon giỏ hàng | Link `wc_get_cart_url()` + badge số lượng, đứng SAU khối hotline. | `inc/woocommerce.php` (`cp_cart_count()`, `cp_header_cart_link()`, filter `woocommerce_add_to_cart_fragments`) + `header.php` + CSS khối CP1.7 | Badge chỉ in khi `count > 0`; `aria-label` có số lượng; fragment AJAX trả lại đúng `a.cp-cart-link` → tự nhảy số sau khi thêm giỏ, không cần tải lại. Hàm in dùng chung cho cả header lẫn fragment nên markup luôn khớp |
| **CP2.1** front-page | Layout trang chủ: khối tĩnh + gọi khối động. | `front-page.php` | Hero (badge/h1 `<em>`/desc/ảnh/2 nút) · strip 4 feature (mảng PHP) · CTA band. Đọc `get_theme_mod('cp_hero_*','cp_cta_*')` với fallback `__()`. `get_template_part` cho 3 khối động |
| **CP2.2** home-blocks | 1 khối động lấy dữ liệu thật + 3 khối repeater (CP2.4/2.5/2.9). | `template-parts/home-categories.php` | `get_terms('product_cat', number 6, orderby count, exclude default)` → `.cp-cats`. Ảnh: `thumbnail_id` term meta / fallback `door-hdf.webp` · **2026-09-15 (yêu cầu Tùng): XOÁ 2 khối "Bán chạy" + "Khuyến mãi" khỏi `front-page.php`** (nội dung 2 nhóm này nay ở khối tab CP2.9) → số `.cp-section-head` trên trang chủ còn **4** (Danh mục + 2 khối danh mục + khối tin), `.cp-link-more` còn **3**; **khối "BLOG" ("Kiến thức & báo giá" / "Bài viết mới nhất") cũng xoá 2026-09-15 → `.cp-posts` = 0, trang chủ còn 8 khối** (390/768/1440 không tràn ngang) · **mobile ≤768px: `.cp-section-head` h2 24px → 20px + gạch nhấn vàng 46×3px (`h2::after`), head xếp dọc, eyebrow 11.5px, `.cp-link-more` → button pill 44px (nền `--cp-primary`/chữ `--cp-on-primary`) · **desktop: `.cp-section-head` style lại (chip eyebrow nền vàng nhạt + h2 `clamp(26px,2.7vw,38px)` + gạch nhấn vàng 56×4px `h2::after` + kẻ mảnh `1px var(--cp-line)` dưới head)** |
| | | ~~`template-parts/home-products.php`~~ **ĐÃ XOÁ 2026-09-15 (yêu cầu Tùng)** — 2 khối "Bán chạy" + "Khuyến mãi" trên trang chủ đã bỏ (nội dung 2 nhóm này nay ở khối tab CP2.9); file chỉ có 2 chỗ dùng đó nên xoá luôn (khôi phục: `git checkout HEAD -- template-parts/home-products.php`) |
| | | ~~`template-parts/home-blog.php`~~ **ĐÃ XOÁ 2026-09-15 (yêu cầu Tùng)** — khối "BLOG" trên trang chủ đã bỏ (tin tức nay do khối CP2.5 đảm nhiệm); khôi phục: `git checkout HEAD -- template-parts/home-blog.php` |
| **CP2.3** customizer | Sửa chữ + ảnh hero/CTA không cần code; **16 control** (2026-09-16: +2 ô hotline toàn site `cp_hotline_display` / `cp_hotline_tel` **chuyển từ `cp_footer`**, priority 90/91 nằm cuối — ID setting giữ nguyên nên giá trị không mất). | `functions.php` (`customize_register`) | Section `cp_home` prio 30. Setting: `cp_hero_badge/title/desc` (title = `wp_kses_post`, còn lại `sanitize_text/textarea_field`), `cp_hero_image` (`WP_Customize_Image_Control`, `esc_url_raw`), `cp_cta_title/desc`. `transport refresh`. **2026-09-15: thêm 6 setting `cp_hero_btn1` / `cp_hero_btn2` (2 nút hero) + `cp_hero_stat1_value/_label` + `cp_hero_stat2_value/_label` (2 ô số liệu "10+ năm"/"5.000+") — cùng `$fields` array, `sanitize_text_field`; `front-page.php` echo `esc_html` + fallback `?: __( … )`. **2026-09-15 (yêu cầu Tùng): thêm 2 TRƯỜNG LINK cho 2 nút** — `cp_hero_btn1_url` + `cp_hero_btn2_url` (type **`url`** → nhánh sanitize mới trong loop: `'url' === $field[1] ? 'esc_url_raw' : …`, giữ được `tel:` / `/lien-he/` / query string); `front-page.php` `?: $cp_shop` và `?: 'tel:' . $cp_tel` → echo `esc_url` → tổng **14 control** trong section**. **2026-09-15 (yêu cầu Tùng) — sửa chữ DÍNH ở 2 ô số liệu hero:** `b`+`small` đều inline, markup không có khoảng trắng giữa `</b><small>` → 1440/768 dính liền "10+ nămKinh nghiệm thi công", 1024/390 xuống dòng tuỳ tiện; thêm `.cp-hero-float > div { display: flex; flex-direction: column; line-height: 1.25 }` (`assets/caophat.css` ~416) → mỗi ô là cột dọc ở mọi bề rộng (float `462.5×56 → 303.7×64.6`) |
| **CP2.4** home-cat-products | Khối "Sản phẩm theo danh mục" trên trang chủ (3 slot, cấu hình Customizer). | `inc/customizer.php`, `assets/customizer-blocks.js`, `assets/customizer-blocks.css`, `front-page.php`, `template-parts/home-cat-products.php`, `assets/caophat.css` | `customize_register`: section `cp_home_blocks_products`, control `select` danh mục `product_cat` (option rỗng = "— Không hiển thị —"), `text` tiêu đề, `select` sắp xếp (`date-desc`/`date-asc`), `number` số SP (1–20, mặc định 8), `select` bố cục (`cols-4`/`cols-3`/`cols-2`/`scroll`). Template part: `get_template_part( 'template-parts/home-cat-products', null, ['index' => N] )`; query `wc_get_products( ['category' => slug, 'limit' => count, 'orderby' => 'date', 'order' => DESC|ASC] )` → `cp_product_card()`; section head = `<h2>` + `.cp-link-more` (href = `get_term_link`) — **2026-09-15 (yêu cầu Tùng): BỎ nhãn `.cp-eyebrow` "Danh mục sản phẩm" phía trên tên danh mục** (khối tin vẫn giữ eyebrow "Tin tức"). Bố cục bằng CSS var `--cp-cols` trên `.cp-cat-block` (+ `.cp-cat-block--scroll` cho carousel cuộn ngang). **2026-09-15 — REPEATER (thay 3 slot cố định):** control tuỳ biến `CP_Blocks_Repeater_Control` (`inc/customizer.php`, `type = cp_blocks_repeater`) render mỗi khối = 1 hàng `.cp-block-row` (head: handle `⋮⋮` + "Khối N" + nút xoá ×; fields: `[data-field=cat|title|order|count|layout]`) + nút `.cp-blocks__add` ("+ Thêm danh mục sản phẩm") + 1 hàng MẪU ẩn `.cp-block-row--tpl` để JS clone; setting `cp_pblocks` = **JSON** mảng khối, sanitize `cp_blocks_sanitize()` (absint cat/count, `sanitize_text_field` title, whitelist order/layout); JS `assets/customizer-blocks.js`: `sync()` gom giá trị → `JSON.stringify` → hidden input + `wp.customize(id).set(json)`, `sortable({ handle: '.cp-block-row__handle', update: sync })`, `sync($wrap)` lúc khởi tạo để đánh số "Khối N" đúng. Template part đọc JSON, gọi 1 LẦN (bỏ `$args['index']`), khối chưa chọn danh mục → bỏ qua; nền xen kẽ theo thứ tự khối ĐÃ RENDER. Đã **xoá code 3-slot cũ** khỏi `functions.php` (`cp_term_choices()` + `cp_customize_block_controls()` + add_action — 187 dòng) và dọn 25 theme mod `cp_pblock_*`/`cp_nblock_*` |
| **CP2.5** home-cat-news | Khối "Tin tức theo chuyên mục" trên trang chủ (2 slot, cấu hình Customizer). | `inc/customizer.php`, `assets/customizer-blocks.js`, `assets/customizer-blocks.css`, `front-page.php`, `template-parts/home-cat-news.php`, `assets/caophat.css` | Cùng cơ chế CP2.4 nhưng cho `category`: section `cp_home_blocks_news`, setting `cp_nblock_N_cat/_title/_order/_count/_layout` (số bài 1–12, mặc định 3; bố cục `cols-3`/`cols-2`/`scroll`); query `WP_Query` (`cat` => term_id, `orderby` => date, `posts_per_page` => count) → thẻ `.cp-news-mini` (tái dùng CP5.2) + `.cp-link-more` → link chuyên mục. **2026-09-15 — REPEATER:** cùng `CP_Blocks_Repeater_Control` với `taxonomy = category`, setting `cp_nblocks` (JSON), nút "+ Thêm chuyên mục tin tức"; template part đọc JSON, gọi 1 lần, bỏ `$args['index']`. **+ mobile ≤768px: link "Xem thêm →" xuống DƯỚI list và canh giữa** (`.cp-section-head { display: contents }` + `order`, không đổi DOM). **2026-09-15 (yêu cầu Tùng): thêm BỐ CỤC `featured` — "Nổi bật: 1 bài trái + 5 bài phải"** — option mới trong select "Bố cục" (GIỮ nguyên 3 bố cục cũ; `cp_blocks_sanitize()` tự nhận vì whitelist lấy `array_keys( $cp_def['layouts'] )`); **"Số lượng" = TỔNG số bài** (bài 1 → thẻ lớn cột TRÁI, `count − 1` → danh sách cột PHẢI). `template-parts/home-cat-news.php`: nhánh `featured` — gọi `$cp_query->the_post()` MỘT lần cho thẻ trái rồi loop phần còn lại (không cần mảng trung gian) → `.cp-news-feature` > `.cp-news-feature__main` (ảnh `large`, `sizes` 700px, 16:9; nhãn chuyên mục; `h3`; ngày `d/m/Y`; trích đoạn 28 từ clamp 3 dòng) + `.cp-news-feature__list` > `.cp-news-row` (thumb `medium` 108px 4:3 + `h3` + ngày, gạch phân cách; bài KHÔNG có ảnh → class `cp-news-row--noimg` về 1 cột). CSS `assets/caophat.css`: lưới `minmax(0,1.35fr) / minmax(0,1fr)` gap 24 + `align-items: stretch` (2 cột cao bằng nhau); ≤1024 → 1 cột + ảnh 21:9; ≤768 → thumb 88px, tiêu đề 18.5px, clamp 2 dòng; rule `.cp-news-row.cp-news-row--noimg` phải 2 class (0,2,0) kẻo bị rule `.cp-news-row` trong `@media ≤768` ở cuối file đè; `.cp-news-feature` thêm vào selector `order: 2` của CP2.4/2.5. Khối demo hiện tại: `cp_nblocks = {"cat":"1","layout":"featured","count":"6"}` (1 bài trái + 5 bài phải); **cao bằng nhau (yêu cầu Tùng 2026-09-15):** `align-items: stretch` + ảnh cột trái giãn (`.cp-news-feature__main .cp-news-feature__media { flex: 1 1 auto; min-height: 0; max-height: 520px }`) + phần chữ `flex: 1 1 auto` + list phải `justify-content: space-between` → đo `lech = mainH − listH = 0` ở **mọi** mức side-by-side 1025–1440 (1440 `608.5/608.5`, trước đổi lệch **−82.8**), ảnh crop nhẹ 1.778 → 1.551, ≤1024 không đổi |
| **CP2.6** home-cats-order | Khối "Danh mục nổi bật" — chọn danh mục, kéo thả thứ tự, bố cục số cột / hiển thị ngang. | `inc/customizer.php`, `assets/customizer-cats.js`, `assets/customizer-cats.css`, `template-parts/home-categories.php`, `functions.php`, `assets/caophat.css` | `CP_Term_Order_Control extends WP_Customize_Control` (type `cp_term_order`, `taxonomy` = `product_cat`): render `ul.cp-term-order > li` (checkbox + tên + `(số SP)` + handle `⋮⋮`) + `input.cp-term-order__value` (hidden, `$this->link()`) → JS `sortable({ handle, axis: y, update })` + `change` checkbox → đồng bộ CSV vào hidden input và `wp.customize(settingId).set(csv)`; setting `cp_cats_selected` sanitize = lọc còn chữ số + dấu phẩy (giữ thứ tự). `cp_cats_layout`: `cols-6` / `cols-4` / `cols-3` / `horizontal`. CSS: `.cp-cats--cols-N` (grid) + `.cp-cats--horizontal` (flex + `overflow-x: auto` + `contain: paint` — bắt buộc theo bài học CP2.4). **2026-09-15:** bỏ dòng đếm `%d sản phẩm` dưới tiêu đề thẻ danh mục (chỉ còn mô tả 8 từ nếu danh mục có mô tả) · **2026-09-15: thêm 2 setting text `cp_cats_label` (nhãn nhỏ) + `cp_cats_title` (h2) cho khối tiêu đề (2 control text trong CÙNG section `cp_home_cats`, cùng `sanitize_text_field`) + bỏ dòng mô tả phụ và link "Xem tất cả" của khối này ở `front-page.php`** · **mobile ≤768px: dải = 1 HÀNG 3 CỘT cuộn ngang** (`display: flex`, gap 10px, mỗi thẻ `flex: 0 0 calc((100% - 20px) / 3)`, ẩn thanh trượt + `contain: paint`) **+ nút ‹ › cuộn (CP2.8)**; bỏ lưới 2 cột (và 1 cột ở ≤520px); chữ trên thẻ gọn lại (`.cp-cat-overlay` padding 10px, `b` 13px, mô tả `-webkit-line-clamp: 2`) |
| **CP2.7** block-products-pick | Chọn sản phẩm ưu tiên cho từng khối SP (AJAX + kéo thả), render ưu tiên trước rồi mới lấp phần còn lại của danh mục. | `inc/customizer.php`, `assets/customizer-blocks.js`, `assets/customizer-blocks.css`, `template-parts/home-cat-products.php` | AJAX `wp_ajax_cp_block_products` (`cat`, nonce `cp_block_products`) trả `[{id,title}]` (kèm `products` đã chọn không thuộc danh mục, có cờ `outside`); JS: panel `.cp-block-products` (toggle) → render `ul.cp-block-products__list > li` (checkbox + tên + handle `⋮⋮`), `sortable({ handle })`, tick/đổi thứ tự → `sync()` gom CSV vào `[data-field="products"]` → JSON `cp_pblocks`; sanitize: CSV `absint` (tối đa 50). Render: `wc_get_product()` theo đúng thứ tự CSV → nối thêm `wc_get_products( category, limit = count − đã chọn, exclude = CSV )` |
| **CP2.8** scroll-arrows | Nút cuộn ‹ › hai đầu cho bố cục "Cuộn ngang" + ẩn thanh trượt. | `functions.php`, `assets/scroller.js`, `assets/caophat.css`, `template-parts/home-cat-products.php`, `template-parts/home-cat-news.php` | `cp_scroller_open()` / `cp_scroller_close()` in `.cp-scroller` + `.cp-scroller__btn--prev/--next` (`aria-label` "Cuộn trái/phải", ký tự `‹` `›`) chỉ khi layout `scroll`; CSS: scrollbar ẩn (`scrollbar-width: none`, `-ms-overflow-style: none`, `::-webkit-scrollbar { display: none }`) nên dải KHÔNG chiếm chỗ thanh trượt, nút `position: absolute` giữa 2 đầu (nền trắng, viền, bóng, 40px; ≤768px 34px), **nhô ra 2 đầu: desktop `left/right: -18px` (yêu cầu Tùng 2026-09-15), mobile ≤768px `-15px` = tỷ lệ 34/40 — đo được lệch 2 đầu bằng nhau (`-18/-18`, `-15/-15`) và nút trái vẫn nằm trong viewport ở 390px**, ẩn bằng `.is-start`/`.is-end` (class do JS gắn); JS: `scrollBy({ left: ±(cardWidth + gap), behavior: 'smooth' })`, `update()` gắn/bỏ class 2 đầu, nghe `scroll` + `resize` + `window.load`, tôn trọng `prefers-reduced-motion` (cuộn tức thì). **+ 2026-09-15: `LIST_SEL = '.cp-products, .cp-cat-news, .cp-cats'`** — dải "Danh mục nổi bật" (`template-parts/home-categories.php` bọc `cp_scroller_open()/close()` LUÔN, không điều kiện) dùng lại cùng nút/cơ chế; desktop lưới không cuộn → JS gắn `.cp-scroller--static` nên 2 nút tự ẩn. **2026-09-16 (CP3.7):** tách thêm 2 lớp `cp_scroller_open_markup()` / `cp_scroller_close_markup()` (TRẢ chuỗi) — hàm `cp_scroller_open()/close()` cũ nay chỉ `echo` bản `_markup()` nên 4 template-part KHÔNG phải sửa; cần bản trả-chuỗi vì `woocommerce_product_loop_start`/`_loop_end` (CP3.7) BẮT BUỘC return chuỗi, không được `echo`. **CP3.7 dùng lại y nguyên markup + JS này** cho dải “Sản phẩm tương tự” (chỉ thêm `ul.products` vào `LIST_SEL` của `assets/scroller.js`) |
| **CP2.9** home-tabs | Khối "tab sản phẩm" trên trang chủ — **4 tab cố định** (Sản phẩm mới · Bán chạy · Hot · Khuyến mãi); mỗi tab: bật/tắt · tiêu đề · số lượng (1–20) · bố cục (Lưới 4/3/2 cột · Cuộn ngang). Vị trí: ngay sau khối "Danh mục nổi bật". | `functions.php`, `inc/customizer.php`, `template-parts/home-tabs.php`, `assets/tabs.js`, `assets/caophat.css`, `front-page.php` | `cp_product_tabs()` (khoá `new/best/hot/sale` + tiêu đề mặc định) · `cp_tab_layouts()` · `cp_tab_products( $source, $limit )` (new = date DESC · best = `meta_key total_sales` `meta_value_num` · hot = `featured => true` (cờ "Nổi bật" WC) · sale = `include` từ `wc_get_product_ids_on_sale()`) + filter `cp_tab_products`. Section `cp_home_tabs` = 16 setting (`cp_tab_<nguồn>_show/_title/_count/_layout`). Render: `.cp-tabs-block > .cp-container > .cp-tablist` (nút `role=tab`) + `.cp-tabpane.cp-cat-block.cp-cat-block--<layout>` (panel `role=tabpanel`, panel không active có `hidden`) → tái dùng lưới `--cp-cols` (CP2.4) + `.cp-scroller` (CP2.8). JS: click/←/→/Home/End đổi `hidden` + `aria-selected` + `tabindex`, bắn `resize` cho scroller đo lại. **Tên class KHÁC hệ tab cũ ở dòng ~747 (`.cp-tabs`/`.cp-tab-panel`/`.cp-tab-nav`) để không dính `display: none`.** **Icon mỗi tab: `cp_tab_icon( $source )` (`functions.php`) = SVG inline 24×24 `stroke: currentColor` (new = tia sáng · best = biểu đồ đi lên · hot = ngọn lửa · sale = thẻ giảm giá) in trong `.cp-tablist__icon` (`aria-hidden`) trước chữ; icon 16px / gap 7px (mobile 14px / 6px)** |




| **CP3.1** shop-danh-mục | Skin trang cửa hàng + lưu trữ product_cat/tag: pagehero + 2 cột (sidebar + lưới), card `.cp-card`. **Giá trong card** phải dùng selector ≥ 5 class: `.woocommerce .cp-shop-main ul.products li.product .price` — nếu chỉ 3 class thì rule WooCommerce `.woocommerce:where(body:not(.woocommerce-uses-block-theme)) ul.products li.product .price` (0,4,2) THẮNG (0,3,2) → giá hiện màu olive `rgb(149,142,9)` và 13.71px (đo bằng cách quét toàn bộ `document.styleSheets`). Giá chuẩn = accent `var(--cp-accent)` 17px/700, giá gốc muted 13px/400 + `ins { text-decoration: none }` (mặc định trình duyệt gạch chân `<ins>`). **Nhãn danh mục (19:40 ngày 2026-09-14):** card KHÔNG còn in `.cp-card-cat` — đã bỏ khỏi `cp_loop_body_open()` (loop shop/related); card trang chủ bỏ ở `cp_product_card()`. Nhãn danh mục chỉ còn 1 chỗ: `cp_single_cat_label()` @ `woocommerce_single_product_summary` prio 4 (đầu trang chi tiết SP). **`alt` cho ảnh (22:00 ngày 2026-09-14):** filter `wp_get_attachment_image_attributes` (`inc/woocommerce.php`) → ảnh thiếu alt lấy tên bài viết cha (sản phẩm) làm alt. ⚠️ Cần thiết thật: kiểm tra DB cho thấy thư viện media của 3 SP mẫu có `_wp_attachment_image_alt` **RỖNG** → nhờ filter mà 15/15 ảnh card ở trang danh mục có alt = tên SP. **Ảnh trong NỘI DUNG** (mô tả danh mục/SP) không đi qua filter đó → thêm filter `the_content` trong `functions.php` dùng `WP_HTML_Tag_Processor` gán `alt=""` cho ảnh thiếu hẳn thuộc tính (28/28 ảnh mô tả danh mục "Cửa Phòng Ngủ"). Kết quả: 46/46 ảnh có thuộc tính `alt`. Muốn alt MÔ TẢ thật (tốt cho SEO ảnh) thì điền trong trình soạn thảo | `inc/woocommerce.php` | Xem "CP3.1 — chi tiết" dưới |
| **CP3.2** single-product | Layout chi tiết sản phẩm theo demo moderndoor.vn: lưới 9/3, thẻ trắng, ô tabs riêng, sidebar (danh mục + hỗ trợ), hộp giá + badge %, nút **ĐẶT HÀNG NHANH** (đổi tên từ "Mua hàng" + bỏ dòng phụ 22:30 ngày 2026-09-14), related carousel nền xám. | `inc/woocommerce.php`, `assets/caophat.css` (khối CP3.2), `assets/single-gallery.js` | Guard `cp_is_single_product()`. `cp_single_open` mở `.cp-single-section > .cp-container > breadcrumb + .cp-single-layout > .cp-single-main`. `cp_single_close` @ `woocommerce_after_main_content`: in `cp_single_spec_table()` → `.cp-single-tabs.cp-card` + `woocommerce_output_product_data_tabs()` (đều trong `.cp-single-main`), đóng `.cp-single-main`, in `<aside.cp-single-side>` (`cp_single_support_box` → `.cp-side-branches` danh sách hotline chi nhánh (mỗi dòng tên bên trái — số bên phải, `justify-content: space-between`, không icon; dữ liệu: **CP1.9** — theme_mod `cp_branches` đọc qua `cp_support_branches()` (`functions.php`; Customizer → “Chi nhánh & Hotline Cao Phát”, thêm/xoá/kéo thả; **trước 2026-09-17** đọc từ plugin `pl-tien-ich-tungleads` qua `tlcp_support_branches()`, option `tlcp_support_branches` dạng "Tên | Số"); helper có CẦU NỐI đọc option cũ nên deploy lệch thứ tự không mất số; filter `cp_support_branches` ghi đè; `tel:` bỏ ký tự không phải số) → `cp_trust_box` ô "Cam kết Cao Phát" `.cp-trust-list` icon SVG + nhãn hoa, filter `cp_trust_items` → `cp_single_new_products_box` (5 SP mới nhất qua `wc_get_products` orderby date, trừ SP đang xem; `.cp-side-news` thumb+tên+giá)), đóng `.cp-single-layout`, rồi `.cp-related-band` + `woocommerce_output_related_products()` + dải CTA. `remove_action` mặc định của `woocommerce_output_product_data_tabs`/`woocommerce_output_related_products` @ `woocommerce_after_single_product_summary`. `cp_single_cat_label` prio 4. `cp_single_price_box_open/close` prio 9/11: `_open` mở `.cp-price-box`, `_close` in `.cp-price-off` "Tiết kiệm &lt;wc_price(diff)&gt;" SAU `.price` rồi đóng div. Helper `cp_regular_sale_price()` (xử lý biến thể), `cp_sale_percent()`. Filter `woocommerce_sale_flash` (prio 10, 3 args) đổi mọi `.onsale` → `-N%`. `cp_single_buynow_btn` @ `woocommerce_before_add_to_cart_button` prio 5 (icon túi SVG inline + `.cp-buynow__txt`). `cp_single_hotline_btn` @ `woocommerce_after_add_to_cart_button` prio 20 → hàng `.cp-contact-row` (grid 1fr 1fr): `.cp-contact-btn--call` (tel, cam, icon phone) + `.cp-contact-btn--zalo` (xanh `#0068ff`, `zalo.me/<số từ cp_hotline_tel>`, filter `cp_zalo_url`, icon bubble). Nhãn nút gọi bọc `.cp-tel-num` — ẩn ở `≤768px` nên mobile chỉ còn "Gọi ngay" (nút CTA cuối trang cũng vậy). Nhãn nút mua đổi qua filter `woocommerce_product_single_add_to_cart_text` → "Thêm giỏ hàng"; CSS `text-transform: uppercase` sẵn có hiển thị "THÊM VÀO GIỎ HÀNG" (mặc định WooCommerce — đã bỏ filter rút ngắn 19:55 ngày 2026-09-14; chữ cần ~195px nhưng cột nút chỉ 152px → đang xuống 2 dòng); icon giỏ qua `::before` data-URI. Dòng `.tagged_as` (Thẻ) ẩn bằng CSS. `cp_gallery_carousel_options` filter `woocommerce_single_product_carousel_options` → `directionNav`. Ô số lượng ẩn bằng CSS. `single-gallery.js` (enqueue @ prio 20, guard `is_product()`): bọc `ol.flex-control-thumbs` vào `.cp-thumbs` (ô riêng dưới ảnh chính) + 2 nút `.cp-thumbs-nav` cuộn strip; `MutationObserver` chờ flexslider dựng xong control nav. Filter `woocommerce_product_description_heading` → `__return_null` (bỏ H2 "Mô tả" lặp trong panel). `cp_single_short_desc()` thay `woocommerce_template_single_excerpt` (@20): hộp `.cp-short-desc` nổi bật (nền surface + viền trái primary), `.cp-short-desc__body` thu gọn `max-height:15em` + fade, nút `.cp-short-desc__toggle` (JS inline ẩn nút nếu nội dung ngắn, toggle `.is-open`). `cp_single_spec_table()` gọi trong `cp_single_close` — nằm trong `.cp-single-main` (dưới thẻ SP, trên khi lưới kết thúc): đọc 8 meta `_tlcp_spec_*` (plugin `pl-tien-ich-tungleads`), bỏ trường rỗng, in `.cp-spec.cp-card` (`<dl.cp-spec__grid>` 2 cột, mỗi dòng icon SVG inline + `dt.cp-spec__label` + `dd.cp-spec__value` — nhãn và giá trị in CÙNG 1 dòng (`dt`/`dd` `display:inline`, dấu `": "` thêm bằng `.cp-spec__label::after`, không dùng flex để khỏi phụ thuộc `gap`)). Nhãn + path icon định ngay trong hàm. CSS khối `.cp-spec*` trong `caophat.css` (2 cột → 1 cột ≤768px, padding mobile 28px → 15px). **Nút zoom gallery (2026-09-14):** WooCommerce định nghĩa `.woocommerce-product-gallery__trigger { position:absolute; top/right .5em; z-index: 99 }` (0,1,0) — 99 > header (50) và > panel menu cấp 2-3 (60) → khi menu mega xổ xuống, nút zoom VẼ ĐÈ lên menu (Tùng báo). Hạ về **`z-index: 9`** bằng selector (0,4,2) `.woocommerce div.product div.images .woocommerce-product-gallery__trigger` trong khối CP3.2 (cạnh rule gallery). ⚠️ Phải hạ xuống dưới **50** chứ không chỉ dưới 60: `.cp-header` có `z-index: 50` → panel menu nằm TRONG stacking context đó, z-index chỉ so được trong cùng context. Đo `elementFromPoint` tại tâm nút zoom: menu ĐÓNG → trả về chính nút ✅ · menu MỞ (hover mục mega) → trả về phần tử trong `.cp-nav` ✅ (panel che nút) · rời chuột → lại là nút ✅; bấm nút thật → WooCommerce tạo `.pswp` (Photoswipe) → chức năng zoom còn nguyên ✅ · vẫn nằm trên ảnh/thumbnail (z-index auto) ✅ |
| **CP3.2** hộp giá 2 hàng | **2026-09-17 (yêu cầu Tùng, sau CP3.9): số đánh giá của khách hàng nằm trên 1 DÒNG RIÊNG, phía dưới là phần giá.** | `assets/caophat.css` (khối `.cp-price-box`, `@media max-width:768px`) | DOM do WooCommerce in cố định `[.woocommerce-product-rating][.price][.cp-price-off]` (rating + price cùng prio 10 ⇒ không chèn được `<div>` bọc) ⇒ đổi **flex row → GRID 2 cột**: `minmax(0,1fr) auto`; hàng 1 = rating `grid-column: 1 / -1` (`display:flex`, sao + chữ cùng hàng), hàng 2 = giá (cột 1) + `.cp-price-off` `justify-self:end` (cột 2). **Đo TRƯỚC → SAU (1440):** rating **159×54** (nằm ngang hàng giá, chữ xuống 2 hàng) → **435×27** ở hàng riêng, giá 300×32 @+20,+51 + tag 123×26 @+332,+54 (cùng tâm y 351.6); hộp **102 → 99px**. **Căn sao ↔ chữ:** bỏ `margin-top` ~0.5em (6.75px) của `.star-rating` — pixel 1440: tâm sao **317 → 313** vs tâm chữ **314** ⇒ lệch **3px → 1px** (DOM: 0.0px). **≤768px** 1 cột: rating / giá / tag (tag canh trái). **SP chưa có đánh giá:** hàng 1 tự bỏ — giá + tag lên hàng 1, hộp 475×64 không hở. Link “(N đánh giá của khách hàng)” **bấm được ở cả 5 khổ** → mở tab “Đánh giá (N)” ✓ · tràn 0 · smoke **24/24**. **2026-09-17 (tiếp): `gap: 0`** cho hàng số đánh giá (trước `8px`) ⇒ chữ dịch trái 8px (668.9 → 660.9), hàng ở 390px 286×35 → **286×27**, hàng 1440 vẫn 435×27, lệch tâm sao↔chữ vẫn 0.0px, link vẫn mở tab “Đánh giá (6)” ✓. | ✅ |
| **CP4.1** màu sao dùng chung | **2026-09-17 (yêu cầu Tùng): lấy màu icon sao của trang chi tiết SP dùng CHUNG cho mọi thẻ card sản phẩm toàn trang.** | `assets/caophat.css` (token `:root` + rule nền `body.cp .star-rating`) | Thêm token **`--cp-star: #e2a53a`** và rule nền **`body.cp .star-rating { color: var(--cp-star); }`** (đặt ngay sau khối token, trước mọi rule khác) + đổi **6 chỗ hard-code `#e2a53a`** sang `var(--cp-star)` (CP3.2 summary · CP3.9 `.cp-rv__score` · thẻ đánh giá · chip `i` · `.cp-rv__row-star i` · `p.stars a::before`). **Đo TRƯỚC:** trang danh mục `/danh-muc/cua-nhua-abs-han-quoc/` card của SP 4816 có `.star-rating` nằm trong `.cp-card-body` ăn **màu CHỮ** `rgb(38, 34, 30)` (không rule nào đặt màu) ⇒ lệch với `rgb(226,165,58)` ở trang chi tiết. **SAU:** card = **`rgb(226,165,58)`** ✓ · trang chi tiết 8/8 sao vẫn `rgb(226,165,58)` ✓ · **sao RỖNG giữ `rgb(207,200,216)`** (`#cfc8d8` — WooCommerce khai trực tiếp trong `.star-rating::before`, cố ý để sao đầy nổi). ⚠️ **Lưu ý khi đo:** trang chủ / cửa hàng / danh mục khác / tìm kiếm hiện có **0 `.star-rating`** vì WooCommerce CHỈ in sao khi SP có ≥1 đánh giá (đo 2026-09-17: chỉ SP 4816 đang có đánh giá) — muốn kiểm màu ở card thì phải mở trang danh mục của SP đã có đánh giá. | ✅ |
| | | `assets/caophat.css` | `.woocommerce div.product` grid 2 cột (gallery \| summary) — `.cp.woocommerce div.product > .woocommerce-product-gallery\|.summary { width:100%; float:none }` ghi đè float/width 48% của `woocommerce-layout.css` để 2 cột đều nhau; tabs + related full-width; reskin `.summary`, `p.price`, `form.cart`, `.product_meta`, `ul.tabs`, `.woocommerce-Tabs-panel`, `table.shop_attributes`, `.related ul.products` → `.cp-card`; mobile `≤768px`: thẻ SP (`.cp-single-main > div.product`) + ô tabs (`.cp-single-tabs.cp-card`) giảm padding 28px → 15px (rule cũ ghi `.cp-single-layout > div.product` — selector không khớp DOM nên là rule chết, đã thay); mobile xếp lại thứ tự: `.cp-single-layout` → `flex column` + `display:contents` cho `.cp-single-main`/`.cp-single-side` (phẳng 2 container để `order` chen được box sidebar vào giữa) → thẻ SP → Hỗ trợ trực tuyến → Cam kết Cao Phát → Thông số kỹ thuật → tabs → Sản phẩm mới. BẮT BUỘC kèm `align-items: stretch`: base rule có `align-items: start` (ở grid là trục dọc, ở flex column là trục ngang) — thiếu nó thì gallery (min-content 592px) không bị kéo giãn và đẩy tràn ngang. **Nút "THÊM VÀO GIỎ HÀNG" 1 dòng (22:00 ngày 2026-09-14):** `form.cart` đổi `grid-template-columns: 2fr 1fr` → `minmax(0, 2fr) minmax(200px, 1fr)` — nhãn đầy đủ cần ~195px (đo bằng `white-space: nowrap`) trong khi tỉ lệ cũ chỉ cho 152px → xuống 2 dòng. Đo sau khi sửa: nút giỏ **200×56** (desktop) / **303×56** (mobile 390) — **1 dòng** (`Range.getClientRects().length = 1`), nút ĐẶT HÀNG NHANH cùng hàng ở desktop (256×56, `y=736`) và cùng 56px ở mobile/tablet. **Chia 50/50 (23:00 ngày 2026-09-14):** `form.cart` dùng `grid-template-columns: repeat(2, minmax(0, 1fr))` → 2 nút luôn bằng nhau (đo 8 mức bề rộng: chênh 0px). ⚠️ Hệ quả đã đo: ở **1440px** mỗi nút 228px → nhãn 1 dòng ✅; nhưng ở **769–1280px** cột form chỉ ~296–437px nên mỗi nút còn **143–212px** — nhỏ hơn mức nhãn "THÊM VÀO GIỎ HÀNG" cần (~195px, đã đo bằng `white-space: nowrap`) → nhãn **xuống 2 dòng**, nút cao 59–79px (mốc 1100px: 143px/2–3 dòng/79px). ≤768px xếp 1 cột full chiều ngang nên không bị. Muốn tránh: (a) xếp dọc dưới 1400px, (b) nới cột sidebar ở 1025–1439px, hoặc (c) chấp nhận nhãn 2 dòng. → **TÙNG CHỐT 2026-09-14: phương án (c) GIỮ NGUYÊN**, không sửa gì thêm. **Chiều cao 2 nút:** `align-items: stretch` chỉ cân bằng khi 2 nút CÙNG hàng; ở ≤768px mỗi nút 1 hàng nên phải khai `min-height: 56px` cho CẢ HAI (`.cp-buynow` thiếu dòng đó thì chỉ 46px, thấp hơn nút giỏ — lỗi Tùng báo 22:45) |
| **CP3.3** dat-hang-nhanh | Nút "Mua hàng" (chỉ trang chi tiết SP) mở popup form → tạo **đơn WooCommerce thật**. | `inc/woocommerce.php` (UI + enqueue), `assets/quick-order.js`, `assets/caophat.css` (khối CP3.3), plugin `pl-tien-ich-tungleads.php` (handler AJAX) | Xem "CP3.3 — chi tiết" dưới |
| **CP3.4** cart-page | Trang giỏ hàng `/cart/` — hero + container + skin bảng giỏ/khối tổng tiền. | `inc/woocommerce.php` (filter `the_content` qua helper `cp_woo_page_wrap()` + filter nhãn), `assets/caophat.css` (khối CP3.4) | Xem "CP3.4 — chi tiết" dưới |
| **CP3.5** checkout-page | Trang thanh toán `/checkout/` — hero + container (cùng helper) + skin form 2 cột/khối đơn hàng/nút "Đặt hàng". | `inc/woocommerce.php` (`cp_is_checkout_page()`, dùng `cp_woo_page_wrap()`), `assets/caophat.css` (khối CP3.5) | Xem "CP3.5 — chi tiết" dưới |
| **CP3.6** thankyou-page | Trang hoàn tất đơn `/checkout/order-received/` — hero + container + skin (đích chuyển trang của cả checkout lẫn popup đặt nhanh). | `inc/woocommerce.php` (`cp_is_order_received_page()`, `cp_thankyou_home_button()`, dùng `cp_woo_page_wrap()`), `assets/caophat.css` (khối CP3.6), `assets/quick-order.js` (chuyển trang), plugin `pl-tien-ich-tungleads` (`redirect`) | Xem "CP3.6 — chi tiết" dưới |
| **CP3.7** related-scroller | Dải “Sản phẩm tương tự” ở trang chi tiết SP: **10 SP** + **nút cuộn ‹ ›** (tái dùng CP2.8). | `inc/woocommerce.php` (`cp_related_per_page()`, `cp_related_scroller_flag()`, 3 filter), `functions.php` (`cp_scroller_open_markup()`/`cp_scroller_close_markup()`), `assets/scroller.js` (`LIST_SEL` += `ul.products`), `assets/caophat.css` (ẩn thanh trượt dải) | TRƯỚC: WC mặc định **4 SP** ⇒ dải `1400 / 1400px`, không cuộn được, **0 nút**. SAU: filter `woocommerce_output_related_products_args` → **10 SP**; cờ `cp_related_scroller_flag()` + filter `woocommerce_product_loop_start`/`woocommerce_product_loop_end` bọc `ul.products` bằng `.cp-scroller` + 2 nút (KHÔNG copy `single-product/related.php`; KHÔNG dùng `is_product()` thay cờ vì `loop_start` chạy cho mọi vòng lặp SP). Đo 1440: `1400 / 2680` ⇒ cuộn **1280px**; NEXT 5 lần `0→270→540→810→1080→1280` (= 1 thẻ 250 + gap 20) → `is-end` + SP cuối trong khung; PREV 5 lần về `0` + `is-start`; nút 40×40 cách mép **2px**; ‹ ẩn khi ở đầu (`opacity: 0` + `pointer-events: none` — cơ chế CP2.8); `elementFromPoint` = **bấm được**, không chồng FAB. Mobile 768/390: cuộn 1952/2330px, nút **34×34** cách mép 5px, `0 → 270` OK, `scrollWidth == innerWidth`. **Hồi quy:** `/san-pham/` 20 SP lưới 4 cột **0 nút**; 3 dải trang chủ vẫn chạy (#1 `--static` ẩn nút vì không cuộn được). 0 PHP notice. |
| **CP3.8** desc-collapse | Thu gọn tab “Mô tả” ở trang chi tiết SP + nút **Xem thêm / Thu gọn**. | `inc/woocommerce.php` (`cp_single_description_tab()` + filter `woocommerce_product_tabs`), `assets/caophat.css` (modifier `.cp-single-desc`) | TRƯỚC: `#tab-description` cao **5229px** (6430 ký tự / 57 `<p>`), **0 nút**. SAU: filter `woocommerce_product_tabs` đổi `callback` của tab `description` (KHÔNG copy `single-product/tabs/description.php`); nội dung render qua `ob_start()` + `the_content()` (giữ shortcode) rồi bọc `.cp-shop-desc` + `.cp-single-desc` (tái dùng NGUYÊN khối CP3.1: nút viên, gradient, `is-open`). JS inline như CP3.2: ẩn nút nếu `scrollHeight <= clientHeight + 4`. CSS modifier: bỏ `margin-top: 36px`/`border-top`/padding của khối dưới lưới; `font-size/line-height/color: inherit` (giữ typography panel tab); ngưỡng **24em**. **BẪY specificity:** override body (0,3,0) đặt SAU rule `.cp-shop-desc.is-open …` (0,3,0) của CP3.1 ⇒ trạng thái MỞ không thắng `max-height` (đo: panel vẫn 499px dù nhãn/`aria` đổi) → phải dùng **(0,4,0)** `.cp-single-tabs .cp-single-desc.is-open .cp-shop-desc__body { max-height: none }`. **Đo:** 499px → **5387px** khi mở → 499px khi thu gọn; `aria-expanded` và nhãn đổi đúng; mobile 390 → 8801px, overflow 0; SP mô tả 74 ký tự → nút `hidden`; SP có tab “Thông tin bổ sung” → đổi tab không vỡ. 0 PHP notice. |
| **CP4.1** css | Toàn bộ giao diện `.cp-*` + phần thích ứng WordPress. | `assets/caophat.css` | Tokens `--cp-*`, buttons, topbar/header/hero/features/section/cats/products/cta/posts/footer/fab, responsive. Cuối file: adapt `wp_nav_menu`, `.screen-reader-text`, WooCommerce price, `main#main` padding. **2026-09-15 — `.cp-section { padding: 40px 0 }`** (trước `64px 0`, yêu cầu Tùng): vì sửa ở rule GỐC nên chỉ đổi các khối KHÔNG có padding riêng — 5 khối trang chủ · `.cp-section` shop/danh mục SP · `.cp-news-section` (CP5.1) · `.cp-section.alt` dải CTA trang chi tiết SP (đo `getComputedStyle`: **64/64 → 40/40**); các khối có padding riêng giữ nguyên (`.cp-single-section` 28/0 · `.cp-cart-section` + `.cp-checkout-section` 40/64 · `.cp-thankyou-section` 40/120 · `.cp-related-band` 40/44 · `.cp-article-section` 26/44 · `.cp-page-section` 28/44) **Màu theo yêu cầu Tùng (19:40 ngày 2026-09-14):** `.cp-btn-accent` nền `#000000` + hover `#333333` (trước là `--cp-accent`/`--cp-accent-dark` — hover phải đổi theo vì đen → cam là lệch tông) → **Tùng giao DeepSeek quyết 2026-09-14: GIỮ `#333333`** — lý do: cả 2 chỗ dùng (dải CTA trang chủ `front-page.php:121` + dải CTA trang chi tiết SP `inc/woocommerce.php:408`) đều nằm trên nền cam gradient `--cp-accent-dark`→`--cp-accent` nên hover sang cam sẽ CHÌM vào nền; convention theme hover cùng tông (`.cp-btn-primary` vàng → vàng đậm; `.cp-btn-ghost` → viền vàng). Đo Playwright tại 1440: thường `rgb(0,0,0)`/`#fff` → hover `rgb(51,51,51)` + `translateY(-1px)` + chữ trắng GIỮ nguyên (không bị `a:hover` ghi đè), tương phản 21:1 → **12.6:1** (vẫn AA/AAA); rời chuột trở về đen ✅; `.woocommerce-message { border-top-color: #fbaf02 }` + `.woocommerce-message::before { color: #fbaf02 }` (WooCommerce mặc định xanh lá #8fae1b) — đo được `rgb(251,175,2)` cho cả viền 3px lẫn icon, NHƯNG icon hiện không hiển thị vì `content` computed = rỗng (parent theme đã bỏ icon font); `.cp-single-side` sticky `top: 120px → 100px`. **`.cp-nav { padding-right: 20px }` (20:30 ngày 2026-09-14):** tạo khoảng 20px giữa mục menu cuối và khối `.cp-header-cta` (đo: mục "Liên hệ" phải 918 → search trái 938 = 20px). KHÔNG ảnh hưởng: panel mega vẫn full chiều rộng (panel neo vào `.cp-header`, không theo nav) và panel mobile (khối `≤768px` đặt lại `padding: 6px 0 10px` bằng shorthand nên `padding-right` base bị ghi đè → đo ở 390px = `6px 0px 10px 0px`). **Nền autofill (20:45 ngày 2026-09-14):** thêm khối `input:-webkit-autofill, :hover, :focus, :active { box-shadow: 0 0 0 1000px #fff inset !important; -webkit-text-fill-color: var(--cp-text) !important; caret-color: var(--cp-text) }` (đặt ngay trước mục "WordPress adaptations"). ⚠️ Rule DevTools hiển thị là `input:-internal-autofill-selected { background-color: rgb(232,240,254) }` **thuộc UA stylesheet của Chrome** (grep toàn bộ child theme + parent theme + plugin + Customizer + DB = 0) → KHÔNG xoá được, author CSS cũng KHÔNG viết được selector `:-internal-*`; `background-color` thường không thắng nên phải dùng box-shadow inset. Đổi màu nền ô nhập thì sửa màu trong khối đó · **2026-09-15: mobile ≤768px `.cp-btn { padding: 10px 14px }`** (trước `12px 22px`; cao 50 → 46px, ngang −16px/nút; desktop giữ `12px 22px`) |
| **CP4.2** images | Ảnh mockup — đã tối ưu sang webp. | `assets/images/{hero.webp, hero-652.webp, door-hdf.webp}` | Tổng **84KB** (trước 2.8MB): `hero.webp` 37KB + `hero-652.webp` 18KB (gốc `hero.png` 986KB) · `door-hdf.webp` 24KB (gốc `door-hdf.png` 656KB). **Đã xoá 6 bản PNG gốc 2026-09-14** (`hero.png`, `door-hdf.png`, `door-mdf.png`, `door-composite.png`, `door-korea.png`, `door-fire.png`) sau khi kiểm tra **0 tham chiếu**: grep toàn bộ child theme + parent theme + plugin (php/css/js) = 0 file, DB `wp_options` (chỉ còn cache hash của Solid Security, không phải tham chiếu) + `wp_posts` = 0 → còn khôi phục được từ lịch sử git. Đã xoá cả thư mục rỗng `patterns/` |
| **CP5.1** news-category | Trang danh mục tin tức (lưu trữ chuyên mục bài viết) — override `category.php` cho category; toàn bộ khối hiển thị ở `inc/news.php`. | `category.php`, `inc/news.php`, `functions.php`, `assets/caophat.css`, `front-page.php` | `get_header()` + `<main id="main">` → hero `.cp-pagehero` (breadcrumb tự viết + H1 = tên chuyên mục + `.cp-pagehero__meta` "N bài viết") → `.cp-section` > `.cp-container` > `.cp-news-layout` (grid `minmax(0,1fr) / 320px`): `.cp-news-main` (danh sách `.cp-news-list` + `.cp-pagination` + mô tả chuyên mục thu gọn) & `.cp-news-aside` (3 hộp `.cp-side-box`, KHÔNG sticky). Thẻ bài viết `.cp-news-card`: grid `300px / 1fr`, ảnh `aspect-ratio: 4/3` + `object-fit: cover`, nhãn chuyên mục accent, ngày có icon đồng hồ, tiêu đề, trích đoạn `line-clamp: 3`; bài đầu trang 1 = `.cp-news-card--featured` (1 cột, ảnh `21/9`). Sidebar: `wp_list_categories` (`category`, `show_count`) + `WP_Query` 5 bài mới + `cp_single_support_box()`. Phân trang: `.cp-pagination` bọc `the_posts_pagination()` (mũi tên ‹ ›, `.screen-reader-text`). Mô tả chuyên mục: `wpautop` + `wp_kses_post` trong `.cp-shop-desc.cp-news-desc` — dùng lại CSS thu gọn của CP3.1 (nút Xem thêm/Thu gọn + JS inline 8 dòng như `cp_shop_desc_below()`). `cp_news_archive_url()` (filter cùng tên) thay link "Xem tất cả →" của khối tin trang chủ (trước đây `page_for_posts` = 0 → link trỏ về trang chủ; nay về `/chuyen-muc/tin-tuc/`). Responsive: ≤1024px về 1 cột, sidebar thành 2 hộp/hàng; ≤768px thẻ về 1 cột (ảnh trên). **Số đo Playwright (2026-09-15):** 1440 → `.cp-news-layout` = `1048px / 320px`, `.cp-news-card` 1046×259 (grid `300px 692px`, ảnh 300×225, tiêu đề 20px), `.cp-news-card--featured` 1048×640 (ảnh 1046×448, tiêu đề 27px); 1100 → `734/300` + thẻ ngang ảnh 260px; 1024 → lưới 1 cột `984px`, `.cp-news-aside` = `482px 482px` và nằm DƯỚI main; 768 → thẻ 1 cột (ảnh 726×311); 390 → 350px. **6/6 mức bề rộng `scrollWidth == innerWidth`.** `elementFromPoint` ở tâm 6 phần tử → đúng phần tử. `.cp-news-aside` cao **1088.7px** (cột phải, desktop) > viewport 900 → **quyết định KHÔNG sticky**. **Bẫy 1 — specificity khi thêm biến thể featured:** bản đầu dùng `@media (max-width:1200px) { .cp-news-card:not(.cp-news-card--featured) {…} }` = (0,2,0) → đè rule `.cp-news-card { grid-template-columns: minmax(0,1fr) }` của `@media (max-width:768px)` (0,1,0) vì rule cuối file thắng khi cùng độ ưu tiên *chỉ khi bằng nhau* → đo ở 390px: cột chữ co còn **38px** (348 bị trừ 260+18+32), `scrollWidth` = **457** (tràn 67px). Sửa: `.cp-news-card--featured { grid-template-columns: minmax(0,1fr); gap: 0 }` cùng (0,1,0) đặt SAU trong cùng media query. **Bẫy 2 — `max-height` + `aspect-ratio` trên ảnh:** `aspect-ratio: 16/9` + `max-height: 460px` → chiều cao bị chặn rồi `aspect-ratio` tính ngược ra BỀ NGANG → ảnh featured chỉ rộng **817.8px** trong thẻ 1046px (hụt 228px, ảnh không full thẻ). Sửa: bỏ `max-height`, dùng `aspect-ratio: 21 / 9` (1046×448). **Ảnh:** khai `sizes` tường minh cho cả 3 chỗ (`the_post_thumbnail`) — thẻ thường `(max-width:900px) 100vw, (max-width:1200px) 260px, 300px` + size `medium`, featured `…, 1040px` + size `large`, thumb sidebar `64px` + size `thumbnail`; vì fallback mặc định 500px của WordPress làm Chrome chọn ứng viên ~533w. **Tồn tại ở LOCAL (không do CP5.1):** 3/12 ảnh thumb `naturalWidth = 0` — metadata `_wp_attachment_metadata` trỏ `-533x400.webp` / `-600x375.webp` nhưng file KHÔNG có trong `uploads/` (đã `ls` trong container) và không code nào sinh `.webp` (grep child+parent+plugin = 0); các size 286x400/533x400/600x375/1067x800 không có trong `get_intermediate_image_sizes()` ⇒ metadata sót từ môi trường khác ⇒ cần `wp media regenerate` (local) và đối chiếu trên production. **Ngoài phạm vi:** `single.php` (chi tiết bài viết) và `home.php` (trang "Bài viết") vẫn dùng template theme cha — `page_for_posts` = 0 nên chưa có trang đó. **2026-09-16 (CP5.3): thân trang đã TÁCH sang `template-parts/news-archive.php`** — `category.php` chỉ còn wrapper mỏng (`get_header` → `get_template_part` → `get_footer`), dùng chung với `tag.php`; số đo không đổi sau khi tách. |
| **CP5.3** tag-archive | Trang **thẻ (tag)** — lưu trữ theo tag `/the/<slug>/` (option `tag_base` = `the`). | `tag.php`, `template-parts/news-archive.php` (thân trang dùng chung), `category.php` (wrapper), `inc/news.php` (`cp_news_tags_box()`, `cp_news_breadcrumb()` nhánh tag), `assets/caophat.css` (`.cp-side-tags`) | TRƯỚC: tag rơi vào `archive.php` của theme cha — `main.tl-main.tl-container` (không `.cp-container`), **0** `.cp-news-card` / 10 `.tl-post-card` (không ảnh), không hero/breadcrumb/cột phải, H1 "Thẻ: …" do WP in. SAU: `tag.php` = `get_header()` + `get_template_part( 'template-parts/news-archive' )` + `get_footer()`; partial suy nhánh bằng `get_queried_object()->taxonomy === 'post_tag'` ⇒ hero `.cp-pagehero` **1440×143** · `.cp-news-layout` **1400×3245** · `.cp-news-aside` **320×1669 / 4 hộp** · **10** `.cp-news-card` (đầu 1048×640) · `.cp-pagination` 1048×42 · H1 = tên thẻ. Breadcrumb **"Trang chủ / Tin tức / Từ khoá: \<thẻ\>"** — mục "Tin tức" chèn chỉ khi `cp_news_archive_url()` ≠ `/`; **KHÔNG** gọi `get_ancestors()/get_cat_name()` bằng ID thẻ (trả rỗng ⇒ crumb trắng). Hộp **"Từ khoá phổ biến"** (`cp_news_tags_box()`): `get_terms( post_tag, orderby=count DESC, number=30, exclude=<thẻ đang xem> )` → lọc `count >= 2` → cắt **12** (filter `cp_news_tags_box_number`) → chip `.cp-article__tag` của CP5.2 + số bài; **KHÔNG dùng `wp_tag_cloud()`** vì site có **483 thẻ / 354 thẻ chỉ 1 bài**. CSS: `.cp-side-tags { display:flex; flex-wrap:wrap; gap:8px }` + `.cp .cp-side-tags a.cp-article__tag` — selector phải là **(0,3,1)** mới hơn `.cp a.cp-article__tag` (0,2,1) của CP5.2, cho `height:auto; min-height:32px; white-space:normal` (tên thẻ dài tràn khỏi chip cao cố định 32px). Đo: chip rộng nhất **216px < cột 320px**, cao 32px, 12 chip; click chip → `/the/cua-go-cong-nghiep/` (H1 + breadcrumb đổi đúng); **`scrollWidth == innerWidth` ở 1024/768/390** (chip xuống 6/4/10 hàng); thẻ rỗng (29 thẻ count 0) → "Thẻ này chưa có bài viết nào." **Hồi quy CP5.1:** layout 1400×3187 · 3 hộp · thẻ đầu 1048×640 · breadcrumb "Trang chủ / Báo giá" — **không đổi**. |
| **CP5.4** no-article-thumb | Bài viết KHÔNG in ảnh đại diện đầu bài (mặc định tắt; bật lại bằng filter, không phải sửa file). | `single.php`, `assets/caophat.css` (giữ `.cp-article__thumb*` cho ca bật lại) | Điều kiện in: `apply_filters( 'cp_article_show_thumb', false ) && has_post_thumbnail()`; markup `<figure class="cp-article__thumb">` (ảnh `large`, `eager` + `fetchpriority="high"`) **giữ nguyên** trong file ⇒ `add_filter( 'cp_article_show_thumb', '__return_true' )` là bật lại được. **Lý do (đo 2026-09-16):** 3/7 bài có ảnh đại diện **lỗi file** (`naturalWidth = 0×0` → khung xám 990×620). **Đo 2 trạng thái:** mặc định (tắt) → 0 `.cp-article__thumb`, gap head→nội dung **22px**, bài **9331px**; bật (test bằng mu-plugin tạm, đã xoá) → figure **1**, gap **666px**, bài **9975px**; `overflow = 0` ở 1440/390; `page.php` (trang tĩnh) lúc đó **vẫn in** ảnh (`/gioi-thieu/` = 1 figure) — **từ CP6.3 đã bỏ hẳn** (xem CP6.3). **KHÔNG phải CSS mồ côi** (`.cp-article__thumb` giữ nguyên để bật lại). **Đã kiểm trước khi sửa:** class KHÔNG bị CSS ẩn ở mức nào (quét `document.styleSheets` + grep child/theme cha/plugin + `git log -S`). |
| **CP5.2** single-post | Trang chi tiết tin tức (bài viết `post`) — override `single.php` của child; tái dùng sidebar + lưới 2 cột của CP5.1. | `single.php`, `inc/news.php`, `assets/caophat.css` | `get_header()` → `<main id="main">` → `.cp-section.cp-article-section` > `.cp-container` > `.cp-news-layout`: `.cp-news-main` + `.cp-news-aside` (`cp_news_sidebar()` của CP5.1). Trong `.cp-news-main`: `.cp-breadcrumb.cp-breadcrumb--bar` (tự viết, `cp_news_single_breadcrumb()`) → `<article class="cp-article">` (thẻ trắng): `.cp-article__head` (h1 + meta: chuyên mục (link) / ngày `<time>` / tác giả / thời lượng đọc), `.cp-article__thumb` (ảnh `large`, `loading="eager"` + `fetchpriority="high"` cho LCP, `sizes` khai 992px), `.cp-article__content` (typography), `.cp-article__tags` (`the_tags` → chip), `.cp-article-nav` (bài trước/sau) → **KHÔNG in khối bình luận** (đã bỏ `comments_template()` — yêu cầu Tùng 2026-09-15: ẩn mục "Để lại một bình luận"; kiểm DB trước khi bỏ: cả 7 comment toàn site đều là ghi chú đơn hàng gắn `shop_order` đã xoá, **không bài tin nào có bình luận thật** → bỏ cả truy vấn, không ẩn bằng CSS; muốn hiện lại thì gọi `comments_template()`, theme cha đã có `comments.php`). Ngoài lưới: dải `.cp-news-related` (nền xám, tiêu đề "Bài viết liên quan", lưới 4 thẻ mini ảnh trên/chữ dưới). Ảnh: mọi `the_post_thumbnail` đều khai `sizes`. KHÔNG copy template WooCommerce; `single-dichvu.php` (CPT dịch vụ) vẫn của theme cha. **Số đo Playwright (2026-09-15, bài "Kích thước cửa phòng ngủ" + bài "Báo giá cửa nhựa giá rẻ TPHCM" + bài không có ảnh):** `.cp-article` **1048×9828** (1440), `.cp-article__content` **990px**, font **16.5px / 30.5px**; H1 **34px**; `h2` **26px + border-bottom 2px**; `ul` = **`disc` + `padding-left: 24px`**; link trong bài **`rgb(200,71,31)` + underline**; meta `Tin tức · Tháng 9 29, 2024 · Ngọc Diễn · 7 phút đọc` (`cp_news_reading_time()` đếm bằng `preg_split` → 7 phút); **34 chip tag** 129×32; `.cp-article-nav` = **`516px 516px`** (bài mới nhất chỉ có "Bài trước" → 516 nửa hàng; bài giữa 2 thẻ `x=20`/`x=552`, mục "Bài sau" `text-align: right`; ≤600px → 1 cột); dải liên quan nền **`rgb(246,244,241)`**, 4 thẻ mini **335px**, cột **4 → 3 (≤1200) → 2 (≤1024) → 1 (≤600)**; **6/6 mức bề rộng `scrollWidth == innerWidth`** (1440/1280/1100/1024/768/390); sidebar **3 hộp** (`Chuyên mục` / `Tin mới nhất` / `Hỗ trợ trực tuyến` — tái dùng CP5.1); **không còn khối bình luận** (`#comments` = 0); 0 PHP notice; ảnh trong bài + ảnh mini không có ảnh hỏng. **Click đã kiểm:** breadcrumb → `/chuyen-muc/tin-tuc/`; chip tag → `/the/cua-composite/`; thẻ mini liên quan → bài; "Bài trước" → bài trước; link trong nội dung → danh mục SP. **Bẫy 1 — tràn ngang từ DỮ LIỆU:** bài có `<span style="font-size:100%">` + 73 ký tự `=` (dải phân cách) → span rộng **731.8px**, `scrollWidth` **769 > 390** (tràn cả ở 768) → `overflow-wrap: break-word` trên `.cp-article__content`. **Bẫy 2 — ảnh đầu bài DỌC (992×1388):** `width: 100%` → cao **1388px**; thêm `max-height` mà giữ `width: 100%` → hộp vẫn 990px, ảnh canh giữa lộ 2 dải nền kem; `object-fit: cover` → mất nội dung ảnh → chốt `max-height: 620px` + `width: auto` + `margin-inline: auto` → **443×620 với ratioBox 0.714 = ratioNat 0.715** (không méo/crop); ảnh ngang 4:3 → **827×620**. **Bẫy 3 — `auto-fit` với mục lẻ:** `repeat(auto-fit, minmax(240px, 1fr))` cho mục lẻ giãn hết **1048×80.9** → `repeat(2, minmax(0,1fr))` + `.cp-article-nav__item--next:only-child { grid-column: 2 }`; ≤600px trả `grid-column: auto` (nếu giữ `2` sẽ tràn). **`str_word_count()` không đếm được từ tiếng Việt có dấu** → phải `preg_split( '/\s+/u' )`. `wp_link_pages()` giữ như theme cha; **khối bình luận KHÔNG in** trên trang chi tiết tin tức (`comments_template()` đã bỏ theo yêu cầu Tùng — xem mục "KHÔNG in khối bình luận" ở trên) |
| **CP6.1** page-content | Trang nội dung tĩnh (Admin → Trang) — override `page.php`; hero + thẻ nội dung + sidebar, tái dùng lưới CP5.1 và typography CP5.2. | `page.php`, `inc/page.php`, `functions.php`, `assets/caophat.css` | `cp_is_woo_endpoint_page()` guard (cart/checkout/order-received/my-account → chỉ `the_content()` cho CP3.4/CP3.5 bọc). Layout: `.cp-page-section` > `.cp-container` > `.cp-news-layout`: `.cp-news-main` (thẻ trắng `.cp-page` chứa `.cp-page__content.cp-article__content` — **cố ý dùng lại class typography của CP5.2**, không nhân đôi CSS) & `.cp-news-aside` (`cp_page_sidebar()`). Hero `.cp-pagehero`: `cp_page_breadcrumb()` + `<h1>` = `the_title()`. `wp_link_pages()` giữ; KHÔNG `comments_template()`. **Số đo Playwright (2026-09-15):** TRƯỚC — `page.php` theme cha: `.tl-main` không `.cp-container`, **không H1**, nội dung trong `.tl-page__content` 720px; SAU — H1 trong `.cp-pagehero` 40px (1440) / 28px (390), breadcrumb 3 cấp `Trang chủ / Chính sách / Chính sách bảo hành`, `.cp-news-layout` `1048px 320px` → `888/320` (1280) → `734/300` (1100) → 1 cột (1024) → `728` (768) → `350` (390), `.cp-page` 1048 wide, nội dung 990px font 16.5/30.5, ảnh đầu trang 990×620; **4/4 trang × 6 mức bề rộng `scrollWidth == innerWidth`**; 0 PHP notice. **Bẫy caption WordPress cũ:** `<figure class="wp-caption" style="width: 1000px">` (từ `[caption width="1000"]`) → `scrollWidth` **1037** ở 1024/768/390 → phải `!important` để đè inline style (`.cp-article__content .wp-caption, .cp-page__content .wp-caption`). **Hồi quy:** `/cart/` + `/checkout/` mỗi trang đúng 1 hero, `.cp-checkout` còn nguyên, **0 `.cp-container` lồng**; `/san-pham/` 1 hero “Sản phẩm”; `/` vẫn `front-page.php`; `/my-account/` không hero (như trước, chưa skin) |
| **CP6.2** page-subnav | Hộp "Trong mục này" cho trang phân cấp. | `inc/page.php`, `assets/caophat.css` | `cp_page_subnav_box()`: guard bằng `wp_get_post_parent_id()` + `get_children()`; gốc nhánh = trang cha cao nhất; `wp_list_pages( ['child_of' => $root, 'depth' => 2, 'title_li' => '', 'echo' => false ] )`; markup `.cp-side-box` + h4 "Trong mục này" + `ul.cp-side-cats.cp-page-nav` (tái dùng CSS CP3.2) → mục đang xem `current_page_item` (đậm + accent), trang cha `current_page_ancestor`. **Số đo:** trang “Chính sách bảo hành” (con) + “Chính sách” (cha) → **10 mục** (gốc + 9 trang con); `current_page_item` = chữ `rgb(200,138,0)` + `font-weight: 700`; click “Chính sách vận chuyển” → `/chinh-sach/chinh-sach-van-chuyen/` + `current_page_item` đổi đúng; `/lien-he/`, `/gioi-thieu/` → 0 mục, cột phải 2 hộp (`Hỗ trợ trực tuyến` + `Cam kết Cao Phát`) |
| **CP6.3** no-page-thumb | Trang tĩnh KHÔNG in ảnh đại diện — nối tiếp CP5.4 (bài viết) theo yêu cầu Tùng “xoá luôn ảnh thumb `cp-article__thumb` trong chi tiết trang”. | `page.php` (xoá hẳn khối figure), `single.php` (chỉ sửa chú thích: `page.php` đã bỏ) | **XOÁ HẲN** `<figure class="cp-article__thumb">` + `the_post_thumbnail( 'large', … )` khỏi `page.php` (KHÁC CP5.4 — bên bài viết giữ markup sau filter `cp_article_show_thumb`; trang không có công tắc, lấy lại bằng `git log -S'cp-article__thumb' -- page.php`). **Lý do (đo 2026-09-16):** 2/2 trang có ảnh đại diện **lỗi file** (`naturalWidth = 0×0`) ⇒ khung xám `--cp-surface` (`/gioi-thieu/` 990×451 · `/chinh-sach-bao-hanh/` 990×620). **Đo 5 mức × 4 trang:** figure **1 → 0**; `.cp-page` `/gioi-thieu/` **1194 → 719px** (1440) · **1838 → 1676px** (390); k/c đỉnh thẻ → nội dung **502 → 27px** (1440) · **181 → 19px** (390); `/chinh-sach-bao-hanh/` **2820 → 2176px** · **671 → 27px**; **2 đường cơ sở không đổi** (`/chinh-sach/` 2579px · bài viết 10852px) ⇒ không hồi quy; **tràn 0 cả 20 lượt**, 0 lỗi PHP. CSS `.cp-article__thumb*` **vẫn giữ** (cho ca bật lại ở bài viết — CP5.4) ⇒ không mồ côi. | ✅ |
| **CP7.1** 404-page | Trang 404 có skin Cao Phát — trước đó rơi vào `index.php` của theme cha (0 phần tử `.cp-*`). | `404.php` (mới), `inc/system.php` (mới), `functions.php` (require), `assets/caophat.css` (khối CP7) | Hero `.cp-pagehero` + breadcrumb `cp_system_breadcrumb()` + H1 + meta; thân trong `.cp-page` (typography `.cp-article__content` CP5.2): `cp_inline_search_form()` (tái dùng `.cp-search` CP1.6 + `.cp-inline-search`) + 4 nút `.cp-btn`; aside `cp_single_support_box()` + `cp_product_cats_box()` (mới, `.cp-side-box`/`.cp-side-cats` CP3.2) + `cp_trust_box()`. Đo 6 mức: hero 1440×143 → 390×150; nút 171×50 → 316×46; ô search 416×44 → 272×44; `elementFromPoint` OK; `tl-main` 0; tràn 0; 0 lỗi PHP. **Bẫy:** `.cp-search` bị CP1.6 ẩn ≤768px ⇒ phải khai lại `display: flex` cho `.cp-search.cp-inline-search` (0,2,0 khai sau nên thắng, không cần `!important`). | ✅ |
| **CP7.2** search-results | Trang kết quả tìm kiếm CHUNG (`?s=…`); tìm sản phẩm vẫn do WooCommerce (`archive-product.php` + skin CP3.1). | `search.php` (mới), `inc/system.php` | Hero + meta `N kết quả`; `cp_search_card()` → sản phẩm dùng `cp_search_product_card()` (`.cp-news-card` + nhãn danh mục SP + **giá** thay ngày đăng), còn lại `cp_news_card()` (CP5.1); rỗng ⇒ `.cp-news-empty` + form tìm lại; phân trang CP5.1; aside 4 hộp. Đo: `?s=cua` 10 thẻ (giá `3.100.000₫…`, ảnh thật 300×300/260/694/316), layout 1400 (`1048/320`) → 1 cột ở 1024; `?s=cua&post_type=product` vẫn `.cp-shop` (không giẫm WC); `tl-main` 0; tràn 0; 0 lỗi PHP. | ✅ |
| **CP7.3** strip-flatsome | Bóc shortcode Flatsome mồ côi trong nội dung cũ (giữ phần chữ bên trong). | `inc/system.php` (`cp_flatsome_shortcodes()`, `cp_strip_shortcode_keep_inner()`, `cp_strip_orphan_flatsome_shortcodes()` + filter), `functions.php` | Filter `the_content` **prio 9**; bỏ qua khi Flatsome chạy (`shortcode_exists('ux_banner'/'ux_text')`) + mỗi tag phải `! shortcode_exists()` ⇒ không phá shortcode plugin đang bật; giữ chữ trong thẻ bao, bỏ thẻ tự đóng; lặp ≤5 lượt (thẻ lồng); `is_admin()` return sớm để trình soạn thảo thấy nội dung GỐC; tắt bằng `cp_strip_flatsome_shortcodes`. Đo `wp eval`: `[section][row][col]A[/col][/row][/section][gap][button]C[/button][features]` → `AC[features …]`; `[contact-form-7]` giữ nguyên; `WP_ADMIN=true` ⇒ y nguyên. `/lien-he/`: 0 shortcode Flatsome còn lại + đã được skin (42 class `.cp-*`, `tl-main` 0). | ✅ |


## CP3.1 — chi tiết trang danh mục

Không copy `woocommerce/archive-product.php`. Dùng hook (theme cha đã gỡ `woocommerce_get_sidebar` ở P4.1; cha cũng set `loop_shop_columns=3`, `loop_shop_per_page=12` ở P4.2).

| Hook / filter | Việc |
| :--- | :--- |
| `woocommerce_before_main_content` (bỏ `woocommerce_output_content_wrapper`, thêm mới) | In `.cp-pagehero` (breadcrumb + `woocommerce_page_title`) + mở `.cp-section > .cp-container > .cp-shop`; in `<aside.cp-shop-aside>` gọi `cp_product_cat_box()` (tái dùng `.cp-side-box`/`.cp-side-cats` của CP3.2); mở `.cp-shop-main` |
| `woocommerce_after_main_content` prio 5 (`cp_shop_desc_below`) | `ob_start` gọi lại `woocommerce_taxonomy_archive_description()` + `woocommerce_product_archive_description()` (đã `remove_action` khỏi `woocommerce_archive_description`); nếu có nội dung → in `.cp-shop-desc > .cp-shop-desc__body` + `<button.cp-shop-desc__toggle>` (span `__more`/`__less`) + `<script>` IIFE toggle class `is-open` + `aria-expanded` |
| `woocommerce_after_main_content` prio 10 (`cp_shop_close`) | Đóng `.cp-shop-main` + `.cp-shop` + `.cp-container` + `.cp-section` |
| filter `loop_shop_per_page` prio 20 → 20 · filter `woocommerce_show_page_title` → `__return_false` | 20 sp/trang (4 cột × 5 dòng); bỏ H1 trùng trong header (pagehero đã in) |
| `woocommerce_before_shop_loop` @19 `cp_shop_toolbar_open` / @35 `cp_shop_toolbar_close` / @25 `cp_price_filter_bar` · `woocommerce_no_products_found` @5 `cp_price_filter_bar` | Bọc `.cp-shop-toolbar` quanh result_count(@20) + ordering(@30); chèn `.cp-price-filter` (4 nút `<a>` link `add_query_arg(min_price/max_price)` trên `get_permalink(shop)`/`get_term_link`, giữ `orderby`; nút active href về base = bỏ lọc). WooCommerce tự lọc qua `posts_clauses` khi có `$_GET['min_price']`/`max_price` — không thêm query logic |
| `woocommerce_before_shop_loop_item_title` prio 5 / 20 | Mở `.cp-card-media` (bọc sale flash + thumbnail) → đóng, mở `.cp-card-body` (KHÔNG còn in tên `product_cat` — bỏ 19:40 ngày 2026-09-14) |
| `woocommerce_after_shop_loop_item` prio 20 | Đóng `.cp-card-body` |
| ~~`woocommerce_loop_add_to_cart_link`~~ | **ĐÃ BỎ (20:05 ngày 2026-09-14)** cùng với `cp_loop_detail_link()`. Card không có nút nào: `remove_action('woocommerce_after_shop_loop_item','woocommerce_template_loop_add_to_cart',10)` để nút "Thêm vào giỏ hàng" mặc định của WooCommerce không xuất hiện. Ảnh + tiêu đề trong card vẫn là link |
| Guard | Chỉ chạy khi `is_shop() \|\| is_product_taxonomy()` (không đụng single product) |

CSS thêm ở `caophat.css`: `.cp-pagehero`, `.cp-shop` (grid 260px/1fr), `.cp-shop-aside` (sticky), **`@media (max-width: 1024px)`: `.cp-shop-aside { order: 2 }` + `.cp-shop-main { order: 1 }` → hộp "Danh mục sản phẩm" xuống DƯỚI lưới sản phẩm (yêu cầu Tùng 2026-09-15; đo 390px: trước main y=1870 vì hộp danh mục cao 1603.8px nằm trên → sau main y=234.6 / aside y=4126.3); **`@media (max-width: 900px)`: lưới sản phẩm `gap: 10px`** (trước 22px, card 141→159px; desktop giữ 22px) + **`.cp-shop-main { padding: 10px }`** (trước 22px — yêu cầu Tùng 2026-09-15; card 390px: 147→159px, 768px: 336→348px, 900px: 402→414px)**, `.cp-shop-main ul.products` → lưới **4 cột** (responsive 1200→3, 900→2), `li.product` → card, `.cp-shop-main .woocommerce-products-header{display:none}`, `.cp-shop-desc*` (thu gọn `max-height:4.8em` + fade, `.is-open` mở), reskin `.woocommerce-pagination` (căn giữa, nút 42px bo tròn, `.prev/.next` → ‹ ›, prefix `.woocommerce` để thắng CSS plugin, **`flex-wrap: wrap` từ 2026-09-15** — 19 trang → dãy 458px gây tràn ngang ở 390px, `scrollWidth` 501 → 390), `.cp-shop-toolbar` (flex, `.cp-price-filter` `margin-left:auto`), `.cp a.cp-price-filter__btn` (chip bo tròn, `.is-active` nền primary), `.woocommerce-ordering select` (42px, mũi tên SVG data-URI), `.cp-shop-main` (thẻ trắng + `var(--cp-shadow)` như `.cp-side-box`). Sidebar dùng lại `.cp-side-box`/`.cp-side-cats` (khối CP3.2).

**`.cp-pagehero` — dùng chung cho 4 trang** (danh mục/cửa hàng qua CP3.1, giỏ hàng CP3.4, thanh toán CP3.5, hoàn tất đơn CP3.6 — chỉ in ở `inc/woocommerce.php` dòng 153 và trong `cp_woo_page_wrap()`), theme cha KHÔNG dùng class này. Spec hiện tại (đổi theo yêu cầu Tùng): `padding: 20px 0` — TRƯỚC là 54px (hero cao 184px → nay 121px desktop / 106px mobile); nền **tông sáng** `linear-gradient(180deg, #fff, var(--cp-surface))` + `border-bottom: 1px solid var(--cp-line)` (TRƯỚC là dải gradient vàng đậm `--cp-primary-dark`→`--cp-primary`); chữ `--cp-text`, H1 `clamp(28px,4vw,40px)`, breadcrumb `.cp-breadcrumb` 13.5px `--cp-muted` + hover `--cp-primary-dark` (đồng tông với `.cp-breadcrumb--bar` ở trang chi tiết SP). Vì là 1 class duy nhất nên đổi 1 chỗ là áp cho cả 4 trang.

## CP3.3 — chi tiết "Đặt hàng nhanh"

Popup form ở trang chi tiết SP → tạo **đơn WooCommerce thật** (COD, trạng thái "Đang xử lý"). UI ở child theme, dữ liệu/hành vi ở plugin.

| Phía | File / hàm | Việc |
| :--- | :--- | :--- |
| Child | `inc/woocommerce.php` → `cp_single_buynow_btn()` | Nút `.cp-buynow` **giữ** `href="tel:"` (fallback JS tắt) + `data-cp-quick-order="<id>"` khi plugin bật |
| Child | `inc/woocommerce.php` → `cp_quick_order_popup()` | Markup popup, in ở `wp_footer` **prio 5** — phải trước `wp_print_footer_scripts` (prio 20), nếu để prio 20 thì JS chạy trước khi có popup và không gắn được listener. Khối `.cp-quick-order__product` (thumb → tên/giá → `SL [n]`) nằm **TRONG `<form>`** — để ngoài form là `FormData` không gửi `qty` |
| Child | `assets/quick-order.js` | Uỷ quyền `click`/`submit` ở `document` (không phụ thuộc thứ tự in markup); `fetch` POST tới `admin-ajax.php`; khoá cuộn `body.cp-qo-open`, đóng bằng Esc/overlay/× |
| Child | `assets/caophat.css` | Khối `.cp-quick-order*`: overlay, dialog 460px, input `font-size:16px` (iOS không auto-zoom), `.cp-quick-order__hp` là honeypot ẩn |
| Plugin | `pl-tien-ich-tungleads.php` → `tlpi_quick_order_handle()` | Action `cp_quick_order` (`wp_ajax_` + `wp_ajax_nopriv_`); nonce `tlpi_quick_order` + honeypot `cp_hp` + rate-limit 5 đơn/IP/10 phút (transient `tlpi_qo_<md5 ip>`) |

**Luồng tạo đơn:** `wc_create_order(created_via='cp-quick-order')` → `add_product()` → `set_address()` cho billing + shipping (`first_name`/`phone`/`address_1`) → `set_payment_method()` COD nếu gateway đang bật (không thì chỉ ghi nhãn) → `set_customer_note()` → `calculate_totals()` → `update_status('processing')` (tự trừ tồn kho + gửi email "Đơn hàng mới" cho admin).

| Nhánh chặn | Thông báo / mã |
| :--- | :--- |
| Nonce sai / hết hạn | "Phiên làm việc đã hết hạn…" (403) |
| Honeypot `cp_hp` có dữ liệu | "Không gửi được yêu cầu." |
| Quá 5 đơn/IP/10 phút | "Bạn vừa gửi quá nhiều yêu cầu…" (429) |
| Tên < 2 ký tự · SĐT không khớp `^0\d{9,10}$` · địa chỉ < 8 ký tự | báo đúng trường sai |
| SP không publish/purchasable, hoặc không đủ tồn kho | báo tương ứng, **không** tạo đơn |
| Đặt thành công | Trả về `message` + `redirect` = `$order->get_checkout_order_received_url()` (đã kèm `?key=`); JS hiện thông báo ~0,8s rồi `window.location.assign()` sang trang hoàn tất đơn (CP3.6). Trong lúc chờ chuyển trang, nút gửi **vẫn bị khoá** (biến `redirecting`) để khách bấm nhanh 2 lần không tạo 2 đơn |

## CP3.4 — chi tiết "Trang giỏ hàng"

`page.php` của theme cha không có container và không in tiêu đề → trang `/cart/` trước đây dính sát mép và không có H1. Xử lý bằng **filter, không copy template**.

| Việc | Cách làm |
| :--- | :--- |
| Hero + container | Filter `the_content` prio 5 (guard: `cp_is_cart_page()` + `in_the_loop()` + `is_main_query()`): trả về `.cp-pagehero` (breadcrumb + H1 "Giỏ hàng") + `.cp-section > .cp-container.cp-cart` bọc nội dung shortcode |
| Nhãn "Shipment" | WooCommerce sinh bằng `_x( 'Shipment', 'shipping packages', 'woocommerce' )` → phải lọc **`gettext_with_context`** (lọc `gettext` không bắt được), khớp cả context |
| Lưới 2 cột | `.cp-cart .woocommerce { grid-template-columns: minmax(0,1fr) 360px }` — đặt trên `div.woocommerce` (shortcode bọc thêm wrapper này) |

**4 cái bẫy CSS đã gặp & cách xử lý** (đừng revert nếu chưa đọc):

1. `div.woocommerce-notices-wrapper` (rỗng) vẫn chiếm ô lưới → form bị đẩy sang cột 2. Dùng `grid-column` tường minh cho từng con + ẩn wrapper khi `:empty`.
2. **KHÔNG** đặt `display: flex` lên `table.cart td.actions`: làm vậy mất `colspan="6"` → ô co về 204px thay vì 783px, nút Cập nhật nhảy sang trái. Giữ `table-cell`, cho `.coupon` float trái + `text-align: right` cho ô.
3. WooCommerce có selector rất mạnh cho cart (`#add_payment_method table.cart img, .woocommerce-cart table.cart img { width: 32px }`, `.woocommerce-cart table.cart td.actions .coupon .input-text`…) → ảnh thumbnail phải ghi bằng `.cp-cart table.cart .product-thumbnail img` (thắng 32px), coupon/button phải thêm tiền tố `.woocommerce-cart .cp-cart`.
4. `woocommerce-smallscreen.css` ép `.woocommerce table.shop_table_responsive tr td { display:block; text-align: right !important }` và có zebra-stripe nền xám → 2 ô cần canh trái (thumbnail, tên SP) buộc phải `!important`, và phải ghi đè `background-color: transparent` bằng selector đủ mạnh.

## CP3.5 — chi tiết "Trang thanh toán"

Cùng cách tiếp cận CP3.4: `cp_is_checkout_page()` (guard **loại trừ** `is_order_received_page()` — trang "đơn đã nhận" dùng chung `is_checkout() === true`, nếu không loại trừ sẽ bị chèn hero "Thanh toán" lên trang cảm ơn) → `cp_woo_page_wrap( 'Thanh toán', 'cp-checkout', $content )`. Một filter `the_content` duy nhất phục vụ cả CP3.4 + CP3.5.

| Việc | Cách làm |
| :--- | :--- |
| Lưới 2 cột | `.cp-checkout form.checkout { display:grid; grid-template-columns: minmax(0,1fr) 400px }` + chỉ định `grid-row` cho `#customer_details` và `#order_review` (đều `1 / span 2`) — hai card bắt đầu cùng hàng 1 nên mép trên ngang nhau (`align-items: start` đã có sẵn trên form) |
| Tiêu đề "Đơn hàng của bạn" | WooCommerce 11 in `<h3 id="order_review_heading">` **RA NGOÀI** `#order_review` (`templates/checkout/form-checkout.php` dòng 54, để phục vụ layout 2 cột) → tiêu đề bị tách hẳn khỏi thẻ trắng (đo được: cách thẻ ~250px). Xử lý: CSS `.cp-checkout #order_review_heading { display: none }` + PHP `cp_order_review_title()` gắn vào hook `woocommerce_checkout_order_review` prio 5 → in bản thật **bên trong** `#order_review`, trước bảng đơn hàng (prio 10). Chuỗi dùng lại `__( 'Your order', 'woocommerce' )` để ăn theo bản dịch tiếng Việt |
| Form | Bọc field bằng grid 2 cột: `.woocommerce-billing-fields__field-wrapper` + `.form-row-wide { grid-column: 1 / -1 }`; input 44px+ (thực tế 48px), select2 của WooCommerce phải style riêng (`select2-selection--single` cao 44px) |
| Khối đơn hàng | `#order_review` card trắng; `tr.order-total .amount` accent 22px |
| Nút đặt hàng | Phải dùng `.cp-checkout #payment #place_order` (2 ID) mới thắng được `.woocommerce #payment #place_order` của WooCommerce (nền tím `#a46497` mặc định) |
| Form rút gọn | `cp_checkout_trim_billing_fields()` (`woocommerce_checkout_fields` prio 20) — bỏ `billing_company`/`billing_country`/`billing_address_2`/`billing_postcode`/`billing_last_name`; gộp họ+tên vào `billing_first_name` (nhãn "Họ và tên", full width); email `required = false`. Còn 5 field + ghi chú, xếp 4 hàng: Họ và tên → SĐT ½ \| Email ½ → Địa chỉ → Thành phố |
| Quốc gia khi đã gỡ field | `cp_checkout_force_base_country()` (`woocommerce_checkout_posted_data`) — chèn lại `billing_country` = quốc gia cơ sở của shop + **điền cả `shipping_country` khi trống** (xem bẫy bên dưới) |

**Bẫy riêng của checkout:** `.col2-set .col-1` (thanh toán) và `.col-2` (giao hàng) bị **đảo cột** khi để auto-placement — DOM order đúng, `order: 0`, không float, `direction: ltr`, nhưng `col-2` lại nằm cột trái. Khắc phục bằng gán cột tường minh `grid-column`, và **reset về `grid-column: 1` trong media query mobile** (nếu không, cột 2 sẽ tạo cột ảo thứ hai ở lưới 1 cột).

**Bố cục hiện tại (theo yêu cầu Tùng):** `.col2-set` để **1 cột** — "Thông tin thanh toán" (`.col-1`) ở trên, "Giao hàng đến một địa chỉ khác?" + "Ghi chú đơn hàng" (`.col-2`) **xuống dưới**, cả hai `grid-column: 1`. Vẫn PHẢI gán `grid-column` tường minh cho cả hai (bẫy đảo cột vẫn còn nếu đổi về 2 cột). Ô ghi chú `#order_comments` cho chiếm hết chiều ngang bằng rule riêng cho `.woocommerce-additional-fields__field-wrapper { grid-template-columns: 1fr }` — field này của WooCommerce chỉ có class `form-row notes` (KHÔNG có `form-row-wide`) nên nếu dùng chung lưới 2 cột thì nó bị nửa card.

**Bẫy AJAX của tiêu đề đơn hàng (đã kiểm chứng):** AJAX `update_order_review` của WooCommerce chỉ thay fragment `'.woocommerce-checkout-review-order-table'` (`includes/class-wc-ajax.php` — gọi trực tiếp `woocommerce_order_review()`, KHÔNG qua hook `woocommerce_checkout_order_review`) → **không** thay cả `#order_review`, nên tiêu đề in bằng hook không bị mất và cũng không bị in trùng sau AJAX. Test thật: trigger `update_checkout` → HTTP 200 + event `updated_checkout` fire → vẫn đúng 1 tiêu đề trong thẻ, cách mép trái thẻ 25px (khớp padding 24px).

## CP3.6 — chi tiết "Trang hoàn tất đơn hàng"

`is_order_received_page()` trả `true` và `is_checkout()` cũng `true`, nhưng `cp_is_checkout_page()` **cố ý loại trừ** trang này → trước đây trang không có container/hero nào (chữ sát mép, đo được `article` rộng 1265px bắt đầu từ x=0). Xử lý: guard riêng `cp_is_order_received_page()` (đặt **trước** `cp_is_checkout_page()` trong filter `the_content`) → `cp_woo_page_wrap( 'Đặt hàng thành công', 'cp-thankyou', $content )`.

| Việc | Cách làm |
| :--- | :--- |
| Nút "Về trang chủ" | `cp_thankyou_home_button()` gắn `woocommerce_thankyou` prio 30 (chạy BÊN TRONG nội dung shortcode, sau card chi tiết đơn) + guard `cp_is_order_received_page()`; dùng lại nút có sẵn `.cp-btn.cp-btn-primary` |
| Chuyển trang từ popup | Plugin trả `redirect` trong `wp_send_json_success`; `assets/quick-order.js` chuyển trang (xem mục CP3.3 ở trên) |

**CSS viết theo ĐÚNG DOM của WC 11 (đã dò trước khi viết, không đoán):** khối cảm ơn là `<p class="woocommerce-notice woocommerce-notice--success woocommerce-thankyou-order-received">` (KHÔNG phải `div`) → phải ghi 2 class mới thắng `.woocommerce-notice--success`; tóm tắt là `<ul class="woocommerce-order-overview woocommerce-thankyou-order-details order_details">` gồm 4 `<li>`; rồi `<section class="woocommerce-order-details">` chứa `table.shop_table`. Đơn test **không** in `.woocommerce-customer-details` nhưng vẫn để sẵn rule cho đơn có địa chỉ giao riêng.

**Bẫy clearfix của WooCommerce trên lưới grid (đã dính, đã sửa):** WooCommerce khai `.woocommerce ul.order_details::before/::after { content: " "; display: table }` = độ ưu tiên **(0,2,1)**, mạnh hơn `.cp-thankyou .woocommerce-order-overview::before` = (0,2,0) → `content: none` KHÔNG ăn, 2 pseudo-element trở thành 2 grid item chiếm 2 ô đầu → 4 ô tóm tắt bị đẩy thành **3 ô hàng 1 + 1 ô hàng 2** (đo được `tops = [134.99, 134.99, 134.99, 220.45]`). Sửa bằng selector 3 class `.cp-thankyou .woocommerce-order-overview.order_details::before` (0,3,0). Kiểm chứng: `getComputedStyle(ul,'::before').content` = `none` và 4 ô cùng 1 hàng ở desktop, 2 hàng ở mobile 390px.

**Bẫy độ ưu tiên trên `<li>` tóm tắt (đã dính, đã sửa):** WooCommerce khai `.woocommerce ul.order_details li` = **(0,2,2)** — mạnh hơn `.cp-thankyou .woocommerce-order-overview li` = (0,2,1) — nên nó ép `padding-left: 0`, `padding-right: 2em`, `font-size: .715em`, `margin-right: 2em`, `float: left`, `border-right: 1px dashed #cfc8d8`. Hệ quả đo được: chữ cách mép ô **1px** (Tùng báo "quá sát lề trái"), ô hụt 22.88px (4 ô không đều), có viền nét đứt bên phải. Sửa: selector `ul.woocommerce-order-overview.order_details li` (0,3,1) + **reset tường minh** `float: none; margin: 0; padding: 14px 16px; border: 1px solid …; font-size: 12px` — và **phải sửa cả selector trong media query mobile**, nếu không rule desktop (0,3,1) sẽ đè padding mobile (0,2,1). Cùng họ với bẫy clearfix ở trên: cứ đè CSS WooCommerce thì phải đếm trước độ ưu tiên của selector họ dùng.

**Cột tiền của bảng chi tiết:** rule `thead th` của ta đặt `text-align: left` nên chữ **"TỔNG"** nằm bên trái trong khi số tiền căn phải (lệch cột). Thêm `.cp-thankyou table.shop_table thead th:last-child { text-align: right }`.

**Bẫy GỠ field `billing_country` (nặng — đã dính và đã sửa):** `WC_Checkout::get_posted_data()` (`includes/class-wc-checkout.php`, đoạn `if ( in_array( 'shipping', $skipped ) )`) tự copy **billing → shipping** cho mọi field giao hàng bằng `$data[ 'billing_' . … ]`. Bản copy này chạy **TRƯỚC** filter `woocommerce_checkout_posted_data`, nên khi `billing_country` bị gỡ khỏi form thì key đó rỗng → `$data['shipping_country'] = ''` → `WC_Checkout::validate_checkout()` (`class-wc-checkout.php:1020`) chặn đơn với lỗi **"Xin hãy nhập một địa chỉ để tiếp tục."** (chuỗi gốc `Please enter an address to continue.`). Lưu ý `$data['ship_to_different_address']` LUÔN tồn tại (giá trị `false` khi không tick) nên **không** dùng `isset()` để đoán khách có tick hay không. Cách sửa đúng: trong filter điền `billing_country` và **điền luôn `shipping_country` nếu nó đang trống** (khách chọn quốc gia giao khác thì giá trị của khách được giữ nguyên). Cùng cơ chế copy này cũng làm `shipping_company`/`shipping_address_2`/`shipping_postcode`/`shipping_last_name` thành rỗng — vô hại vì VN không bắt buộc các field đó và option `woocommerce_shipping_cost_requires_address` = `no`.

**Bẫy độ ưu tiên CSS (đã dính, đã sửa):** (a) `.cp-checkout .form-row` (0,2,0) **BỊ** `.woocommerce form .form-row-first` (0,2,2) đè → ô nửa hàng co còn 47% (đo được **170px** thay vì 362px); phải ghi rõ `.cp-checkout .form-row.form-row-first` và `-last` (0,3,0). (b) `.cp-checkout .form-row textarea` (0,2,1) **BỊ** `.woocommerce form .form-row textarea` (0,2,2) đè → bo góc còn 4px (`--wc-form-border-radius`) thay vì 10px; phải khai `border-radius` trong `.cp-checkout .form-row.notes textarea` (0,3,1).

---

## Ngoài theme — plugin `button-call-zalo-tungleads` (“Button call/zalo - TungLeAds”)

Widget liên hệ nổi (Gọi điện + Zalo) neo sát **lề phải**, in ở `wp_footer` **prio 5**; số nhập ở **Settings → Button Call/Zalo**. Đây là **repo git riêng** (`wordpress/wp-content/plugins/button-call-zalo-tungleads/`), **KHÔNG** thuộc child theme — **không** đọc/ghi gì của theme. (v1.0.0 từng có tuỳ chọn “Ẩn nút gọi nổi của theme” in `.cp-fab{display:none!important}`; **bỏ ở v1.0.1** vì theme xoá hẳn `.cp-fab` cùng ngày — CP1.3.)

| Phần | Việc |
|---|---|
| `button-call-zalo-tungleads.php` | Option `tlcz_settings` (`enabled` · `custom_css` · `custom_js` · `buttons[]`; mỗi nút: `type`/`label`/`value`/`color`/`text_color`/`icon_id`/`enabled`), `tlcz_render()` in widget ở `wp_footer` prio 5, trang Settings (repeater tối đa 8 nút + 2 ô Custom CSS/JS + 2 dòng ghi công), `tlcz_sanitize()` + `tlcz_sanitize_code()` (bỏ dòng chưa nhập giá trị · cắt 8 · `array_values` · hex color qua `sanitize_hex_color()` · icon qua `absint()` · lọc `</style`/`</script` · xoá hết = về 4 số mặc định) |
| `assets/contact-widget.css` | CSS **nguyên bản** thiết kế “Contact Floating Widget v1.3” (class `.wd-contact-*`): nút 58px, hover mở 210px, chỉ nút đang hover đổi, mobile giữ đúng layout desktop, pulse ở nút đầu nếu là loại Gọi. **v1.1.0 thêm (chỉ THÊM, không sửa số đo gốc):** màu mặc định cho `.wd-contact-facebook` (xanh FB) + `.wd-contact-custom` (xám) + ô tròn chữ `f`; `.wd-has-bg`/`.wd-has-fg` đọc CSS variable `--wd-bg`/`--wd-fg` (màu riêng từng nút, kể cả viền ô chữ Z/f); `.wd-contact-icon img` (ảnh icon tải lên lấp ô 44×44, `object-fit: contain`) |
| `assets/contact-widget.js` | Uỷ nhiệm click từ `document` → bắn `wd-contact:click` (`detail.type` = `phone`/`zalo`, `detail.phone` = số đã bỏ ký tự không phải số) để site tự gắn GTM/GA4 |
| `assets/admin.js` | Thêm/xoá dòng nút · color picker · chọn ảnh icon qua Media · placeholder theo kiểu (chỉ nạp ở trang cài đặt) |

**Bẫy đã dính (đã sửa):** widget in ở `wp_footer` **prio 20** = cùng prio với `wp_print_footer_scripts`, mà core đăng ký callback trước plugin ⇒ **script in TRƯỚC markup** ⇒ JS chạy khi `.wd-contact-widget` chưa có trong DOM ⇒ `querySelector` trả `null` ⇒ **sự kiện click không bao giờ bắn** (chỉ đo bằng Playwright mới thấy; đọc code sẽ tưởng đúng). Sửa: in ở **prio 5** + JS uỷ nhiệm từ `document` (không phụ thuộc thứ tự).

**Số đo Playwright (2026-09-16):** 4 loại trang (trang chủ · chi tiết SP · bài viết · trang tĩnh) — 4 nút, hộp `1230..1440 x 322..578` ⚠️ **2026-09-16 (sau yêu cầu Tùng): `.wd-contact-widget { top: 50% → 75% }` (plugin v1.1.3) ⇒ hộp DỜI XUỐNG `1230..1440 x 547..803` ở 1440×900** (tâm cụm ở 75% chiều cao khung nhìn; 6 khổ đo đều vừa màn hình, trừ khung nhìn thấp <~512px bị cắt đáy ~3px), `z-index 999999`, `overflow 0`; hover nút 1 → rộng `58 → 210px` còn **3 nút kia vẫn 58px** và mép phải không đổi, nhãn `opacity 0 → 1`; click-through: điểm trong hộp nhưng ngoài nút → trúng nội dung trang (container `pointer-events: none`); **không chồng `.cp-fab`** ở 6 kích thước (1440×900 · 1440×600 · 1280×500 · 768×1024 · 390×844 · 390×600; đo lúc theme CÒN FAB — FAB đã xoá 2026-09-16 nên phép đo này không còn cần nữa); **Admin E2E**: “+ Thêm nút” → lưu → frontend 5 nút, `+84 (909) 123-456` → `https://zalo.me/84909123456`, bỏ tick “Hiện widget” → **0 nút + 0 asset**, xoá dòng → về đúng 4 nút mặc định. Repo plugin: commit baseline `cc1e770`.

**v1.1.0 (2026-09-16, yêu cầu Tùng) — kiểu mới + màu + icon + custom CSS/JS + ghi công:** thêm 2 kiểu **Facebook** (`https://m.me/<username>` khi nhập username/ID trang, giữ nguyên nếu dán link `http…`) và **Link tuỳ chỉnh** (giữ scheme `https:`/`mailto:`/`tel:`/`sms:`, thiếu scheme ⇒ tự thêm `https://`); **màu nút + màu chữ riêng cho TỪNG NÚT** (2 ô color picker Iris, để trống = màu mặc định theo kiểu) truyền qua CSS variable `--wd-bg`/`--wd-fg` + class `wd-has-bg`/`wd-has-fg`; **upload ảnh icon** qua thư viện Media (`icon_id`, `wp_get_attachment_image()`) thay icon mặc định; **2 ô Custom CSS / Custom JS** (in ở `wp_head` prio 99 và `wp_footer` prio 6, **chỉ khi widget hiển thị**; `tlcz_sanitize_code()` lọc `</style`/`</script`); **2 dòng ghi công** ở trang Settings — `Phiên bản 1.1.0 | Bởi Tung Le Ads` + `Plugin được phát triển bởi: Tùng Lê Ads | www.tungleads.com` (cả 2 đều link `https://tungleads.com/`, version lấy **ĐỘNG** từ header plugin). **Dòng ghi công áp dụng cho cả plugin `pl-tien-ich-tungleads`** (lên v0.1.1). **E2E (Playwright + user admin tạm, đã xoá):** UI mới đủ (4 kiểu · 8 color picker cho 4 dòng · cột “Màu sắc”/“Ảnh icon” · 2 textarea custom · 2 dòng ghi công có link); chọn ảnh qua modal Media (**phải bấm tab “Tất cả tập tin”** thì `.attachment` mới render) → icon `11661` + ảnh xem trước + nút “Xoá ảnh” hiện; lưu → **frontend**: dòng 1 có `wd-has-bg wd-has-fg` + `style="--wd-bg: #0b7a3b; --wd-fg: #ffd400"` → đo `background rgb(11,122,59)`, `color rgb(255,212,0)`, **icon là ảnh 44×44** (thay SVG); dòng Facebook → `https://m.me/caophatdoor` + ô tròn chữ `f`; dòng Link tuỳ chỉnh → `mailto:caophatdoor@gmail.com` (giữ scheme) + icon SVG quả cầu; **custom CSS có hiệu lực** (widget `top: 462px` = 42% viewport 1100) và **custom JS đã chạy** (`window.tlczTest`); khôi phục dữ liệu → về đúng 4 nút mặc định, không còn thẻ custom. `overflow = 0`, 0 PHP notice.
**v1.1.1 / v0.1.2 (2026-09-16) — ghi công ở `/wp-admin/plugins.php`:** thêm header **`Author URI: https://tungleads.com/`** cho **cả 2 plugin của theme**; WP core (`wp-admin/includes/class-wp-plugins-list-table.php` ~dòng 1254–1265) in `Phiên bản <ver>` rồi `Bởi <author>` và **tự bọc `<a href="AuthorURI">` cho tên tác giả khi có Author URI** ⇒ dòng meta thành `Phiên bản 1.1.1 | Bởi Tung Le Ads` (**có link tungleads.com**), không cần filter `plugin_row_meta` và không gây trùng dòng. Version trên dòng đó lấy từ header ⇒ **tự đúng khi bump**. Đo bằng Playwright (admin tạm, đã xoá): `Button call/zalo - TungLeAds` → `Phiên bản 1.1.1 | Bởi Tung Le Ads` + link `https://tungleads.com/ :: Tung Le Ads`; `TL Site — Cao Phát` → `Phiên bản 0.1.2 | Bởi Tung Le Ads` + link tương tự; plugin khác (WooCommerce, Rank Math…) giữ nguyên author của họ.

**v1.1.2 (2026-09-16, Tùng chốt) — ĐỔI ICON NÚT GỌI:** Tùng báo *“icon nút gọi ngoài front end cho plugin — hiện tại icon như bị lỗi”*; **đo 4 loại trang × 2 khổ (1440 · 390)** thấy icon vẫn đúng (`svg 22×22`, `.wd-contact-icon` `fill:none` · `stroke:rgb(255,255,255)` · `color:#fff` do nút mặc định có `--wd-fg:#ffffff`, nằm gọn trong ô 44px, `scrollWidth==innerWidth`, 0 PHP notice) ⇒ **KHÔNG tái hiện được lỗi**; **phân tích pixel ảnh chụp** (raster hoá trong canvas của browser): ô nút GỌI nền `rgb(233,90,93)` + **249 pixel trắng** (hình ống nghe) ⇒ không phải mảng lỗi/ô trống; **quét ảnh vỡ toàn trang: 96 ảnh** `naturalWidth=0` nhưng **đều là thumbnail sản phẩm** (`cp-section`, không nằm trong widget) ⇒ ca `wp media regenerate` đã biết, KHÔNG phải icon nút. ⇒ Dựng **trang xem thử 6 phương án** (`python3 -m http.server 8899` phục vụ `/tmp/cp-pw/icon-preview/index.html`; script raster `/tmp/cp-pw/icon-preview-verify.mjs` kiểm cả 6 phương án không bị rỗng/méo) để Tùng chọn ⇒ **Tùng chốt phương án C = “điện thoại + 2 gợn sóng”** (Feather `phone-call`). Sửa `tlcz_icon_default()` nhánh `phone`: **3 `path`** (2 cung sóng `M15.05 5A5 5 0 0 1 19 8.95` + `M15.05 1A9 9 0 0 1 23 8.94`, rồi ống nghe như cũ), **giữ nguyên `svg viewBox="0 0 24 24"` + CSS cũ** (icon 22px, nét 2px, `currentColor`) ⇒ **không đụng CSS/JS/kích thước**; plugin lên **v1.1.2**. **Đo sau khi sửa (4 URL × 1440/390, Playwright):** `soPath=3` · `svgBox 22×22` · `bboxPath 2.1,1.0 20.9×20.9` · `namTrongTile=true` (cách lề 11px mỗi bên) · `stroke #fff` / `fill none` · tràn ngang **0** · **pixel vùng icon 249 → 356** (+107 = 2 cung sóng) ⇒ icon mới **thật sự hiện trên màn hình**; nút Zalo giữ nguyên **761**. ⚠️ Nếu nút đó được gắn **ảnh tải lên** (`icon_id`) thì ảnh vẫn **thay thế** icon này (hành vi không đổi).

## CP1.8 — Header & Topbar quản trị trong Customizer (2026-09-16)

**Customizer → “Header & Topbar Cao Phát”** (section `cp_header`, priority **25**, **11 control**): topbar bật/tắt · màu nền · màu chữ · chữ bên trái (textarea, 1 dòng = 1 mục) · nhãn trước hotline · header màu nền · **chiều cao** (range 56–140, mặc định 76) · **cỡ logo** (range 80–400, mặc định 250) · **6 mục header: bật/tắt + kéo thả thứ tự** · **khối HTML** (`wp_kses_post`) · “khối HTML chỉ desktop”.

| Việc | File |
| :--- | :--- |
| Mặc định + getter + CSS động | `functions.php` — `cp_header_defaults()`, `cp_header_get()`, `cp_header_flag()`, `cp_header_items_all()`, `cp_header_items()`, `cp_header_item_on()`, `cp_topbar_lines()`, `cp_header_html()`, `cp_header_style()` (`wp_head` prio 100 → `<style id="cp-header-css">`) |
| Markup hàng header + topbar soạn được | `header.php` — topbar lặp `cp_topbar_lines()`, mỗi mục có class `cp-hitem cp-hitem--<key>`, mục bị tắt ⇒ class `cp-hide-<key>` ở `<header>` |
| UI Customizer | `inc/customizer.php` — class `CP_Header_Items_Control` (type `cp_header_items`) + 2 callback `customize_register` của section `cp_header` + enqueue `customizer-header.*` |
| JS/CSS admin | `assets/customizer-header.js` (jQuery UI sortable + sync CSV), `assets/customizer-header.css` |
| CSS tĩnh front end | `assets/caophat.css` (khối CP1.8 cuối file: rule ẩn mục bị tắt + `.cp-hitem--html*`) |
| Icon giỏ hàng | `inc/woocommerce.php` — `cp_header_cart_link()` thêm `cp-hitem cp-hitem--cart` (markup dùng cả ở fragment AJAX) |

**Quy tắc breakpoint (Tùng chốt 2026-09-16):** thứ tự **>1024px** · chiều cao + cỡ logo **>768px** · màu sắc + bật/tắt **mọi khổ** · khối HTML mặc định **chỉ >1024px**. **Ẩn mục:** `header.php` gắn `cp-hide-<key>`, `caophat.css` ẩn (`.cp-hide-search` ẩn cả `.cp-search-toggle`, có `!important` để thắng rule mở dải search ở ≤768px).

**Số đo (Playwright):** mặc định không đổi so với trước (1440: logo `250×46`, menu x435, search x958, hotline x1202, giỏ x1385; 390: logo `115,17 160×29`); khi áp cấu hình thử: container `110px`, logo `180×33`, topbar `rgb(18,52,86)` + chữ trắng, header `rgb(17,17,17)`, khối HTML **cùng hàng** (lệch tâm so với logo **0px**), hotline ẩn, thứ tự `logo→html→menu→search→cart`, **overflow 0** ở 1440/1100/1024/768/390. Chi tiết + 4 bẫy ở dòng `CP1.8` trong `CLAUDE.md`.

## CP2.10 — 4 cụm nội dung nổi bật trên trang chủ (quản trị trong Customizer, 2026-09-16)

**Customizer → “4 cụm nổi bật Cao Phát”** (section `cp_features`, priority **36**, **12 control** = 4 cụm × (tiêu đề · mô tả · icon)).

| Việc | File |
| :--- | :--- |
| Mặc định + getter + icon | `functions.php` — `cp_features_defaults()` (chữ + **path SVG** của 4 icon gốc; cụm 4 lấy `cp_hotline_display()`), `cp_feature_get()`, `cp_feature_icon()` |
| Markup dải nổi bật | `front-page.php` — vòng `for (1..4)` gọi `cp_feature_icon()` + `cp_feature_get()` (không giữ chuỗi nào) |
| UI Customizer | `inc/customizer.php` — section `cp_features`; mỗi cụm: `cp_feature{n}_title` (text) · `cp_feature{n}_text` (text) · `cp_feature{n}_icon` (**`WP_Customize_Media_Control`** = attachment ID) |
| CSS ảnh icon | `assets/caophat.css` — `.cp-feature .ic img { width/height:100%; object-fit:contain }` |

**Nguồn sự thật:** chữ + icon mặc định chỉ nằm ở `cp_features_defaults()`; ô để trống ⇒ dùng lại mặc định. Cụm 4 để trống mô tả ⇒ tự bám hotline toàn site.

**Số đo (Playwright):** mặc định khớp 100% trước refactor (1440 `20,696 1400×92`; 390 `20,801 350×424`; icon SVG 22×22 trong ô 42×42); sửa chữ + upload icon (id `11661`) ⇒ cụm 1 là `<img>` 42×42, 3 cụm kia giữ SVG; **overflow 0** ở 1440/390 cả 2 trạng thái; 0 lỗi PHP.

**Bẫy đã dính:** xoá `$cp_disp` trong `front-page.php` (tưởng biến chết) nhưng nút CTA `Gọi ngay %s` vẫn dùng ⇒ PHP `Warning: Undefined variable` in vào nhãn nút + `nowrap` ⇒ nút 1065px ⇒ tràn ngang **723px** (đã sửa, biến có ghi chú “KHÔNG xoá”). Chi tiết ở dòng `CP2.10` trong `CLAUDE.md`.

| **CP3.9** đánh giá ở trang chi tiết SP | **Tab “Đánh giá” có hộp tổng kết + lọc sao + form tiếng Việt** (yêu cầu Tùng 2026-09-17, layout demo tham khảo banhocthongminhbsuc.com). | `inc/woocommerce.php` (tag `// CP3.9`), `assets/reviews.js` (**mới**), `assets/caophat.css` (khối CP3.9), `functions.php` (enqueue theo `is_product()`) | **Kiến trúc:** đổi `callback` tab qua `woocommerce_product_tabs` (prio 21, cùng kiểu CP3.8) — KHÔNG copy template của WooCommerce ⇒ `comments_template()` vẫn lo danh sách/phân trang/form/duyệt. Mảnh thêm vào: `cp_single_reviews_summary()` (điểm TB `get_average_rating()` + 5 thanh từ `get_rating_counts()` + nút CTA), `cp_single_reviews_thanks()` (dải xác nhận khi có `?unapproved=`), filter `comment_class` gắn `cp-rv-star-N` cho từng `<li>` (chỉ front-end + post type `product`), filter `woocommerce_product_review_comment_form_args` dựng lại `comment_field` tiếng Việt **giữ nguyên `id`/`name`/`required`** để WooCommerce vẫn validate `$_POST['rating']`, filter `gettext` đổi “verified owner” → “Đã mua tại caophat.vn”, filter `get_comment_date` → `d/m/Y` cho comment của SP. JS `assets/reviews.js`: lọc `<li>` bằng class `.is-hidden` + dòng “Không có đánh giá nào ở mức N sao.”, cuộn mượt + focus ô chọn sao. **Đo (Playwright, 1440/1280/1024/768/390, SP 4816 với 5 đánh giá mẫu 3×5★·1×4★·1×3★):** hộp tổng kết **986×187** (1440) → **314×386 1 cột** (390); thanh 5★ **197/328 = 60%** khớp `width:60%`; 5 thẻ đánh giá 114px (390: 242px); form 940×564 → 268×791; nút gửi **161×48 nền `rgb(0,0,0)`** (trước khi sửa: `rgb(233,230,237)` do WC đè bằng selector 2 id ⇒ phải `!important`); **không tràn ngang cả 5 khổ**. Tương tác: chip 4★ → 1 hiện · 2★ → 0 + thông báo · Tất cả → 5 (aria-pressed đúng); E2E khách vãng lai gửi đánh giá → `comment_approved = 0` (chờ duyệt) + dải xác nhận 986×52; smoke test **24/24**, `php -l`/`node --check` sạch. **Còn lại (chờ Tùng):** ảnh/video trong đánh giá + nút “thích” + “Trả lời” (phải viết ở plugin `pl-tien-ich-tungleads` — upload/AJAX/meta, không phải việc của CSS); **46 SP `comment_status = closed` ĐÃ MỞ KHOÁ 2026-09-17** (363/363 `open`, thêm `default_comment_status=open`) ⇒ mọi SP đều có tab “Đánh giá” — script `docs/prod-open-product-reviews.php`; đặt lại 2 option trên production khi deploy (`woocommerce_enable_reviews=yes`, `comment_registration=0`). | ✅ |
| **CP3.10** đường dẫn đăng nhập | **Ẩn `wp-admin`/`wp-login.php` với khách + đổi trang đăng nhập sang đường dẫn riêng** (plugin `pl-tien-ich-tungleads` **v1.1.0**, option `tlpi_login`; cấu hình Settings → PL Tiện Ích). | `includes/login-path.php` (**mới**, ~400 dòng) — `tlpi_login()/tlpi_login_sanitize()/tlpi_login_active()/tlpi_login_slug()/tlpi_login_url()/tlpi_login_request_path()/tlpi_login_path_allowed()/tlpi_login_is_old_path()/tlpi_login_serve()/tlpi_login_block()/tlpi_login_rewrite_url()/tlpi_login_settings_ui()`; `pl-tien-ich-tungleads.php` (require + `register_setting` + gọi UI + bỏ tracking ở trang login) | **Kiến trúc:** `init` prio 1 (sớm hơn `auth_redirect()`); slug đúng ⇒ `require_once ABSPATH.'wp-login.php'` + `exit` (mọi action core y như trước, dùng cùng 1 file core ⇒ không lệch hành vi); đường dẫn cũ + chưa đăng nhập ⇒ 404 tự chứa (`status_header(404)` + `X-Robots-Tag: noindex`, KHÔNG nạp theme) hoặc `wp_safe_redirect(home_url())`; `tlpi_login_path_allowed()` giữ nguyên AJAX/admin-post/load-styles/scripts/file tĩnh `wp-admin/(css\|js\|images\|fonts)/`/REST/XML-RPC/cron/wp-cli **vì trang đăng nhập tải CSS+JS từ `/wp-admin/`** (chặn nhầm ⇒ trang login trắng bệch); filter `site_url`/`network_site_url`/`wp_redirect` chỉ đổi URL chứa `wp-login.php` (KHÔNG filter `site_url` toàn cục để khỏi phá `admin_url()`/REST); `tlpi_login_active()` = bật + có slug hợp lệ + **permalink khác “Mặc định”**. **Đo (đã bật thật rồi tắt):** URL mới **200** + form + `action` trỏ URL mới + **0 request lỗi** (6 file CSS/JS core tải được); `wp-login.php` · `wp-admin/` · trang Settings (khách) = **404**; `admin-ajax.php` **400** (không bị chặn) · `/wp-json/` **200** · trang chủ/SP **200**; đăng nhập bằng trình duyệt qua URL mới ⇒ `/wp-admin/` OK + admin bar; link **Đăng xuất** ở admin bar = `…/dang-nhap-quan-tri/?action=logout&_wpnonce=…` ⇒ thoát OK (`?loggedout=true`); link “Quên mật khẩu” + **link ở trang bảo trì** (request thật) = URL mới; **tracking ở trang đăng nhập = 0**; smoke **24/24** (cả khi bật lẫn tắt). **Trạng thái local: TẮT, slug đã điền `dang-nhap-quan-tri`.** | ✅ |
| **CP3.11** 2FA TOTP | **Xác minh 2 lớp bằng app xác thực** (plugin **v1.2.0**, option `tlpi_2fa` + user meta `_tlpi_totp_secret`/`_tlpi_totp_codes`/`_tlpi_totp_want`; cấu hình Settings → PL Tiện Ích, quản lý ở trang Hồ sơ). | `includes/two-factor.php` (**mới**, 842 dòng) — `tlpi_2fa*()` (option/quyền), `tlpi_2fa_b32_encode/decode()`, `tlpi_2fa_code_at/now/verify()` (RFC 6238), `tlpi_2fa_encrypt/decrypt()` (sodium), `tlpi_2fa_secret_get/set/reset()`, `tlpi_2fa_codes_new/code_use()`, `tlpi_2fa_device_sig/set/ok()`, `tlpi_2fa_session_start/get()`, `tlpi_2fa_gate()` (`authenticate` 99), `tlpi_2fa_redirect_step2()` (`wp_login_failed`), `tlpi_2fa_step2()` + `tlpi_2fa_render()` (hook `login_form_tlpi_totp`), `tlpi_2fa_profile_box/handle()`; `pl-tien-ich-tungleads.php` (require + `register_setting` + UI). | **Kiến trúc:** giữ đúng luồng core — chặn ở `authenticate` ⇒ `WP_Error('tlpi_2fa_required')` ⇒ `wp_login_failed` (chạy TRƯỚC khi wp-login in ra) chuyển sang `?action=tlpi_totp&token=…`; màn nhập mã do hook `login_form_tlpi_totp` tự lo bằng `login_header()/login_footer()`; phiên chờ = transient 10 phút (uid · remember · redirect · enroll · secret · tries); xong ⇒ `wp_set_auth_cookie()` + `do_action('wp_login')` + `wp_validate_redirect()`. Ghi danh tự động khi `tlpi_2fa_required()` mà chưa có secret ⇒ sinh Base32 + QR-free (`otpauth://` link) + **10 mã dự phòng in 1 lần** (hash HMAC buộc user, xoá khi dùng). **Lỗ hổng đã vá:** POST `tlpi_done` phải kèm cờ `codes_ready` trong transient, nếu không thì bỏ qua được bước nhập mã. **Đo:** TOTP khớp **2 vector RFC 6238** (`287082`, `081804`) với bản python độc lập; curl: 302 → màn mã · không lộ lại secret · “Còn 4 lần thử” · mã dự phòng dùng lại bị huỷ · màn mã ⇒ `wp-admin` 200 + cookie `tlpi_2fa_dev`; Playwright: 2 bước ⇒ `/wp-admin/` + admin bar · Hồ sơ “Đã bật”/“Chưa bật” · **Đặt lại 2FA** ⇒ ghi danh lại ra **secret MỚI**; 0 lỗi PHP; smoke **24/24**. **Trạng thái local: TẮT** (roles=[administrator], remember=30). | ✅ |
| **CP3.12** trình soạn thảo cho mô tả danh mục SP | **Ô “Miêu tả” ở màn thêm/sửa DANH MỤC SẢN PHẨM thành trình soạn thảo đầy đủ như trang thêm bài viết** (Tùng 2026-09-18). | `inc/admin-editor.php` (**mới**), `assets/admin-term-editor.js` (**mới**), `functions.php` (require), `assets/caophat.css` (khối CP3.12 ở cuối), `docs/measure/term-editor-check.mjs` | **Kiến trúc:** in THÊM field `wp_editor()` (id **`cp-description`**, `textarea_name=description`) qua 2 hook `{tax}_add_form_fields` + `{tax}_edit_form_fields` — cả 2 hook chạy **SAU** field core ⇒ khi trùng `name="description"` giá trị của mình thắng; field core ẩn `.term-description-wrap{display:none}` + `disabled` (JS) ⇒ chỉ 1 field gửi đi; JS `assets/admin-term-editor.js` gọi `tinymce.triggerSave()` ở **pha capture trên `document`** cho `click` + `submit` vì màn THÊM dùng `wp-admin/js/tags.js` (bắt click `#submit` rồi `serialize()`, KHÔNG submit form); đổi `pre_term_description` từ `wp_filter_kses` → `wp_kses_post` để giữ `<p>/<ul>/<h1..h6>`; CSS cuối `caophat.css` mở lại `list-style` trong `.cp-shop-desc__body` (reset `.cp ul` dòng 49 xoá dấu đầu dòng). **Đo:** cấu hình toolbar editor **giống hệt** editor bài viết (khác duy nhất `dfw`); AJAX add-tag payload + DB lưu `<p>` ✓; form update lưu `<ul><li>` ✓; đồng bộ submit ⇒ textarea có HTML ✓; front-end render HTML, `list-style: disc`, không tràn ngang 1440/390 ✓; smoke 24/24. | ✅ |




## CP8 — Mục lục nội dung (plugin `pl-tien-ich-tungleads` **v0.4.0 → v0.4.1**, Tùng yêu cầu 2026-09-17)

**Việc:** **2 cách hiển thị, bật/tắt ĐỘC LẬP** — (1) nút **DỌC** cố định ở mép màn hình (“Mục lục nội dung”) mở **drawer 340px** liệt kê heading; (2) **khối mục lục NGAY TRONG NỘI DUNG BÀI** (`<details>`, đặt đầu bài hoặc sau đoạn mở đầu, mở sẵn hoặc thu gọn). Cả 2 đều **đánh số `1 · 2 · 2.1`**, bấm 1 mục → cuộn tới heading (bù header sticky), cuộn trang → mục trong drawer được **tô nền**. Layout tham khảo demo `tungleads.com/khoa-hoc/khoa-hoc-quang-cao-google-ads-thuc-chien`. **Toàn bộ ở PLUGIN — child theme KHÔNG đổi file nào** (CSS/JS đi kèm plugin nhưng lấy màu từ token theme ⇒ vẫn khớp skin).

| Việc | File |
| :--- | :--- |
| Cấu hình + sanitize + UI Settings | `plugins/pl-tien-ich-tungleads/includes/toc.php` (**mới**) — option `tlpi_toc` (**15 khoá**: `on` · `types` · `min` · `depth` · `number` · `label` · `side` · `color` · `spy` · `mobile` · **`float`** · **`inline`** · **`inline_pos`** · **`inline_open`** · `exclude`), `tlpi_toc_defaults()` / `tlpi_toc()` / `tlpi_toc_active()` / `tlpi_toc_post_types()` / `tlpi_toc_sanitize()` / `tlpi_toc_excluded_ids()`, `tlpi_toc_settings_ui()` (gọi ở cuối `tlpi_support_page()`) |
| Quét heading + 2 chỗ in | cùng file — `tlpi_toc_should_run()` (singular + post type + không nằm trong `exclude`) · `tlpi_toc_scan()` (regex `<h2-4>` → **thêm `id`** cho heading thiếu + số thứ tự) · `tlpi_toc_id()` (`sanitize_title()` bỏ dấu tiếng Việt, trùng → `-2`) · filter `the_content` **prio 12** · `tlpi_toc_inline_html()` + `tlpi_toc_inline_insert()` (khối trong nội dung) · in nút/drawer ở `wp_footer` **prio 5** (chỉ khi `float` bật) |
| Skin + hành vi | `plugins/pl-tien-ich-tungleads/assets/toc.css` + `assets/toc.js` (**mới**) — nạp qua `wp_enqueue_scripts` prio 30 **chỉ khi post type đã bật + còn ít nhất 1 cách hiển thị**; `html { scroll-padding-top: var(--tlpi-scroll-pad) }` cho deep link; nút `z-index 45` (dưới header sticky 50), drawer `90`; `body.tlpi-toc-open` ẩn widget nổi `button-call-zalo-tungleads`. JS 2 phần độc lập: khối trong nội dung (cuộn cho link) + drawer (mở/đóng + scroll-spy), dùng chung `offsetTop()`/`goTo()`/`bindAnchors()` |
| Nạp mô-đun + hằng số | `plugins/pl-tien-ich-tungleads/pl-tien-ich-tungleads.php` — `TLPI_FILE/DIR/URL/VERSION`, `require_once includes/toc.php`, version **0.4.1** |
| Script đo | `docs/measure/toc-probe3.mjs` (chốt spec demo) · `toc-e2e.mjs` (drawer 1440+390) · `toc-check.mjs` (các ca) · `toc-scrolldbg.mjs` + `toc-inline-dbg.mjs` (chẩn đoán cuộn hụt) · `toc-admin.mjs` (E2E Settings) · `toc-inline.mjs` (khối trong nội dung) · `toc-vars.mjs` (7 biến thể trong 1 lần) · `toc-left.mjs` |

**Số đo (Playwright 2026-09-17, bài `/bao-gia-cua-nhua-gia-re-tphcm/` 87KB · trang `/chinh-sach/chinh-sach-doi-tra-hang/`):** nút **35×160** mép phải, nền `rgb(200,71,31)` (= `--cp-accent`), chữ `writing-mode: vertical-rl` · drawer **340×900** `z-index 90` (1440) / **320px** (390), `inert` khi đóng · **14 mục** (bài) / **5 mục** (trang) — 100% heading trong nội dung được gắn `id` · đánh số `1 · 2 · 2.1 · 2.2` (H2 `w700` màu chữ đậm, H3 thụt 22px `w500`) · **bấm mục → heading dừng đúng 14px dưới header sticky** ở cả 1440 (91−77) và 390 (79−65) · spy: `is-active` + `aria-current` + nền `color(srgb .974 .913 .895)` (= 12% `--cp-accent` qua `color-mix`) · SP **không bật** post type ⇒ **không nạp cả CSS/JS** · `min=10` ⇒ ẩn (và **không thêm `id` nào**) · `exclude=5637` ⇒ ẩn riêng bài đó · `mobile=off` ⇒ nút `display: none` ở 390 · **tràn ngang 0** ở 1440/390 · E2E Settings: lưu `side=left` + tắt đánh số + màu `#0068ff` ⇒ front-end đổi đúng (nút sang mép trái, 0 số, `rgb(0,104,255)`).

**2 BẪY đã dính (đừng lặp lại):** ① **in markup ở `wp_footer` phải prio 5**, không phải 20 — `wp_print_footer_scripts` của core gắn cùng prio 20 nên markup ra SAU thẻ `<script>` ⇒ JS chạy lúc chưa có nút, bấm không mở (`aria-expanded` đứng `false`); ② **cuộn tới heading phải “dò lại bám đích”** — ảnh `loading="lazy"` tải xong làm trang CAO THÊM (đo: cuộn đúng 5493px nhưng `scrollHeight` 43469 → 46010 ⇒ hụt **1565px**); JS bám đích tối đa ~3s và tự huỷ khi người dùng tự cuộn (`wheel`/`touchstart`/`keydown`).

**v0.4.1 — thêm KHỐI MỤC LỤC TRONG NỘI DUNG** (Tùng: *“thêm tuỳ chọn mục lục hiển thị trong phần nội dung bài viết”*): 2 cách hiển thị **bật/tắt độc lập** — `float` (nút dọc + drawer) và `inline` (khối trong nội dung) + `inline_pos` (`top` đầu bài / `p1` sau đoạn mở đầu) + `inline_open` (mở sẵn). Khối dùng `<details>/<summary>` HTML gốc ⇒ **thu/mở chạy được cả khi tắt JS**; `<ol>` + số lấy từ `tlpi_toc_scan()` nên khớp đúng danh sách của drawer.

**Số đo (Playwright, 2026-09-17, bài `/bao-gia-cua-nhua-gia-re-tphcm/`):** khối **990×226 @y320** khi `inline_pos=top` (nằm trong `.cp-article__content`, TRÊN heading đầu) và **990×616 @y507** khi `p1` (DƯỚI đoạn mở đầu); thu gọn (`inline_open=''`) còn **60px** và bấm `<summary>` mở ra (`open false → true`); viền trái 4px + số màu `rgb(255,163,5)` khớp màu Settings; bấm mục trong khối → heading dừng **14px dưới header** (91−77) y như drawer. **7 biến thể kiểm bằng `?toc_test=…`** (mu-plugin tạm, không ghi đè option): `float_off` → chỉ còn khối (`nutDoc:false`, khối 14 mục) · `inline_off` → chỉ còn drawer (khối 0) · **`both_off` → cả trang KHÔNG nạp `toc.css`/`toc.js`** · `collapse` · `p1` · `top` · `depth3` (14 mục). Trang SP (Tùng bật `product`) → khối nằm trong tab mô tả của WC (**986×432**, 11 mục). **Tràn ngang 0**; smoke test **24/24**.

**BẪY THÊM (v0.4.1):** ① **màu Settings phải truyền vào khối inline** bằng `style="--tlpi-toc-color:…"` — khối nằm trong `the_content`, KHÔNG nằm trong wrapper `.tlpi-toc` (nơi biến này được gắn) ⇒ thiếu là khối lệch màu so với nút dọc; ② **cuộn quãng DÀI phải NHẢY THẲNG** (`behavior:'auto'` khi `delta > 1,5 × chiều cao khung nhìn`), KHÔNG cuộn mượt: ảnh lazy nạp dần trong lúc cuộn mượt làm trang cao thêm liên tục — đo được đích **36.567px bị đẩy tới 78.599px**, 12 lần dò trong 3s vẫn hụt, trong khi nhảy thẳng thì khớp sau 1–2 lần chỉnh (quãng ngắn vẫn cuộn mượt); ③ thêm **`html { scroll-padding-top: var(--tlpi-scroll-pad) }`** (JS ghi biến theo chiều cao header) để **DEEP LINK** `…#ten-muc` (Google “jump to”, link khách dán) cũng dừng dưới header sticky.


## CP1.9 — “Chi nhánh & Hotline Cao Phát” trong Customizer (2026-09-17, chuyển từ plugin về theme)

**Việc:** danh sách hotline từng chi nhánh (hiện ở box “Hỗ trợ trực tuyến”, sidebar trang chi tiết SP) **chuyển từ plugin `pl-tien-ich-tungleads` (Settings → Cao Phát, option `tlcp_support_branches`, gõ textarea) về THEME**, quản trị trong **Customizer → “Chi nhánh & Hotline Cao Phát”** với **repeater kéo thả** (thêm/xoá từng dòng). Lý do (Tùng 2026-09-17): đây là **nội dung hiển thị**, không phải business logic; hotline CHÍNH `cp_hotline_tel` vốn đã ở Customizer ⇒ gộp về **1 chỗ sửa mọi số điện thoại**. Tùng chốt dùng **repeater có sẵn của theme** (không làm trang admin riêng: ngược quy ước theme = trình bày + mất preview).

| Việc | File |
| :--- | :--- |
| Control kéo thả + section + sanitize | `inc/customizer.php` — `CP_Branches_Repeater_Control` (type `cp_branches_repeater`, hàng = tên + số + nút × + tay kéo ⋮⋮, input ẩn `$this->link()`), section `cp_branches` (priority 26), setting `cp_branches` (JSON), `cp_sanitize_branches()` (bỏ dòng thiếu tên/số, tối đa 20, giữ thứ tự) |
| Asset Customizer | `assets/customizer-branches.js` (**mới** — clone hàng từ mẫu ẩn, xoá, `jquery-ui-sortable`, `sync()` gom JSON → input ẩn + `wp.customize().set()`) + `assets/customizer-branches.css` (**mới**) — enqueue ở `customize_controls_enqueue_scripts` cùng cụm blocks/header |
| Helper dữ liệu (1 nguồn sự thật) | `functions.php` — `cp_support_branches_default()` (4 chi nhánh) · `cp_support_branches_default_json()` (default của setting) · `cp_support_branches()` = **theme_mod → CẦU NỐI option cũ `tlcp_support_branches` (đọc THẲNG option, dạng `Tên \| Số`) → mặc định** + filter `cp_support_branches` |
| Nơi tiêu thụ | `inc/woocommerce.php` — `cp_single_support_box()` chỉ còn `$branches = cp_support_branches();` (bỏ đoạn `function_exists('tlcp_support_branches')` + mảng mặc định tại chỗ) |
| Plugin gỡ mục | `plugins/pl-tien-ich-tungleads/pl-tien-ich-tungleads.php` (**v0.5.0**) — bỏ `TLPI_BRANCHES_OPTION` · `tlcp_support_branches_default()/()`/`_text()` · `register_setting` · textarea; thay bằng khối ghi chú “đã chuyển về theme”; trang Settings còn 3 mục + dòng `description` link sang Customizer |
| Chuyển dữ liệu production | `docs/prod-migrate-branches.php` (repo gốc) — DRY-RUN mặc định, `go` để ghi, `force` để ghi đè; đã thêm vào `deploy-caophat.sh` bước 5b |

**Số đo (Playwright, 2026-09-17):** Customizer mở `?autofocus[section]=cp_branches` → tiêu đề **“Chi nhánh & Hotline Cao Phát”**, **4 dòng mặc định** (CN Quận 7 · Bình Tân · Bến Cát · Giải đáp thắc mắc), `ui-sortable` đã init, JS+CSS nạp; “+ Thêm chi nhánh” → **5 dòng**; điền dòng mới ⇒ input ẩn + `wp.customize('cp_branches').get()` cập nhật JSON; **KÉO CHUỘT THẬT** (mouse down/move/up 12 bước) dòng cuối lên đầu ⇒ DOM + JSON đổi đúng thứ tự ⇒ Lưu ⇒ `wp theme mod get cp_branches` khớp JSON mới; front-end box “Hỗ trợ trực tuyến” (SP 4816): **5 dòng** khi có dòng test (`tel:0834484484` — bỏ dấu chấm đúng), **xoá theme_mod ⇒ về 4 dòng mặc định** + box **320×352** + **tràn ngang 0**; **CẦU NỐI**: `wp option update tlcp_support_branches 'CN Cầu Nối \| 0900.999.888'` ⇒ front-end hiện **đúng 1 dòng đó** (theme_mod rỗng vẫn không mất số); script chuyển: dry-run in dòng sẽ chuyển, `go` ⇒ theme_mod ghi + **option cũ bị xoá** (kiểm lại: option trống, theme_mod có dòng) rồi đã trả site về mặc định; **smoke test 24/24**; `php -l` ×4 + `node --check` sạch.

**Bẫy đã dính:** ① **`-- --go` KHÔNG chạy được** với `wp eval-file` — WP-CLI báo `unknown --go parameter` ⇒ phải truyền tham số **VỊ TRÍ** (`go`, `force`); ② **cầu nối phải đọc THẲNG option**, không gọi `tlcp_support_branches()` — plugin v0.5.0 đã gỡ hàm nên `function_exists()` trả false ⇒ mất số cũ (đã sửa); ③ tham số của `eval-file` chỉ tới script qua **`$args`** — file phải tự parse, không tự có biến nào khác.

---

## CHI TIẾT CP — chuyển NGUYÊN VĂN từ `CLAUDE.md` (2026-09-18, tối ưu token)

> Nội dung dưới đây TRƯỚC ĐÂY nằm trong bảng CP của `CLAUDE.md` (file nạp tự động mỗi phiên). Nay
> `CLAUDE.md` chỉ còn **chỉ mục**; chi tiết (quyết định đã chốt sau khi đo · số đo · bẫy · lệnh kiểm) ở đây.
> **Tra 1 CP:** `grep -n "^### CPx.y" .ai/FEATURE_MAP.md` rồi `sed -n "X,Yp" .ai/FEATURE_MAP.md` — KHÔNG mở cả file.

### CP1.1

Bootstrap child — enqueue font + `caophat.css` (sau bundle parent), `add_theme_support('custom-logo')` + **`title-tag`**, body class `cp`, helper `cp_hotline_*()`, `cp_product_card()`. **2026-09-16 (phát hiện khi làm CP5.3): theme CHA KHÔNG khai `title-tag` khi có plugin SEO active** — `Features\SEO::shouldBoot()` trả `false` (site dùng **Rank Math**), nhưng Rank Math chỉ **lọc** nội dung tiêu đề (`document_title_parts`) chứ **không tự in** thẻ ⇒ **toàn site mất thẻ `<title>`** (đo: `/the/cua-nhua-gia-go/`, `/chuyen-muc/tin-tuc/`, trang chủ đều **0** `<title>`, chỉ có `og:title` của Rank Math; **lỗi có cả ở production**). Child khai lại ở `functions.php` (`add_theme_support()` idempotent) → đo lại **1 thẻ `<title>`/trang**, nội dung do Rank Math cấp. **Việc của repo parent: sửa `shouldBoot()` để vẫn đăng ký `title-tag`.** | ✅

### CP1.2

Header — topbar, header sticky (logo `the_custom_logo`/fallback, `wp_nav_menu` primary → `.cp-nav`, hotline) | ✅

### CP1.2b

**Header mobile (≤768px) — 1 HÀNG:** burger trái · logo giữa · icon search + icon giỏ phải · **ẩn hotline** (trước là 3 hàng / 163px → nay 1 hàng / 65px). Logo căn ĐÚNG TÂM container — 2 cách SAI đã đo được: (a) `position: absolute; left: 50%` → logo 188px ĐÈ lên icon search, icon không bấm được; (b) `flex: 1 1 auto; justify-content: center` → vẫn lệch vì khối phải (2 icon 80px) ≠ burger (44px) **và** `a.custom-logo-link` bị KÉO GIÃN hết khung nên ảnh nằm ở ĐẦU khung (đo: `a` 361px / ảnh 160px → lệch −157px ở 768px). Đang dùng: `.cp-logo { flex: 1 1 auto; min-width: 0; justify-content: center; padding-left: 36px }` (36 = khối phải 80 − burger 44, bù phần lệch) + `margin: 0 auto` cho ảnh → **lệch tâm = 0** ở 320/360/390/414/600/768. Kích thước ảnh `.cp-header .cp-logo .custom-logo` (0,3,0) — buộc mạnh hơn rule `.cp-header .custom-logo` (0,2,0) ở CUỐI file (`width: 250px`), **chỉ clamp 1 chiều** `max-width: min(160px, 100%)` (clamp thêm `max-height` → box 169×26 méo tỉ lệ 5.44 → 6.5) + `object-fit: contain` phòng hờ → logo 81×15 / 121×22 / 151×28 / 160×29 theo bề rộng. Icon search + giỏ hàng = **35×35px** (yêu cầu Tùng 23:35; trước 44px — nay dưới mức tap target 44px khuyến nghị — **TÙNG ĐÃ CHỐT GIỮ 35px (2026-09-14)**, không thêm lớp phủ 44px; muốn tăng lại thì sửa `.cp-cart-link`/`.cp-search-toggle`): nút `.cp-search-toggle` (base `display: none`, ≤768px `display: grid`) đứng ngay TRƯỚC icon giỏ trong `.cp-header-cta`; bấm → JS (`assets/nav-menu.js` bước 4) gắn `.cp-header.is-search-open` + `aria-expanded` + focus input → ô search trượt xuống **dải riêng full chiều ngang** (`position: absolute; left/right: 0; top: 100%` — KHÔNG cho `flex-wrap`: `.cp-header-cta` bị đẩy xuống hàng mới làm vỡ layout, header 111px); đóng bằng Esc (trả focus về nút) / click ngoài / bấm lại nút. KHÔNG khai báo trùng `gap`/`min-height` của `.cp-header .cp-container` (rule cuối media query đã đặt `12px`/`64px`) | ✅

### CP1.3

Footer — **3 hoặc 4 cột** (mỗi cột = tiêu đề + ô soạn thảo văn bản), nền tuỳ biến (màu + ảnh + độ đậm lớp màu), dòng bản quyền. **2026-09-15 (yêu cầu Tùng) — mục Customizer “Footer Cao Phát”** (`inc/customizer.php`, section `cp_footer`, priority **40**, **18 control** — 2026-09-16: 2 ô hotline đã CHUYỂN sang section `cp_home` “Trang chủ Cao Phát”, xem `CP2.3`): nền `cp_footer_bg_color` (`WP_Customize_Color_Control`) · `cp_footer_bg_image` · `cp_footer_bg_opacity` (range 0–100) · `cp_footer_cols` (select **3 \| 4**) · `cp_footer_logo` (checkbox) · **mỗi cột 1–4: `cp_footer_colN_title` + `cp_footer_colN_source` (Ô soạn thảo \| Menu “Footer”) + `cp_footer_colN_content`** (control `CP_Text_Editor_Control` → **TinyMCE**, JS `assets/customizer-footer.js`) · `cp_footer_copyright` / `cp_footer_credit` · 2 trường hotline toàn site (`cp_hotline_display` / `cp_hotline_tel`) — **2 ô này đã CHUYỂN sang section `cp_home` “Trang chủ Cao Phát” ngày 2026-09-16 (xem `CP2.3`)**: ID setting GIỮ NGUYÊN nên giá trị đã nhập không mất. **Nền:** `cp_footer_style()` (`wp_head`, `<style id="cp-footer-css">`) in `--cp-footer-bg` + `--cp-footer-bg-op`; `.cp-footer` = `background-color` + `background-image` (cover) và **`.cp-footer::before` là LỚP MÀU đè lên ảnh** với `opacity: var(--cp-footer-bg-op)` (+ `.cp-footer > .cp-container { position: relative; z-index: 1 }`) → **100% = chỉ thấy màu, 0% = chỉ thấy ảnh**; không chọn ảnh → lớp màu trùng nền nên giao diện y như trước (đo: `bgColor rgb(33,29,25)`, `bgImage none`, `::before` opacity 0.7). **3 cột** = lưới `.cp-footer-grid--cols-3` (`repeat(3, minmax(0,1fr))`) + chỉ render Cột 1–3 (dữ liệu Cột 4 vẫn giữ). **1 NGUỒN SỰ THẬT cho default:** `cp_footer_defaults()` + `cp_footer_columns_defaults()` (`functions.php`), đọc bằng `cp_footer_get()` / `cp_footer_flag()` / `cp_footer_col_get()` / `cp_footer_columns()`; token `{hotline}` `{hotline_tel}` `{email}` `{year}` `{site}` thay bằng `cp_footer_tokens()` → `footer.php` không giữ chuỗi nào, ô để trống = dùng lại mặc định. **3 BẪY đã dính + đã sửa:** (1) **`wp.editor.getEditor()` KHÔNG tồn tại** (`wp.editor` chỉ có `initialize` / `remove` / `getContent` / `getDefaultSettings`) → phải dùng `tinymce.get( id )`; (2) textarea của control **thiếu `id`** → TinyMCE không init (probe: `ids: ["", ""…]`) → thêm `id="cp-editor-<setting>"`; (3) sync cả khi `init`/`blur` → mở section rồi Publish là ghi theme mod cho **cả 4 cột** (giá trị = default nhưng làm “đóng băng” default) → chỉ sync khi khác nội dung gốc (`change keyup undo redo`, so với `$ta.val()` lúc init) — đo lại: mở section **không setting nào dirty** ✓. **Bẫy CSS:** email dài trong cột 157px ở 390px (`caophatdoor@gmail.com` rộng **183px**) → `scrollWidth` **396 > 390** → `overflow-wrap: break-word` cho `.cp-col__body` → **390** ✓. **E2E (admin tạm + Playwright, đã xoá user):** section **18 control** đúng danh sách (đo lại 2026-09-16, sau khi chuyển 2 ô hotline đi); mở section → **4 editor TinyMCE** (toolbar ✓, iframe **275×200px**), gõ trong editor → setting đổi theo, Publish → front-end đổi đúng (`--cp-footer-bg:#c8471f`, `cp-footer-grid--cols-3`, tiêu đề + nội dung cột 1 theo editor, cột 2 `menu → editor`); test bằng theme mod: ảnh nền + opacity 30 → `background-image: url(…)` + `::before` opacity **0.3**. **Đo mặc định 6 mức bề rộng:** 1440 → **318.7px** · 1280 ×318.7 · 1100 ×343.4 · 1024/768 ×487.3 · 390 → **656.9** (mô hình cũ: 349.2 / 373.9 / 520.8 / 809.1 — nay gọn hơn vì `<ul>` trong cột không còn margin mặc định); **6/6 mức `scrollWidth == innerWidth`**; 8 link `elementFromPoint` đều = `A`; click “Chính sách vận chuyển” → đúng trang; 0 lỗi JS, 0 PHP notice. **Mặc định mọi cột = ô soạn thảo** (vị trí menu `footer` hiện CHƯA gán menu nào ở local nên để `menu` thì cột chỉ có tiêu đề). **2026-09-15 (yêu cầu Tùng) — ô soạn thảo là WYSIWYG TUYỆT ĐỐI:** đã **XOÁ** rule ép màu `.cp-col__body strong, b { color:#fff }` (Tùng yêu cầu), bỏ ép màu/cỡ ở `h5` (giữ `margin: 0 0 10px`), hover link đổi `color:#fff` → `opacity:.78`. **Lý do:** Tùng đặt nền footer SÁNG (`cp_footer_bg_color #d6d6d6` + ảnh, opacity 95 → nền thực tế `rgb(214,214,214)`) và nhập chữ `#000000`; ép `#fff` làm chữ đậm trắng trên nền sáng = **mất chữ**. **E2E:** đặt `cp_footer_col2_content` test `color:#b8860b;font-size:19px` + `#0a4d8c/11px` → front-end hiện **đúng y** 4 thuộc tính, rồi xoá mod test; chữ mặc định về đen `rgb(0,0,0)` contrast **17.78**. Đo **1440 · 1024 · 768 · 390**: không tràn ngang. ⚠️ **Còn 3 màu mặc định của theme (KHÔNG thuộc ô soạn thảo) đang chìm trên nền sáng:** `h4` tiêu đề cột `#fff` contrast **1.18** · `p` `#9a8f80` **1.44** · link cột 2–3 (nội dung MẶC ĐỊNH trong `functions.php`) `#cabfb2` **1.11** → hướng xử lý = cho màu chữ tự đổi theo độ sáng nền (CHƯA làm, chờ Tùng quyết). **2026-09-15 (yêu cầu Tùng) — 2 thay đổi tiếp:** (1) **bỏ HẾT tiêu đề/nội dung mặc định của cột:** `cp_footer_columns_defaults()` nay trả `title=''` + `content=''` (giữ `source='editor'`) ⇒ ô để trống = cột TRỐNG (trước đây rỗng thì rơi về chữ mẫu “Sản phẩm / Hỗ trợ / Liên hệ”); `footer.php` vốn chỉ in tiêu đề/khối nội dung khi chuỗi KHÁC rỗng nên cột trống không in gì — đo: cột để trống = `0 B`, KHÔNG có `.cp-col__body`. (2) **Chữ do theme quyết định tự đổi theo nền:** thêm `cp_footer_bg_is_light()` (độ sáng cảm nhận > 0.55 = nền sáng, `#rgb`/`#rrggbb`, không đọc được → coi như nền tối) → `cp_footer_style()` in `--cp-footer-title/fg/muted/line` tông ĐẬM khi nền SÁNG; CSS dùng `var(--cp-footer-…, <bảng màu nền tối cũ>)` cho `.cp-footer` (color) · `h4` · `a:hover` · `.cp-logo-text b/span` · `p` · `.cp-footer-bottom` (chữ + viền trên) ⇒ **nền tối không đổi gì**. Đo nền trắng: chữ nền `rgb(43,39,35)` **5.12** · `h4` `rgb(20,18,15)` **8.65** · bản quyền `rgb(87,80,74)` (≈7.6 theo WCAG); đổi thử nền tối `#211d19` → không in var, `h4` về `#fff` như cũ ✓; 4 mức bề rộng không tràn ngang. (3) **Danh sách `ul`/`li` trong ô soạn thảo nay HIỆN DẤU như đã nhập:** trước đây bị `list-style: none; padding: 0` ở **2 lớp** (reset chung `.cp ul` + rule `.cp-footer .cp-col__body ul, ol`) ⇒ `<ul>/<li>` ra như dòng chữ thường (đo: chữ lệch so với `<ul>` = **0px**); nay `ul { list-style: disc }` · `ol { decimal }` · `ul ul { circle }` · `ol ol { lower-alpha }` + `padding-left: 20px`. ⚠️ **Đây là quyết định ĐẢO** so với bản CP1.3 đầu (bản đầu cố tình bỏ dấu đầu dòng — nay bỏ vì Tùng yêu cầu ô soạn thảo WYSIWYG): KHÔNG revert về `list-style: none`. Cột nguồn “Menu” không đi qua `.cp-col__body` nên vẫn không dấu (không đổi). Đo: 6/6 danh sách `disc` · chữ lệch **20px** · điểm ảnh thấy dấu chấm ở page x 23–27 · `overflow = 0` ở 1440/1024/768/390. (4) **Nút “Mã HTML” (icon `<>`) trong thanh công cụ ô soạn thảo:** WP 7.1 (TinyMCE **4.9.11**) **KHÔNG** bundle plugin `code` + Customizer chỉ có 1 TinyMCE ⇒ không có chỗ xem/sửa mã HTML. Đã tự đăng ký plugin TinyMCE 4 **`cpcode`** trong `assets/customizer-footer.js` (`tinymce.PluginManager.add` → `editor.addButton('cpcode', { icon:'code', tooltip:'Mã HTML' })` mở hộp thoại textarea monospace 620×380: `getContent({source_view:true})` → `setContent()` + `undoManager.add()` + **`fire('change')`** vì `setContent` KHÔNG tự fire nên `sync()` cần nó để đẩy vào setting). Nối vào config: `plugins: '…,cpcode'` · `toolbar1: <toolbar cũ>,cpcode` (nút nằm CUỐI thanh công cụ, icon `.mce-i-code` của skin `lightgray`). **E2E (admin tạm, KHÔNG Publish, đã xoá user):** 4/4 editor có nút; bấm → dialog title “Mã HTML” nạp đúng HTML hiện tại; sửa + bấm **“Đồng ý”** → đóng dialog, editor VÀ `wp.customize('cp_footer_col1_content')` đều có dấu test (`dirty=true`); DB **không** ghi gì (kiểm = 0 dấu); 0 lỗi JS từ code mới (lỗi mạng còn lại là Vite theme cha + thumbnail 404 có sẵn ở local). (5) **Nội dung CỘT 4 không hiện ra front-end — nguyên nhân: `<iframe>` bị KSES xoá:** `cp_footer_col4_content` trong DB chỉ còn **`<p></p>`**; `wp_kses_post` **KHÔNG** cho `iframe` (đo: `IN: <iframe src="…maps…"></iframe><p>VT</p>` → `OUT: <p>VT</p>`) và bị xoá ở **CẢ 2 lớp** — sanitize lúc lưu (`inc/customizer.php`) + lúc in (`footer.php: wp_kses_post`). **Đã sửa:** thêm `cp_footer_kses_content()` + `cp_footer_iframe_host_ok()` (`functions.php`) = `wp_kses` với bảng cho phép **thêm ĐÚNG thẻ `iframe`** (src/width/height/style/title/loading/allow/allowfullscreen/frameborder/referrerpolicy; **KHÔNG** thêm `script`/`style`/`embed`) rồi kiểm lại `src` phải **https** + host thuộc danh sách tin cậy (`google.com` `google.com.vn` `maps.google.com` `youtube.com` `youtube-nocookie.com` `youtu.be` `openstreetmap.org`) — không đạt thì bỏ thẻ. Dùng ở **cả 2 chỗ**: `'sanitize_callback' => 'cp_footer_kses_content'` + `footer.php` in qua hàm này. **Kiểm:** google-maps/youtube giữ nguyên · host lạ + `http://` bị bỏ · `<script>` bị xoá (chỉ còn chữ) · front-end render thật ra `<div class="cp-col__body"><iframe src="…maps…">` ✓ · host lạ trên front-end = **0**. Kèm CSS chống tràn: `.cp-footer .cp-col__body iframe { max-width:100%; border:0; border-radius; display:block }` (iframe mã nhúng ghi `width="600"` cứng → đo trước khi thêm: `overflow` **257/234/423** ở 1440/768/390; sau: **0** và iframe = đúng bề rộng cột 323/346/157px). (6) **2 nút mới ở hàng 2 thanh công cụ: chọn CỠ CHỮ + MÀU CHỮ** (`toolbar2: 'fontsizeselect,forecolor'` + plugin `textcolor` + `fontsize_formats` px `12→32px`) — E2E: **4/4 editor có 2 hàng toolbar** với `fontsizeselect` + `forecolor`; **gõ phím ở Cột 4 → editor VÀ setting đồng bộ** (`dirty=true`) ⇒ khẳng định đường sync Cột 4 KHÔNG lỗi (mất nội dung là do KSES xoá iframe); nút “Mã HTML” vẫn chạy cùng hàng 2; 0 lỗi JS. ⚠️ **ĐÍNH CHÍNH (Tùng báo “vẫn chưa thấy cỡ chữ/màu chữ”):** đặt 2 nút ở `toolbar2` là **SAI** — WP **ẩn hàng toolbar thứ 2** (chỉ hiện khi bấm “Toolbar Toggle”) nên nút CÓ trong DOM nhưng **không hiển thị** (đo: `rect = 0×0`, `offsetParent = null`); lần kiểm trước tôi chỉ đếm trong DOM nên tưởng đạt. **Đã sửa:** chuyển `fontsizeselect,forecolor` vào **`toolbar1`** (sidebar hẹp nên TinyMCE tự xuống dòng) + `toolbar2: ''`. **Đo lại (đúng cách):** cả 4 editor đều hiện **Cỡ chữ `121×26px`** và **Màu chữ `47×26px`**, `offsetParent = true`, toolbar `275×71px`, không tràn ngang; **chạy thật:** chọn `20px` → `<span style="font-size: 20px">` ✓; nút Màu chữ là **split-button** — bấm icon áp màu hiện tại, bấm **mũi tên `.mce-open`** mở **bảng 40 màu** → áp ra `<span style="color: #000000">` ✓; `aria-label` = “Màu chữ văn bản”. **Bằng chứng end-to-end:** Tùng đã Publish **bản đồ Google Maps thật** vào Cột 4 (`cp_footer_col4_content` chứa `<iframe src="https://www.google.com/maps/embed?pb=!1m14…">`) và front-end render đúng iframe, co theo cột (323/346/157px), **overflow = 0** ở 1440/768/390 ⇒ luồng KSES → Publish → render hoạt động thật. (7) **Dòng “Thiết kế bởi tungleads.com” = markup CỐ ĐỊNH (yêu cầu Tùng 2026-09-16):** bỏ hẳn khỏi admin — xoá setting + control `cp_footer_credit` (`inc/customizer.php`, 21 → **20 control**) và xoá key `credit` khỏi `cp_footer_defaults()`; nay in cứng ở `footer.php`: `printf( esc_html__( 'Thiết kế bởi %s' ), '<a href="https://tungleads.com/" target="_blank" rel="noopener">tungleads.com</a>' )` trong `<span class="cp-footer-credit">` (KHÔNG `nofollow` vì là site của chính Tùng). CSS: `.cp-footer-credit a { color: inherit; text-decoration: underline }` (`.cp a` chung bỏ gạch chân nên phải thêm lại) + hover `--cp-footer-title`. **Đo:** front-end có `<a href="https://tungleads.com/" target="_blank" rel="noopener">` (màu `rgb(87,80,74)`, gạch chân, rect 95×16) ở 1440/768/390, không tràn ngang; Customizer còn **20 setting**, không còn control `credit` (chỉ còn “Dòng cuối — bản quyền”). **Sửa kèm lỗi phát hiện khi hit-test:** nút gọi nổi `.cp-fab` (fixed 54px, cách phải 18px) **đè lên link ghi công** khi cuộn tới đáy (đo 1440/768: giao **832px²**, click giữa link trúng nút gọi) → thêm `@media (min-width:561px) { .cp-footer-bottom { padding-right: 84px } }` → giao **0px²**, click giữa link trúng `A` ✓. **2026-09-16 (Tùng yêu cầu) — XOÁ HẲN NÚT GỌI NỔI (FAB):** xoá markup `<div class="cp-fab">` (1 nút `A.call` → `tel:`) khỏi `footer.php`, xoá `.cp-fab` / `.cp-fab a` / `.cp-fab .call` / `.cp-fab .zalo` + `@keyframes cp-pulse` (CHỈ FAB dùng) khỏi `caophat.css`, xoá 2 biến chết `$cp_tel` / `$cp_disp` (chỉ FAB dùng — đã grep xác nhận). Chức năng chuyển sang plugin riêng **`button-call-zalo-tungleads`**. **Đo lại (1440):** `.cp-fab` = **0 phần tử + 0 rule CSS**; bấm **giữa** link ghi công ở đáy trang nay trúng **`A`** (link ghi công) thay vì `A.call` ⇒ hết hẳn lỗi phủ **832px²** mà không cần `padding-right` nào. | ✅

### CP2.1

Trang chủ — `front-page.php`: hero + 4 feature + CTA (chuỗi i18n / theme_mod) + gọi 3 template-part động | ✅

### CP2.2

Khối động trang chủ — `home-categories` (6 product_cat), ~~`home-products`~~ (best=total_sales \| sale) **đã xoá 2026-09-15 (yêu cầu Tùng)**, ~~`home-blog`~~ (3 bài mới) **đã xoá 2026-09-15 (yêu cầu Tùng)** — tin tức nay dùng khối CP2.5. **2026-09-15 (yêu cầu Tùng) — mobile ≤768px, tiêu đề khối `.cp-section-head`:** h2 **24px → 20px** + gạch nhấn vàng 46×3px dưới tiêu đề (`h2::after`), head xếp **dọc** (`flex-direction: column`, gap 14px), eyebrow 11.5px; link "Xem thêm →" thành **button pill** (nền `--cp-primary` + chữ `--cp-on-primary` 8.9:1, cao **44px**, bo 999px) — phải dùng selector `.cp-section-head .cp-link-more` (0,2,0) mới thắng `.cp a { color: inherit }` (0,1,1) của rule 1 class cũ. Đo 390px: h2 20/27px + bar 46×3px, nút 136×44 (khối thường) / 129×44 (khối danh mục), `elementFromPoint` ở tâm **và** sát đáy nút → đều `A.cp-link-more`, click → đúng URL; 360/390/768/1024/1440 **không tràn ngang**; desktop 1440 KHÔNG đổi (`h2 34px`, head `row`, link trong suốt 25.6px). **2026-09-15 (yêu cầu Tùng) — DESKTOP: tiêu đề khối hiện đại hơn:** `.cp-section-head .cp-eyebrow` → **chip** bo `999px`, nền `rgba(251,175,2,.16)`, chữ `--cp-accent-dark` (**5.91:1** trên nền trắng / **5.46:1** trên nền kem `.cp-section.alt`; dùng `--cp-accent` chỉ 4.32:1 → trượt AA nên đã đổi), h2 **`clamp(24px,3vw,34px)` → `clamp(26px, 2.7vw, 38px)`** + `line-height: 1.2` + `letter-spacing: -.6px`, **gạch nhấn vàng 56×4px** dưới tiêu đề (`h2::after`; mobile 46×3px), head `gap 20 → 28px` + `padding-bottom: 22px` + **kẻ mảnh `1px var(--cp-line)` dưới head** + `margin-bottom: 34px`. Đo: 1440 → h2 **38px/45.6** (trước 34px), 1280 → 34.56 · 1100 → 29.7 · 1024 → 27.65 · 769 → 26px; 768/390 **giữ nguyên bản mobile** (đã reset `padding-bottom: 0` + `border-bottom: 0`); 7/7 link `elementFromPoint` = `A.cp-link-more` + click → `/san-pham/`; **7 mức bề rộng không tràn ngang**; `/san-pham/` + `/chuyen-muc/tin-tuc/` **0 `.cp-section-head`** (không bị ảnh hưởng) | ✅

### CP2.3

Customizer — section `cp_home` ("Trang chủ Cao Phát", **16 control** — 2026-09-16 (yêu cầu Tùng): thêm **2 ô hotline toàn site** `cp_hotline_display` (“Hotline — số hiển thị (toàn site)”) + `cp_hotline_tel` (“Hotline — số để gọi (link tel:)”) **chuyển từ section `cp_footer` “Footer Cao Phát”**; ID setting GIỮ NGUYÊN nên giá trị đã nhập không mất; đặt `priority` **90/91** ⇒ nằm CUỐI danh sách của section (các control còn lại priority mặc định 10)): hero (nhãn/tiêu đề `<em>`/mô tả/**2 nút + 2 link**/**2 ô số liệu**/ảnh) + CTA (tiêu đề/mô tả). `front-page.php` đọc `get_theme_mod` fallback default i18n. **2026-09-15 — 6 trường nhập đầu tiên** — `cp_hero_btn1` ("Xem sản phẩm") · `cp_hero_btn2` ("Nhận báo giá 24/7") · `cp_hero_stat1_value` ("10+ năm") · `cp_hero_stat1_label` ("Kinh nghiệm thi công") · `cp_hero_stat2_value` ("5.000+") · `cp_hero_stat2_label` ("Công trình hoàn thiện"), `sanitize_text_field` + transport `refresh`, để trống → mặc định (`?: __( … )`). **2026-09-15 (yêu cầu Tùng) — thêm 2 trường LINK cho 2 nút:** `cp_hero_btn1_url` ("trống = trang cửa hàng") + `cp_hero_btn2_url` ("trống = gọi hotline"), control type **`url`** → sanitize **`esc_url_raw`** (KHÔNG dùng `sanitize_text_field` như các field khác; `esc_url_raw` giữ `tel:`, đường dẫn tương đối `/lien-he/`, query string); `front-page.php` fallback `?: $cp_shop` / `?: 'tel:' . $cp_tel` rồi echo `esc_url`. E2E: admin tạm → section có **14 control**, 2 control link là `input[type=url]` mặc định rỗng; nhập `/lien-he/` + `tel:0901234567` → Publish → hero `href` đúng 2 giá trị đó; xoá mod thử → về `…/san-pham/` và `tel:0834021021`. **2026-09-15 (yêu cầu Tùng) — sửa lỗi chữ DÍNH trong 2 ô số liệu hero:** `b` (số liệu) + `small` (nhãn) đều là **inline** và markup KHÔNG có khoảng trắng giữa `</b><small>` (`front-page.php`) → ở 1440/768 hai chữ nằm cùng 1 dòng **dính liền** ("10+ nămKinh nghiệm thi công"), ở 1024/390 thì `small` tự xuống dòng tuỳ tiện; thêm `.cp-hero-float > div { display: flex; flex-direction: column; line-height: 1.25 }` (`assets/caophat.css` ~416) → mỗi ô là **cột dọc** ở MỌI bề rộng. Đo 1440/1024/768/390 (giống nhau): `b` **126.4×25** tại `top 595` · `small` **126.4×15.6** tại `top 620` → khe hở dọc **0** (đã xuống dòng), 2 ô thẳng hàng; khối float **462.5×56 → 303.7×64.6**; 4/4 mức `scrollWidth == innerWidth`, 0 PHP notice. | ✅

### CP2.4

**Khối "Sản phẩm theo danh mục" trên trang chủ** — 3 slot (1–3), cấu hình trong Customizer (section `cp_home_blocks_products`), ẩn khi chưa chọn danh mục. Mỗi slot: danh mục `product_cat` (`cp_pblock_N_cat`), tiêu đề (`…_title`, mặc định = tên danh mục), sắp xếp `Mới nhất / Cũ nhất` (`…_order`), số sản phẩm 1–20 (`…_count`, mặc định 8), bố cục `Lưới 4 / Lưới 3 / Lưới 2 cột / Cuộn ngang` (`…_layout`). Render bằng `template-parts/home-cat-products.php` (dùng lại `cp_product_card()` của CP1.1), nút "Xem thêm →" = link trang danh mục (`get_term_link`). **2026-09-15 (yêu cầu Tùng): chuyển từ 3 slot CỐ ĐỊNH sang REPEATER** — Customizer có nút **"+ Thêm danh mục sản phẩm"** thêm khối tuỳ ý, mỗi hàng có nút **xoá (×)** + **kéo thả** đổi thứ tự; setting `cp_pblocks` lưu **JSON** mảng khối (sanitize `cp_blocks_sanitize()`); `template-parts/home-cat-products.php` đọc JSON và gọi 1 lần (bỏ tham số `index`); code 3-slot cũ (`cp_term_choices()` + `cp_customize_block_controls()`) đã **xoá khỏi `functions.php`** (187 dòng) và 25 theme mod cũ đã dọn. **BỎ nhãn `.cp-eyebrow` "Danh mục sản phẩm" phía trên tên danh mục** (khối tin vẫn giữ "Tin tức"). **2026-09-15 (yêu cầu Tùng): mobile ≤768px — link "Xem thêm →" chuyển xuống DƯỚI list và canh giữa:** `.cp-cat-block .cp-container` thành `flex column` + `.cp-section-head { display: contents }` rồi đặt `order` (title 1 · list/scroller 2 · link 3 + `align-self: center`) — **KHÔNG đổi DOM/JS**; đo ở 390px: link nằm dưới list (`top 7970 ≥ list bottom 7950`), **lệch tâm = 0**, desktop 1440 không đổi (link vẫn trên-phải, `display: flex`). | ✅

### CP2.5

**Khối "Tin tức theo chuyên mục" trên trang chủ** — 2 slot (1–2), cấu hình trong Customizer (section `cp_home_blocks_news`), ẩn khi chưa chọn chuyên mục. Mỗi slot: chuyên mục (`cp_nblock_N_cat`), tiêu đề (`…_title`), sắp xếp `Mới nhất / Cũ nhất` (`…_order`), số bài 1–12 (`…_count`, mặc định 3), bố cục `Lưới 3 / Lưới 2 cột / Cuộn ngang` (`…_layout`). Render bằng `template-parts/home-cat-news.php` (thẻ dùng lại `.cp-news-mini` của CP5.2), nút "Xem thêm →" = link chuyên mục. **2026-09-15 (yêu cầu Tùng): chuyển sang REPEATER** — nút **"+ Thêm chuyên mục tin tức"**, mỗi hàng có xoá (×) + kéo thả; setting `cp_nblocks` lưu JSON mảng khối; `template-parts/home-cat-news.php` đọc JSON và gọi 1 lần (bỏ `index`). **2026-09-15 (yêu cầu Tùng): thêm BỐ CỤC `featured` — "Nổi bật: 1 bài trái + 5 bài phải"** (GIỮ nguyên 3 bố cục cũ): option mới trong select "Bố cục" của khối tin; **"Số lượng" = TỔNG số bài** (bài 1 → thẻ lớn cột TRÁI, `count − 1` bài còn lại → danh sách cột PHẢI). Markup riêng `.cp-news-feature` (KHÔNG dùng `.cp-news-card` của CP5.1 — thẻ ở đây là `h3` vì `h2` của khối đứng trên, và cần `sizes` ảnh riêng cho cột ~700px): `.cp-news-feature__main` = ảnh 16:9 + nhãn chuyên mục + `h3` + ngày `d/m/Y` + trích đoạn 28 từ (`.cp-news-feature__excerpt`, clamp 3 dòng); `.cp-news-feature__list` = các `.cp-news-row` (thumb 108px 4:3 + `h3` + ngày, gạch phân cách, dòng cuối bỏ gạch). CSS lưới `minmax(0,1.35fr) / minmax(0,1fr)` gap 24 + `align-items: stretch` (2 cột cao bằng nhau — xem mục "CAO BẰNG NHAU" cuối dòng này); ≤1024 → 1 cột + ảnh 21:9; ≤768 → thumb 88px, tiêu đề 18.5px, clamp 2 dòng. `.cp-news-feature` đã thêm vào selector mobile của CP2.4/2.5 → link "Xem thêm →" vẫn xuống DƯỚI và canh giữa. **BẪY đã dính + đã sửa:** bài KHÔNG có ảnh đại diện → `.cp-news-row__body` (con duy nhất) rơi vào cột `108px` (đo 1440: body **108** thay vì 547.5, tiêu đề cao 65px) → thêm class `cp-news-row--noimg` + rule **`.cp-news-row.cp-news-row--noimg`** (2 class = 0,2,0 — 1 class 0,1,0 sẽ bị rule `.cp-news-row` trong `@media (max-width: 768px)` ở CUỐI file đè lại). **Số đo Playwright:** 1440 → `790.45px / 585.55px` gap 24, thẻ trái 790.5×608.5 (ảnh 16:9 = **1.778**), list 585.5×525.8, **5 dòng** ×110px (thumb 108×81 = **1.333**); 1280 → `698.5/517.5` · 1100 → `595.1/440.9` · 1024 → 1 cột `984` + ảnh 21:9 (**2.333**) · 768 → `728` + thumb 88×66 · 390 → `350` + thumb 88×66; **6/6 mức bề rộng không tràn ngang**; `elementFromPoint` tại đầu dòng tiêu đề (thẻ trái + 5 dòng) và link "Xem thêm →" đều trả về `<a>`; click → đúng URL (bài trái `/kich-thuoc-cua-phong-ngu/`, bài dòng 3, `/chuyen-muc/tin-tuc/`); mobile 390 link "Xem thêm →" nằm DƯỚI khối (gap **20px**) + bấm được. Khối demo `cp_nblocks` đặt `layout=featured` + `count=6` (1 trái + 5 phải). **CAO BẰNG NHAU (yêu cầu Tùng 2026-09-15):** `.cp-news-feature` đổi `align-items: start → stretch` → **2 cột luôn cùng chiều cao**; phần chênh hấp thụ có chủ đích thay vì hở nền trắng — cột TRÁI: ảnh giãn (`flex: 1 1 auto; min-height: 0; max-height: 520px`) + phần chữ `flex: 1 1 auto`; cột PHẢI: `justify-content: space-between` (dòng giữ nguyên **110px**, phần chênh thành khoảng giữa các dòng). Đo 13 mức: **`lech` (mainH − listH) = 0 ở MỌI mức side-by-side** — 1440 `608.5/608.5` · 1366 `584.6` · 1280/1100/1025 `559/559` (trước đổi: 1440 lệch **−82.8**, 1100 lệch +34.5); ảnh crop nhẹ khi cột trái phải giãn: **1.778 @1440/1366 (không đổi) · 1.773 @1280 · 1.726 @1220 · 1.666 @1150 · 1.621 @1100 · 1.584 @1060 · 1.551 @1025**; trần `max-height: 520px` chặn ảnh thành ảnh DỌC khi "Số lượng" lớn (đo `count=12` = 11 dòng: ảnh dừng **520px**, thẻ vẫn cao **bằng** cột phải `1185.8/1185.8`, phần chênh dồn xuống phần chữ); ≤1024 xếp 1 cột **KHÔNG đổi** (ảnh 21:9 = **2.334** — mỗi khối là 1 hàng lưới riêng nên `stretch` vô hiệu); **13/13 mức `scrollWidth == innerWidth`**, hit-test + click vẫn đúng, 0 lỗi JS. 0 PHP notice. | ✅

### CP2.6

**Khối "Danh mục nổi bật" trên trang chủ — chọn danh mục + kéo thả thứ tự + bố cục** — section Customizer `cp_home_cats`: control tuỳ biến `CP_Term_Order_Control` (`inc/customizer.php`) = danh sách checkbox toàn bộ `product_cat` + **kéo thả** (jQuery UI sortable, handle `⋮⋮`) → setting `cp_cats_selected` lưu **CSV term_id theo đúng thứ tự hiển thị**; control `select` `cp_cats_layout` = `Lưới 6 / 4 / 3 cột` + `Hiển thị ngang (cuộn)`. `template-parts/home-categories.php` đọc selection (rỗng → fallback như cũ: 6 danh mục nhiều sản phẩm nhất) + in class `.cp-cats--<layout>`; asset chỉ nạp trong màn Customizer (`assets/customizer-cats.js` + `.css`). **2026-09-15 (yêu cầu Tùng): BỎ dòng đếm "N sản phẩm" dưới tiêu đề danh mục** trên thẻ — danh mục có mô tả thì vẫn hiện mô tả (8 từ đầu), không có mô tả thì chỉ còn tiêu đề (`grep -c '>[0-9]+ sản phẩm<'` trên trang chủ = 0). **2026-09-15 (yêu cầu Tùng): 2 TRƯỜNG NHẬP LIỆU cho khối tiêu đề** — setting `cp_cats_label` (nhãn nhỏ, mặc định "Danh mục nổi bật") + `cp_cats_title` (h2, mặc định "Khám phá theo dòng sản phẩm"), `sanitize_text_field` + transport `refresh`, để trống → `front-page.php` dùng lại mặc định qua `?: __( … )` (giống pattern hero/CTA). **BỎ dòng mô tả phụ** ("Đầy đủ các dòng cửa cho phòng ngủ…") + **BỎ link "Xem tất cả →"** của khối này → head chỉ còn chip + h2 (đo: cao **188 → 127px** ở 1440 · **183 → 73px** ở 390; toàn trang còn **6** `.cp-link-more`, riêng khối này = **0**). E2E: tạo admin tạm → Customizer "Trang chủ — Danh mục nổi bật" có **4 control** (`cp_cats_label` "Nhãn nhỏ trên tiêu đề" + `cp_cats_title` "Tiêu đề khối" + 2 control cũ) → nhập giá trị thử + bấm Publish → theme mod lưu đúng, trang chủ hiển thị đúng chuỗi mới (đã xoá mod thử + user tạm sau khi test). **2026-09-15 (yêu cầu Tùng): MOBILE ≤768px — "Danh mục nổi bật" = 1 HÀNG 3 CỘT + cuộn ngang có nút ‹ ›** (CP2.8): bỏ lưới 2 cột (và 1 cột ở ≤520px) → mọi `cp_cats_layout` đều thành `display: flex; gap: 10px; overflow-x: auto; scroll-snap-type: x mandatory; contain: paint` + ẩn thanh trượt, mỗi thẻ `flex: 0 0 calc((100% - 20px) / 3)`; chữ trên thẻ gọn lại (`.cp-cat-overlay` padding 10px, `b` 13px, mô tả `-webkit-line-clamp: 2`). Đo: 360 → thẻ 100px · 390 → **110px, 3 thẻ/hàng**, 520 → 153 · 768 → 236 (scrollW 2942 > 728); thanh trượt chiếm **0px**; `elementFromPoint` tâm nút › = **BUTTON** (nút ‹ ẩn ở đầu dải → click xuyên qua thẻ, đúng thiết kế), bấm › → `scrollLeft 0 → 120` = 1 thẻ (110+gap 10) & nút ‹ hiện, bấm ‹ → về 0; cuối dải → `.is-end` (nút › ẩn); **desktop 1024/1440 KHÔNG đổi** (`display: grid`, gap 22, `cp-scroller--static` + 2 nút `opacity 0`); không tràn ngang ở 360/390/520/768/1024/1440; 0 PHP notice. | ✅

### CP2.7

**Chọn sản phẩm ưu tiên trong khối "Sản phẩm theo danh mục"** — mỗi khối trong repeater CP2.4 có thêm mục **"Sản phẩm ưu tiên"**: bấm để tải danh sách sản phẩm **của danh mục đã chọn** qua AJAX (`wp_ajax_cp_block_products`, guard `check_ajax_referer` + `current_user_can('edit_theme_options')`, `inc/customizer.php`), tick từng sản phẩm + **kéo thả** sắp thứ tự → lưu vào khoá `products` (CSV ID) trong cùng JSON `cp_pblocks`. Khi render: sản phẩm đã chọn hiển thị **TRƯỚC theo đúng thứ tự** (`wc_get_product()` từng ID, giữ thứ tự), phần còn thiếu của `count` mới lấy thêm từ danh mục (`exclude` = các ID đã chọn). | ✅

### CP2.8

**Nút cuộn ‹ › cho bố cục "Cuộn ngang"** (khối SP/tin trên trang chủ) — **ẩn thanh trượt** của dải cuộn và thêm 2 nút mũi tên ở hai đầu danh sách: markup `.cp-scroller` (bọc list + 2 `.cp-scroller__btn`) do `cp_scroller_open()/cp_scroller_close()` (`functions.php`) in ra CHỈ khi bố cục là `scroll`; CSS ẩn scrollbar (`scrollbar-width: none` + `::-webkit-scrollbar{display:none}` — vẫn cuộn được bằng kéo/trackpad/nút), nút tròn 40px nổi 2 đầu — **nhô ra `-18px` (desktop) / `-15px` (≤768px, tỷ lệ 34/40; yêu cầu Tùng 2026-09-15), 2 bên đối xứng** (tự ẩn khi `.is-start`/`.is-end`); JS `assets/scroller.js` (vanilla, không jQuery) cuộn theo 1 thẻ + kiểm lại trạng thái khi `scroll`/`resize`/load ảnh. **+ 2026-09-15: dải "Danh mục nổi bật" (`.cp-cats`, mobile 1 hàng 3 cột) cũng dùng lại cơ chế này** — `template-parts/home-categories.php` bọc `cp_scroller_open()/close()` (LUÔN bọc; desktop lưới không cuộn → JS tự gắn `.cp-scroller--static` để ẩn nút) và `scroller.js` thêm `.cp-cats` vào `LIST_SEL`. | ✅

### CP2.9

**Khối "tab sản phẩm" trên trang chủ — 4 tab CỐ ĐỊNH** (yêu cầu Tùng 2026-09-15, chốt "4 tab cố định"): **Sản phẩm mới · Bán chạy · Hot · Khuyến mãi**. Section Customizer **`cp_home_tabs`** (priority 30) = **16 control** (mỗi tab: `cp_tab_<nguồn>_show` checkbox · `_title` text · `_count` number 1–20 · `_layout` select `cols-4/3/2/scroll`), mặc định: bật cả 4 tab · tiêu đề = tên tab · 8 sản phẩm · `cols-4`. Nguồn sản phẩm (`cp_tab_products()`, `functions.php`): `new` = mới nhất theo ngày · `best` = `total_sales` giảm dần (giống khối "Bán chạy" CP2.2) · **`hot` = cờ "Nổi bật" của WooCommerce** (`featured => true` — WooCommerce không có chỉ số "hot" tự tính; đổi cách lấy bằng filter `cp_tab_products`) · `sale` = `wc_get_product_ids_on_sale()`. Render bằng `template-parts/home-tabs.php` (gọi trong `front-page.php` ngay SAU khối "Danh mục nổi bật"): tab = nút `.cp-tablist__btn` (pill; tab đang xem nền `--cp-primary` + chữ `--cp-on-primary` **8.9:1**, cao 42px — mobile 40px), panel `.cp-tabpane` **TÁI DÙNG `.cp-cat-block cp-cat-block--<layout>`** (CP2.4) + `cp_scroller_open()/close()` (CP2.8) → lưới/cuộn/nút ‹ › chạy y như khối sản phẩm theo danh mục; tab bị tắt trong Customizer hoặc không có sản phẩm → tự ẩn. JS `assets/tabs.js` (vanilla): đổi tab + `role=tab/tablist/tabpanel` + `aria-selected`/`tabindex` + mũi tên ← → Home End, và **bắn `resize` sau mỗi lần đổi tab** để `scroller.js` đo lại dải cuộn vừa hiện (panel ẩn có `clientWidth = 0` → trạng thái nút ‹ › tính sai nếu không đo lại). ⚠️ **BẪY tên class:** KHÔNG dùng `.cp-tabs` / `.cp-tab-panel` — đã bị hệ tab CŨ (mockup, hiện không template nào dùng) chiếm ở `caophat.css` ~dòng 747 (`.cp-tab-panel { display: none }` + `.cp-tabs { margin-top: 56px }`) → bản đầu dùng tên đó cho ra panel cao **0px**; đã đổi sang `.cp-tablist` / `.cp-tablist__btn` / `.cp-tabpane`. **Số đo:** 1440 → 4 tab (140×42 · 180×42 · 137×42 · 195×42), lưới **4 cột / card 334px**; 1024 → 3 cột (313px) · 768 → 2 (359px) · 390 → 2 (170px); `elementFromPoint` ở tâm **và** sát đáy cả 4 tab = `BUTTON.cp-tablist__btn`; bấm tab 3 rồi ← → `aria-selected`/`hidden` đúng (luôn đúng 1 panel hiện); đặt tab "bán chạy" layout `scroll` → nút › `elementFromPoint` = BUTTON, bấm → `scrollLeft 0 → 270` = 1 thẻ (250+20) + nút ‹ hiện; không tràn ngang ở 390/768/1024/1440. E2E: tạo admin tạm → Customizer section có **16 control** → nhập tiêu đề mới + bỏ tick tab "hot" → Publish → trang chủ còn **3 tab**, nhãn tab 1 = chuỗi vừa nhập (đã xoá mod thử + user tạm sau khi test). Tab "hot" trên local hiện có **2 sản phẩm** (2 SP đang tick "Nổi bật") — tab nào 0 sản phẩm thì tự ẩn. **2026-09-15 (yêu cầu Tùng): thanh tab trên MOBILE = 1 DÒNG + TRƯỢT NGANG** (trước là `flex-wrap: wrap` → 4 tab xuống 2 hàng): trong `≤768px` đổi thành `flex-wrap: nowrap; overflow-x: auto; scroll-snap-type: x proximity; contain: paint` + ẩn thanh trượt, nút `flex: 0 0 auto` (mặc định `flex-shrink: 1` sẽ bóp chữ). Đo 390px: thanh **48px / 1 hàng**, nút 125·162·123·176, `scrollWidth 613 > clientWidth 350` → **trượt được**, thanh trượt **0px**, tab cuối nhô ra **259px**; trượt hết → `scrollLeft 263` + tab "Sản phẩm khuyến mãi" hiện đủ, `elementFromPoint` = **BUTTON** và bấm được (panel 4 hiện); 360/520 cũng trượt được; **768 vừa khít (không cần trượt)**; `≥769px` giữ `flex-wrap: wrap` + `overflow-x: visible` (desktop KHÔNG đổi); không tràn ngang ở cả 7 mức đo. **2026-09-15 (yêu cầu Tùng): ICON trước mỗi nhãn tab** — `cp_tab_icon( $source )` (`functions.php`) trả SVG inline 24×24 `stroke: currentColor` (new = 2 tia sáng · best = biểu đồ đi lên · hot = ngọn lửa · sale = thẻ giảm giá), in trong `.cp-tablist__icon` (`aria-hidden`) TRƯỚC chữ; CSS `.cp-tablist__btn { gap: 7px }` + `.cp-tablist__icon svg { 16px }` (mobile 14px / gap 6px). Đo 1440: nút **163·203·160·218** (trước 140·180·137·195), cao vẫn **42px**; 390: icon 14px, nút 40px, dải vẫn trượt được; nút active nền `rgb(251,175,2)` + chữ/icon `rgb(36,29,5)` = **8.9:1** (`stroke: currentColor` tự đổi màu — đo `iconKeThuaMauChu = true`); bấm ĐÚNG tâm icon (`elementFromPoint = path`) → đổi tab ✓; `ArrowRight` → tab kế ✓; không tràn ngang 390/768/1440. | ✅

### CP2.10

**4 cụm nội dung nổi bật trên trang chủ — quản trị trong Customizer** (yêu cầu Tùng 2026-09-16): section **“4 cụm nổi bật Cao Phát”** (`inc/customizer.php`, id `cp_features`, priority **36**, **12 control** = 4 cụm × (tiêu đề · mô tả · icon)). **Icon:** control **`WP_Customize_Media_Control`** (`mime_type => image`) lưu **attachment ID** vào `cp_feature{n}_icon`; có ảnh ⇒ `wp_get_attachment_image()` **THAY** SVG mặc định, không có ⇒ SVG inline như cũ. Mặc định = **1 NGUỒN SỰ THẬT** `cp_features_defaults()` (`functions.php` — chứa cả **path SVG** của 4 icon gốc); đọc bằng `cp_feature_get($n, 'title'|'text')` + `cp_feature_icon($n)`; `front-page.php` chỉ còn vòng `for (1..4)`, KHÔNG giữ chuỗi nào. **Cụm 4 “Báo giá 24/7”:** mô tả để trống ⇒ `cp_feature_get()` trả `cp_hotline_display()` (bám hotline toàn site) nên setting để `default => ''` — điền vào mới "đóng băng" giá trị. CSS: `.cp-feature .ic img { width:100%; height:100%; object-fit:contain }` (ảnh lấp ô 42×42). **BẪY (đã dính + đã sửa):** xoá biến `$cp_disp` ở `front-page.php` (tưởng là biến chết sau refactor) trong khi **nút CTA `Gọi ngay %s` (dòng ~128) VẪN dùng** ⇒ PHP in `Warning: Undefined variable` **vào ngay trong nhãn nút** + `white-space: nowrap` ⇒ nút rộng **1065px** ⇒ **tràn ngang 723px ở 390px**; bài học: khi xoá biến phải `grep` TOÀN file (grep bị cắt bằng `cut` rất dễ bỏ sót) — nay biến có ghi chú “KHÔNG xoá”. ⚠️ Mẫu kiểm lỗi PHP phải gồm **`Warning:` / `Notice:` / `Undefined`**: mẫu cũ (`PHP Warning\|PHP Notice`) đã **bỏ sót** đúng lỗi này. **Đo (Playwright):** mặc định khớp 100% trước refactor (1440: `20,696 1400×92` · 390: `20,801 350×424`; 4 icon SVG 22×22 trong ô 42×42; cụm 4 hiện `0834.021.021`); sửa chữ + upload icon `11661` ⇒ cụm 1 hiện **`<img>` 42×42** thay SVG, 3 cụm kia giữ SVG; **overflow 0 ở 1440/390 cả 2 trạng thái**; **0 lỗi PHP** trên 3 URL. **E2E Customizer** (user tạm, đã xoá): section đủ **12 control** đúng thứ tự (mỗi cụm: tiêu đề → mô tả → icon) · **4 nút “Chọn ảnh”** · modal Media mở (tab “Tất cả tập tin” có 160 ảnh → chọn → lưu `11661`) · Publish ghi 6 theme mod ⇒ xoá mod trả về mặc định. | ✅

### CP3.1

Trang danh mục / cửa hàng — hook `woocommerce_before/after_main_content` dựng pagehero + layout 2 cột (sidebar `product_cat` tái dùng `.cp-side-box`/`.cp-side-cats` của CP3.2 + lưới **4 cột × 5 dòng = 20 sp/trang** qua `loop_shop_per_page`) **+ 2026-09-15 (yêu cầu Tùng):** ≤1024px (lưới về 1 cột) khối sidebar "Danh mục sản phẩm" bị đẩy **XUỐNG DƯỚI** `.cp-shop-main` bằng `order` (`.cp-shop-aside { order: 2 }` + `.cp-shop-main { order: 1 }` — không đổi DOM) và **≤900px lưới sản phẩm `gap: 22px → 10px`**. Số đo ở 390px (`/danh-muc/phu-kien-cua/`): TRƯỚC — hộp danh mục cao **1603.8px** nằm TRÊN, `.cp-shop-main` bắt đầu ở **y=1870**, gap **22px**, card 141px; SAU — `.cp-shop-main` y=**234.6** (products ngay dưới hero), hộp danh mục y=**4126.3** (sau main), gap **10px**, card **147px**; **7/7 mức bề rộng (1440/1280/1100/1024/900/768/390) không tràn ngang**, desktop ≥1100px giữ nguyên `260px / 1fr` + gap 22px (aside vẫn bên trái). **`.cp-shop-main { padding: 10px }` trên mobile (yêu cầu Tùng 2026-09-15, cùng block `≤900px`):** trước `22px` → card sản phẩm ở 390px **147 → 159px**, 768px **336 → 348px**, 900px **402 → 414px**; desktop (≥1024px) giữ `22px` — đo 7 mức bề rộng vẫn không tràn ngang. **`flex-wrap: wrap` cho `ul.page-numbers` (2026-09-15):** shop có **19 trang** → dãy phân trang rộng **458px** trong khung 350px ở 390px làm TRÀN NGANG cả trang (`scrollWidth` **501 > 390**) → thêm `flex-wrap: wrap` + `justify-content: center` → 390px còn **390** (không tràn), desktop vẫn 1 hàng (chỉ wrap khi hết chỗ). Loop item bọc `.cp-card` qua hook. Card **KHÔNG** có nút nào (20:05 ngày 2026-09-14: bỏ cả nút "Thêm vào giỏ hàng" mặc định của WooCommerce lẫn link "Xem chi tiết" cũ) — ảnh + tiêu đề vẫn là link tới sản phẩm. Mô tả danh mục (`woocommerce_archive_description`) gỡ khỏi header mặc định → render DƯỚI lưới trong `.cp-shop-desc` (thu gọn sẵn + nút "Xem thêm/Thu gọn", ~8 dòng JS inline). `woocommerce_show_page_title` = false (pagehero đã có H1). Thanh công cụ `.cp-shop-toolbar` (bọc `woocommerce_before_shop_loop` @19/@35): `[số kết quả]` … `.cp-price-filter` (nút Tất cả / 1–3tr / 3–5tr / >5tr qua query var `min_price`/`max_price` sẵn có, bấm nút đang chọn = bỏ lọc, giữ `orderby`; cũng in ở `woocommerce_no_products_found` để bỏ lọc khi 0 kết quả) `[sắp xếp]` (`select` reskin, mũi tên SVG data-URI). `.cp-shop-main` = thẻ trắng shadow như `.cp-side-box`. Phân trang reskin căn giữa, nút bo tròn 42px, mũi tên ‹ ›. **Giá card:** selector phải đủ 5 class `.woocommerce .cp-shop-main ul.products li.product .price` — WooCommerce có rule `.woocommerce:where(...) ul.products li.product .price` = (0,4,2) đè rule 3-class → ép giá về màu olive `rgb(149,142,9)` + `font-size:.857em` (13.71px); `ins` cần `text-decoration:none` (mặc định trình duyệt gạch chân, `.cp-price` trang chủ đã tắt). Card **KHÔNG** in nhãn danh mục (`.cp-card-cat`) — nhãn này chỉ còn ở đầu trang chi tiết SP. KHÔNG copy template WooCommerce | ✅

### CP3.2

Chi tiết sản phẩm (layout theo demo moderndoor.vn) — breadcrumb mảnh + lưới 9/3 `.cp-single-layout`: `.cp-single-main` (thẻ trắng `div.product` gallery\|summary + `.cp-spec` thông số + ô tabs riêng `.cp-single-tabs.cp-card`) \| `.cp-single-side` (box "Hỗ trợ trực tuyến" `cp_single_support_box()` — số chính + `.cp-side-branches` danh sách hotline chi nhánh (mỗi dòng tên bên trái — số bên phải, canh đều 2 mép, không icon, `tel:`; **dữ liệu: CP1.9 — theme_mod `cp_branches`** (Customizer → “Chi nhánh & Hotline Cao Phát”, thêm/xoá/kéo thả từng dòng; đọc qua `cp_support_branches()` ở `functions.php`; **trước 2026-09-17** nằm ở plugin `pl-tien-ich-tungleads` — Settings → Cao Phát, option `tlcp_support_branches`); ghi đè bằng filter `cp_support_branches`) + box "Cam kết Cao Phát" `cp_trust_box()` — icon + chữ hoa, filter `cp_trust_items` + box "Sản phẩm mới" `cp_single_new_products_box()` (5 SP mới nhất)). Hộp giá `.cp-price-box` (cột dọc): giá + tag `.cp-price-off` "Tiết kiệm &lt;số tiền&gt;" (= giá gốc − giá bán). Badge sale trên ảnh (shop / trang chủ / liên quan / chi tiết) đổi qua filter `woocommerce_sale_flash` + helper `cp_sale_percent()` → **tag chữ nhật `-N%`** đỏ tươi (`--cp-sale` `#e30613`), dính sát góc trái-trên ảnh, chỉ bo góc dưới-phải; fallback `SALE` khi không tính được %. Trang chủ: `cp_product_card()` in `.cp-badge` cũng dùng `cp_sale_percent()`. Mô tả ngắn → hộp `.cp-short-desc` nổi bật + thu gọn `max-height:15em` (nút Xem thêm/Thu gọn). Dòng "Thẻ:" (`.tagged_as`) ẩn bằng CSS. Nút `.cp-buynow` (tel:, icon túi) cạnh nút mua — **nhãn "ĐẶT HÀNG NHANH"** (đổi từ "Mua hàng" và bỏ dòng phụ "Gọi điện xác nhận…" 22:30 ngày 2026-09-14) — nhãn nút mua lấy MẶC ĐỊNH của WooCommerce (`Thêm vào giỏ hàng`), CSS `text-transform: uppercase` hiển thị "THÊM VÀO GIỎ HÀNG" (icon giỏ qua `::before`) — 19:55 ngày 2026-09-14 đã **BỎ filter `woocommerce_product_single_add_to_cart_text`** (trước đó rút ngắn thành "Thêm giỏ hàng"); hệ quả: chữ dài hơn ~43px nên nút cần ~195px trong khi cột chỉ 152px → đang xuống 2 dòng, → **Tùng đã chốt GIỮ NGUYÊN 50/50 (2026-09-14)**: chấp nhận nhãn xuống 2 dòng ở 769–1280px, KHÔNG sửa thêm; dưới là hàng `.cp-contact-row` = "Gọi ngay" (cam, số hotline bọc `.cp-tel-num` ẩn ở `≤768px` — mobile và nút CTA cuối trang chỉ còn "Gọi ngay") + "Chat Zalo" (xanh `#0068ff`, `zalo.me/<số>`, filter `cp_zalo_url`), mỗi nút có icon. Ẩn ô số lượng, nút hotline phụ full-width. Gallery bật mũi tên flexslider (`woocommerce_single_product_carousel_options`); `single-gallery.js` tách dải thumbnail ra ô riêng `.cp-thumbs` dưới ảnh chính + 2 nút mũi tên cuộn. Related tách khỏi `div.product` → dải `.cp-related-band` nền xám, carousel cuộn ngang (scroll-snap). Dải CTA cuối. Ngay dưới ô tabs: thẻ `.cp-spec` "Thông số kỹ thuật" (`cp_single_spec_table()`) — đọc 8 meta `_tlcp_spec_*` do plugin `pl-tien-ich-tungleads` lưu, lưới 2 cột, mỗi dòng 1 hàng ngang (nhãn + giá trị cùng dòng qua `dt`/`dd` `display:inline`, nhãn thêm `": "` bằng `::after`) + icon SVG inline. KHÔNG copy template · **Nút zoom gallery** (`.woocommerce-product-gallery__trigger`): WooCommerce đặt `z-index: 99` — cao hơn cả header (`50`) lẫn panel menu cấp 2-3 (`60`, nằm TRONG stacking context của header) → nút zoom vẽ ĐÈ lên menu khi menu xổ xuống; đã hạ về **`z-index: 9`** bằng selector `.woocommerce div.product div.images .woocommerce-product-gallery__trigger` (0,4,2 > 0,1,0 của WooCommerce). ⚠️ Phải hạ xuống DƯỚI 50, không phải chỉ dưới 60 — vì panel menu nằm trong stacking context của `.cp-header`. Đo `elementFromPoint` tại tâm nút: menu MỞ → trả về phần tử trong `.cp-nav` ✅ · menu ĐÓNG → chính nút zoom + vẫn bấm được (tạo `.pswp`) ✅ **2026-09-17 (yêu cầu Tùng, sau CP3.9) — HỘP GIÁ TÁCH 2 HÀNG:** “số đánh giá của khách hàng nằm trên 1 DÒNG RIÊNG, phía dưới là phần giá”. DOM do WooCommerce in cố định `[.woocommerce-product-rating][.price][.cp-price-off]` (rating + price cùng prio 10 ⇒ không chèn được `<div>` bọc giữa) ⇒ đổi `.cp-price-box` từ **flex row → GRID 2 cột** (`minmax(0,1fr) auto`): hàng 1 = rating `grid-column: 1 / -1` (`display:flex` ⇒ sao + chữ cùng hàng, WC đặt `.star-rating` `float:right` nhưng flex bỏ qua float), hàng 2 = giá (cột 1) + `.cp-price-off` `justify-self:end` (cột 2). **Đo TRƯỚC → SAU (1440):** rating **159×54** (nằm NGANG hàng giá; chữ “(N đánh giá của khách hàng)” xuống 2 hàng vì chỉ được 159px) → **435×27** ở hàng riêng, dưới là giá 300×32 @+20,+51 + tag 123×26 @+332,+54 (cùng tâm y **351.6**); hộp giá **102 → 99px**; 1280: 355×27 · 1024: 403×27. **Căn sao ↔ chữ:** bỏ `margin-top` ~0.5em (**6.75px**) mà WooCommerce/theme đặt cho `.star-rating` ⇒ đo PIXEL 1440: tâm glyph sao **317 → 313** vs tâm chữ **314** = lệch **3px → 1px** (đo theo DOM: **0.0px**). **≤768px:** 1 cột — rating / giá / tag xếp dọc, tag canh TRÁI (đúng thứ tự yêu cầu). **SP chưa có đánh giá** (`/san-pham/gia-cua-nhua-han-quoc/`): hàng 1 tự biến mất ⇒ giá + tag lên hàng 1, hộp 475×64 (không hở khoảng trống). **Link “(N đánh giá của khách hàng)” bấm được ở cả 5 khổ** (`elementFromPoint` trả về chính `<a>`) → mở tab **“Đánh giá (6)”** ✓ · **tràn ngang 0** ở 1440/1280/1024/768/390 · smoke test **24/24**. **2026-09-17 (tiếp, yêu cầu Tùng) — `gap: 0` cho hàng số đánh giá:** `.woocommerce div.product .summary .cp-price-box .woocommerce-product-rating { gap: 0 }` (trước `8px`) ⇒ sao + chữ sát nhau: chữ dịch trái 8px (x 668.9 → **660.9**), hàng ở 390px **286×35 → 286×27** (chữ vừa 1 dòng nên hàng thấp hơn 8px), hàng 1440 vẫn 435×27, **lệch tâm sao↔chữ vẫn 0.0px**, link vẫn bấm được → tab “Đánh giá (6)” ✓. | ✅

### CP3.3

Đặt hàng nhanh — nút "Mua hàng" (chỉ trang chi tiết SP) mở popup form (họ tên / SĐT / địa chỉ / số lượng / ghi chú) → gửi AJAX tạo **đơn WooCommerce thật** (không phải CPT riêng). UI (popup + CSS `.cp-quick-order*` + JS) ở child theme; xử lý đơn (tạo order, gán billing/shipping, thông báo admin) ở plugin `pl-tien-ich-tungleads` (handler `tlpi_quick_order_handle()` — nonce + honeypot + rate-limit 5 đơn/IP/10 phút). JS tắt → nút vẫn là link `tel:` (progressive enhancement); đặt thành công → plugin trả về `get_checkout_order_received_url()` và JS chuyển trang sang trang hoàn tất đơn (CP3.6) | ✅

### CP3.4

Trang giỏ hàng (`/cart/`) — hero `.cp-pagehero` (breadcrumb + H1 "Giỏ hàng") + bọc nội dung shortcode trong `.cp-section > .cp-container.cp-cart` bằng filter `the_content` (KHÔNG copy template — `page.php` theme cha không có container, không in tiêu đề). Lưới 2 cột giỏ \| khối tổng tiền (grid đặt trên `div.woocommerce` vì shortcode bọc thêm wrapper này); skin bảng giỏ (card trắng, thumb 72px, nút xoá tròn, ô số lượng), coupon/actions, `.cart_totals` card + nút "Tiến hành thanh toán" — ⚠️ **tên class `.cp-cart-section` sinh ĐỘNG trong PHP** (`cp_woo_page_wrap()`: `'cp-section ' . $class . '-section'` với `$class = 'cp-cart'`; CP3.5 tương tự) ⇒ **grep tên class trong PHP sẽ KHÔNG thấy**: audit "CSS mồ côi" bắt buộc đối chiếu **DOM THẬT** của `/cart/` + `/checkout/` rồi mới được xoá (2026-09-15 đã xoá nhầm 2 rule rồi phục hồi nguyên văn — xem §2 WORKLOG) | ✅

### CP3.5

Trang thanh toán (`/checkout/`) — hero `.cp-pagehero` (breadcrumb + H1 "Thanh toán") + container qua filter `the_content` (dùng chung helper `cp_woo_page_wrap()` với CP3.4); lưới 2 cột `#customer_details` \| `#order_review`; skin form (input 44px, nhãn đậm), card đơn hàng, danh sách phương thức thanh toán, nút "Đặt hàng" accent; guard **loại trừ trang "đơn đã nhận"** (`is_order_received_page()`) | ✅

### CP3.6

Trang hoàn tất đơn hàng (`/checkout/order-received/`) — WooCommerce tách trang này khỏi `is_checkout()` nên trước đây không có container/hero (chữ sát mép). Bọc bằng cùng helper `cp_woo_page_wrap()` với guard mới `cp_is_order_received_page()`: khối "Cảm ơn" nổi bật + tóm tắt đơn 4 ô (mã đơn/ngày/tổng/phương thức) + card "Chi tiết đơn hàng" + nút "Về trang chủ". Đây cũng là đích chuyển trang sau khi đặt hàng nhanh (CP3.3) | ✅

### CP3.7

**Dải “Sản phẩm tương tự” ở trang chi tiết SP — 10 sản phẩm + nút cuộn ‹ ›** (yêu cầu Tùng 2026-09-16). **Số lượng:** WooCommerce mặc định **4** (`woocommerce_output_related_products()` → `posts_per_page = 4`) — đo TRƯỚC: dải `1400 / 1400px` ⇒ **không cuộn được, 0 nút**; nay lọc `woocommerce_output_related_products_args` → `posts_per_page = cp_related_per_page()` = **10** (đổi số bằng filter `cp_related_per_page`). 10 thẻ × 250px + gap 20 = **2680px > 1360px** ⇒ cuộn được **1280px**. **Nút ‹ ›: TÁI DÙNG NGUYÊN CP2.8** (`.cp-scroller` + `.cp-scroller__btn` + JS `assets/scroller.js`) — không viết lại CSS/JS; chỉ mở rộng `LIST_SEL` của JS thêm `ul.products` (KHÔNG gắn class `.cp-products` cho `ul.products`: `.cp-products` là GRID 4 cột, gắn vào phải đấu specificity với `.woocommerce .cp-related-band ul.products` đang set flex + cuộn ngang). **Cách bọc KHÔNG copy template `single-product/related.php`:** cờ `cp_related_scroller_flag( true/false )` bật quanh ĐÚNG lệnh `woocommerce_output_related_products()` + 2 filter `woocommerce_product_loop_start` / `woocommerce_product_loop_end` (chèn `cp_scroller_open_markup()` trước `<ul>` và `cp_scroller_close_markup()` sau `</ul>`) — **KHÔNG thay cờ bằng `is_product()`** vì `loop_start` chạy cho MỌI vòng lặp sản phẩm (shop, shortcode, upsell…). Helper CP2.8 được tách 2 lớp `_markup()` (TRẢ chuỗi) + hàm in (giữ nguyên tên cũ cho 4 template-part) vì filter BẮT BUỘC trả chuỗi, không được `echo`. CSS: ẩn thanh trượt dải bằng 3 thuộc tính (`scrollbar-width` + `-ms-overflow-style` + `::-webkit-scrollbar`). **Số đo Playwright (2026-09-16, 1440, `/san-pham/bang-gia-cua-nhua-pvc-cao-cap/`):** 10 SP (250×448/thẻ), dải `1400 / 2680` ⇒ cuộn **1280px**; đầu dải `is-start` → nút ‹ `opacity: 0` + `pointer-events: none` (ẩn có chủ đích theo CP2.8), nút › hiện; bấm NEXT **5 lần**: `0 → 270 → 540 → 810 → 1080 → 1280` (đúng 1 thẻ + gap) → `is-end` + **SP cuối nằm trọn trong khung**; PREV 5 lần về `0`, `is-start`, SP đầu trong khung; `elementFromPoint` (đã cuộn dải vào khung): › **bấm được**, ‹ bấm được khi đã rời đầu; 2 nút cách mép **2px**, **không chồng FAB**. **Hồi quy:** `/san-pham/` = 20 SP lưới 4 cột, **0** `.cp-scroller` / **0** nút (nút KHÔNG lây sang vòng lặp khác); 3 dải cuộn trang chủ vẫn chạy (#1 `--static` ẩn nút do không cuộn được — đúng thiết kế, #2 và #3 `0 → 270` OK); 3 SP khác đều 10 SP / cuộn 1280px. **Mobile:** @768/390 cuộn 1952/2330px, nút **34×34** cách mép **5px**, bấm `0 → 270` OK, **`scrollWidth == innerWidth`**. 0 PHP notice. | ✅

### CP3.8

**Thu gọn tab “Mô tả” ở trang chi tiết SP** — nút **Xem thêm / Thu gọn** (yêu cầu Tùng 2026-09-16). **Đo TRƯỚC:** panel `#tab-description` cao **5229px** (nội dung **6430 ký tự / 57 `<p>` / 0 ảnh**) — “bức tường chữ” ngay dưới thanh tabs, **không có nút thu gọn**; mô tả NGẮN ở cột phải đã có nút từ **CP3.2** (`.cp-short-desc`) nên chỗ thiếu đúng là tab này. **Cách làm — KHÔNG copy template:** chỉ đổi `callback` trong filter `woocommerce_product_tabs` (`description` → `cp_single_description_tab()`), bên trong đệm `the_content()` qua `ob_start()` y như callback gốc của WooCommerce (shortcode/page-builder trong mô tả vẫn chạy) rồi bọc bằng khối **`.cp-shop-desc` của CP3.1** (thanh nút viên + gradient mờ dần + class `is-open` — **không viết CSS/JS mới**, chỉ thêm modifier `.cp-single-desc`). JS inline theo đúng mẫu CP3.2: **ẩn nút nếu nội dung vốn ngắn hơn ngưỡng thu gọn** (`scrollHeight <= clientHeight + 4`) ⇒ sản phẩm mô tả ngắn không hiện nút thừa. CSS modifier: bỏ `margin-top: 36px` + `border-top` + padding của khối dưới lưới danh mục; **giữ typography của panel tab** (`font-size/line-height/color: inherit` — không để body ăn 14.5px/1.7 xám của CP3.1); ngưỡng thu gọn **24em** (không dùng 12em của CP3.1 — quá ngắn với mô tả dài). **BẪY specificity (đã dính + đã sửa):** rule `.cp-single-tabs .cp-single-desc .cp-shop-desc__body` (0,3,0) nằm **SAU** rule gốc `.cp-shop-desc.is-open .cp-shop-desc__body` (cũng 0,3,0) của CP3.1 ⇒ trạng thái mở chạy nhưng `max-height: none` KHÔNG thắng → đo được: bấm “Xem thêm” nhãn + `aria-expanded` đổi đúng mà panel **vẫn 499px**; phải nâng selector mở lên **(0,4,0)** `.cp-single-tabs .cp-single-desc.is-open .cp-shop-desc__body`. **Số đo Playwright (2026-09-16, 1440, `/san-pham/bang-gia-cua-nhua-pvc-cao-cap/`):** thu gọn **499px** (giảm **90%** so 5229) → bấm Xem thêm **5387px** (body client 5272 = đủ nội dung), bấm Thu gọn về **499px**, `aria-expanded` `false → true → false`, nhãn `Xem thêm ⇄ Thu gọn`; `elementFromPoint` = **bấm được**; mobile 390 **499 → 8801px**, `overflow = 0`, nút cao 49px. **Ca biên:** SP mô tả 74 ký tự → **nút `hidden`** (body 115/115); SP có **2 tab** (mô tả + thông tin bổ sung) → đổi tab vẫn đúng, quay lại Mô tả vẫn 499px và mở rộng 4229px; 3 SP khác đều 499px + nội dung 1685–3825px. 0 PHP notice. | ✅

### CP3.9

**Đánh giá & bình luận ở trang chi tiết SP** (yêu cầu Tùng 2026-09-17, “layout demo tham khảo banhocthongminhbsuc.com”): tab **“Đánh giá”** có **hộp tổng kết** (điểm TB số lớn + “Đánh giá trung bình” + `.star-rating` + “N đánh giá”) · **5 thanh %** theo số sao (5→1: số sao + thanh vàng `--cp-primary` + % + số lượt) · nút **“Đánh giá ngay”** (cuộn mượt tới form, focus vào ô chọn sao) · **thanh lọc chip** “Tất cả / 5★ / 4★ / 3★ / 2★ / 1★” kèm số lượng · **danh sách** mỗi đánh giá 1 thẻ (sao · tên · badge **“(Đã mua tại caophat.vn)”** · ngày `dd/mm/yyyy` · nội dung) · **form** tiếng Việt theo demo (placeholder “Họ tên *”, “Email *”, “Mời bạn chia sẻ thêm một số cảm nhận…”, nhãn sao “Bạn cảm thấy thế nào về sản phẩm? (Chọn sao)”, nút **“Gửi đánh giá”** đen bo tròn, ghi chú duyệt). | `inc/woocommerce.php` (tag `// CP3.9`: filter `woocommerce_product_tabs` prio **21** đổi callback ⇒ `cp_single_reviews_tab()` + `cp_single_reviews_summary()` + `cp_single_reviews_thanks()`; filter `woocommerce_product_review_comment_form_args` prio 20; filter `comment_class` gắn `cp-rv-star-N`; filter `gettext` đổi badge; filter `get_comment_date` đổi ngày), `assets/reviews.js` (**mới**, enqueue chỉ khi `is_product()` trong `functions.php` prio 20), `assets/caophat.css` (khối CP3.9 cuối file) | **GIỮ NGUYÊN core** (không copy template `single-product-reviews.php`): `comments_template()` vẫn render danh sách + form + cơ chế duyệt; chỉ in thêm tiêu đề/hộp tổng kết/chip phía trên và ẩn tiêu đề trùng `.woocommerce-Reviews-title` (thứ tự demo: tiêu đề → tổng kết → danh sách). ⚠️ **Phải bật 2 option mới có tab**: `woocommerce_enable_reviews = yes` (trước là `no` ⇒ đếm 0 tab trên cả 363 SP) và `comment_registration = 0` (khách để lại đánh giá không cần đăng nhập, như demo) — **PHẢI đặt lại trên production khi deploy**. ✅ **ĐÃ SỬA 2026-09-17 — “có SP hiện, có SP KHÔNG hiện tab đánh giá”:** nguyên nhân là **46/363 sản phẩm có `comment_status = closed`** (đo: 317 `open`) — WC chỉ thêm tab khi `comments_open()` của TỪNG SP (`wc-template-functions.php`) nên **không chỉ phụ thuộc option toàn cục**; đã mở khoá cả 46 + đặt `default_comment_status = open` (SP MỚI tự bật đánh giá) ⇒ **363/363 `open`**, `comments_open()` = TRUE toàn bộ (kiểm 5949 → “Đánh giá (0)”, 4816 → “Đánh giá (6)”). **Production:** `wp eval-file docs/prod-open-product-reviews.php` rồi `… go` (đã thêm vào `deploy-caophat.sh` bước 5b). **Kiểm 1 SP bất kỳ:** Sản phẩm → tab “Dữ liệu sản phẩm” → **“Nâng cao” → tick “Bật đánh giá”** (= `comment_status`); hoặc `wp eval 'echo comments_open(<ID>) ? "MO" : "KHOA";'`. **Bẫy đã dính (đo 2026-09-17):** (1) WooCommerce's JS **ẩN `<select id="rating">`** rồi dựng widget `p.stars` (5 `<a role="radio">` dùng font icon WC) ⇒ CSS phải style `p.stars a::before` (đo màu `rgb(226,165,58)` sau khi sửa) chứ không style select; (2) nút `#submit` bị `.woocommerce #respond input#submit` (2 id) đè ⇒ nền thành `rgb(233,230,237)` + bo góc 3px ⇒ phải `!important`; (3) đánh giá CHỜ DUYỆT đã có sẵn dòng của WC `<em class="woocommerce-review__awaiting-approval">` ⇒ **không in thêm** (bản đầu in trùng 2 dòng). **Số đo (Playwright, SP 4816 `cua-nhua-han-quoc-kos-2`, 5 đánh giá mẫu: 3×5★ · 1×4★ · 1×3★, avg 4.40):** TRƯỚC (core thô) `#reviews` **525px**, **0 `.star-rating`**, 0 `<li>`; SAU — 1440: hộp `.cp-rv` **986×1561**, tổng kết **986×187** (điểm 4.4 cỡ 44px), thanh 5★ = **197/328 = 60%** ✓ khớp `style=width:60%`, 6 chip (`Tất cả 5` active + 5 mức), 5 `<li>` (mỗi thẻ 114px, badge “(Đã mua tại caophat.vn)” ở 3 bài có meta `verified`, ngày `08/09/2026`), form **940×564**, nút gửi **161×48 nền `rgb(0,0,0)`**, nút CTA **153×53**; 1280: hộp 826×1611 · 1024: tổng kết xuống **2 cột** (256px, CTA sang hàng dưới) · 768: form 1 cột (Tên/Email xếp dọc) · 390: hộp **314×2444**, tổng kết **1 cột** 386px, nút CTA 137×49. **Không tràn ngang ở cả 5 khổ** (`scrollWidth = innerWidth`). **Tương tác:** chip 4★ → còn **1** `<li>` hiện (4 bài kia `is-hidden`), chip 2★ → **0** + dòng “Không có đánh giá nào ở mức 2 sao.”, “Tất cả” → **5** ✓ (kèm `aria-pressed`); nút “Đánh giá ngay” → `scrollY 0 → 1935` + focus vào `a.star-1` (`role=radio`) ✓. **E2E gửi đánh giá (khách vãng lai):** tạo comment `comment_approved = 0` ✓ (đúng cơ chế `comment_previously_approved = 1`), chuyển hướng `?unapproved=8808&moderation-hash=…#comment-8808`, dải xác nhận **986×52** “Cảm ơn bạn! Đánh giá đang chờ duyệt…” ✓, chip `awaiting-approval` chỉ khách đó thấy ✓; đã xoá comment test 8808, số về 5/avg 4.40. **Hồi quy:** smoke test **24/24** (12 trang × 1440/390) 0 lỗi PHP, 0 tràn; `php -l` + `node --check` sạch. **Trạng thái SP CHƯA có đánh giá (đa số SP — đo `/san-pham/gia-cua-nhua-han-quoc/`):** tab vẫn có `Đánh giá (0)`, `.cp-rv--empty` + tiêu đề **“Chưa có đánh giá nào”** + gợi ý “Hãy là người đầu tiên đánh giá sản phẩm này.”, điểm **0.0 · 0 đánh giá**, đủ 5 thanh nhưng `width:0%`, **0 chip lọc** (ẩn cả thanh lọc), dòng “Chưa có đánh giá nào.” của core bị ẩn (`display: none`), form vẫn đủ (5 sao + 3 ô + nút “Gửi đánh giá”), panel **935px** (1440) / **1366px** (390), không tràn ngang. | ✅

### CP8

**Mục lục nội dung** (yêu cầu Tùng 2026-09-17, layout demo `tungleads.com/khoa-hoc/khoa-hoc-quang-cao-google-ads-thuc-chien`) — **nút DỌC cố định ở mép màn hình** (“Mục lục nội dung”, chữ `writing-mode: vertical-rl`) mở **drawer 340px** liệt kê heading của bài; **đánh số `1 · 2 · 2.1`**; bấm 1 mục → cuộn tới heading **có bù header sticky**; cuộn trang → mục đang xem **tô nền**; đóng bằng ✕ / `Esc` / bấm ra ngoài; drawer `inert` khi đóng. **TOÀN BỘ Ở PLUGIN `pl-tien-ich-tungleads` v0.4.0 — child theme KHÔNG đổi file nào:** `includes/toc.php` (mới) + `assets/toc.css`/`assets/toc.js` (mới) + `pl-tien-ich-tungleads.php` (`TLPI_FILE/DIR/URL/VERSION`, `require_once`, version 0.4.0) + `require` UI `tlpi_toc_settings_ui()` ở cuối `tlpi_support_page()`. **Option `tlpi_toc` — 11 khoá, sửa ở Settings → Cao Phát, MẶC ĐỊNH TẮT:** bật/tắt · post type (mặc định `post`+`page`) · số cấp heading (H2 / H2+H3 / H2+H3+H4) · **số heading tối thiểu** (bài ngắn không hiện; không đủ thì trả nội dung NGUYÊN BẢN, không thêm `id` nào) · nhãn nút · mép trái/phải · màu nút (để trống = `--cp-accent` của theme) · hiện trên mobile ≤768px · scroll-spy · đánh số · danh sách ID loại trừ. **Cách chạy:** filter `the_content` **prio 12** (sau `do_shortcode` prio 11) → regex quét `<h2>/<h3>/<h4>`, **thêm `id`** còn thiếu bằng `sanitize_title()` (bỏ dấu tiếng Việt, trùng → `-2`) + số thứ tự; markup in ở **`wp_footer` prio 5**; CSS/JS nạp ở `wp_enqueue_scripts` prio 30 **chỉ khi post type đã bật** (SP không bật ⇒ KHÔNG nạp cả CSS/JS). **Số đo (Playwright, 2026-09-17, bài `/bao-gia-cua-nhua-gia-re-tphcm/` + trang `/chinh-sach/chinh-sach-doi-tra-hang/`):** nút **35×160** mép phải nền `rgb(200,71,31)`; drawer **340×900** `z-index 90` (nút 45 — dưới header sticky 50, mega 60); **14 mục** / **5 mục**, 100% heading trong nội dung được gắn `id`; **bấm mục → heading dừng đúng 14px dưới header sticky** ở 1440 (91−77) và 390 (79−65); spy `is-active` + `aria-current` nền `color(srgb .974 .913 .895)` (12% `--cp-accent` qua `color-mix`); `min=10` ⇒ ẩn · `exclude=5637` ⇒ ẩn riêng bài đó · `mobile=off` ⇒ `display: none` ≤768; **tràn ngang 0**; E2E Settings (đổi `side=left`, tắt đánh số, màu `#0068ff`) → front-end đổi đúng (`rgb(0,104,255)`, 0 số). ⚠️ **2 BẪY:** ① **`wp_footer` prio 5, KHÔNG 20** — `wp_print_footer_scripts` của core cùng prio 20 ⇒ markup prio 20 ra SAU thẻ `<script>`, JS chạy lúc chưa có nút ⇒ bấm không mở (`aria-expanded` đứng `false`); ② **cuộn phải DÒ LẠI** — ảnh `loading="lazy"` làm trang cao thêm (cuộn đúng 5493px nhưng `scrollHeight` 43469 → 46010 ⇒ hụt **1565px**), JS bám đích ~3s + tự huỷ khi người dùng tự cuộn. Chi tiết + bẫy: `README.md` plugin · `.ai/FEATURE_MAP.md` mục CP8 · script đo `docs/measure/toc-*.mjs`. **v0.4.1 (cùng ngày, yêu cầu Tùng “thêm tuỳ chọn mục lục hiển thị trong phần nội dung bài viết”) — 2 CÁCH HIỂN THỊ, bật/tắt ĐỘC LẬP:** `float` (nút dọc + drawer, mặc định ON) và **`inline` = KHỐI MỤC LỤC NGAY TRONG NỘI DUNG** (`tlpi_toc_inline_html()` → `<details class="tlpi-toc-inline">` + `<summary>` = nhãn, dùng thẻ HTML gốc nên **mở/thu được cả khi tắt JS**; `tlpi_toc_inline_insert()` chèn theo `inline_pos` = `top`/`p1` sau đoạn mở đầu; `inline_open` = mở sẵn). **Đo:** khối trong bài **990×226 @y320 (top)** / **990×616 @y507 (p1, dưới đoạn mở đầu)** / thu gọn **60px**; viền trái 4px + số màu `rgb(255,163,5)`; 7 biến thể đúng — `float_off` (chỉ còn khối, không có nút) · `inline_off` (chỉ còn drawer) · **`both_off` ⇒ KHÔNG nạp cả CSS/JS** · `collapse` (open=false → bấm tiêu đề mới mở) · `p1` · `top` · `depth3`; bấm mục trong khối → heading dừng **14px dưới header** (91−77) như drawer ⇒ **tràn ngang 0**, smoke 24/24. ⚠️ **BẪY THÊM:** ① **màu ở Settings phải truyền vào khối inline** bằng `style="--tlpi-toc-color:…"` (khối nằm trong `the_content`, KHÔNG trong wrapper `.tlpi-toc` nơi biến này được gắn ⇒ thiếu là lệch màu); ② **cuộn quãng DÀI phải NHẢY THẲNG, không cuộn mượt** — ảnh `loading="lazy"` nạp dần khi cuộn mượt làm trang cao thêm liên tục: đo được đích **36.567px bị đẩy tới 78.599px**, 12 lần dò trong 3s vẫn hụt ⇒ quy tắc `delta > 1,5 × chiều cao khung nhìn` thì dùng `behavior:'auto'` (khớp sau 1–2 lần chỉnh), quãng ngắn vẫn mượt; ③ **`html { scroll-padding-top: var(--tlpi-scroll-pad) }`** (JS ghi biến) để DEEP LINK `…#ten-muc` của Google “jump to” cũng dừng dưới header sticky. | ✅

### CP3.10

**Đổi đường dẫn đăng nhập + ẩn wp-admin (plugin `pl-tien-ich-tungleads` v1.1.0, yêu cầu Tùng 2026-09-17)** — option `tlpi_login` (`on` · `slug` · `old_path` = `404`/`home`), cấu hình **Settings → PL Tiện Ích → “Đường dẫn đăng nhập”**. Hook `init` prio 1: vào ĐÚNG slug ⇒ `require_once ABSPATH.'wp-login.php'` (mọi action core chạy y như trước: login · logout · lostpassword · resetpass · postpass), vào đường dẫn CŨ với KHÁCH ⇒ **404 tự chứa** (`X-Robots-Tag: noindex`) hoặc 302 về trang chủ; người ĐÃ đăng nhập vẫn dùng `/wp-admin/` bình thường (không đổi `admin_url()`); filter `site_url`/`network_site_url`/`wp_redirect` đổi link WP sinh ra. **KHÔNG chặn**: admin-ajax · admin-post · load-styles/scripts · `/wp-admin/(css\|js\|images\|fonts)/` · REST · cron · XML-RPC · wp-cli. **Cần permalink đẹp** (đang “Mặc định” ⇒ tự TẮT + cảnh báo đỏ); slug dễ đoán (admin/login…) bị chặn; tracking tự bỏ qua trang đăng nhập. Cứu hộ: `wp option delete tlpi_login`. Đo: `docs/measure/login-path-e2e.mjs` + `login-assets-check.mjs`. **Local: TẮT nhưng đã điền sẵn slug `dang-nhap-quan-tri`** (tick 1 ô là bật).

### CP3.11

**Xác minh 2 lớp (2FA) bằng APP XÁC THỰC — TOTP (plugin v1.2.0, Tùng chốt phương án B: app thay vì email, 2026-09-17)** — option `tlpi_2fa` (`on` · `roles=[administrator]` · `remember`=30 ngày nhớ thiết bị) ở **Settings → PL Tiện Ích → “Xác minh 2 lớp (2FA)”**; **TOTP RFC 6238 tự viết** (±30s cho đồng hồ lệch), chặn ở `authenticate` prio 99 (mật khẩu đúng vẫn **KHÔNG có phiên**) → `wp_login_failed` chuyển sang `wp-login.php?action=tlpi_totp&token=…` (hook `login_form_tlpi_totp`, màn hình dùng `login_header()`/`login_footer()` của core) → mã 6 số hoặc **mã dự phòng** ⇒ `wp_set_auth_cookie()` + `do_action('wp_login')`; **tự ghi danh** khi tài khoản bắt buộc chưa có mã bí mật (mã Base32 32 ký tự + link `otpauth://` + **10 mã dự phòng in MỘT LẦN**, lưu hash, mỗi mã dùng 1 lần); cookie “nhớ thiết bị” HMAC buộc với user + mã bí mật (đặt lại 2FA ⇒ mọi thiết bị cũ hết hiệu lực); tối đa **5 lần nhập sai** ⇒ huỷ phiên; mã bí mật lưu **mã hoá sodium secretbox** (khoá từ `AUTH_KEY`+`SECURE_AUTH_SALT`); trang **Hồ sơ** có trạng thái + “Đặt lại 2FA” + “Tạo lại 10 mã dự phòng” + ô **tự nguyện bật** (meta `_tlpi_totp_want`). Cứu hộ: `wp user meta delete <ID> _tlpi_totp_secret`. Đo: `docs/measure/2fa-e2e.mjs` + `2fa-api-check.sh` (TOTP đối chiếu **vector chuẩn RFC 6238**; đã vá lỗ hổng bỏ qua bằng POST `tlpi_done` ⇒ cờ `codes_ready`). **Local: TẮT** — bật thì mọi script đo có đăng nhập sẽ không chạy được (phải qua 2 bước).

### CP3.12

**Ô “Miêu tả” của DANH MỤC SẢN PHẨM dùng TRÌNH SOẠN THẢO ĐẦY ĐỦ như trang thêm bài viết** (Tùng 2026-09-18) — `inc/admin-editor.php` + `assets/admin-term-editor.js` (**mới**), require ở `functions.php`, khối CSS cuối `assets/caophat.css`. Đo TRƯỚC khi làm: core vẽ `<textarea>` thuần ở 2 màn (`edit-tags.php:521` id `tag-description` · `edit-tag-form.php:205` id `description`) và 2 màn này **không nạp TinyMCE** (`typeof tinymce === 'undefined'`, `mceInit = []`); trang bài viết/sản phẩm ở site này là **editor CỔ ĐIỂN** (`#postdivrich`, không có block editor) ⇒ dùng `wp_editor()` là ra đúng bộ nút (kể cả nút của TinyMCE Advanced). **3 cái bẫy đã dính & đã xử lý:** (1) `wp-admin/js/tags.js` ở màn THÊM **không submit form** mà bắt **click `#submit`** rồi `$('#addtag').serialize()` ⇒ phải gọi `tinymce.triggerSave()` ở **pha capture trên `document`** cho cả `click` lẫn `submit` (chỉ bắt `submit` ⇒ lưu xong mô tả **rỗng**); (2) core gắn `wp_filter_kses` vào `pre_term_description` = chỉ cho thẻ INLINE ⇒ **bóc sạch `<p>/<ul>/<h3>`** khi lưu (đo: `<p>A</p><ul><li>B</li></ul><strong>C</strong>` → `AB<strong>C</strong>`) ⇒ đổi sang `wp_kses_post` (đúng bộ thẻ bài viết, vẫn chặn `<script>`); (3) màn SỬA core cũng dùng **`id="description"`** ⇒ editor của mình phải đặt **id riêng `cp-description`** (gửi dữ liệu do `name` quyết định ⇒ vẫn `name="description"`), nếu để trùng id thì TinyMCE init nhầm vào ô core đang ẩn/disabled ⇒ editor rỗng, không gõ được. Field core bị ẩn `.term-description-wrap{display:none}` + `disabled` bằng JS ⇒ **chỉ 1 field `description`** được gửi; ô mới dùng class `cp-term-editor-wrap` (KHÔNG dùng lại class của core kẻo bị CSS ẩn luôn). **Đo (Playwright + wp-cli, 2026-09-18):** cấu hình editor **giống hệt** bài viết (`toolbar1`/`toolbar2`/`plugins`/`block_formats` — khác duy nhất nút `dfw` vì WP chỉ thêm nút đó cho editor `content`); màn THÊM: payload AJAX `add-tag` chứa `<p>…</p>` ✓ + DB lưu đúng HTML ✓; màn SỬA: bấm “Cập nhật” ⇒ DB có `<ul><li>…</li></ul>` ✓; đặt nội dung rồi phát sự kiện `submit` ⇒ textarea nhận `<p>…<ul>…` ✓; front-end `/danh-muc/<cat>/`: `.cp-shop-desc__body` in ra `<p> <strong> <h3> <ul> <li> <a>` (không bị escape) ✓, `list-style: disc` + `padding-left: 22px` + `h3 18px`, **không tràn ngang** @1440 & @390 ✓; smoke **24/24**; `php -l`/`node --check` sạch. Script đo: `docs/measure/term-editor-check.mjs`.

### CP5.1

**Trang danh mục tin tức** (lưu trữ chuyên mục bài viết) — override `category.php` trong child, **CHỈ cho category** (tag / tác giả / ngày vẫn dùng `archive.php` của theme cha). Khối hiển thị nằm ở `inc/news.php` (tag `// CP5.1`): `cp_news_breadcrumb()` — breadcrumb tự viết "Trang chủ / … cha / chuyên mục" (KHÔNG dùng `woocommerce_breadcrumb()`: trang tin không thuộc WooCommerce), dấu ngăn `.cp-breadcrumb__sep`; `cp_news_card()` — thẻ bài viết `.cp-news-card` (ảnh 300px bên trái + nhãn chuyên mục + ngày + tiêu đề + trích đoạn 32 từ), bài ĐẦU TIÊN của trang 1 thêm `.cp-news-card--featured` (ảnh trên / chữ dưới, ảnh size `large`); `cp_news_sidebar()` — 3 hộp `.cp-side-box` tái dùng CSS CP3.2: "Chuyên mục" (`wp_list_categories` + `.cp-side-cats`, mục đang xem là `.current-cat`), "Tin mới nhất" 5 bài (`.cp-side-news` + class mới `.cp-side-news__date`), "Hỗ trợ trực tuyến" (`cp_single_support_box()` của CP3.2); `cp_news_pagination()` — `the_posts_pagination()` bọc trong `.cp-pagination`, mũi tên ‹ › + `.screen-reader-text` "Trang trước/sau"; `cp_news_desc()` — mô tả chuyên mục render DƯỚI danh sách, thu gọn sẵn (dùng lại class thu gọn `.cp-shop-desc` của CP3.1 + `.cp-news-desc`); `cp_news_archive_url()` — link "Xem tất cả →" ở khối tin trang chủ trước đây trỏ về trang chủ (`page_for_posts` = 0) → nay fallback chuyên mục `tin-tuc`, ghi đè bằng filter `cp_news_archive_url`. Lưới `.cp-news-layout` = `minmax(0,1fr)` / `320px`, **sidebar KHÔNG sticky** (khác `.cp-shop-aside` / `.cp-single-side`): đo cột phải cao **1088.7px** > viewport 900px → `position: sticky` sẽ chốt đỉnh và cắt mất đáy, không cuộn tới được. **Số đo Playwright (2026-09-15):** 1440 → lưới `1048/320`, thẻ thường 1046×259 (ảnh 300×225, tiêu đề 20px), thẻ đầu `.cp-news-card--featured` 1048×640 (ảnh 1046×448, tiêu đề 27px); 1100 → `734/300`, thẻ ngang ảnh 260px; 1024 → 1 cột + sidebar 2 hộp/hàng (aside nằm DƯỚI main); 768 → thẻ 1 cột (ảnh 726×311); 390 → 350px. **Không tràn ngang ở cả 6 mức** (`scrollWidth == innerWidth`). `elementFromPoint` tại tâm 6 phần tử (tiêu đề + ảnh thẻ, nút phân trang, 3 link sidebar) → đúng phần tử, không bị đè. Bấm "2" → `/chuyen-muc/tin-tuc/page/2/` (10 bài, KHÔNG còn thẻ featured, current = 2); bấm tiêu đề → bài viết; bấm "Báo giá" → `/chuyen-muc/bao-gia/` + `current-cat`. **2 bẫy đã dính + đã sửa:** (1) **specificity** — bản đầu viết `@media (max-width:1200px) { .cp-news-card:not(.cp-news-card--featured) {…} }` = (0,2,0) → ĐÈ rule 1-cột của `@media (max-width:768px)` (0,1,0) → đo ở 390px: cột chữ co còn **38px**, `scrollWidth` **457 > 390** (tràn 67px); sửa bằng `.cp-news-card--featured` cùng (0,1,0) đặt SAU trong cùng media query — KHÔNG dùng `:not()`; (2) **`max-height` + `aspect-ratio`** — `max-height: 460px` trên ảnh featured làm `aspect-ratio` tính ngược ra bề ngang → box còn **817.8px** trong thẻ 1046px (hụt 228px, ảnh không full thẻ) → bỏ `max-height`, dùng `aspect-ratio: 21 / 9`. Ảnh: khai `sizes` tường minh (thẻ thường 300px, featured 1040px, thumb sidebar 64px) vì fallback mặc định 500px làm Chrome chọn ứng viên ~533w. **Tồn tại ở LOCAL (KHÔNG do CP5.1):** 3/12 ảnh thumb hỏng (`naturalWidth = 0`) vì metadata trỏ `-533x400.webp` / `-600x375.webp` mà **file không có trong `uploads/`** (đã `ls` trong container) và theme/plugin **không** sinh `.webp` (grep = 0); các size 286x400 / 533x400 / 600x375 / 1067x800 không có trong `get_intermediate_image_sizes()` → metadata sót từ môi trường khác ⇒ cần `wp media regenerate` (local) + đối chiếu production — thuộc phần bàn giao production | ✅

### CP5.2

**Trang chi tiết tin tức** (bài viết — post type `post`) — override `single.php` trong child, **chỉ cho `post`** (`single-dichvu.php` của theme cha vẫn giữ cho CPT dịch vụ). Khối hiển thị thêm ở `inc/news.php` (tag `// CP5.2`): `cp_news_single_breadcrumb()` (Trang chủ / chuyên mục (link) / tiêu đề rút gọn), `cp_news_reading_time()`, `cp_news_article_meta()`, `cp_news_article_tags()` (chip tag), `cp_news_article_nav()` (bài trước / bài sau), `cp_news_related()` (cùng chuyên mục, 4 bài). **Tái dùng CP5.1**: `cp_news_sidebar()` + lưới `.cp-news-layout` / `.cp-news-main` / `.cp-news-aside` (không viết lại CSS lưới → responsive ≤1024/≤768 dùng lại). Nội dung bài: typography `.cp-article__content` (h2 gạch vàng, h3, ul/ol, blockquote, table, ảnh bo góc, figcaption). **KHÔNG in khối bình luận** (`.tl-comments` của theme cha): bỏ `comments_template()` trong `single.php` theo yêu cầu Tùng 2026-09-15 (ẩn mục "Để lại một bình luận"); kiểm DB trước khi bỏ — cả 7 comment toàn site đều là ghi chú đơn hàng gắn `shop_order` đã xoá, **không bài tin nào có bình luận thật** nên không giấu mất nội dung; bỏ truy vấn (không ẩn bằng CSS); các template khác không đổi. **Số đo Playwright (2026-09-15, bài "Kích thước cửa phòng ngủ"):** breadcrumb `Trang chủ/Tin tức/Kích thước cửa phòng ngủ`; H1 34px; meta `Tin tức · Tháng 9 29, 2024 · Ngọc Diễn · 7 phút đọc`; `.cp-article` 1048×9828, `.cp-article__content` 990px, font 16.5px/30.5px; `h2` 26px + `border-bottom` 2px; `ul` → **`list-style: disc` + `padding-left: 24px`** (đã đè reset); link trong bài accent `rgb(200,71,31)` + underline; 34 chip tag 129×32; `.cp-article-nav` 2 cột `516px 516px` (bài mới nhất chỉ có "Bài trước" → 516 nửa hàng; bài giữa: 2 thẻ `x=20`/`x=552`, "Bài sau" `text-align: right`); dải liên quan nền `rgb(246,244,241)`, 4 thẻ mini 335px, cột **4 → 3 (≤1200) → 2 (≤1024) → 1 (≤600)**; sidebar 3 hộp; **không có khối bình luận** (`#comments` = 0 phần tử, đã kiểm bằng `curl | grep -c`); 0 PHP notice. **6/6 mức bề rộng `scrollWidth == innerWidth`.** Click đã kiểm: breadcrumb → `/chuyen-muc/tin-tuc/` · chip tag → `/the/cua-composite/` · thẻ mini liên quan → bài · "Bài trước" → bài trước · link trong nội dung → danh mục SP (`elementFromPoint` tại **dòng đầu** của link trả về chính `<a>`). **Bẫy 1 — tràn ngang do DỮ LIỆU bài:** bài mẫu chứa `<span style="font-size:100%">====…</span>` 73 ký tự "=" không có khoảng trắng → span rộng **731.8px**, `scrollWidth` **769 > 390** (tràn ở cả 768 và 390) → sửa bằng `overflow-wrap: break-word` trên `.cp-article__content` (chỉ cắt khi từ không vừa dòng). **Bẫy 2 — ảnh đầu bài là ảnh DỌC:** 992×1388 → nếu `width: 100%` thì cao **1388px**, `max-height` + `width: 100%` thì hộp vẫn 990px và lộ 2 dải nền kem, `object-fit: cover` thì mất nội dung → dùng `max-height: 620px` + `width: auto` + `margin-inline: auto` → **443×620, ratioBox 0.714 = ratioNat 0.715** (không méo, không crop; ảnh ngang 4:3 còn 827×620). **Bẫy 3 — `auto-fit` cho điều hướng lẻ:** `repeat(auto-fit, minmax(240px,1fr))` làm mục lẻ giãn hết **1048×80.9** → đổi sang `repeat(2, minmax(0,1fr))` + `.cp-article-nav__item--next:only-child { grid-column: 2 }`, ≤600px về 1 cột và trả `grid-column: auto`. **`str_word_count()` KHÔNG dùng được cho tiếng Việt** (chỉ đếm A–Z, bỏ qua chữ có dấu) → `preg_split( '/\s+/u' )`. | ✅

### CP5.3

**Trang THẺ (tag)** — lưu trữ theo tag `/the/<slug>/` (option `tag_base` = `the`). **Dùng CHUNG thân trang với CP5.1:** `template-parts/news-archive.php` giữ toàn bộ hero + lưới 2 cột; `category.php` và `tag.php` chỉ còn `get_header()` → `get_template_part( 'template-parts/news-archive' )` → `get_footer()`; nhánh chuyên mục/thẻ suy từ `get_queried_object()` **trong partial** (không truyền `$args` ⇒ không thể truyền sai nhánh) ⇒ **không copy template**, sửa 1 chỗ ăn cả 2 trang. Khác biệt của thẻ: (1) `cp_news_breadcrumb()` rẽ nhánh SỚM cho `post_tag` → **"Trang chủ / Tin tức / Từ khoá: \<thẻ\>"**, mục "Tin tức" chỉ chèn khi `cp_news_archive_url()` ≠ trang chủ (nếu không sẽ có 2 crumb cùng trỏ `/`); (2) câu rỗng **"Thẻ này chưa có bài viết nào."**; (3) cột phải thêm hộp **"Từ khoá phổ biến"** — `cp_news_tags_box()`: site có **483 thẻ, 354 thẻ chỉ gắn 1 bài** nên KHÔNG dùng `wp_tag_cloud()` (đổ hết là rác, loãng link nội bộ) → `get_terms( orderby=count DESC, number=30, exclude=thẻ đang xem )` rồi **lọc `count >= 2`** và cắt **12** (filter `cp_news_tags_box_number`), chip tái dùng `.cp-article__tag` của CP5.2 + số bài. **Số đo Playwright (2026-09-16, 1440):** TRƯỚC — `main.tl-main.tl-container` (không `.cp-container`), **0** `.cp-news-card` / **10** `.tl-post-card` (không ảnh), không hero/breadcrumb/cột phải, H1 do WP in "Thẻ: cửa nhựa giả gỗ"; SAU — hero `.cp-pagehero` **1440×143**, `.cp-news-layout` **1400×3245**, cột phải **320×1669** với **4 hộp** (Chuyên mục · Tin mới nhất · Hỗ trợ trực tuyến · Từ khoá phổ biến), **10** `.cp-news-card` (thẻ đầu 1048×640), phân trang 1048×42, H1 = tên thẻ; trang 2 → 2 bài; thẻ rỗng (`/the/bao-gia-cac-mau-cua-nhua/`) → thông báo mới; breadcrumb 2 link (`/` + `/chuyen-muc/tin-tuc/`); chip **12 cái, cao 32px, rộng nhất 216px < cột 320px**; click chip → sang đúng `/the/cua-go-cong-nghiep/` (H1 + breadcrumb đổi đúng); **`scrollWidth == innerWidth` ở 1024/768/390** (chip tự xuống 6/4/10 hàng, không tràn trong cột). **Hồi quy CP5.1:** `/chuyen-muc/tin-tuc/` + `/chuyen-muc/bao-gia/` giữ NGUYÊN hero 1440×143 · layout 1400×3187 · aside 320×1091 · **3 hộp, KHÔNG có hộp Từ khoá** · thẻ đầu 1048×640 · breadcrumb "Trang chủ / Báo giá". | ✅

### CP5.4

**Bài viết KHÔNG in ảnh đại diện đầu bài** (Tùng chốt 2026-09-16 — “bỏ hẳn khối ảnh đại diện”). Trước CP5.4 `single.php` in `<figure class="cp-article__thumb">` (ảnh `large`, `loading="eager"` + `fetchpriority="high"` vì là LCP). **Lý do (đo 2026-09-16):** **3/7 bài** test có ảnh đại diện **lỗi file** (`naturalWidth = 0×0` — `/bao-gia-cua-nhua-gia-re-tphcm/` → `2299-1067x800.webp`; `/cac-loai-cua-nha-ve-sinh-bao-gia-cua-toilet/` → `cua-phong-ngu-dep.jpg`) ⇒ chỉ còn **khung xám 990×620** (`background: var(--cp-surface)`) giữa tiêu đề và nội dung. **Cách làm:** đổi điều kiện in thành `apply_filters( 'cp_article_show_thumb', false ) && has_post_thumbnail()` — markup **giữ nguyên trong file** ⇒ **bật lại KHÔNG cần sửa code**: `add_filter( 'cp_article_show_thumb', '__return_true' );`. **ĐÃ ĐO CẢ 2 TRẠNG THÁI:** mặc định (tắt) → `.cp-article__thumb` **0**, khoảng cách head → nội dung **22px**, bài **9331px**; bật (test bằng mu-plugin tạm rồi xoá) → figure trở lại **1**, khoảng cách **666px**, bài **9975px**; `overflow = 0` ở 1440 và 390. **Phạm vi:** chỉ `single.php` (chi tiết TIN TỨC) — `page.php` (trang tĩnh, CP6.1) lúc đó **vẫn in** ảnh đại diện (đo `/gioi-thieu/` = **1** figure) — **từ CP6.3 (2026-09-16) trang tĩnh cũng đã bỏ hẳn** (xem `CP6.3`). **KHÔNG phải CSS mồ côi:** `.cp-article__thumb` + `.cp-article__thumb img` (max-height 620px) vẫn giữ trong `caophat.css` cho ca bật lại. **⚠️ Đã kiểm TRƯỚC khi sửa — class này KHÔNG hề bị ẩn:** `display: block` + `visible` ở 1440/1024/768/390; quét `document.styleSheets` chỉ thấy `margin`/`border-radius`/`overflow`/`background` (+`margin-bottom: 18px` @≤768); grep child + **theme cha** + plugin `pl-tien-ich-tungleads` = **0** chỗ ẩn; `git log -S'cp-article__thumb'` = **chưa từng** có rule ẩn ⇒ đừng đi tìm “rule ẩn class này”. | ✅

### CP6.1

**Trang nội dung tĩnh** (trang tạo trong Admin → Trang) — override `page.php` trong child, **TÁI DÙNG** lưới 2 cột của CP5.1 (`.cp-news-layout` / `.cp-news-main` / `.cp-news-aside`) + typography rich-text của CP5.2. Khối hiển thị ở `inc/page.php` (tag `// CP6`): `cp_page_breadcrumb()` (Trang chủ / … trang cha … / tiêu đề), `cp_page_sidebar()` (hộp sub-nav CP6.2 nếu có + `cp_single_support_box()` + `cp_trust_box()` của CP3.2), `cp_is_woo_endpoint_page()` — guard cho cart/checkout/order-received/my-account: `page.php` chỉ gọi `the_content()` để CP3.4/CP3.5 tự bọc hero+container (`the_content` filter), KHÔNG bọc 2 lần. Hero `.cp-pagehero` (breadcrumb + H1 = tiêu đề trang — tiêu đề trang ngắn nên để trong hero); nội dung trong thẻ trắng `.cp-page`; KHÔNG in khối bình luận (giống CP5.2). **Số đo Playwright (2026-09-15, trang `/chinh-sach/chinh-sach-bao-hanh/` + `/chinh-sach/` + `/lien-he/` + `/gioi-thieu/`):** TRƯỚC CP6.1 trang dùng `page.php` theme cha → `.tl-main` **KHÔNG có `.cp-container`**, **KHÔNG in H1** (tiêu đề trang không xuất hiện ở đâu), nội dung bó trong `.tl-page__content` 720px; SAU: hero `.cp-pagehero` + `<h1>` (40px ở 1440 / 28px ở 390), breadcrumb `Trang chủ / Chính sách / Chính sách bảo hành` (2 link); lưới `.cp-news-layout` **`1048px / 320px`** ở 1440 → `888/320` (1280) → `734/300` (1100) → **1 cột** (1024) → `728` (768) → `350` (390); thẻ `.cp-page` 1048 wide, nội dung **990px font 16.5/30.5** (y hệt bài viết vì tái dùng CP5.2), ảnh đầu trang 990×620; **4/4 trang × 6 mức bề rộng `scrollWidth == innerWidth`**. **Bẫy — caption WordPress cũ:** `[caption width="1000"]` in ra `<figure class="wp-caption aligncenter" style="width: 1000px">` → ở 1024/768/390 figure vẫn 1000px, đo được `scrollWidth` **1037** (tràn 647px); inline style CHỈ bị đè bởi `!important` của author CSS → `.cp-article__content .wp-caption, .cp-page__content .wp-caption { max-width: 100% !important; width: auto !important }` + `img { width: auto; max-width: 100%; height: auto }` (áp cho cả bài viết vì cùng kiểu caption). **Hồi quy WooCommerce đã kiểm:** `/cart/` 1 hero “Giỏ hàng”, `/checkout/` 1 hero “Thanh toán” + `.cp-checkout`, **0 `.cp-container` lồng trong**; `/san-pham/` 1 hero “Sản phẩm” (archive, không qua `page.php`); `/` vẫn `front-page.php` (`.cp-hero` 1, `.cp-page` 0); `/my-account/` **không hero** (giữ nguyên như trước, chưa skin — xem "việc tiếp theo"). 0 PHP notice. | ✅

### CP6.2

**Hộp sub-nav "Trong mục này"** cho trang phân cấp — `cp_page_subnav_box()`: chỉ in khi trang đang xem CÓ trang cha hoặc CÓ trang con → in cây trang bằng `wp_list_pages` (`child_of` = gốc của nhánh, `depth: 2`, `title_li: ''`) trong `.cp-side-box` + `.cp-side-cats` (tái dùng CSS CP3.2); WordPress tự gắn `current_page_item` cho mục đang xem và `current_page_ancestor` cho trang cha. Trang đứng riêng (không cha/con) → KHÔNG in hộp này. **Số đo:** trang con “Chính sách bảo hành” và trang cha “Chính sách” → hộp có **10 mục** (gốc “Chính sách” + 9 trang con, `wp_list_pages( child_of = 3719, depth = 2 )`); mục đang xem `current_page_item` → chữ accent `rgb(200,138,0)` + `font-weight: 700`; click “Chính sách vận chuyển” → `/chinh-sach/chinh-sach-van-chuyen/`, `current_page_item` đổi đúng sang trang đó. Trang `/lien-he/`, `/gioi-thieu/` (không cha/con) → **0 mục sub-nav**, cột phải chỉ còn 2 hộp (`Hỗ trợ trực tuyến` + `Cam kết Cao Phát`). | ✅

### CP6.3

**Trang tĩnh KHÔNG in ảnh đại diện** (Tùng chốt 2026-09-16 — “Xoá luôn ảnh thumb class `cp-article__thumb` trong chi tiết trang”, nối tiếp CP5.4 của bài viết). **Cách làm:** XOÁ HẲN khối `<figure class="cp-article__thumb">` + `the_post_thumbnail( 'large', … )` khỏi `page.php` — **KHÔNG** giữ markup sau `if ( has_post_thumbnail() )` như CP5.4 (trang không cần công tắc bật lại; muốn có lại thì `git log -S'cp-article__thumb' -- page.php`). **Lý do (đo 2026-09-16):** **2/2 trang** có ảnh đại diện đều **lỗi file** (`naturalWidth = 0×0`) ⇒ chỉ còn khung xám `--cp-surface` giữa hero và nội dung — `/gioi-thieu/` **990×451**, `/chinh-sach/chinh-sach-bao-hanh/` **990×620**. **Đo TRƯỚC/SAU (5 mức 1440/1280/1024/768/390 × 4 trang = 20 lượt):** figure **1 → 0**; `.cp-page` `/gioi-thieu/` **1194 → 719px** (1440) · **1838 → 1676px** (390); k/c đỉnh thẻ → nội dung **502 → 27px** (1440) · **181 → 19px** (390); `/chinh-sach-bao-hanh/` **2820 → 2176px** · **671 → 27px** (1440) — nội dung nay bắt đầu đúng bằng padding của `.cp-page` (27px/19px), y hệt trang vốn không có ảnh. **2 đường cơ sở KHÔNG đổi ⇒ không hồi quy:** `/chinh-sach/` (không có ảnh đại diện) **2579px** · bài viết `/kich-thuoc-cua-phong-ngu/` **10852px** (CP5.4 còn nguyên). **`scrollWidth == innerWidth` cả 20 lượt** + 0 lỗi PHP. Sau CP6.3 **không template nào in ảnh đại diện mặc định** (`single.php` vẫn giữ markup nhưng bị chặn bởi filter `cp_article_show_thumb` — quyết định CP5.4, không revert). | ✅

### CP7.1

**Trang 404** — `404.php` (child) + khối hiển thị ở `inc/system.php`. TRƯỚC: rơi vào `index.php` của theme CHA — đo `/khong-ton-tai-xyz/`: `<main class="tl-main tl-container">` + **0** phần tử `.cp-*` ⇒ mất hẳn skin. SAU: hero `.cp-pagehero` + breadcrumb `Trang chủ / Không tìm thấy trang` (`cp_system_breadcrumb()`) + H1 + meta, thân trong thẻ trắng `.cp-page` (tái dùng typography `.cp-article__content` của CP5.2): ô tìm kiếm sản phẩm (`cp_inline_search_form()` — dùng lại NGUYÊN `.cp-search` CP1.6 + class `.cp-inline-search`) + 4 nút `.cp-btn` (Tất cả sản phẩm · `Gọi <hotline>` · Tin tức · Trang chủ); cột phải `.cp-news-aside` = `cp_single_support_box()` + `cp_product_cats_box()` (10 danh mục SP nhiều hàng nhất, markup `.cp-side-box`/`.cp-side-cats` CP3.2) + `cp_trust_box()`. **SỐ ĐO (1440/1280/1100/1024/768/390):** hero **1440×143** (H1 40px) → **390×150** (28px), **768×131** (30.72px); `.cp-news-layout` 1440 = **1400** (`1048 / 320`), 1100 = `1060` (`734 / 300`), ≤1024 = **1 cột** (aside xuống dưới); thẻ `.cp-page` 1048×256; 4 nút **171×50** (≥44px) ở desktop → **316×46** (giãn full hàng) ở 390; ô search trong nội dung **416×44** → **272×44** (390); `elementFromPoint` ở tâm ô input / nút kính lúp / nút #1 = **OK cả 6 mức**; `tl-main` = **0**; **tràn ngang 0 + 0 lỗi PHP cả 6 mức**. **BẪY:** CP1.6 ẩn `.cp-search` ở ≤768px (`.cp-search { display: none }`) nên ô search TRONG NỘI DUNG cũng bị ẩn theo (đo ban đầu `0×0` + `elementFromPoint` **BI-CHE**) → phải khai lại `.cp-search.cp-inline-search { display: flex }` — (0,2,0) khai SAU rule ẩn (0,1,0) nên thắng, KHÔNG cần `!important`. | ✅

### CP7.2

**Trang kết quả tìm kiếm** — `search.php`, CHỈ áp cho tìm kiếm CHUNG (`?s=…`). Tìm **sản phẩm** KHÔNG đi qua đây: ô search trên header luôn gửi `post_type=product` (CP1.6) ⇒ WooCommerce dùng `archive-product.php` và đi qua skin shop CP3.1 — kiểm lại sau khi làm: `?s=cua&post_type=product` có `.cp-shop` **1** / `.cp-news-list` **0** ⇒ không giẫm chân WC. TRƯỚC: tìm kiếm chung rơi vào `index.php` của cha (`main.tl-main.tl-container`, **0** `.cp-*`). SAU: hero `.cp-pagehero` (breadcrumb + H1 `Kết quả cho “…”` + meta `N kết quả`) + `.cp-news-layout`; mỗi kết quả qua `cp_search_card()` — **sản phẩm** dùng `cp_search_product_card()` (khung `.cp-news-card` CP5.1 + nhãn danh mục SP + **GIÁ** `get_price_html()` THAY cho ngày đăng), loại khác dùng `cp_news_card()`; rỗng ⇒ `.cp-news-empty` + form tìm lại; phân trang `cp_news_pagination()`; cột phải 4 hộp (hỗ trợ · danh mục SP · tin mới · cam kết). **SỐ ĐO:** `?s=cua` → **10 thẻ**, giá in đúng (`3.100.000₫ Giá gốc là:…`), ảnh `naturalWidth` **300×300 / 260×260 / 694×694 / 316×316** theo khổ ⇒ ảnh thật (không vỡ như ảnh đại diện CP5.4/CP6.3); 1440 layout `1400` (`1048 / 320`), 1100 `1060` (`734 / 300`), 1024 **1 cột**, 768 thẻ 1 cột; `?s=zzzzkhongcogi` → `.cp-news-empty` **1** + ô tìm lại 416×44 (`input`/`btn` = OK); `tl-main` **0**; **tràn ngang 0 + 0 lỗi PHP cả 6 mức**. | ✅

### CP7.3

**Bóc shortcode Flatsome còn sót trong nội dung cũ** — `cp_strip_orphan_flatsome_shortcodes()`, filter `the_content` priority **9** (trước `wpautop` 10 / `do_shortcode` 11). Vì sao: nội dung cũ dựng bằng UX Builder; bỏ Flatsome là `[section] [row] [col] [featured_box] [contact]…` in ra **chữ thô** giữa trang (đo `/lien-he/`, `/zalo-cao-phat/`). **Luật bóc = tag KHÔNG còn ai đăng ký:** (1) bỏ qua khi Flatsome/UX Builder đang chạy (`shortcode_exists('ux_banner')`/`'ux_text'`); (2) từng tag phải `! shortcode_exists($tag)` mới bóc ⇒ KHÔNG bao giờ phá shortcode của plugin đang bật; (3) giữ phần CHỮ trong thẻ bao, bỏ hẳn thẻ tự đóng; lặp ≤5 lượt để bóc hết thẻ LỒNG; tắt bằng filter `cp_strip_flatsome_shortcodes`; danh sách tag ở `cp_flatsome_shortcodes()` (filter cùng tên). **BẪY SỐNG CÒN:** phải `return` sớm khi `is_admin()` ⇒ trình soạn thảo LUÔN thấy nội dung GỐC (thiếu lớp này: mở trang sửa rồi bấm Cập nhật là mất phần đã lọc, không lấy lại được). **ĐO (wp eval, gọi thẳng hàm):** `[section][row][col]A[/col][/row][/section][gap height="20px"][button text="B"]C[/button][features features="x"]` → **`AC[features …]`** (5 tag Flatsome “đã bóc”, giữ chữ A/C, giữ `[features]` lạ); `[contact-form-7 id="12"]` **giữ nguyên**; `define('WP_ADMIN', true)` ⇒ trả về **y nguyên** ✓. Trên HTML thật `/lien-he/`: **0** shortcode Flatsome còn lại (các `[style^=…]` còn trong HTML là CSS của mình, không phải shortcode) và trang đã được skin (`.cp-page post-313`, **42** class `.cp-*` trong `<main>`, `tl-main` **0**). **Phạm vi:** 6 nội dung còn `[ux_` trong DB nhưng chỉ **3** được theme render (Liên hệ 313 · Zalo 4203 · 404 6030); trang chủ 279 + 2 “UX Block” KHÔNG bị in (`front-page.php` không gọi `the_content()`) ⇒ không cần xử lý. | ✅

### CP1.4

Menu chính (desktop) — dropdown 2 cấp: mục cha có mũi tên chi dấu, submenu nền trắng bo góc nổi trên header, mở bằng `:hover` (trễ 120ms) + `:has(:focus-visible)` cho bàn phím — KHÔNG dùng `:focus-within` (click chuột làm panel dính mở mãi); link trong submenu hover dùng nền kem thay vì gạch chân. `depth => 3` nên menu in đủ 3 cấp. Panel ĐÓNG có `pointer-events: none` (`opacity: 0` KHÔNG chặn chuột → thiếu dòng này thì dải panel full-width tự bắt hover, menu mở dù chưa tới chữ; mobile bật lại `auto`). | ✅

### CP1.5

Menu mobile — nút burger mở panel dọc (`.cp-nav.is-open`) qua `assets/nav-menu.js`: `aria-expanded` + `aria-controls`, đóng bằng Esc / click ngoài / resize lên desktop, khoá cuộn body; mục cha có nút mở menu con riêng (`.cp-nav__toggle`, JS tạo — KHÔNG lồng `<button>` trong `<a>`) dạng accordion · kích thước nút **30×30px**, neo `position: absolute; top: 5px; right: 8px; z-index: 2` trong `<li>` (`.cp-nav li { position: relative }` ở rule base — thiếu nó là absolute neo sai chỗ) + lớp phủ trong suốt `.cp-nav__toggle::before` **44×44** căn giữa nút để **vùng bấm đạt 44px** trong khi nút vẫn hiển thị 30px (**`top` 11px → 5px theo yêu cầu Tùng 2026-09-15:** đo ở 390px — `<li>` cao 51.8px, nút cách mép hàng 5px, tâm nút 20px (trước 26px = tâm hàng) → chevron cao hơn tâm hàng **5.9px** và vùng bấm 44px phủ `-2 .. 42px` **nhô 2px** lên mép hàng trên (trước phủ `4 .. 48px`, nằm gọn trong hàng); đã kiểm: `elementFromPoint` ở tâm/1px trên/5px trên/5px dưới nút đều trả về nút, bấm 2 lần mở rồi đóng submenu, không điều hướng; muốn về đúng tâm hàng thì đổi lại `top: 11px`) · `top` PHẢI là số CỐ ĐỊNH, KHÔNG dùng `top: 50%` (khi mở menu con `<li>` cao thêm cả submenu → nút bị đẩy xuống giữa submenu) | ✅

### CP1.6

Ô tìm kiếm trên header — form GET về trang chủ với `post_type=product` (chỉ tìm sản phẩm), pill bo tròn, icon kính lúp là nút submit. Nằm **TRONG khối `.cp-header-cta`** (đứng ĐẦU khối → desktop vẫn ở ngay trái hotline) — 20:20 ngày 2026-09-14 chuyển vào theo yêu cầu; ≤1024px khối cta `flex: 1 1 100%` + `flex-wrap` + `justify-content: flex-end` → hàng 1 = hotline/giỏ/burger canh phải, hàng 2 = search full chiều ngang; bỏ `margin-left: 20px` của `.cp-search` (dùng `gap: 14px` của khối cta). Ô search có `autocomplete="off" autocapitalize="off" spellcheck="false"` để Chrome không gợi ý/autofill giá trị đã gõ (nền xanh `#e8f0fe`), + rule đè riêng `.cp-search input[type=search]:-webkit-autofill { box-shadow: 0 0 0 1000px var(--cp-surface) inset !important }` (màu pill, KHÔNG dùng `#fff` như rule chung). Nút xoá nhanh (×) của Chrome/Safari đã ẩn: input có `-webkit-appearance: none` (điều kiện bắt buộc từ Chrome 83) + `::-webkit-search-cancel-button/-decoration/-results-button/-results-decoration { display: none }` · ≤768px ô search **ẩn hẳn**, mở bằng nút `.cp-search-toggle` (xem `CP1.2b`) | ✅

### CP1.7

Icon giỏ hàng trên header (bên phải khối hotline) — link `wc_get_cart_url()` + badge số lượng (`cp_cart_count()`), badge ẩn khi giỏ trống; tự cập nhật sau khi thêm giỏ bằng AJAX qua filter `woocommerce_add_to_cart_fragments` (fragment `a.cp-cart-link`, hàm in `cp_header_cart_link()` trong `inc/woocommerce.php`) | ✅

### CP1.8

**Header + Topbar quản trị trong Customizer** (yêu cầu Tùng 2026-09-16) — section **“Header & Topbar Cao Phát”** (`inc/customizer.php`, priority **25**, **11 control**: topbar bật/tắt · màu nền · màu chữ · chữ bên trái (textarea, mỗi dòng 1 mục) · nhãn trước hotline · header màu nền · chiều cao (range 56–140) · cỡ logo (range 80–400) · **bật/tắt + kéo thả thứ tự 6 mục** (control `CP_Header_Items_Control`, giá trị = CSV key **chỉ gồm mục đang bật**) · **1 khối HTML tự do** (`wp_kses_post`) · tuỳ chọn “khối HTML chỉ desktop”). Màu để TRỐNG = KHÔNG in CSS ⇒ giữ màu gốc `caophat.css` (mở Customizer lần đầu không đổi gì). **Phạm vi Tùng chốt:** THỨ TỰ chỉ **>1024px** · CHIỀU CAO + CỠ LOGO chỉ **>768px** · MÀU SẮC + BẬT/TẮT áp **mọi khổ**. **Kỹ thuật bố cục:** ở >1024px `.cp-header-cta { display: contents }` để search/hotline/giỏ trở thành mục riêng của hàng (đặt được ở BẤT KỲ vị trí) — DOM giữ nguyên nên ≤1024px không đổi; `gap: 14px` cũ tái tạo bằng `margin-left: 14px` (mục CTA ĐẦU TIÊN cố ý KHÔNG có lề vì đã có `padding-right: 20px` của nav) + “mỏ neo” `margin-left: auto` ở menu ⇒ **bố cục mặc định trùng khít trước khi sửa**. Khối HTML nằm CUỐI DOM nhưng xếp vị trí bằng `order` (mặc định chỉ >1024px, class `cp-hitem--html-desk`) để không phá bố cục tablet/mobile đã đo. **4 BẪY đã dính + đã sửa:** (1) **ID setting phải KHỚP tên theme mod mà getter đọc** — `cp_header_get('header_height')` đọc `cp_header_header_height` nên nếu đặt setting là `cp_header_height` thì chiều cao KHÔNG bao giờ áp (đo được: container vẫn 76px dù đã lưu 110) → đã đổi tên cho khớp quy ước `cp_header_<key>`; (2) CSS in ở `wp_head` prio 100 đứng **SAU** `caophat.css` **kể cả trong media query** ⇒ `min-height: 76px` đè mất `min-height: 64px` của mobile (đo: logo/burger/giỏ lệch xuống **6px**) → phải bọc `@media (min-width:769px)`; (3) tắt mục search phải ẩn LUÔN `.cp-search-toggle` **kèm `!important`** vì ở ≤768px `.cp-header.is-search-open .cp-search` (0,3,0) mạnh hơn selector ẩn (0,2,0) và sẽ mở lại dải search; (4) khi test kéo thả bằng Playwright: **phải `scrollIntoViewIfNeeded()` trước** (tay nắm nằm ở y≈1309 ngoài viewport 950 ⇒ `elementFromPoint` = **null**, chuột bấm vào chỗ trống → tưởng control hỏng) và **phải loại `.ui-sortable-placeholder`** khỏi truy vấn DOM (jQuery UI COPY class của mục gốc sang placeholder nên nó cũng có `cp-horder__item` nhưng không có checkbox). **Đo (Playwright):** MẶC ĐỊNH (sau khi xoá hết theme mod `cp_header*`) trùng khít trước khi sửa — 1440: topbar `1440×30` bg `rgb(251,175,2)` fg `rgb(36,29,5)` 3 mục · header `1440×77` (container **76**) · logo `250×46` · menu x**435** · search x**958** · hotline x**1202** · giỏ x**1385** (y 50→51 do hộp 46px của `.cp-header-cta` thành hộp 76px của container — cùng tâm 50,5px) · 390: logo `115,17 160×29` · burger `20,10 44×44` · giỏ `335,15 35×35` · **overflow 0**. ÁP THỬ (`#123456`/`#ffffff` · header `#111111` · cao **110** · logo **180** · thứ tự `logo,html,menu,search,cart` · tắt hotline · HTML `<b>Gọi ngay…</b>`): 1440 → topbar `rgb(18,52,86)` + chữ trắng ✓ · container **110px** ✓ · logo **180×33** ✓ · khối HTML `214,76 164×18` **cùng hàng** (lệch tâm so với logo **0px**) ✓ · hotline ẨN ✓ · thứ tự đúng `logo→html→menu→search→cart` ✓; 1100/1024/768/390 **overflow 0** ✓ và ≤1024 vẫn là bố cục tablet/mobile gốc (khối HTML tự ẩn) ✓. **E2E Customizer** (user admin tạm, đã xoá): section hiện đủ **11 control** ✓ · sortable khởi tạo (`ui-sortable`) ✓ · **kéo thả THẬT bằng chuột**: có `.ui-sortable-helper` + `.ui-sortable-placeholder`, thả xong DOM đổi và `cp_header_items` = `logo,html,menu,search,cart` ✓ · bỏ tick 1 mục ⇒ CSV bỏ mục đó ✓ · Publish lưu đúng **7 theme mod** ✓. | ✅

### CP1.9

**“Chi nhánh & Hotline Cao Phát” quản trị trong Customizer** (Tùng 2026-09-17: *“chuyển phần (Hotline chi nhánh) trong plugin ra ngoài phần cài đặt … trong theme sẽ hợp lý hơn”* — Tùng chốt phương án **dùng REPEATER có sẵn của theme**, KHÔNG làm trang admin riêng). **CHUYỂN CHỖ dữ liệu từ plugin `pl-tien-ich-tungleads` (Settings → Cao Phát, option `tlcp_support_branches`) VỀ THEME**, lý do: đây là **NỘI DUNG hiển thị** (không phải business logic) và hotline CHÍNH `cp_hotline_tel` vốn đã ở Customizer ⇒ **1 chỗ sửa mọi số điện thoại**. **Theme:** `inc/customizer.php` — control `CP_Branches_Repeater_Control` (type `cp_branches_repeater`: mỗi dòng = tên + số, có nút × xoá và tay kéo `⋮⋮`) · section `cp_branches` (priority **26**) · setting `cp_branches` (giá trị = **JSON** `[{"name","tel"}…]`) · `cp_sanitize_branches()` (bỏ dòng thiếu tên HOẶC thiếu số, tối đa 20) · asset `assets/customizer-branches.js` + `.css` (enqueue `jquery` + `jquery-ui-sortable` + `customize-controls`). **Helper `functions.php`:** `cp_support_branches_default()` (1 nguồn sự thật: 4 chi nhánh) · `cp_support_branches_default_json()` (default của setting) · `cp_support_branches()` — đọc **theme_mod → CẦU NỐI option cũ `tlcp_support_branches` (dạng textarea `Tên \| Số`, đọc THẲNG option vì plugin v0.5.0 đã gỡ hàm) → mặc định**, filter `cp_support_branches` giữ nguyên. `inc/woocommerce.php` bỏ đoạn đọc plugin (chỉ còn `$branches = cp_support_branches();`). **Plugin v0.5.0 GỠ HẲN mục Hotline** (option + textarea + `tlcp_support_branches*()` + `register_setting`); trang Settings còn **3 mục** (Chèn mã tracking · Chế độ bảo trì · Mục lục nội dung) + 1 dòng `description` dẫn link sang Customizer. **Chuyển dữ liệu production:** `docs/prod-migrate-branches.php` — mặc định **DRY-RUN**, ghi thật khi thêm tham số **VỊ TRÍ** `go` (⚠️ **`-- --go` KHÔNG dùng được**: WP-CLI báo `unknown --go parameter`), có `force` để ghi đè; đã thêm **2 dòng** vào checklist `deploy-caophat.sh` bước 5b. **Đo (Playwright, Cline 2026-09-17):** Customizer mở đúng section `cp_branches`, **4 dòng mặc định** (CN Quận 7/Bình Tân/Bến Cát/Giải đáp thắc mắc) + `ui-sortable` đã init + JS/CSS nạp; bấm “+ Thêm chi nhánh” → **5 dòng**, điền xong ⇒ input ẩn VÀ `wp.customize('cp_branches').get()` cập nhật JSON ngay; **kéo CHUỘT THẬT** dòng cuối lên đầu ⇒ DOM + JSON đổi đúng thứ tự ⇒ Lưu ⇒ `wp theme mod get cp_branches` khớp; front-end box “Hỗ trợ trực tuyến” (SP 4816) đọc theme_mod: **5 dòng** khi có dòng test (link `tel:` bỏ dấu chấm đúng: `0834.484.484` → `tel:0834484484`), **xoá theme_mod ⇒ về 4 dòng mặc định**, box **320×352**, **tràn ngang 0**; **CẦU NỐI** kiểm bằng `wp option update tlcp_support_branches 'CN Cầu Nối \| 0900.999.888'` ⇒ front-end hiện **đúng 1 dòng đó** (theme_mod rỗng vẫn không mất số khi deploy lệch thứ tự) ✓ · script chuyển dữ liệu: dry-run in đúng dòng sẽ chuyển, `go` ⇒ ghi theme_mod + **option cũ bị xoá** ✓ (đã trả site về mặc định, xoá bản copy trong `uploads/`); smoke test **24/24**; `php -l` ×4 + `node --check` sạch. | ✅

### CP1.4b

Mega menu 3 cấp (mục "Các Loại Cửa") — mục cấp 1 có menu 3 cấp: JS gắn `.cp-mega` + `.sub-menu--mega`, CSS xếp cấp 2 thành **cột** (tiêu đề chữ hoa + gạch chân vàng), cấp 3 là danh sách phẳng trong cột; panel là dải ngang full chiều rộng dưới header (`depth => 3`). Link cấp 3 dài quá bề ngang cột thì cắt đuôi `…` (`overflow:hidden` + `text-overflow:ellipsis`); cột đầu của HÀNG mới (khi panel xuống dòng) được JS gắn `.cp-row-start` để bỏ vạch dọc + thụt lề — grid `auto-fit` nên CSS không tự biết cột nào xuống hàng. **2026-09-16 (yêu cầu Tùng): BỎ “cột gom” `.cp-mega__misc`** — các mục cấp 2 KHÔNG có danh mục con trước đây bị JS gom vào 1 cột bọc `ul.sub-menu` ⇒ mang style **cấp 3** (14px chữ thường) nên nhìn như mục CON — nay **mỗi mục cấp 2 = 1 CỘT riêng** (in hoa 13px + gạch vàng); đo 1440: 10 → **12 cột**, panel 422 → **618px**, 1024 → 693px, 769 → 1054px; hover/bấm được ✓, 0 lỗi JS ✓, mobile hết lồng cấp ✓ (chi tiết + số đo ở `.ai/FEATURE_MAP.md` CP1.4b). **2026-09-16 (tiếp) — CHẶN PANEL CAO QUÁ KHUNG NHÌN:** 769px panel 1054px trong khi viewport 900px ⇒ cột dưới không tới được (1024px cũng chỉ vừa khít: đáy 888). Header sticky cao THAY ĐỔI theo khổ (đo 77/161/186px) nên không tính được bằng CSS thuần → JS `setMegaMax()` đặt biến `--cp-mega-max` = viewport − đáy header − 10px (tính lại khi resize); CSS dùng `max-height: var(--cp-mega-max, calc(100vh - 120px))` + `overflow-y: auto` ⇒ đo lại: panel **648 / 693 / 674px**, đáy **724 / 882 / 889** (≤ 900 ✓), 769px cuộn **380px** và cột cuối bấm được ✓. **CẦU HOVER ĐỔI CƠ CHẾ:** pseudo cũ nằm TRONG panel nên bị `overflow` CẮT ⇒ nay `margin-top: calc(-1 * var(--cp-bridge))` + `padding: calc(var(--cp-bridge) + 18px) 20px 18px` + `background-clip: content-box` (dải trong suốt vẫn là phần của panel = con `<li>` ⇒ giữ `li:hover`); **chỉ bật khi khoảng hở ≤ 32px** (1440 = 30px ✓ hover OK) còn 1024/769 hở 126/115px ⇒ để 0 vì dải sẽ phủ `.cp-search`/`.cp-hotline`/`.cp-cart-link` (đo: cả 3 bị chặn bấm). **2026-09-17 (yêu cầu Tùng: “nền trắng không phủ hết tất cả các ô menu” + “toàn bộ ô menu dịch lên trên quá cao”) — TRẢ LẠI CÁCH HIỂN THỊ TRƯỚC 2026-09-16:** bỏ `margin-top` âm + `background-clip: content-box` (hộp panel cao hơn thân panel 30px ⇒ mép trên nằm TRONG header: đo 1440 `panel.top` **76** vs đáy header **107** = −31px, kèm vạch border 1px cắt ngang header; nền trắng chỉ phủ content box ⇒ hở **48px** trên · **20px** hai bên · **18px** dưới — đo pixel 1440 mép trái rgb(249,247,245) = nền trang) ⇒ nay `padding: 18px 20px`, `background-clip` mặc định (border-box): **đo lại 1440/1280/1024/769 mép trái · mép phải · dải trên · dải dưới đều rgb(255,255,255)** ✓, `panel.top − header.bottom = −1` (liền mạch header) ✓, nội dung vẫn bắt đầu `header.bottom + 18` ✓. **Cầu hover chuyển sang pseudo của CHÍNH `<li>`** (`.cp-nav li.cp-mega:hover::before`, `top: calc(100% − var(--cp-bridge))` + `height: var(--cp-bridge)`): `<li>` không phải khung cuộn nên KHÔNG bị `overflow-y: auto` cắt (lý do 2026-09-16 phải bỏ pseudo) mà không cần kéo mép panel; `:hover` nằm trong selector ⇒ **không có vùng “ma”** (đo: chuột ở dải cầu hover khi CHƯA hover chữ = `hidden` ở 1440/1280; hover chữ rồi đi xuống dải = `visible`; rời chuột = `hidden`). JS `setMegaMax()` đo thẳng `header.bottom − link.bottom + 2` (không cần tạm đặt `--cp-bridge: 0` như trước). | ✅

### CP4.1

Skin trong `caophat.css` — màu nút/thông báo + tinh chỉnh khung: `.cp-btn-accent` nền `#000000` (hover `#333333` — **Tùng giao DeepSeek quyết 2026-09-14: GIỮ**: cả 2 chỗ dùng nút (dải CTA trang chủ + dải CTA trang chi tiết SP) đều nằm trên nền cam gradient nên hover sang `--cp-accent` sẽ chìm vào nền; convention theme là hover cùng tông; đo Playwright: hover `rgb(51,51,51)` + nhấc 1px, chữ trắng không bị rule `a:hover` ghi đè, tương phản 21:1 → 12.6:1), `.woocommerce-message` viền trên + icon `#fbaf02`, `.cp-single-side` sticky `top:100px`, `.cp-topbar .cp-container` `min-height:30px`, `.cp-nav` `padding-right:20px` (khoảng cách menu ↔ ô search). **`.cp-section` `padding: 64px 0 → 40px 0` (yêu cầu Tùng 2026-09-15):** đổi ở rule GỐC nên chỉ ảnh hưởng các khối KHÔNG có padding riêng — **5 khối trang chủ** · `.cp-section` trang cửa hàng/danh mục SP (CP3.1) · `.cp-section.cp-news-section` (CP5.1) · dải CTA `.cp-section.alt` trang chi tiết SP → đo `getComputedStyle` đều **64/64 → 40/40**; các khối có padding RIÊNG giữ nguyên: `.cp-single-section` **28/0**, `.cp-cart-section` + `.cp-checkout-section` **40/64**, `.cp-thankyou-section` 40/120, `.cp-related-band` **40/44**, `.cp-article-section` **26/44**, `.cp-page-section` **28/44** (đo trên 8 URL, 390px không URL nào tràn ngang). Nền xanh autofill của Chrome bị chặn bằng `input:-webkit-autofill { box-shadow: 0 0 0 1000px #fff inset !important; -webkit-text-fill-color: var(--cp-text) !important }` — rule `input:-internal-autofill-selected` là của trình duyệt, KHÔNG xoá/sửa được. **Loạt fix theo review (22:00 ngày 2026-09-14):** dải CTA đổi nền sang `--cp-accent-dark`→`--cp-accent` để chữ trắng đạt AA (4.8–6.6:1); focus ring 2px vàng cho `a`/`button:focus-visible`; tôn trọng `prefers-reduced-motion`; tap target ≥44px (hotline/giỏ/burger/ô search ≤1024px); `hero.png` 986KB → `hero.webp` 37KB (+ bản 652px 18KB, có `srcset` + `fetchpriority=high`); `door-hdf.png` 656KB → `.webp` 24KB; **6 bản PNG gốc không còn file nào tham chiếu → đã xoá 2026-09-14** (`git rm`, còn trong lịch sử git; `assets/images/` 2.8MB → 84KB); `alt` cho ảnh (card: `wp_get_attachment_image_attributes`; ảnh trong nội dung: thêm `alt=""` bằng `WP_HTML_Tag_Processor` trong `the_content`); `.cp-thankyou-section` chừa 120px đáy cho FAB. **2026-09-15 (yêu cầu Tùng): mobile ≤768px `.cp-btn { padding: 10px 14px }`** (trước `12px 22px`) — nút gọn hơn: cao **50 → 46px** (vẫn ≥ 44px tap target), bề ngang mỗi nút **−16px** (đo 390px: `Xem sản phẩm` 155→139 · `Nhận báo giá 24/7` 187→171 · `Gọi ngay` 219→203), desktop 1024/1440 giữ nguyên `12px 22px`/50px, không tràn ngang ở 390/768/1024/1440. **2026-09-17 — TOKEN MÀU SAO DÙNG CHUNG:** thêm `--cp-star: #e2a53a` vào `:root` + rule nền `body.cp .star-rating { color: var(--cp-star); }` (đặt ngay sau khối token, TRƯỚC mọi rule khác) ⇒ **mọi chỗ có sao dùng CHUNG 1 màu** lấy từ trang chi tiết SP; 6 chỗ hard-code `#e2a53a` (CP3.2 summary + CP3.9 tổng kết/chip/thẻ đánh giá + `p.stars` của form) đã đổi hết sang `var(--cp-star)`. **Sao RỖNG giữ `#cfc8d8`** — WooCommerce khai trực tiếp trong `.star-rating::before`, cố ý để sao đầy nổi. | ✅

---

## GHI CHÚ KHÔNG THUỘC BẢNG CP — khôi phục từ `CLAUDE.md` (2026-09-18, tối ưu token)

> Khi nén `CLAUDE.md` thành chỉ mục, các dòng dưới đây KHÔNG nằm trong bảng CP nên được chuyển nguyên văn về đây.
> Vẫn là **quyết định đã chốt sau khi đo** — không revert.

### Skin: màu · font · bẫy selector (thuộc CP4.1 / CP1.1)

- Font: **Be Vietnam Pro**. Palette: **primary vàng nghệ `#fbaf02`** (tông chủ đạo, chữ trên nền primary dùng `--cp-on-primary` `#241d05`) · accent cam đất `#c8471f` (CTA/hotline, tương phản) · kem `#f6f4f1` (biến `--cp-*` trong `caophat.css`).
- ⚠️ **Bẫy selector:** link có class nút (`.cp-btn-*`, `.cp-buynow`, `.cp-single-hotline`, `.cp-side-support-tel`) **phải** viết `.cp a.<class>` mới thắng được `.cp a { color: inherit }`.

### Hotline (thuộc CP1.3)

- Hotline mặc định `0834.021.021` — filter `cp_hotline_display` / `cp_hotline_tel`.

### Ranh giới parent ⇄ child

- Parent lo: boot, FeatureRegistry, SiteMode, Setup, Enqueue, Performance, Security, SEO, WooCommerce integration, templates blog/archive/page.
- Child override: `header.php`, `footer.php`, `front-page.php`, `woocommerce.php`/hook shop, `assets/caophat.css`, template-parts trang chủ. Không business logic, không build step.

### Plugin ngoài theme

- Plugin **`pl-tien-ich-tungleads`** — tên hiển thị “PL Tiện Ích - TungLeAds” (đổi tên 2026-09-17 ⇒ **dùng chung nhiều site**; v1.0.0 đổi tên SẠCH: thư mục/file/text-domain `pl-tien-ich-tungleads`, tiền tố `tlpi_`/`TLPI_`/`tlpi-`, slug Settings `tlpi-support`). Business logic (CPT, taxonomy, API riêng) → plugin, KHÔNG vào child theme.
- Widget nút liên hệ nổi **`button-call-zalo-tungleads`** (“Button call/zalo - TungLeAds”): widget in ở `wp_footer`, số nhập ở **Settings → Button Call/Zalo**. Theme đã **xoá hẳn** `.cp-fab` (CP1.3, 2026-09-16) và plugin cũng bỏ tuỳ chọn ẩn nó ở v1.0.1 ⇒ hai bên **độc lập hoàn toàn**.
- ⚠️ **Quyết định đã chốt (Tùng 2026-09-16, xem trang thử):** icon nút **GỌI = “điện thoại + 2 gợn sóng”** (Feather `phone-call`) — **KHÔNG đổi lại thành icon nét trơn 1 path**.

### Phân vai model (Tùng chốt 2026-09-14)

- `deepseek` **chỉ xây dựng chức năng + commit LOCAL**; **push/deploy production + mọi thao tác trên production là phần của Tùng + `claude`**. Việc production còn tồn nằm ở mục “còn lại” của `.ai/WORKLOG.md`.

### File huấn luyện agent `.github/` (bị `deploy-caophat.sh` loại trừ ⇒ không lên production)

| File | Khi nào áp dụng |
| :--- | :--- |
| `.github/instructions/theme-php-conventions.instructions.md` | Tự gắn khi tạo/sửa `**/*.php` |
| `.github/instructions/theme-assets.instructions.md` | Tự gắn khi sửa `assets/**` (CSS/JS) |
| `.github/skills/cp-ui-verify/` | Quy trình đo & kiểm chứng UI trước khi báo xong |
| `.github/prompts/cp-wrapup.prompt.md` | Lệnh `/cp-wrapup` — chốt phiên: WORKLOG + đồng bộ 3 lớp + commit |


### Tối ưu tài liệu 2026-09-18 — `CLAUDE.md` chỉ còn CHỈ MỤC (Tùng yêu cầu)

**Vì sao:** Claude Code nạp **trọn `CLAUDE.md`** mỗi phiên ⇒ file 131,6 KB ≈ **38.000 token/phiên**; lại thêm nội dung trùng giữa các file.

**Đã làm:**
1. Chuyển **45 hàng bảng CP (102.470 ký tự, NGUYÊN VĂN — không mất chữ nào)** từ `CLAUDE.md` sang mục `### CPx.y` ở **file này** (`.ai/FEATURE_MAP.md`).
2. `CLAUDE.md` = **chỉ mục**: 113.863 → **6.546 ký tự** (-94,3% ≈ -36.000 token/phiên) — chỉ còn: 3 file cần đọc trước khi code · chỉ mục CP (sắp theo số CP) · bảng Nhóm · mục “Khi cần thêm” (script đo · skill · nhật ký · README plugin).
3. **Khôi phục 6 nội dung không nằm trong bảng CP** (mục `## GHI CHÚ KHÔNG THUỘC BẢNG CP` bên trên): palette/font + bẫy `.cp a.<class>` · hotline mặc định · ranh giới parent⇄child · plugin ngoài theme + **quyết định icon nút GỌI** · phân vai model · mapping `.github/`.
4. `AGENTS.md` thêm bảng **hạn mức chống phình** (CLAUDE.md ≤ 8 KB · AGENTS ≤ 10 KB · dòng §2 ≤ 300 ký tự · hàng CP ≤ 3 KB · §1 ≤ 10 dòng) + luật **“CẤM chép lại nội dung giữa các file — chỉ được trỏ”** + “một sự thật một nhà” (luật → AGENTS · chi tiết CP → FEATURE_MAP · việc theo phiên → WORKLOG). `§1 WORKLOG` gọn còn ≤ 10 dòng.

**Số đo trước → sau (bytes):** `CLAUDE.md` 131.618 → **6.911** · `AGENTS.md` 6.605 → 8.533 (thêm hạn mức) · `FEATURE_MAP.md` 137.215 → 259.969 (nhận chi tiết) · `WORKLOG.md` 195.807 → ~175.000 (§1 gọn). **Tổng 536 KB → ~451 KB**, nhưng quan trọng nhất: **file nạp tự động giảm ~95%**.

**Còn lại (đề xuất, chưa làm):** chuyển các dòng §2 CŨ trong WORKLOG sang `.ai/WORKLOG-archive-2026-Q3.md` (chỉ DI CHUYỂN nguyên văn) ⇒ §2 sống còn ~40 dòng; và tách vài hàng CP > 3 KB trong file này thành mục riêng.
